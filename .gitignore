# ── Elixir / Erlang
/_build/
# #1263 — per-worker build caches (GRAPPA_CACHE_ID): one
# _build/deps/priv/plts triplet per id, opt-in, never shared.
/.caches/
/cover/
/coverage/
/deps/
/doc/
/.fetch
/.hex/
/.mix/
/.cache/
/.sobelow/
erl_crash.dump
*.ez
*.beam
.elixir_ls/
/.dialyxir_*
*.plt
/tmp

# ── PLT cache (Dialyzer)
priv/plts/*.plt
priv/plts/*.plt.hash

# ── Releases
/grappa-*.tar
/grappa-*.tar.gz
/release/

# ── Mix-generated
*.zip

# ── Local config / state (NEVER commit secrets or live DB)
/grappa.toml
/grappa_dev.db
/grappa_test*.db*
*.db-shm
*.db-wal
*.db-journal
/runtime/*
!/runtime/.gitkeep
# Bake bind-mount target dirs so a fresh `git clone` doesn't hit
# Docker auto-creating them as `root:root` on first
# `docker compose --profile prod up cicchetto-build` (container UID
# 1000 then fails to write Vite's `dist/` or bun's cache).
!/runtime/cicchetto-dist/
/runtime/cicchetto-dist/*
!/runtime/cicchetto-dist/.gitkeep
!/runtime/bun-cache/
/runtime/bun-cache/*
!/runtime/bun-cache/.gitkeep
# Same reason for the #1027 bridge: `--profile ircd` bind-mounts this as the
# shottino XDG data home, and Docker would create it root-owned on a fresh
# clone. The bridge writes its token cache and ircd.log in there as UID 1000.
!/runtime/shottino/
/runtime/shottino/*
!/runtime/shottino/.gitkeep

# ── Docker / env
.env
.env.local
*.local

# ── Personal compose overrides (machine-specific bindings, hostnames).
# Examples are committed (compose.{,prod.}override.yaml.example);
# real overrides stay local.
/compose.override.yaml
/compose.prod.override.yaml

# ── Local cluster worktrees (self-contained, machine-specific)
/.worktrees/

# ── Ephemeral implementation plans (scratch, per-feature; deleted at
# feature completion — durable rationale goes to docs/DESIGN_NOTES.md
# first). See CLAUDE.md "Docs map". docs/superpowers/specs/ stays tracked.
/docs/plans/
/docs/superpowers/plans/

# ── Claude Code session state (locks, scratch, output)
# Skills + settings.json are repo-shared content; everything else
# (locks, scratch, settings.local.json) stays local.
.claude/*
!.claude/skills/
!.claude/settings.json

# ── Superpowers brainstorm session artifacts (mockups, state events)
.superpowers/

# ── Editors / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
*.iml
.vscode-test/

# ── Node / PWA (cicchetto, future)
/node_modules/
/dist/
/.vite/
/.svelte-kit/
*.log

# ── Native terminal shottino build artifacts
/frontends/shottino/shottino
/frontends/shottino/*.o
# Header dependencies the compiler writes (-MMD -MP).
/frontends/shottino/*.d
# Test binaries — `make check` rebuilds these from tests/*.c on demand.
# The sources and the header are tracked; the binaries are not.
/frontends/shottino/tests/test_*
!/frontends/shottino/tests/test_*.c
!/frontends/shottino/tests/test.h

# GitHub CLI config
.gh

# orchestrator handoff brain (ephemeral run state, durable across reboot, never committed)
.orchestrate/

# shottino call helper (opt-in `make call`)
frontends/shottino/shottino-call
frontends/shottino/call/*.o
