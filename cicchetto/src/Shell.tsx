import type { Component } from "solid-js";

const Shell: Component = () => {
  return (
    <main class="shell">
      <h1>cicchetto</h1>
      <p class="muted">walking-skeleton — sub-tasks 3-7 wire login, channels, scrollback.</p>
    </main>
  );
};

export default Shell;
