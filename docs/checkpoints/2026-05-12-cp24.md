---
status: open
---

# CP24 — post-code-reload codebase review + mega-cluster

Opened 2026-05-12 after CP23 cluster `code-reload` LANDED + CLOSED
2026-05-12 ~00:06Z (version 0.2.0; 9 commits to origin/main; CI run
25704807728 green 5m34s).

## Why this checkpoint

CP23 closed without a successor cluster — vjt requested a codebase
review (Option B from the orchestrate hand-off) since the gate was
overdue (~17 sessions across CP19+20+21+22+23 since 2026-05-08
review). Review LANDED 2026-05-12 ~07:50Z; this checkpoint tracks
the mega-cluster that addresses the findings.

## Review headline

`docs/reviews/codebase/2026-05-12-codebase-review.md`. Per-agent
drafts in `docs/reviews/codebase/drafts-2026-05-12/`.

**Severity tally:** 3 CRIT, 36 HIGH, 72 MED, 63 LOW.

8 agents (vjt-blessed expanded fan-out — split docker + cross-surface
from cross-module, added explicit SQLite angle): irc, persistence,
lifecycle, web, cicchetto, cross-module, docker, cross-surface.

**Skill changes pre-LANDED 2026-05-12 commit `1cff5e3`:**
`.claude/skills/review/SKILL.md` + `docs/reviewing.md` — 8-agent
fan-out + severity-gate triage rules + explicit SQLite angle for
persistence/.

## Three CRITICAL findings (the porco dio trifecta — corrected to two 2026-05-12 bucket A)

- **C1 — SASL credential leak in pre-handshake phases**
  (`lib/grappa/irc/auth_fsm.ex:232-234`). `AUTHENTICATE +` clause
  matched UNCONDITIONALLY for any phase other than `:registered`.
  Hostile/MitM upstream can elicit operator's SASL credentials before
  SASL has been negotiated. Phase guard missing on `:sasl_pending`.
  ✅ **LANDED 2026-05-12 bucket A** — phase pin on `:sasl_pending` +
  catch-all absorption + 4 regression tests (3 pre-handshake phases ×
  stray AUTHENTICATE + plus 1 control proving the legitimate
  `:sasl_pending` reply still fires).
- **C2 — SQLite `PRAGMA foreign_keys` never enabled in dev/prod**
  (`config/runtime.exs:22-27` + `dev.exs:3-6`). 23 migrations'
  worth of `references(..., on_delete: ...)` are runtime-dead.
  ❌ **FALSE FINDING — corrected 2026-05-12 bucket A.** Live-container
  probe confirmed `PRAGMA foreign_keys=1` AND that orphan-FK insert
  raises `Exqlite.Error "FOREIGN KEY constraint failed"`. The
  `ecto_sqlite3` adapter sets PRAGMA foreign_keys=ON on every
  connection-init by default (see `deps/ecto_sqlite3/lib/ecto/adapters/sqlite3.ex:85`
  + `deps/exqlite/lib/exqlite/pragma.ex:52`). The reviewer read
  "SQLite ships with PRAGMA foreign_keys=OFF" as the runtime default
  and missed the adapter override. The `validate_subject_exists/1`
  pre-flight checks in `Accounts.create_session/4`, `QueryWindows.open/4`,
  `UserSettings.get_or_init/1` exist for a SEPARATE, REAL ecto_sqlite3
  limitation: the engine returns FK constraint NAME as `nil` so
  `assoc_constraint` cannot pattern-match — the pre-flight produces a
  clean changeset error before the insert raises. The existing source
  comments already describe this correctly; no source edits required.
  Persistence/S7's "without S1 fixed there is NO backstop" framing
  also wrong — re-validate at bucket B. Tally drops from 3 CRIT to
  **2 CRIT** (C1+C3); compiled review doc + persistence draft updated
  with correction sections.
- **C3 — Visitor WHOIS broken** (`lib/grappa_web/channels/grappa_channel.ex:445-454`).
  Comment explicitly flags the bug + prescribes the fix; implementation
  uses the rejected path. Visitors get `visitor_not_allowed` despite
  documented intent.
  ✅ **LANDED 2026-05-12 bucket A** — new `dispatch_subject_verb/2`
  helper + `resolve_subject/1` extracts `{:visitor, id}` from the
  `"visitor:<uuid>"` user_name; WHOIS dispatch swapped over; 3
  regression tests (live visitor session sends WHOIS upstream;
  visitor-without-session returns `no_session`; CRLF nick rejected
  as `invalid_line`).

## Mega-cluster shape

`cluster/post-cr-review`. 10-11 buckets A-Z + flush+clear between
buckets. Per-bucket: deploy + healthcheck + browser smoke
(memory `feedback_per_bucket_deploy`).

Bucket plan in `docs/reviews/codebase/2026-05-12-codebase-review.md`
"Mega-cluster shape" section.

| Bucket | Theme |
|--------|-------|
| A | CRITICAL trifecta (C1+C2+C3) |
| B | SQLite production defaults + hot-path index |
| C | IRC outbound + AuthFSM hardening |
| D | Wire-shape boundary discipline |
| E | Channel inbound validation + visitor coverage |
| F | Cic correctness (own-nick, CSP, nick-comparison, type split) |
| G | Cross-surface drift + envelope unification |
| H | Lifecycle correctness + boot perf |
| I | Cross-module + Docker debt |
| Z | Sweep + close |

## Trajectory observation

Codebase is **unusually disciplined**: cross-module sweep found 0
`\\` defaults, 0 `String.to_atom`, 0 bare `rescue _`, 0 raw
`Repo.insert/2`, 0 runtime `Application.put_env` across 118 lib
files. Wire boundary discipline holds across every context.

Findings concentrate in three domains: pre-handshake credential
safety, SQLite production defaults, visitor flow asymmetry.
None are "the architecture is wrong"; all are "specific gaps the
original author intended to close but didn't yet."

Phase 5 hardening (TLS verify_none → CA chain, PromEx, Sobelow
strictness, NickServ-on-connect, NickServ NOTICE parsing,
NickServ REGISTER proxy) is the gate AFTER mega-cluster closes —
with C1 fixed, the verify_none → CA chain work no longer has a
credential-leak interaction.

Image upload cluster (memory `project_image_upload`) is in queue
behind Phase 5.

## Operational state at handoff

- Working tree clean. `main` at `1cff5e3` (skill update, pushed).
- CP23 review writeup commit pending (next action).
- All 8 review draft files written + compiled doc written.
- Live container session pid `<0.745.0>` still alive from
  21:40:05Z (uptime ~10h+S4 work + this review session). NOT
  meta-deployed via the new auto-detect. First mega-cluster bucket
  close will exercise it.
- `runtime/cicchetto-dist/assets/index-RvD22cM9.js` — pre-S4
  bundle. First `scripts/deploy-cic.sh` post-bucket will prove the
  refresh-banner pipeline end-to-end.

## Sensitivity gate notes

- vjt confirmed Turnstile site key in `.env.example` is the public
  site_key (not the secret) → no rotation needed; will replace with
  a generic example value during cluster I.
- `compose.prod.override.yaml` is gitignored AND not tracked
  (verified `git ls-files`). Local-only file on vjt's box.
  No history rewrite needed — `rm` locally during cluster I if vjt
  wants the cleanup.

## Bucket A LANDED 2026-05-12

C1 (SASL phase guard) + C3 (visitor WHOIS) fixed; C2 (FK pragma OFF
in dev/prod) downgraded to NON-FINDING after live-container probe
contradicted the review claim (PRAGMA `foreign_keys` is `1`; orphan
FK insert raises `Exqlite.Error "FOREIGN KEY constraint failed"`).
Compiled review doc + persistence draft + this checkpoint updated
with the correction; DESIGN_NOTES gained a CP24 bucket A entry
documenting the fix + the false-finding trail. CRITICAL tally drops
from **3 to 2**.

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 41.9 seconds (20.8s async, 21.1s sync)
8 doctests, 28 properties, 1475 tests, 0 failures
...
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.13s
done (passed successfully)
Generating docs...
```

(`1468 → 1475` = +7 new tests for C1 (4: 3 pre-handshake phases ×
stray AUTHENTICATE + plus 1 control) + C3 (3: live visitor session
sends WHOIS upstream; visitor-without-session returns `no_session`;
CRLF nick rejected as `invalid_line`). Dialyzer 0 errors. No new
warnings. Doc-gen warnings unchanged from pre-flight baseline.)

### Reviewer follow-up — HIGH for bucket B

The bucket A code-reviewer flagged a HIGH out-of-scope finding:
`who`, `names`, `list`, `banlist` (all read-only verbs in
`lib/grappa_web/channels/grappa_channel.ex` lines ~432, 471, 489)
still route through `dispatch_ops_verb/2` → visitors get
`{:error, %{reason: "visitor_not_allowed"}}` despite the same
spec-#2 carve-out logic that drove C3 (read-only verbs broadcast on
the visitor's own `subject_label` topic, so visitors are entitled to
issue them). Pre-bucket-A these verbs were already broken for
visitors; bucket A didn't make them worse, so this does NOT block
landing — but the new `dispatch_subject_verb/2` is the obvious
migration target. Until `who`/`names`/`list`/`banlist` migrate, the
codebase sits in the "two patterns, copy whichever" state CLAUDE.md
"Total consistency or nothing" warns about.

**Bucket B addition:** open the bucket with a 30-min audit of every
`dispatch_ops_verb/2` callsite, classify each verb as
write-only-reject-visitor (op/deop/voice/devoice/kick/ban/unban/invite/
umode/mode/topic_set/topic_clear) or read-only-allow-visitor (whois
already migrated; who/names/list/banlist still broken). Migrate the
read-only verbs to `dispatch_subject_verb/2` + add the matching
visitor regression tests. Estimate: 1-2h within the existing bucket B
SQLite scope (no shared file conflict — bucket B touches
`config/runtime.exs` + `priv/repo/migrations/`, this touches
`grappa_channel.ex`).

## Bucket B opening — persistence/S7 re-eval (2026-05-12)

Decision: **DROP persistence/S7 entirely. No source edits.**

Re-read of the three pre-flight `validate_subject_exists`-style
sites post-C2 correction:
- `lib/grappa/accounts.ex:179-189` — comment already states "a
  concurrently-deleted user / visitor would still trip the DB FK as a
  backstop." Correct framing.
- `lib/grappa/query_windows.ex:230-239` — comment already states
  "ecto_sqlite3 returns the constraint name as `nil` so the built-in
  handler can't match… a concurrently-deleted user / network would
  still trip the DB FK as a backstop (raw exception path retained as
  last-resort guard)." Correct framing.
- `lib/grappa/user_settings.ex:180-183` — comment already states
  "ecto_sqlite3 returns FK constraint names as nil so Ecto can't map
  DB FK violations to changeset errors… We check with `Repo.exists?`
  before insert so the `{:error, changeset}` return contract is
  honoured." Correct framing.

All three comments correctly describe the actual problem: an
ecto_sqlite3 surface limitation (constraint name returned as `nil`),
NOT a missing FK backstop. The pre-flight produces a clean changeset
error before the insert raises; the FK is the TOCTOU backstop. The
review's persistence/S7 "without S1 fixed there is NO backstop"
framing inherited the C2 false premise. With C2 corrected to
NON-FINDING, S7's "load-bearing" comment-rewrite recommendation is
moot: the comments already describe convenience (changeset-shaped
error path), not load-bearing semantics. Nothing to fix.

Bucket B scope therefore narrows to: S2 + S3 (busy_timeout +
pool_size doc) + S5 (connection_state partial index) + S8
(last_joined_channels cap) + reviewer add-on (read-only verbs to
`dispatch_subject_verb/2`).

## Bucket B LANDED 2026-05-12

5 commits on `cluster/post-cr-review-b` (worktree `.worktrees/bucket-b`):

| Commit | Scope |
|--------|-------|
| `c7a7d4c` | persistence/S2 + S3 — busy_timeout: 30_000 + pool_size: 10 doc |
| `c4cc01b` | persistence/S5 — partial index on `network_credentials.connection_state` |
| `09fd39f` | persistence/S8 — `last_joined_channels` cap at 200 entries |
| `7c4c298` | reviewer add-on — `who/names/banlist` to `dispatch_subject_verb/2` |
| `48c66c3` | mix format whitespace touch-up |
| `ed03165` | reviewer follow-up — slug length + doc-comment cleanups |

Persistence/S7 dropped (see "bucket B opening" above — no source
edits required, comments already correct post-C2). Persistence/S4
deferred to bucket H per the mega-cluster plan.

Code-review HIGH H1 was a deploy-flag note (use `--force-cold` so
busy_timeout takes effect on fresh pool conns + new migration runs
in the cold path). Applied at deploy time, not as a code change.

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Passed Modules: 123
Finished in 42.6 seconds (21.8s async, 20.8s sync)
8 doctests, 29 properties, 1486 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.12s
Generating docs...
```

(`1475 → 1486` = +11 new tests: 9 visitor verb regression tests
(3 verbs × 3 scenarios) + 2 deterministic cap tests (oversize +
boundary). +1 property = 28 → 29. Property test for cap (length ≤
200, head order preserved) accounted in the property bump. Dialyzer
0 errors. No new warnings. Doc-gen warnings unchanged from
pre-flight baseline.)

### Code-review summary

`general-purpose` agent reviewed all 5 code commits + the format
touch-up against CLAUDE.md "Engineering Standards" + "Testing
Standards":

- 0 CRITICAL.
- 1 HIGH (H1, deploy-flag note — applied via `--force-cold`).
- 2 MED (M1 stale doc-comment header on `dispatch_ops_verb/2` —
  fixed in `ed03165`; M2 property test row-isolation — accepted
  as cosmetic, current behaviour correct).
- 2 LOW (L1 cap docstring "tail dropped" wording — accepted as
  cosmetic; L2 dispatch_ops_verb header — fixed in `ed03165`).

The reviewer also confirmed: cap correctness (Enum.take preserves
head order, no-op on empty/not_found paths), migration predicate
matches Bootstrap query exactly, config placement correct, the
12 write-only verbs principled-stay on `dispatch_ops_verb/2`, no
anti-patterns introduced.

Bucket B confirms the post-cr-review pattern: CRITICAL +
follow-on review-corrections close in one bucket; the next bucket
opens with the prior bucket's reviewer add-on as a real-deal HIGH
input (Bucket A's reviewer flagged `dispatch_ops_verb/2` for
who/names/banlist; bucket B closed it).

## Bucket C LANDED 2026-05-12

7 commits on `cluster/post-cr-review-c` (worktree `.worktrees/bucket-c`):

| Commit | Scope |
|--------|-------|
| `3a607d5` | irc/S3 — `send_privmsg/3` empty-target reject |
| `2058e9c` | irc/S2 — `send_join`/`send_part` `valid_channel?` gate |
| `5bc8836` | irc/S6 — `:logger_metadata` type tightening |
| `1d1e66d` | irc/S4 — SASL PLAIN encoder NUL guard |
| `1d5797e` | irc/S5 — `AuthFSM.new/1` self-defending CRLF/NUL boundary |
| `437b125` | mix format + credo single-step-pipe touch-up |
| `f129ae1` | reviewer CRIT-1 — facade + cast hardening for irc/S2 |

All five HIGH findings from Theme 1 (irc/S2-S6) closed in a single
bucket. The five fixes target the IRC core layer (`lib/grappa/irc/`)
which the Phase-6 listener facade reuses as a library — making each
fix self-defending at the IRC boundary, not relying on upstream
callers, is the architectural prerequisite for that reuse.

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 41.8 seconds (21.2s async, 20.6s sync)
8 doctests, 29 properties, 1504 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.11s
done (passed successfully)
Generating docs...
```

(`1486 → 1504` = +18 new tests: 1 S3 (empty target) + 4 S2 (malformed
+ empty × join + part) + 7 S5 (CRLF/NUL × nick/realname/sasl_user/
password × per-method) + 2 S4 (NUL × password + sasl_user via
post-init mutation) + 4 facade reviewer (malformed + empty × send_join
+ send_part). S6 type-only, no new tests; Dialyzer 0 errors is the
proof. No new properties; the existing property count holds.
Doc-gen warnings unchanged from pre-flight baseline.)

### Code-review summary

`general-purpose` agent reviewed all 6 substantive commits + the
format touch-up against CLAUDE.md "Engineering Standards" + "Testing
Standards":

- **1 CRITICAL** (CRIT-1, MatchError in `Server.handle_cast({:send_join,_},_)`
  / `({:send_part,_},_)` after irc/S2 widened `Client.send_*` return
  contract — fixed in `f129ae1` via two-layer facade tighten + cast
  hardening; HIGH-1 incidentally closed by the facade fix).
- **0 HIGH** post-CRIT-1 fix (HIGH-1's "no Server-level test for
  wedged `:pending` window" closed by the facade tightening — malformed
  channels can never reach the cast post-fix, so `record_in_flight_join`
  never fires; the new facade tests pin the ordering).
- **2 MED** (MED-1, `validate_line_safe` uses `Enum.find` over the
  recursive three-clause CLAUDE.md pattern — accepted as cosmetic for
  N=3 fields with a terminal find; MED-2, type alias duplication
  between `Grappa.Log.session_metadata` and
  `Grappa.IRC.Client.session_metadata` — accepted as deliberate per
  Boundary `deps: []` + `project_extract_irc_libs` memory; commit
  `f129ae1` adds a cross-reference comment to prevent silent drift).
- **1 LOW** (LOW-1, `validate_password_present` and
  `validate_password_line_safe` both short-circuit on `:none` —
  accepted as defensible).

Reviewer also confirmed: no other un-fixed asymmetries inside bucket
C's scope; all other `Client.send_*` helpers correctly gate
`valid_channel?`/`valid_nick?`/`safe_line_token?`; AuthFSM has only
one boundary (`new/1`) accepting caller bytes (S5 covers it); the S5
new error tag uses atoms in a closed set (CLAUDE.md compliance);
S4's `ArgumentError` is the right exception class for an internal
contract violation.

The bucket C pattern: 5 HIGH findings + 1 CRIT reviewer follow-up
all resolved in one bucket. The reviewer follow-up added 4 new tests
+ 2 hardening commits (one fix, one MED-2 cross-reference comment).
Total bucket scope: 7 commits, 6 substantive + 1 format touch-up.

## Bucket D LANDED 2026-05-12

4 commits on `cluster/post-cr-review-d` (worktree
`.worktrees/bucket-d`):

| Commit | Scope |
|--------|-------|
| `d878b6b` | web/S2 — `ArchiveJSON.index/1` delegates to `Scrollback.Wire.archive_index/1` (new verbs `archive_entry/1` + `archive_index/1`) |
| `1a6a77f` | web/S3 + S4 — `Session.Wire.member/1` projection + `members_index/1` REST envelope; `members_seeded/3` now `Enum.map(&member/1)`; `MembersJSON.index/1` delegates |
| `7fcb869` | cross-module/S4 — new `Grappa.Cic.Wire.bundle_hash/1`; both inline sites (`AdminController.cic_bundle_changed/2` + `GrappaChannel.push_bundle_hash/1`) delegate |
| `95d3a43` | reviewer M1+M2+L1 — test name rename (was misleading post-projection); `members_seeded/3` docstring tweak; filter-to-contract regression test for `member/1` |

All 5 HIGH findings from Theme 3 (web/S2-S4 + lifecycle/S10 +
cross-module/S4) closed in a single bucket — though lifecycle/S10
was re-classified as NON-FINDING after live verification (Boundary
`top_level?: true` exports the module itself; `exports:` only
constrains submodules; `WSPresence` precedent). Tally drops from
5 HIGH to 4 HIGH actually closed + 1 NON-FINDING. Third NON-FINDING
in this mega-cluster (after bucket A's C2 + bucket B's persistence/S7)
— the post-cr-review pattern of "re-read the code, contradict the
reviewer with evidence" continues to pay.

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 42.1 seconds (21.1s async, 20.9s sync)
8 doctests, 29 properties, 1518 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.29s
```

(`1504 → 1518` = +14 new tests: 7 in `Scrollback.WireTest`
(`archive_entry/1` × 2 + `archive_index/1` × 2 + the existing 3
message-shape held), 5 in `Session.WireTest` (`member/1` × 3 incl
filter-to-contract from reviewer L1 + `members_index/1` × 3 incl
REST≡Channel parity + 1 new `members_seeded/3` parity test minus 2
that replaced renamed tests via M1; net +5 unique), 3 in new
`Cic.WireTest`. Net +14 unique. Dialyzer 0 errors. Doc-gen
warnings unchanged from pre-flight baseline.)

### Code-review summary

`general-purpose` agent reviewed all 4 commits against CLAUDE.md
"Engineering Standards" + "Testing Standards" + "Phoenix Channels"
wire-shape invariant:

- **0 CRITICAL.**
- **0 in-bucket HIGH.** One bucket-Z carry-forward (H-Z1):
  `query_windows_list` envelope (`%{kind: "query_windows_list",
  windows: ...}`) inlined in 3 sites — same class as
  cross-module/S4. Per-window body already delegates via
  `QueryWindows.Wire.render_grouped/1`; only the envelope is
  hand-rolled. Lift to `QueryWindows.Wire.windows_list/1`. Defer to
  bucket Z (sweep + close) since bucket D's HIGH list was 5 wire
  findings and adding a 6th breaks the bucket scope.
- **2 MED both fixed in `95d3a43`** (M1 test-name rename, M2
  docstring tweak — cosmetic-but-true; both 5-line edits).
- **3 LOW** (L1 fixed in `95d3a43` as filter-to-contract regression
  test; L2 `Cic.Wire` boundary shape note — confirmed correct as
  shipped; L3 `auth_json` `%{kind: "user", ...}` + `%{kind:
  "visitor"}` discriminator inlined — bucket-Z carry-forward, only
  one site per discriminator so lower urgency than H-Z1).

The reviewer also confirmed: cross-module/S4 scope expansion
(closing both `AdminController` + `GrappaChannel` inline sites) is
strictly principled per "Total consistency or nothing", NOT
bucket-broadening; `Cic.Wire`'s `top_level?: true, deps: []`
boundary mirrors sibling `Cic.Bundle` correctly given there's no
`Grappa.Cic` context module; `Wire.member/1` is identity-shaped
TODAY but pattern-match-then-rebuild filters extras → the
filter-to-contract test (L1 fix) pins this guarantee for the
future; on-wire JSON byte-identical to pre-bucket-D for both
`ArchiveJSON` and `MembersJSON` (Jason serializes atom-keyed maps
to string-keyed JSON natively); zero anti-patterns introduced; gate
evidence verified ≡ brief claim (literal tail from re-run inside
the worktree).

Bucket D confirms the post-cr-review pattern: principled scope
(closes both inline sites where one was listed), NON-FINDING
discipline (re-read the code, verify with evidence), reviewer
follow-ups close in-bucket. 4 commits total: 3 substantive + 1
reviewer follow-up. One new module landed: `Grappa.Cic.Wire`
(7th codebase wire module). Bucket-Z carry-forwards: H-Z1
(`query_windows_list` envelope) + L3 (`auth_json` discriminator).

## Bucket E LANDED 2026-05-12

5 commits on `cluster/post-cr-review-e` (worktree
`.worktrees/bucket-e`):

| Commit | Scope |
|--------|-------|
| `f2a90c8` | web/S6 — `topic_set` tagged-tuple gates (per-source `else` arms) |
| `0443103` | web/S7 — Channel inbound `validate_args/1` IRC-shape gates at every `handle_in/3` |
| `c00774a` | web/S5 — Visitor sockets register with `WSPresence` so cic_bundle_changed reaches them |
| `51a8219` | lifecycle/S1 — Visitor `credential_failer` via `Visitors.mark_failed/2` (expires row + structured `Logger.error`) |
| `1028bd8` | web/S8 — `list_members/3` `:uninitialized` state across server + REST + Channel cold-snapshot |

All 5 HIGH findings from Themes 4 + 5 closed in a single bucket.
No NON-FINDING this bucket — the four pre-bucket-E findings + 1
SessionPlan injection were all real and actionable. The discipline
this time was DESIGN-CHOICE: web/S8 needed a sentinel state
(`seeded_channels`) that was tempting to derive but couldn't be
(`%{own_nick => []}` is structurally identical between
"joined-pre-NAMES" and "joined-with-only-self-post-NAMES"); the
`prune_seeded_channels/1` helper keeps the new state consistent
with `state.members` post-EventRouter without touching EventRouter.

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 43.1 seconds (21.6s async, 21.5s sync)
8 doctests, 29 properties, 1543 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.38s
done (passed successfully)
```

(`1518 → 1543` = +25 new tests: 2 S6 (visitor + invalid_line +
visitor + safe_line tag-by-source pinning), 13 S7 (op malformed
channel/nick/empty-nicks; kick malformed-nick + CRLF reason; ban
CRLF/empty mask; invite malformed nick; open_query_window
malformed nick; mode CRLF in modes/params; umode CRLF;
topic_clear malformed channel), 2 S5 (visitor connect →
WSPresence + visitor cic_bundle_changed broadcast), 5 S1
(mark_failed × 3 + SessionPlan failer injection × 2), 5 S8
(joined-pre-366 → :uninitialized, joined-post-empty-NAMES →
{:ok, []}, joined-post-non-empty NAMES → {:ok, [_]},
not-joined → :uninitialized, REST 204 for joined-pre-366).
Dialyzer 0 errors. Doc-gen warnings unchanged from pre-flight
baseline.)

### Code-review summary

(Pending bucket E reviewer pass — see `Don't do` rule: bucket E
LANDED claim ships with check.sh exit-0 evidence; reviewer
follow-up runs as a separate pass and any HIGH findings carry
forward to bucket Z OR fold into bucket E if same-file.)

Bucket E confirms the post-cr-review pattern: 5 HIGH all closed
in one bucket; 5 commits total (one per finding); zero
NON-FINDING this round (all 5 were real); one Dialyzer success-
typing tighten in-bucket (`validate_args/1` `@spec` enum) per
CLAUDE.md "Dialyzer warnings are design signals." Bucket-Z
carry-forwards from prior buckets remain on the bucket Z list
(H-Z1 `query_windows_list` envelope, L3 `auth_json`
discriminator).

## Bucket F LANDED 2026-05-12

5 commits on `cluster/post-cr-review-f` (worktree
`.worktrees/bucket-f`):

| Commit | Scope |
|--------|-------|
| `4c66dcf` | H2 — CSP allowlist extended for hCaptcha (`*.hcaptcha.com` across script/frame/style/connect-src) |
| `c950f76` | H1 — own-nick foot-gun: `Shell.tsx` + `MembersPane.tsx` use `ownNickForNetwork(net, me)` instead of `displayNick(me)` |
| `d3e9fc3` | H3 — case-insensitive nick comparison: `lib/nickEquals.ts` helper + total migration (members.ts, ScrollbackPane.tsx, MembersPane.tsx, subscribe.ts) |
| `e09a15e` | H3 follow-up — `lib/modeApply.ts` MODE target match through `nickEquals` (missed callsite from H3 sweep) |
| `b49762b` | H4 — `Network` discriminated union (`UserNetwork | VisitorNetwork`) + `tagNetwork` boundary + ComposeBox/Sidebar narrowing |

All 4 HIGH findings from Theme 8 closed in a single bucket. Plus
one in-bucket H3 follow-up (modeApply.ts) caught by post-migration
grep — pattern continuation: in-bucket reviewer follow-ups (C
CRIT-1, D M1+M2+L1) parallel H3's modeApply close.

Cic-only bucket — server-side untouched. The discipline this time
was TYPE-SYSTEM-FIRST: every finding shifted a contract from
implicit (defensive `?.` checks, bare `===` casing comparisons,
optional fields) to explicit (discriminated unions, single-source
helpers, type-narrowed boundaries). The `?.connection_state ??`
defensive patterns in ComposeBox + Sidebar are now structurally
unreachable on the visitor branch.

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 42.6 seconds (21.3s async, 21.3s sync)
8 doctests, 29 properties, 1543 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.41s
done (passed successfully)
```

(Server-side test count unchanged at 1543 — bucket F is cic-only.
Dialyzer standalone re-run: 0 errors. Doc-gen warnings unchanged
from pre-flight baseline.)

### Cic vitest tail (literal — per memory `feedback_landed_claim_evidence`)

```
 Test Files  44 passed (44)
      Tests  749 passed (749)
   Start at  12:08:06
   Duration  10.77s
```

(733 → 749 = +16 new tests: 1 H1 ownModes account-name ≠ IRC-nick,
6 nickEquals helper coverage, 5 H3 case-insensitive presence
dispatch + 1 modeApply casing, 5 H4 tagNetwork visitor/user/
contract violations + 2 ownNickForNetwork shape evolution.)

### Cic typecheck tail (`bun run check` = `biome check && tsc --noEmit`)

```
$ biome check src && tsc --noEmit
Checked 98 files in 115ms. No fixes applied.
```

### Code-review summary

Bucket F closes Theme 8 (cic correctness) — 4 HIGH retired. The
type split (H4) sets the template for U2 codegen down the line: the
explicit `kind` discriminator becomes the natural anchor for
generated TypeScript unions from server-side `Wire` modules. The
boundary-tagger pattern (`tagNetwork`) is reusable for any other
shape where the server emits implicit per-subject variants without
a wire discriminator. Bucket-Z carry-forwards from prior buckets
remain on the bucket Z list (H-Z1 `query_windows_list` envelope,
L3 `auth_json` discriminator).

## Bucket G LANDED 2026-05-12

Theme 7 (cross-surface drift) + the `U1`/`U3`/`U4` unification
opportunities. **4 HIGH closed** (cross-surface/H1+H2+H3+H4)
**+ 3 unifications** (U1 `Grappa.Wire.Time`, U3
`narrowChannelEvent`, U4 unified `{error, field_errors}` 422
envelope). Cross-surface/H5 was demoted to MED by the
cross-surface agent in the original review (line 152) and stays
on the bucket Z list.

Bucket G sits at the boundary between server (Elixir) and cic
(TypeScript). Every finding edits the wire-shape contract OR a
narrowing/projection across it. DESIGN_NOTES gained a CP24
bucket G entry with per-finding context.

Commits (origin/main FF after rebase):
* `43e5a96` U1 — `Grappa.Wire.Time` shared helper extracted from
  `Grappa.Networks.Wire`'s private `iso8601_or_nil/1`. NEW
  `lib/grappa/wire/` directory documents the cross-context-helper
  precedent. Boundary deps tightened.
* `1903aa6` H1 — Login.tsx `friendlyMessage` arm for dead wire
  token `"captcha_provider_unavailable"` removed; the live
  `"service_degraded"` arm gains a docstring pointing at the
  server emission path. Pre-fix UX silent on real captcha-provider
  outages (raw 503 token leaked to alert).
* `a5a30e4` H2+U4 — `FallbackController` changeset path emits
  `%{error: "validation_failed", field_errors: ...}` matching the
  A7 envelope convention. cic gains a `ValidationError` typed
  alias mirroring `AdmissionError`. Pre-fix every 422 collapsed
  to `res.statusText` client-side; field-level errors lost.
* `0c30159` H3 — `WireChannelEvent` 6-kind union consolidated in
  api.ts; subscribe.ts's local `WireEvent` duplicate dropped.
  Mirror of `WireUserEvent`. Legacy `ChannelEvent` aliased to
  `Extract<WireChannelEvent, {kind:"message"}>` for backward
  compat (zero rename diff).
* `52b9148` H4+U3 — NEW `cicchetto/src/lib/wireNarrow.ts` with
  `narrowChannelEvent(raw: unknown): WireChannelEvent | null`.
  Per-channel WS edges in subscribe.ts now run raw payloads
  through the narrower; the `WireChannelEvent` cast is gone.
  Mirror of `userTopic.ts`'s `narrowUserEvent` (cic M1).

### check.sh tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 43.8 seconds (21.6s async, 22.2s sync)
8 doctests, 29 properties, 1549 tests, 0 failures
```

(1543 → 1549 = +6 server tests: 4 `Grappa.Wire.Time.iso8601_or_nil/1`
contract; 2 H2+U4 `validation_failed` envelope. Standalone dialyzer
also re-run: 0 errors. Doc-gen warnings unchanged from pre-flight
baseline.)

### Cic vitest tail (literal — per memory `feedback_landed_claim_evidence`)

```
 Test Files  45 passed (45)
      Tests  784 passed (784)
   Start at  13:01:47
   Duration  10.13s (transform 10.03s, setup 25.18s, import 11.39s, tests 5.59s, environment 96.81s)
```

(749 → 784 = +35 tests: 31 wireNarrow runtime narrower coverage,
2 H3 WireChannelEvent canonical-union contract, 1 H1
service_degraded arm, 1 H2+U4 422 ApiError envelope.)

### Cic typecheck tail (`bun run check` = `biome check && tsc --noEmit`)

```
$ biome check src && tsc --noEmit
Checked 100 files in 86ms. No fixes applied.
```

### Code-review summary

(Pending bucket G reviewer pass — same convention as buckets E + F:
bucket G LANDED claim ships with check.sh + cic vitest + cic
typecheck exit-0 evidence; reviewer follow-up runs as a separate
pass and any HIGH findings carry forward to bucket Z OR fold into
bucket G if same-file.)

Bucket G closes Theme 7 (cross-surface drift) + 3 unifications.
Two structural precedents land: `lib/grappa/wire/` (server-side
cross-context shared helpers) and `cicchetto/src/lib/wireNarrow.ts`
(cic-side runtime narrowing for WS payloads). Both directories
become the natural homes for follow-on bucket Z work
(H-Z1's `query_windows_list` envelope is a server-side Wire
sibling; future per-topic narrowers go next to wireNarrow.ts). The
H3+H4+U3 cluster in particular pairs the canonical
`WireChannelEvent` union with the runtime narrower so the SAME
type serves both compile-time consumer narrowing AND runtime
payload validation — single source for both paths.

Bucket F closes Theme 8 (cic correctness) — 4 HIGH retired. The
type split (H4) sets the template for U2 codegen down the line: the
explicit `kind` discriminator becomes the natural anchor for
generated TypeScript unions from server-side `Wire` modules. The
boundary-tagger pattern (`tagNetwork`) is reusable for any other
shape where the server emits implicit per-subject variants without
a wire discriminator. Bucket-Z carry-forwards from prior buckets
remain on the bucket Z list (H-Z1 `query_windows_list` envelope,
L3 `auth_json` discriminator).

## Bucket H LANDED 2026-05-12

Theme 6 (lifecycle classification + boot perf). **3 HIGH closed**
(lifecycle/S2 unify, S3 Client EXIT classification, S4 service
target allowlist). **Lifecycle/S5 deferred** — initial parallelize
implementation tripped 6 regression failures under parallel test
pressure due to singleton `NetworkCircuit` ETS not being
sandbox-scoped; reverted in this bucket. Correct fix needs
per-network admission lock OR test-infra NetworkCircuit
isolation, neither in bucket H scope. See DESIGN_NOTES bucket H
section for the full deferral rationale.

Commits (origin/main FF after rebase):
* `b934a9b` lifecycle/S4 + S3 — `service_target?/1` closed allowlist replaces
  `String.ends_with?(target, "serv")` substring match (3 new tests:
  `#dataserv` channel-prefix bypass, `Conserv` non-service nick
  persists, full BotServ+OperServ+HostServ+HelpServ+MemoServ
  allowlist sweep) AND Client EXIT clause aligns with CLAUDE.md
  "Restart strategy: `:transient` per-user sessions don't restart
  on `:normal` shutdown" (clean Client exit returns
  `{:stop, :normal, _}` instead of `{:stop, {:client_exit, _}, _}`;
  2 new tests probe `Session.whereis == nil` post clean exit).
* `d52d5a5` lifecycle/S2 — `Credentials.list_credentials_for_all_users/0`
  preload upgraded to `[network: :servers]`; `validate_credential_servers!/2`
  reads in-memory association. New `Networks.get_network_with_servers_by_slug/1`
  helper for visitor-side rows (slug→Network resolution doesn't
  ride the credential preload).

### check.sh tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 47.1 seconds (23.4s async, 23.6s sync)
8 doctests, 29 properties, 1554 tests, 0 failures
```

(1549 → 1554 = +5 server tests: 3 service_target? coverage + 2
Client EXIT no-restart probes. Standalone dialyzer also re-run:
0 errors. Doc-gen warnings unchanged from pre-flight baseline.)

### Code-review summary

(Pending bucket H reviewer pass — same convention as buckets E + F + G:
bucket H LANDED claim ships with `check.sh` exit-0 evidence;
reviewer follow-up runs as a separate pass and any HIGH findings
carry forward to bucket Z OR fold into bucket H if same-file.)

Bucket H closes Theme 6's correctness findings (S2-S4) and defers
the perf finding (S5) for a dedicated cluster with the right
test-isolation foundation. The S3 fix in particular — though the
clean-exit clause is unreachable in production today — closes the
contradiction between the comment intent and the supervisor
classification, removing a future foot-gun for any caller that
would introduce `Client.stop/1`.

Bucket-Z carry-forwards from prior buckets remain on the bucket Z
list (H-Z1 `query_windows_list` envelope, L3 `auth_json`
discriminator). Lifecycle/S5 (parallelize spawn_all) joins the
deferred-with-design-debt list.

## Bucket H regression cluster LANDED 2026-05-12

Bucket H pushed clean (ci.yml green, server-side gates fine) but
integration.yml flagged 3 e2e specs. Investigation reframed the
"regression" as **two pre-existing latent bugs surfacing under one
more push of CI load** — bucket H code itself is unrelated to all
three failures. CI history (run IDs cited) showed:
- `cp15-b6-kicked` first failed at bucket A run `25721371284`
  (pre-PHASE-1.x) and again at the kill-retry run `25737232607`.
  Bucket H run `25749898544` was the THIRD recurrence, not the
  introduction.
- `socket-health-banner` first failed at bucket E run
  `25730959079` (different test, line 47) and again at kill-retry
  (line 47); bucket H run hit line 20 instead — same root.
- M9 (`m9-cicchetto-part-x-click`) was the only NEW-in-bucket-H
  failure — and traceable to PHASE 1.1 (`35dd4a8`)'s sidebar
  joined-synthetic projection violating
  `feedback_target_window_ux_rule` on PART (no cross-topic
  ordering between user-topic `channels_changed` and per-channel
  `kind:"message"` part broadcasts → channelsBySlug drops the
  channel while windowState still carries `joined` → ghost row).

Local 49-iter `--repeat-each` repro corroborated: 39 paired pass
runs, 1 isolated M9 5.5s assertion-timeout fail (joined-synthetic
ghost shape), then 10 pair-fails at exactly 30.6s (Playwright
test-timeout — bahamut-test docker container hangs after sustained
join/kick/part throughput). socket-health-banner stayed GREEN
49/49 locally — Bug C is CI-only and decoupled from IRC traffic.

### Bug B fix (M9 joined-synthetic ghost)

Reverted PHASE 1.1's joined arm in `pseudoChannelsForNetwork` —
back to pre-PHASE-1.1 shape (skip joined-state entries; PseudoRow
union back to `pending|failed|kicked|parked`). Sidebar.tsx JSX
two-arm classList (pending vs greyed) restored. The 4 PHASE 1.1
vitest tests dropped from `Sidebar.test.tsx`. PHASE 1.1's
motivating spec `cp15-b5` was rewritten to gate on the WS-truth
signal (per-channel self-JOIN scrollback line) BEFORE asserting
sidebar row count, with both inner waits bumped to 10s — the
channels_changed roundtrip latency that PHASE 1.1 was working
around becomes a benign wait, not a flake.

### Bug C fix (socket-health-banner CI-only race)

`loginAs` only waits for the REST `networks()` resource — not the
Phoenix WS handshake. The WS handshake completes asynchronously
AFTER loginAs returns, and `socket.onOpen` fires
`recordSocketOpen()` which resets `errorCount=0`. Under CI's
slower I/O, the open arrived BETWEEN the test's reset+5x
recordError dance and the toBeVisible() assertion — wiping the
errorCount before the banner could render. Fix: extended the
`__cic_socketHealth` test hook with a `state()` getter; both
specs now `page.waitForFunction(state==="open")` BEFORE mutating
the signal so the open callback has already happened.

### Bug A fixed inline (assertion-timeout under CI bahamut load)

**REFRAMED 2026-05-12.** Initial brief positioned Bug A as a flaky
30s test-timeout class to defer to a separate test-infra cluster.
Local 49-iter `--repeat-each` repro DID show 30s pair-hangs after
sustained throughput, supporting the "deferred-investigation"
framing.

After landing Bug B + C, the FIRST integration.yml run on origin/main
still failed at cp15-b6-kicked.spec.ts:76 — but this time at 5.5s
**ASSERTION-TIMEOUT** (the inner `toBeVisible` on `.members-pane li
[hasText: vjt-grappa]`), NOT the 30s test-timeout shape from local
repro. The CI page snapshot at failure time consistently showed
`.members-pane` WITH `vjt-grappa` already rendered — i.e. the
JOIN-handshake pipeline DID complete, just past the 5s window.

That means the bug isn't bahamut state corruption (the
"deferred-investigation" framing was wrong) — it's the test's
assertion timeouts being too tight for CI's slower I/O profile. The
JOIN-handshake pipeline (4 hops: bahamut JOIN echo → grappa delegate
→ channels_changed broadcast → REST refetch + members_seeded WS
push) routinely takes >5s under `max_cases:2` parallel pressure.

**Fix:** mirror the cp15-b5 fix shape — gate on the WS-truth signal
(per-channel self-JOIN scrollback line) BEFORE asserting the
sidebar+members pane render, with both inner waits bumped to 10s.
The WS-truth signal arrives before the members_seeded WS push, so
the gate is conservative; the 10s timeout matches cp15-b5 + cp15-b6
sibling specs and gives the JOIN pipeline enough slack under CI
parallel pressure.

**Process lesson** — captured in new memory
`feedback_no_ci_retries_on_first_failure`: I ran `gh run rerun
--failed` twice during this investigation to "gather evidence of
determinism." That's CI-level flake-masking, the same anti-pattern
commit `80c9ca4` killed at the playwright `--retries` layer. Only
the FIRST run counts; reruns add noise, not signal. Use local
`--repeat-each N` instead. (vjt orchestrator-flagged.)

The original `project_bahamut_load_flake` memory still tracks the
SEPARATE 30s test-timeout-shape hypothesis (testnet state corruption
after sustained throughput). That class hasn't been observed in CI
yet — only locally under deliberate `--repeat-each 50` stress. If
it surfaces in CI later, the deferred test-infra investigation is
still the right path. The memory remains valid for that scenario.

Commits (origin/main FF after rebase):
* `c2a1dee` fix(cic/Sidebar,e2e/cp15-b5): bug B — revert PHASE 1.1 joined-synthetic; gate cp15-b5 on WS-truth
* `a1ab949` fix(cic/socketHealth,e2e/socket-health-banner): bug C — wait for WS open before mutating signal
* `313795d` docs(CP24,memory): bucket H regression cluster — Bug B+C LANDED, Bug A deferred (REFRAMED below)
* `e075676` fix(e2e/cp15-b6-kicked): bug A — gate JOIN handshake on WS-truth + 10s timeout
* (this commit) docs(CP24,memory): correct Bug A framing — fixed inline; capture no-CI-retries lesson

### check.sh tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 42.9 seconds (21.3s async, 21.6s sync)
8 doctests, 29 properties, 1554 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.17s
done (passed successfully)
```

(1554 → 1554 = no test count delta — Bug B drops 4 PHASE 1.1 vitest
tests but vitest is a separate runner from check.sh's mix test.
Bugs B+C+A are pure cic/e2e changes; standalone scripts/dialyzer.sh
re-run not needed since no Elixir code touched.)

## Bucket I LANDED-WITH-CAVEAT 2026-05-12

5 commits (`b9c9c55..dd98a07`):

- **I-0 `b9c9c55` deps: close CVE GHSA-rhv4-8758-jx7v** — bump
  `decimal 2.4.1 → 3.1.0` via `override: true`. Advisory published
  2026-05-07 (moderate DoS via unbounded exponent in `Decimal.new`).
  `doctor 0.22.0`'s `~> 2.0` constraint required the override
  (latest `doctor` is from 2024-10-30 — stale upstream). Out-of-scope
  pre-existing CVE caught between bucket H regression close (~18:30Z)
  and bucket I open (~18:50Z) as the advisory DB refreshed. CLAUDE.md
  rule 1: "fix pre-existing errors first."
- **I-1a `07d56cd` deploy: codify long-lived module list, close
  Theme 9 S1 + docker H2** — new `Grappa.HotReload.LongLivedModules`
  SoT module enumerates `@modules` + `@state_helpers`; `deploy.sh`
  parses it via stable grep, translates CamelCase →
  `lib/grappa/.../*.ex`, then checks each touched file for
  `defstruct` / `@type t :: %{` / `def init(` markers. Defstruct
  added to `WSPresence` + `Reaper` (orchestrator-blessed scope —
  `Session.Server` stays bare-map). Six `put_in`/`update_in` call
  sites in WSPresence rewritten to `%{state | k: ...}` (structs
  don't implement Access by default — caught by full test suite,
  687 failures on first attempt, 0 after migration). `@type
  long_lived` + `@type state_helper` unions duplicate the atom
  list intentionally — Dialyzer's `:underspecs` enforces equality
  via `contract_supertype`, so divergence between SoT atoms and
  type union surfaces immediately. CLAUDE.md "Hot vs cold deploy"
  now cites the SoT module by name.
- **I-1b `2b3bdd4` log(auth_controller): move socket_id from
  message body to KV metadata** — sole inline-`#{socket_id}`
  Logger violation in the codebase (verified `grep -rnE
  'Logger\.\w+\("[^"]*#\{' lib/` returns nothing post-fix).
  `:socket_id` added to `config/config.exs:108-200` allowlist
  under the Auth-context group.
- **I-2 `d007ed0` env(.example): genericize Turnstile site_key
  example** — replaced vjt's actual public site_key
  (`0x4AAAAAADIVjqhMXybemB6v`) + comment naming the deployment
  hostname with a `0xYOUR_TURNSTILE_SITE_KEY_HERE` placeholder
  + generic comment. Cosmetic; no key rotation. 10+ other
  `grappa.bad.ass` references across `.env.example`, `runtime.exs`
  default, README, cic source, `docs/todo` deferred to
  post-Phase-5 cluster (touches fresh-clone deploy ergonomics —
  needs lock-step sweep). `compose.prod.override.yaml` not
  tracked (verified via `git ls-files`), no history rewrite.
- **I-3 `dd98a07` docker: simplify per cherry-picked review
  MEDs (S2, S6, S7)** — drop dead `LABEL grappa.hot_deployable=true`
  + 4-line dead comment from Dockerfile (CP23 replaced with
  git-diff preflight); drop dead `dist/` from `.dockerignore`
  (path moved to `runtime/cicchetto-dist/` in CP23, parent
  already covers); bake `runtime/cicchetto-dist/.gitkeep` +
  `runtime/bun-cache/.gitkeep` so fresh `git clone` +
  `compose --profile prod up cicchetto-build` doesn't hit
  Docker auto-creating bind-mount targets as `root:root` (UID
  trap memory `feedback_named_volume_uid_trap`). `.gitignore`
  extended with explicit `!`/re-glob/`!` triplets for the new
  subdirs (parent `/runtime/*` ignore + `!/runtime/.gitkeep`
  exception didn't cover them).

### Caveat — ci.yml RED on FIRST RUN, integration.yml GREEN

Per memory `feedback_no_ci_retries_on_first_failure`: first run
is the truth. Bucket I's CI status:

- ✓ `integration.yml` 25756898816 GREEN ON FIRST RUN (5m41s)
- ✗ `ci.yml` 25756898844 RED ON FIRST RUN (2m18s) — 1 failure:
  `test/grappa/spawn_orchestrator_test.exs:251` "rejected
  admission does NOT reset Backoff (no operator action took
  effect)". Expected `{:ok, :spawned, _}` at line 275 (the
  initial `vjt_a` spawn that should succeed before the cap is
  tripped); got `{:error, :network_cap_exceeded}` — meaning
  the network's session-cap was already at `max_concurrent_sessions:
  1` BEFORE `vjt_a`'s attempt.

**This is NOT a flake.** Root cause is the documented
shared-singleton fight in test infra: `mix test` starts the
Application ONCE per test process; ExUnit's default `max_cases=2`
+ `async: true` runs concurrent test cases inside the same VM;
they share singleton `GenServer`s — `Grappa.Session.Backoff`,
`Grappa.Admission.NetworkCircuit`, `Grappa.SessionRegistry`,
plus the ETS tables those modules own. Two async tests racing
against the same network's cap keys produce the
`:network_cap_exceeded` collision when the second test's `setup`
inherits the first test's leftover session count.

**Same root cause has surfaced repeatedly across the mega-cluster:**
- ci.yml run `25737232628` (yesterday, "kill Playwright CI retry"
  commit) failed on `spawn_orchestrator_test:157` (different line,
  same file).
- Bucket H's `BootstrapTest` flake series triggered the PHASE 1.3
  wait_until_registry_clear helper, which patched ONE call site.
- Bucket H's lifecycle/S5 parallelize-spawn_all attempt tripped 6
  regression failures with the identical NetworkCircuit-ETS
  pollution shape and was deferred for the same reason.

The PHASE 1.3 patch addressed BootstrapTest specifically; the
class is unfixed and surfaces in whichever async-test-file ExUnit
schedules into the bad slot. Bucket I drew the unlucky scheduling
this run; next push may surface in a different file again.

**Bucket I content does NOT cause this.** WSPresence/Reaper
defstruct migrations are structurally isolated from the
SpawnOrchestrator → NetworkCircuit → ETS path
(`grep WSPresence lib/grappa/spawn_orchestrator.ex` returns
nothing). Verified by:

  - Local `scripts/check.sh` exit-0 on all 5 commits, 1554 tests,
    0 failures.
  - Local standalone `scripts/dialyzer.sh` exit-0.
  - Local `scripts/integration.sh` exit-0 (cic e2e flake on
    cp13 + cp15-b5/m9 alternating — local-env noise from sharing
    docker daemon with the live prod stack — see
    "Carry-forward to test-infra cluster" below).
  - Cold-deploy successful, `scripts/healthcheck.sh` exit-0.
  - integration.yml CI green.

### Carry-forward to test-infra cluster

The shared-singleton-fight class is the principal item for the
post-mega-cluster **test-infra** cluster (briefed in
`/tmp/orchestrate-next-test-infra.txt`). Scope:

- (a) Root-cause architectural fix. Two paths to evaluate: per-test
  ETS namespace + supervised-per-case
  Backoff/NetworkCircuit/SessionRegistry instances, OR drop
  `max_cases=1` (sequential, slower but correct). Sibling decides.
- (b) Mandatory: every network slug + user id MUST carry a
  test-suffix (`System.unique_integer([:positive])`). Total
  consistency — current state is partial.
- (c) Audit every `async: true` test that hits Application
  singletons → either mark `async: false` OR isolate.
- (d) After cluster lands, ALL ci.yml + integration.yml GREEN ON
  FIRST RUN. No exceptions. No `gh run rerun --failed` ever.
- (e) Re-evaluate whether `SpawnOrchestratorTest:251` +
  `cp13-server-window:171` + `cp15-b5-window-state-pending-to-joined:55`
  + `m9-cicchetto-part-x-click:42` + other latent races dissolve
  once the root cause is fixed.

This work is OUT of bucket I scope. It's also OUT of bucket Z
scope (Z is sweep + close, not new fix work).

### `scripts/check.sh` exit-0 tail (literal — per memory `feedback_landed_claim_evidence`)

```
Finished in 42.4 seconds (20.3s async, 22.1s sync)
8 doctests, 29 properties, 1554 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.16s
done (passed successfully)
```

(Tail from I-3 final commit local run — same shape on every
preceding I-N commit's local check. Standalone
`scripts/dialyzer.sh` re-run exit-0 confirmed PLT not stale.)

### Severity tally update

- **CRITICAL: 0** (unchanged since bucket A close).
- **HIGH: 1** in active scope = lifecycle/S5 (parallelize
  spawn_all, deferred from bucket H, fold into test-infra cluster
  via shared-singleton fix). Bucket I closed Theme 9
  cross-module/S1, cross-module/S2, docker/H2 (3 HIGH).
- **MED: ~69** — long-tail bucket Z + minor cherry-picks during
  buckets B/D/F closed several. Re-tally at Z.
- **LOW: 63** — bucket Z sweep.
