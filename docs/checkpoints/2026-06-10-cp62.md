---
status: closed
---

# CP62

Previous: cp61 (uploads-2 cluster shipped). This session (same day,
post-cluster dogfood saga): **the video pipeline actually works on
prod now** — three stacked latent failures found and fixed live with
vjt's iPhone as the test rig.

## The debug chain (each fix exposed the next)

1. **Symptom**: instant "too large" on a 1'44" video, no transcode
   phase. Transcode-failure reason was console.warn-only — invisible
   on iOS Safari, the dogfood platform. Fixed: reason rides the error
   UI (`Video processing failed (<reason>); the original is too
   large…`), `TranscodeError.unsupported` gained `detail` incl.
   mediabunny `discardedTracks` codes (`2e38f96`).
2. **With diagnostics visible**: "unreadable video metadata" → the
   `<video>` blob: duration probe was **CSP-blocked** (no `media-src`
   directive → fell back to `default-src 'self'`). Fixed: `media-src
   blob:` + `worker-src 'self' blob:` (mediabunny spawns codec
   workers from blob: URLs — would have been the next block)
   (`6f3327c`). e2e never saw it: nginx-test carries NO CSP — parity
   now todo High. SW-precached shell pins old CSP headers → bundle
   bump (`1346f96`, probe logs MediaError code — code 4 is the CSP
   signature) rotated the SW so installed PWAs refetch.
3. **With transcode working**: generic "network error" on upload →
   **host m42 nginx `client_max_body_size 4m`** (vhost
   `irc.openssl.it`, hand-managed, NOT in repo) cut every >4MiB body
   at the edge; conn reset mid-stream = XHR "network error". Fixed
   live: 4m → 160m (backup at /root/irc.openssl.it.bak-20260610 —
   NOTE: backups must NOT live in sites/, the wildcard include eats
   them → duplicate upstream). THIRD transport ceiling under the
   policy caps (Plug 8MB→128MiB `6e57a8d`, jail nginx 100m→160m,
   host 4m→160m). Transport stack now: host 160m → jail 160m →
   Plug 128MiB → admin caps (policy).

## Also shipped today (pre-saga)

- Bitrate ceilings (`videoPolicy.pickEncodeBitrate`): 720p ≤4Mbps /
  480p ≤2Mbps — the budget used to FILL the cap (104s @ 100MiB cap →
  95MiB output; now ~52MiB) (`2e38f96`).
- Upload-row CSS: filename ellipsized + 25% cap, buttons
  flex-shrink:0 (`16cc1e3`, `bb673e2`).
- Docs sweep: prod = m42 jail everywhere, "Docker prod" purged,
  migrations-bullet contradiction fixed, CLAUDE.md dev-cycle step 3
  rewritten (`e814832`).

## Layer 4 (post-/clear session, same day): playback

vjt retest: upload WORKED (transcode + store + 🎬 link, healthy
faststart H.264/AAC container verified by ffprobe) but the video
didn't PLAY. Two delivery defects, both fixed + deployed
(`eef7075`..`7f44f43`, cold deploy 14:22 + jail snippet install):

1. **No Range support**: `GET /uploads/:slug` answered `Range:` with
   200 full body; iOS Safari hard-requires 206 to play media at all.
   New `GrappaWeb.ByteRange` (RFC 9110 single-range parser,
   property-tested) + controller 206/416/200 wiring, zero-copy
   `send_file/5`. Review hardening: zero-size-file crash path,
   `+`-signed positions, cacheable-416, mirror-oracle properties all
   fixed (`79d70b5`).
2. **CSP regression from layer 2's own fix**: `media-src blob:`
   REPLACED the default-src fallback → self-hosted media lost
   'self'; direct-nav media documents are governed by the response's
   CSP. Now `media-src 'self' blob:` + the general lost-'self' rule
   hoisted top-of-snippet (frame-src = documented exception).

Live-verified on the original upload URL: 206 + content-range +
accept-ranges + `media-src 'self' blob:`. DESIGN_NOTES entry has the
altitude decision (BEAM-side vs nginx X-Accel). e2e todo extended:
CSP-parity run should also pin a ranged 206 through nginx.

## Also shipped (post-/clear session): network-error copy (task 1)

`73871d4` + `3609b5b` (merged, cic bundle deployed 14:46 hash
bOe-GbC7): `friendlyErrorMessage` network case now threads the
attempt's last progress event. Three honest branches: progress never
OBSERVED → generic copy (litterbox has supportsProgress: false —
claiming "no bytes" there would lie); observed 0 → "no bytes were
sent (network or server unreachable)"; observed >0 → "connection
dropped (12 of 52 MB sent)". Review fixes folded: null≠0 split,
mbLabel "<0.1" floor + absorbed the two inline cap-format sites
(spelling now "N MB" everywhere), ProgressSnapshot deleted for
uploadHost's UploadProgress. Snapshot is per-attempt closure — retry
can't leak stale bytes-sent (pinned by test).

## CLOSED: playback saga (4 layers)

vjt confirmed 2026-06-10 (post-deploy): **the 🎬 video plays.**
Layer 1 invisible transcode error → layer 2 CSP media-src blob: →
layer 3 host-nginx 4m body cap → layer 4 Range/206 + lost-'self'.

## SHIPPED: metadata-strip cluster (#39 + skip-transcode gate)

Both tasks landed, reviewed, deployed, live-verified (commits
`4b0f189..ee7d17b` + rc.d fix, prod cold deploy 22:21 + rc.d
restart 22:25). Closes the iOS-picker "double processing"
discussion item: privacy = server guarantee, transcode = pure
performance.

- **B (server strip)**: `Grappa.Uploads.MetadataStrip` inside
  `Uploads.create/3`, fail-CLOSED → 422 `metadata_strip_failed`.
  Empirical tool verdicts: exiftool `-all=` lossless + clean on
  jpeg/png/gif/webp/apng AND mp4/mov (faststart preserved); webm via
  ffmpeg remux (exiftool can't write Matroska). `row.bytes` = stored
  (stripped) size. Hardened per review: allowlist env scrub (hostile
  bytes, exiftool CVE-2021-22204), `timeout -s KILL 30` on every
  tool call, controller-allowlist↔strip-map lockstep test.
  Committed GPS fixtures + `Grappa.UploadFixtures` marker contract.
- **A (cic gate)**: skip transcode iff DECLARED video/mp4 + demuxed
  Mp4InputFormat + avc track + overall bitrate ≤ pickEncodeBitrate
  (policy height) + 128k audio reserve + size ≤ cap. Sits before the
  capability gate (no-WebCodecs platforms skip too). Main bundle
  flat at 306.05kB — mediabunny stays lazy. Spec's transcode-always
  superseded at the source.
- **Prod war story**: pkgs installed + jail-verified, yet first live
  probe 422'd — rc(8) stock PATH has no /usr/local, the BEAM
  couldn't see exiftool. Fail-closed surfaced it in one log line.
  Fix: rc.d PATH prepend + OPERATIONS notes that deploy.sh does NOT
  reinstall rc.d (manual cp + restart). Restart hit the documented
  epmd node-name race; plain `service grappa start` recovered.
  Live probe post-fix: gps.jpg 574→194 bytes, markers `:nomatch`.
- Lifecycle tests moved to text/plain (passthrough keeps byte
  arithmetic exact); media coverage went UP with real fixtures
  (vjt challenged the swap mid-flight; rationale held, one
  concession: real-mp4 ranged-206 test added).

NOT verified yet: vjt dogfood upload from the iPhone (PWA needs the
refresh-banner tap — bundle rotated in the 22:21 cold deploy).

## NEXT SESSION (vjt 2026-06-10: "fix this first thing")

1. **Substrate-aware preflight** — `image_substrate?/1` in
   `lib/grappa/deploy/preflight.ex` is substrate-blind; Dockerfile
   diffs cold the JAIL (today's double prod restart). Todo Medium
   entry has the spec; full brief in /tmp/orchestrate-next.txt
   (next session reads it). Acceptance test: the fix's own deploy
   classifies HOT on the jail.
2. Then new things + known fixes: /start → menu = e2e CSP parity
   (todo High), fail2ban recheck (≥06-16), roadmap (voice TTS/STT →
   UI polish), REV carry-forwards. vjt picks.

## Still open

- vjt iPhone dogfood: refresh-banner tap + one upload, then
  `exiftool` the stored URL — final device-level strip verification.
- e2e CSP parity (todo High) + ranged-206-through-nginx spec.
- fail2ban /read-cursor exemption recheck ≥2026-06-16 (todo Low).
- Dev pi note: grappa dev stack found DOWN (idle is normal); dev
  image rebuilt with exiftool+ffmpeg for test.sh oneshots.
