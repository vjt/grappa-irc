---
status: active
---

# CP12

Previous: CP11 closed at 30 logical sessions / 4768 lines / `cluster/t31-cleanup` LANDED + DEPLOYED + closed
through S30. The full t31-cleanup arc (74-finding pay-down) shipped
and the cluster wrapped clean; main HEAD at `60e58d5` post-S30,
production stable. CP11 also captured the brainstorm-CLOSED for
`channel-client-polish` (21 features + cluster-wide rules,
vjt-blessed 2026-05-04). CP12 picks up at the start of cluster
execution.

## State at checkpoint creation

- **Branch:** main @ `208558b` (post-S1 ff-merge of
  `cluster/channel-client-polish`).
- **Open clusters:** `channel-client-polish` (active — bucket S1 just
  LANDED). 13 buckets total per
  `docs/plans/2026-05-04-channel-client-polish.md`; bucket order is
  S1 → S2 → S3 → S4 → S5 → C1 → C2 → C3 → C4 → C5 → C6 → C7 → C8 → Z.
- **Worktree:** `cluster/channel-client-polish` at
  `/Users/mbarnaba/code/grappa/.worktrees/channel-client-polish` —
  branched from local main per CLAUDE.md cluster-first discipline.
  Will continue to live across all S/C buckets; cleaned at Z.
- **Tests:** server **946 tests / 0 failures / 7 doctests / 13 properties
  / 0 warnings**; cicchetto **24 files / 225 tests** + biome + tsc clean.
- **Gates:** `scripts/check.sh` exits 0 cleanly; standalone
  `scripts/dialyzer.sh` `Total errors: 0`; cicchetto bun-side fully
  green. Doctor closed in S1.4 (Admission.Config + Admission.Telemetry
  pre-existing 0% docs gap landed alongside lenient :failed triggers).
  Only residual warnings are the long-standing
  `Visitors.Reaper`/`Application` hidden-doc references — not gates.
- **Production:** `http://voygrappa.bad.ass` live; **NOT YET REDEPLOYED
  for bucket S1**. Deploy is an end-of-cluster step per the plan; per-
  bucket pushes go to origin/main but `scripts/deploy.sh` waits for Z.

## S31 — 2026-05-04 — channel-client-polish bucket S1 LANDED

S1 (T32 connection_state + verbs end-to-end): 5 plan tasks + 1
fold-forward fix + 1 README touch = 6 commits + 1 hygiene amendment.
HEAD `208558b`.

**Features closed:**
- `connection_state` enum (`:connected | :parked | :failed`) +
  `connection_state_reason` + `connection_state_changed_at` columns
  on `Networks.Credential`. Backfill of existing rows to `:connected`.
- `Networks.connect/1` + `Networks.disconnect/2` +
  `Networks.mark_failed/2` context functions with state-transition
  matrix (idempotency rules, `:user_parked` reject from `:parked`,
  `Session.send_quit/3` added to facade for explicit upstream QUIT
  before stop_session).
- `Credentials.list_credentials_for_all_users/0` filters
  `connection_state == :connected` so Bootstrap skips parked + failed.
- `PATCH /networks/:net` extension: accepts
  `{connection_state: "connected" | "parked", reason?}`. `:failed`
  rejected from public endpoint (server-set only). Spawn orchestration
  on `:connected` transition lives at the controller — admission +
  `Backoff.reset` + `Session.start_session/3` (same shape as
  Bootstrap's `spawn_with_admission`). `Wire.credential_to_json/1`
  exposes the three new columns to the client.
- `Session.Server` lenient `:failed` triggers — k-line (465
  ERR_YOUREBANNEDCREEP) + permanent SASL failure (904/906 marked
  permanent). Transient errors (timeouts, refused, dns, max-backoff)
  keep continuous reconnect with `:connected`. Per Decision C
  ("just be polite"). New `handle_terminal_failure/2` private fn.
- Cicchetto slash-command parser + dispatch for `/quit /disconnect
  /connect`. `/quit` nuclear flow uses `Promise.allSettled` across
  all bound networks → logout (reactive WS close + `/login` redirect
  via auth signal propagation). `/disconnect` surgical
  (active-window slug if no arg). `/connect` requires named slug.
  `patchNetwork` wrapper added to `cicchetto/src/lib/api.ts`.

**Plan amendment captured in commit `d94531c` body (S1.4) — needs
DESIGN_NOTES lift at bucket Z (Z.1):** the `credential_failer`
closure pattern injected via `Networks.SessionPlan.build_plan/4`.
Reason: `Session.Server` cannot call `Networks.mark_failed/2`
directly (circular Boundary dep + sync deadlock —
`mark_failed → stop_session` waits for the GenServer to exit while
the GenServer is the caller). Closure runs after `{:stop, :normal}`
via `Task.start/1`; `stop_session`'s whereis→nil makes it a no-op;
DB transition proceeds.

**Plan amendment captured in commit `76d5935` body (S1.2) — needs
DESIGN_NOTES lift at bucket Z (Z.1):** `Networks.spawn_session/1`
dropped from plan. `Networks.{connect,disconnect,mark_failed}` do
DB transition + PubSub broadcast + `Session.stop_session/2` only.
The spawn-after-`connect` path (admission + `Session.start_session/3`)
duplicated at `NetworkController` for `/connect` and at `Bootstrap`
for boot. Both already dep Admission cleanly. If a third caller
appears, extract a shared `Grappa.SessionSpawner` then.

**Fold-forward fix `ed0f6a9`:** the parser's `SlashCommand` union
had two arms with discriminator `"connect"` (one with `network`,
one with `error`), forcing `as SlashCommand` casts at returns and
`"error" in cmd` runtime shape inspection at the consumer.
Discriminated unions exist precisely to make compile-time
exhaustiveness possible. Fix: rename the error arm to
`"connect-error"` so each arm has a unique discriminator; drop
both casts; replace runtime checks with proper `case "connect-error"`.
Per CLAUDE.md "Atoms or `@type t :: literal | literal` — never
untyped strings for closed sets" + "Half-typed is worse than
untyped". Reviewer-finding flagged by orchestrator during S1.5
brief verification — sibling missed it on first pass.

**README touch (S1.6):** new "Slash commands (cicchetto)" section
listing every verb the compose box parses today (/me /join /part
/topic /nick /msg + /quit /disconnect /connect). Dedicated
`connection_state` model subsection (table + transition matrix).
PATCH `/networks/:net` row added to the REST surface table. Per
`feedback_readme_currency` per-bucket touch obligation.

**Pre-existing setupTests.ts fix folded into `7da4df1`:** Node 25's
built-in localStorage conflicted with jsdom's window.localStorage
in vitest 4.x, blocking the cicchetto test setup. Fix: `beforeEach`
installs a fresh in-memory `Storage` mock via `vi.stubGlobal`
(survives `vi.unstubAllGlobals()` from individual test files).
Test-infra-only fix (no production code changes); folded into the
S1.5 commit because it blocked the TDD step entirely. Worth
flagging because it's the kind of test-infra surprise that may
recur across S/C buckets — keep an eye on Node 25 + vitest 4.x
quirks for future cicchetto tasks.

**Out-of-cluster hygiene since S30 (carry-forward not previously
captured):** `a5dcc93` bootstrap fail-fast on networks without
enabled servers; `7e3a785` `docs(todo)` not-connected UX bug filed
for cluster scope; `e58477b` `.gitignore /.worktrees/`; `01e5ae5`
`docs(plan)` host rename `grappa.bad.ass` → `voygrappa.bad.ass`.

**Doctor pre-existing baseline closed in S1.4:** Admission.Config +
Admission.Telemetry @doc adds (3 public fns documented). check.sh
now exits 0 cleanly with "Doctor validation has passed!" Per
CLAUDE.md "Fix pre-existing errors first."

**Gates (literal tail):**
- `scripts/check.sh`: exit 0; only output is the long-standing
  `Visitors.Reaper`/`Application` hidden-module doc-link warnings
  (NOT gate failures — Doctor passed in same run).
- `scripts/dialyzer.sh`: `Total errors: 0, done (passed successfully)`.
- `cd cicchetto && bun run test`: `Test Files 24 passed (24) / Tests
  225 passed (225)`.
- `cd cicchetto && bun run check`: `Checked 55 files in 35ms. No
  fixes applied.`

**Worktree state:** `cluster/channel-client-polish` at HEAD `208558b`;
no rebase needed (origin/main was at `01e5ae5` with no commits
ahead of worktree branch point); ff-merged `01e5ae5..208558b` to
main; pushed `origin main` per `feedback_push_autonomy`. Worktree
branch retained for buckets S2+.

**Process notes (S31):**
- Sibling-implementer dispatch for S1.5 went via `general-purpose`
  subagent (sonnet). Worker shipped the feature but did NOT paste
  literal gate-tail evidence on first response — orchestrator
  re-ran independently per `feedback_reviewer_gate_evidence`. The
  reviewer-template upgrade language needs to be sharper for
  cicchetto-side dispatches; the brief mentioned gate-tail-paste as
  a MUST and the worker still skipped. Consider hard-stop
  enforcement in next dispatch.
- Discriminated-union review-finding caught on diff inspection
  (slashCommands.ts:40-41 + compose.ts:192,206). Worth keeping the
  "read the diff post-implementer-claim" muscle memory active —
  agents' summaries describe intent, not actual code shape.
- README-touch-on-cluster-branch discipline: orchestrator first
  edited README on main repo dir, caught it before commit, reverted,
  re-applied in worktree. Per plan S1.6: docs(readme) touch goes on
  the cluster branch so the merge picks it up — NOT a docs-only
  commit on main directly. Easy slip when both repos are mounted.
