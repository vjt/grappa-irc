---
status: active
---

# CP04

The active checkpoint. Append a `## Sn` section per session. Rotate (mark
this `status: done`, create `cp05`) when this file hits ~200 lines or
8 sessions.

CP03 closed at S11 / 290+ lines / 3 sessions — covered Phase 1 Task 8
(walking-skeleton: IRC parser → client → session → bootstrap), all
sub-tasks 8a-pre through 8d done + code review round-trip + merged to
main. CP02 closed at S8 covering Tasks 5–7. CP01 closed at S5 covering
Phase 0 + Tasks 1–4. See prior checkpoints for the back-story.

---

## State at checkpoint creation

Frozen at main commit `75e29b9` (`session+bootstrap: address code review
SHOULD-FIX items`). Working tree clean (CP04-open + S11 docs commit
about to land). Worktree `phase1-task8-session` has been merged via
fast-forward and can be pruned after the next deploy.

**Phase 1 progress (walking-skeleton plan):**
- Task 1 ✅ — sqlite + Ecto wiring
- Task 2 ✅ — `Grappa.Repo` + worktree-aware scaffold
- Task 3 ✅ — `Grappa.Scrollback` context
- Task 4 ✅ — Bandit-backed `Phoenix.Endpoint` + Router + HealthController
- Task 5 ✅ — `GET /networks/:net/channels/:chan/messages`
- Task 6 ✅ — `POST /networks/:net/channels/:chan/messages` + PubSub
- Task 7 ✅ — `UserSocket` + `GrappaChannel`
- Task 8 ✅ (code) — IRC parser + Client + Session.Server + Bootstrap.
  Sub-task 8e (smoke + Pi deploy) pending operator confirmation.
- Tasks 9–10 pending.

**Codebase shape:**
- ~2,100 LOC of Elixir under `lib/` (was 810 at CP03 open)
- 121 tests + 5 properties (was 41 at CP03 open)
- ci.check ~17s on Pi (worktree, PLT hot)
- 56 commits on main since project init

**Tooling baseline:** Dialyxir (`:underspecs`), Credo (`--strict`),
Sobelow, mix_audit, doctor (100% module health), Boundary, ExUnit,
StreamData, Mox, Bypass, ExMachina, excoveralls. All green.

**Outstanding from CP03 / pre-CP04:**
- Sub-task 8e — smoke + live deploy on Pi (`scripts/deploy.sh` →
  `scripts/healthcheck.sh`). Awaiting operator green-light. Unblocks
  the long-deferred `/healthz` round-trip from every prior task.
- Phase 5 hardening: lift `signing_salt` (currently `"rotate-me"` in
  `lib/grappa_web/endpoint.ex`) to `runtime.exs`; same for the
  `verify: :verify_none` TLS posture. (No urgency — both flagged in
  CLAUDE.md security section.)

**Codebase review gate:** not due. First review threshold is
2026-05-09 or S12, whichever comes first. Currently S11 / past CP02
codebase-review.

---
