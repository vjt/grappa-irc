---
status: active
---

# CP45 — FLAKE-D close → GREEN-CI cluster (2026-05-23)

## Status

**FLAKES cluster CLOSED 2026-05-23 on commit `0efa550`.** Same-day
vjt override: GREEN-CI cluster opened — *"i want fucking ci green
and testing actual functionality"* — to fix the 30 specs the
FLAKES cluster had quarantined as "load class deferred."

**GREEN-CI batch 1 (4 buckets, same-day)** cleared the cascade:
30 → 7 failures (154 → 177 passed); 23 specs unlocked by SPEC-4
alone (m9b-victim sacrificial user, fixes `.first()` lottery).

**GREEN-CI batch 2 (4 buckets, same-day)** cleared the residual 7
individual failures + 1 latent CI flake. **CI confirms FULLY GREEN
at `b17fd71`: integration 184 passed / 0 failed (4.4m), ci 2m29s
all gates ✓.**

Roadmap unblocked: per locked vjt 2026-05-22 mandate, next is UX-8
scroll cluster → wireTypes.ts codegen → bastille. Memory:
`project_post_rev_roadmap.md` → `docs/todo.md` ★ block.

Standing autopilot mandate continues per
`project_post_tmu_full_review_scheduled` +
`feedback_orchestrator_autonomy` + `feedback_push_autonomy`.

## CP44/CP45 session arc

CP44 S1 (FLAKE-C, 7-for-7 SPEC ROT):
- `2132bea` flake-c(i2-image-upload): split embedded + litterbox specs
- `5562ae7` flake-c(members): drop vjt-as-op assumption (M-seed)
- `632148f` flake-c(p0d): route Server lookup through sidebarWindow
- `b05c88e` flake-c(p0e): vjt joins fresh channel as +o pre-/invite
- `1d17010` flake-c(m9): drop empty-state assertion post-UX-4-B/E
- `214fce6` flake-c(names-n3): delete obsolete spec (UX-4-B redesign)
- `0a9b7cd` flake-c(nick-case): scope sidebar to .shell-sidebar

CP45 S1 (FLAKE-D, 2-real-race / 2-batched-FP / 0-product-bugs):
- `64d6e0b` flake-d(cp14-b3): waitForDmListenerReady before peer.privmsg
- `0efa550` flake-d(ux-6-k): same fix, same factored helper

CP45 S2 (GREEN-CI batch 1, 4 buckets, 0 product bugs, single root cause for cascade):
- `ee20035` green-ci(spec-1): drain SessionRegistry stale entries at AdminEventsTest setup
- `31c7295` green-ci(spec-2+3): cic-members-panel parked sub-test removal (unreachable) + m10 U-1 testid rot + NULL cap handling
- `2502d81` green-ci(spec-4): fix `.first()` lottery cascade — add m9b-victim sacrificial user + reconnect dance + bahamut-test user-cap bump to 10

CP45 S3 (GREEN-CI batch 2, 3 buckets, 0 product bugs, residual 7 flakes + 1 latent):
- `45e69b3` green-ci-2(chromium-3): fix 3-way op-race latent flakes (b0-invite, members-prefix, ux-5-bc2-nick-render) — 3 specs hardened via dedicated-channel pattern; m9b-victim raised autojoin race for +o from 2→3, breaking 3 specs that assumed deterministic vjt op-status on #bofh. ux-5-bc2 sub-test #1 was wrong by design (asserted color on MembersPane's `noColor` site) — switched probe to scrollback sender.
- `85d2b1c` green-ci-2(webkit-iphone): fix iOS-3 #bofh PART cascade + ux-6-d bugs — `ios-3-bottom-bar-close.spec.ts:40` + `ios-z-cluster-journey.spec.ts:99` tapped close × which PARTed vjt from #bofh with no restoration, cascading to selectChannel timeouts in 2 downstream specs. Added `afterEach` rejoin. Plus ux-6-d (d) needed selectChannel pre-load (HomePane default has no compose-box) + (f) had a broken admin-promote helper (`adminToken = "admin-vjt"` literal, not a bearer) AND drove desktop shell-chrome cog on mobile (admin-console-entry outside viewport).
- `b17fd71` green-ci-2(admin-events): bump SessionRegistry-drain budget 500ms → 2s — CI runner ETS contention exceeded SPEC-1's poll window in 7 of 10 AdminEventsTest tests; pure setup-timing fix, no production code.

Cluster summary entries in DESIGN_NOTES (2026-05-23) for both FLAKES
+ GREEN-CI (batch 1). Memory entries pending: `project_flakes_cluster_closed.md`
(written) + `project_green_ci_cluster_closed.md` (written, batch 1
only — needs batch 2 extension).

## What's deployed RIGHT NOW

- **Server**: `bf3ba3a` on `origin/main` base (REV-K cold-deploy
  2026-05-22). HEAD now `b17fd71` — all GREEN-CI commits (batch 1
  + batch 2) are test-infra (e2e fixtures + ExUnit test setup),
  NO server / cic src touched. Healthcheck: `ok`.
- **cic bundle**: `34TrT3jr` (REV-K) — unchanged across FLAKES +
  GREEN-CI clusters.

## Pending

### Next cluster — UX-8 scroll (per locked roadmap)

Per vjt 2026-05-22 evening lock:
1. UX-8 scroll cluster — (a) channel-switch scroll position
   interference; (b) read-cursor update on scroll = NEW server
   contract (`Grappa.ReadCursor.set/4` currently fires on focus-leave
   + browser-blur only).
2. wireTypes.ts codegen from `Grappa.*.Wire` typespecs.
3. Bastille deploy workstream (GitHub issue #8).

See `docs/todo.md` ★ block for full rationale. Memory pointer:
`project_post_rev_roadmap.md`.

### Carry-forward from FLAKES (deferred → REVALIDATE post-GREEN-CI)

- **27 "SPEC-ROT (load class)" files** quarantined behind suite-
  level isolation noise (FLAKE-B Part 2 manifest). GREEN-CI SPEC-4
  showed the cascade root cause was a single `.first()` lottery in
  m9b + u5; **23 of the 27 unlocked automatically post-SPEC-4** —
  they were cascade victims, not load-class. Remaining 4 were
  individual real flakes — **all fixed in GREEN-CI batch 2.**

### CI status (post-GREEN-CI batch 2 close)

CI is **FULLY GREEN** at `b17fd71`:
- integration: 184 passed / 0 failed (4.4m) on `85d2b1c` (test-only
  commit `b17fd71` doesn't trigger integration workflow per path
  filter)
- ci (test+lint+audit+dialyzer): 2m29s exit-0 on `b17fd71`

vjt's "FUCKING FIX THE FUCKING CI" mandate satisfied.

### REV-J.5 (M1+M5) still deferred
Dockerfile UID prep prerequisite for M1+M5 anonymous-volumes
refactor; standalone bucket between flakes and codegen if bandwidth
permits, else future infra-polish cluster.

### Baseline flakes (CP43/CP44 status — POSSIBLY CLEARED)
- `Grappa.AdmissionTest` ETS-singleton-leak class — unchanged, not
  touched by GREEN-CI
- `AdminEventsTest:197` assert_receive class — **FIXED by GREEN-CI
  SPEC-1** (`ee20035`)
- cic biome 2 baseline errors + 21 baseline warnings — unchanged
