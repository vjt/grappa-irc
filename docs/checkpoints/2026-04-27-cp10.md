---
status: active
---

# CP10

Previous: CP09 closed at 4 logical sessions / 422 lines / Phase 3
walking skeleton landed in prod + iPhone PWA acceptance + `/review`
skill extended for cicchetto/. CP09 covered the full Phase 3 arc
from the codebase-review gate (S1) through the S29 carryover sweep
(S2) through the cicchetto walking-skeleton wrap (S3) and the
review-skill update (S4). All sub-tasks landed; production stable
at `http://grappa.bad.ass`.

## State at checkpoint creation

- **Branch:** main @ `b5b35ec`.
- **Commit count:** 175 commits on main.
- **Tests:** server 424/0 (8 properties, 0 failures); cicchetto 44/44
  vitest (16 files Biome + tsc strict).
- **Gates:** Dialyzer 0, Credo 0, Sobelow 0, format clean, audits
  clean, doctor 100%/100% (only pre-existing
  `Grappa.Application` hidden-doc warning).
- **Production:** `http://grappa.bad.ass` live (nginx :80 → grappa:4000
  via grappa_internal Docker bridge); DNS authoritative (Technitium
  ns1.bad.ass); CSP + security headers verified on the wire; bundle
  size 62.13 KB JS / 21.45 KB gzip — well under the 200 KB pre-Vosk
  budget.
- **Bouncer:** connected to Azzurra as `grappa`, joined `#grappa`,
  NickServ IDENTIFY accepted (Phase 2 deploy persists; Phase 3 added
  the front-end without touching the IRC plumbing).
- **iPhone PWA acceptance:** verbatim "works. can log in and see
  scrillback and send messages. vidual layout is messy but i guess
  we do that later. verified also session persists app clisure"
  (operator confirmation 2026-04-27).
- **Worktrees:** clean — `phase3-cicchetto-walking-skeleton` removed
  post-merge; `phase2-auth` at `/home/vjt/code/IRC/grappa-phase2`
  still dead weight (logged in todo).

## Pending at checkpoint open

1. **`/review codebase` next session** — the review skill was
   extended in CP09 S4 to cover cicchetto/ + cross-cutting concerns
   (`compose.prod.yaml` env vars vs `runtime.exs`, `nginx.conf`
   route allowlist vs `router.ex`, package.json/bun.lock sync,
   etc.). Run the line-level pass against the full repo (server +
   cicchetto) to surface anything the post-merge code-reviewer agent
   missed (it focused on security + correctness; the codebase pass
   covers idiom, patterns, dead code, type drift). Then trajectory
   review.
2. **Phase 4 brainstorm** — irssi-shape UI redesign. README's
   Roadmap + DESIGN_NOTES "Mobile is an ergonomics layer on
   irssi-shape" are the design constraints; user's "vidual layout
   is messy but i guess we do that later" is the natural opening.
   Use `superpowers:brainstorming` (must precede any creative work
   per skill description) before touching code.
3. **Codebase review gate** — last codebase review ran 2026-04-26
   (CP09 S1). 12-session/2-week cadence means the next is due
   approximately 2026-05-10 OR after ~12 sessions, whichever first.
   /review codebase next session will reset the clock.
4. **Operator hygiene:** `phase2-auth` worktree at
   `/home/vjt/code/IRC/grappa-phase2` is dead weight; remove via
   `git worktree remove ~/code/IRC/grappa-phase2`.

## Phase 5+ deferred items (carried from CP09)

Refer to `docs/todo.md` "High → Phase 5 hardening cluster" for the
full enumerated list. Headline items added during CP09 S3 from the
Phase 3 review's CONSIDER tier:

- **C1:** Service Worker requires HTTPS / secure context — `http://`
  origin makes iOS Safari silently fail SW registration. Resolves
  with Phase 5 TLS rollout.
- **B2-bonus:** Move bearer token off the WS query string. Currently
  `?token=…` rides the upgrade URL; Phoenix `:filter_parameters` +
  nginx `access_log off` redact at log layer, but the bearer is
  still on the wire pre-redaction (browser devtools, on-path
  observers). Phase 5 hardening — needs a phoenix.js + UserSocket
  protocol change.
- **C5:** `loadMore` concurrency guard — REST request dedupe with a
  per-key in-flight Set.
- **C6:** A11y pass — channel sidebar tree semantics for iOS
  VoiceOver, tap-target sizing, focus-state contrast (web.dev
  a11y guidelines).
- **C2:** Pin Docker tags to digests (`oven/bun:1@sha256:…`,
  `nginx:alpine@sha256:…`) for reproducible builds.
- **C3:** nginx upstream `keepalive 32` is dead weight without
  `proxy_set_header Connection "";` on the API allowlist block —
  perf nit, measurable only under load.

## Architectural debt acknowledged (carried from CP08 + CP09)

- **Networks ↔ Scrollback structural cycle** — broken via Boundary
  `dirty_xrefs: [Grappa.Networks.Network]` on Scrollback; closing
  requires lifting Network schema into a leaf boundary. Phase 5+.
- **Session cached-opts restart regression** — `:transient` restart
  replays the opts captured at first `start_session/3`. Live
  credential changes only propagate when
  `Networks.unbind_credential/2` runs INSIDE the prod BEAM (via
  `bin/grappa rpc`). Phase 5 may add `Session.refresh/2`.
- **`Grappa.Application` hidden-doc warning** at
  `lib/grappa.ex:24` — pre-Phase-1 inheritance, low priority.
- **Server export from Session boundary** (S32 reviewer F6) — kept
  for test-only `Logger.put_module_level/2`. Phase 5 trajectory
  item: lift the per-module log helper into a test helper, drop the
  export.
- **Phase 5+ deferred from S29 codebase review** (carried across
  CP07-CP09): reconnect/backoff in IRC.Client, Session.Server clean
  `terminate/2`, TLS cert chain, pre-deploy lint cross-checking
  runtime.exs vs compose env, Logger NOTICE handling,
  `Grappa.Operator` CLI lift, NickServ REGISTER proxy.

## Other open items (carried from CP08 + CP09)

- `test/grappa_web/channels/grappa_channel_test.exs:76` flake
  (`assert_receive %Phoenix.Socket.Message{}` intermittent timeout).
  Race between channel join + PubSub subscribe before broadcast
  lands. ~1-in-5 hit rate under `mix ci.check` parallelism. Hit S17.
  **Did NOT resolve naturally during Phase 2 or pre-Phase-3 cleanup
  or Phase 3** — re-check at next codebase review.
- `Database busy` flake on `Grappa.Networks.WireTest` setup under
  `max_cases: 2` write-heavy parallelism. Hit during S33 ci.check
  re-runs AND Phase 3 sub-task 8 first server gate run. Re-runs
  always clean. Pre-existing under sqlite contention, documented in
  DataCase comments.

---

## S1 — 2026-04-27 — `/review codebase` (extended skill, Phase 3 trajectory)

First post-Phase-3 codebase review with the extended `/review`
skill (cicchetto/ + cross-cutting infra, landed CP09 S4). Six
parallel agents dispatched against irc/, persistence/, lifecycle/,
web/, cicchetto/, cross-module+infra. Output:
`docs/reviews/codebase/2026-04-27-codebase-review.md`.

**Findings: 55 total (after dedupe of one cross-module ↔ lifecycle
overlap). 0 CRITICAL, 4 HIGH, 17 MEDIUM, 34 LOW.**

| Scope             | CRIT | HIGH | MED | LOW | Total |
|-------------------|:----:|:----:|:---:|:---:|:-----:|
| irc/              |  0   |  1   |  2  |  7  |  10   |
| persistence/      |  0   |  0   |  1  |  5  |   6   |
| lifecycle/        |  0   |  0   |  6  |  4  |  10   |
| web/              |  0   |  0   |  1  |  8  |   9   |
| cicchetto/        |  0   |  3   |  5  |  7  |  15   |
| cross-module/infra|  0   |  0   |  2  |  3  |   5   |

**Top-of-stack (HIGH):**
1. **irc S1** — `IRC.Client.init/1` does blocking TCP+TLS+handshake
   synchronously (no `:timeout`). Move to `handle_continue/2`,
   add explicit timeouts. Pairs with **lifecycle S12**
   (`Session.Server.init/1` mirror).
2. **cicchetto S2** — SW `CACHE = "cicchetto-shell-v1"` never
   bumps; combined with byte-identical `sw.js`, the SW never
   re-installs. Operators get perma-stale shell after first
   post-install deploy.
3. **cicchetto S3** — SW shell-cache references hashed `/assets/*`
   that are NOT cached. After any deploy bumping asset hashes the
   cached `/` references dead asset paths.
4. **cicchetto S4** — `MessageKind` union is 3-of-10 narrower than
   server `Message.kind()` enum. Phase 5 presence-event capture
   will silently render JOIN/PART/etc. as PRIVMSG. Pairs with
   **cicchetto S15** (non-exhaustive switch in `ScrollbackLine`).

**Trajectory recommendation:** land cicchetto S2+S3 BEFORE Phase 4
begins — Phase 4 is by definition a stream of asset-bumping deploys
and will trigger the SW cache bug on the operator's pocket. Then
Phase 4 brainstorm → implementation, with the persistent post-Phase-2
hygiene cluster (M3/M5/H11/M2/M12 — carried since CP07 across two
phases) folded in as a dedicated mini-cluster OR explicitly closed
as won't-fix. "Ride it naturally" has not worked.

**Risk surface added by this review:** SW cache busting (S2+S3,
prod-impact whenever Vite asset hashes change), blocking init pair
(S1+S12, latent until a second upstream is bound), `MessageKind`
narrow (S4+S15, latent until Phase 5 broadcasts a presence event).
All four are documented in the review's Trajectory section.

**Architecture review:** NOT run alongside this codebase review.
Should be queued — Phase 3 introduced the entire `cicchetto/`
subsystem and the server↔client wire-shape contract; structural
review of cross-boundary concerns is overdue.

**Codebase review gate clock reset:** next due 2026-05-11 OR after
12 sessions, whichever first.

---

## S2 — 2026-04-27 — C1 cluster: vite-plugin-pwa swap (HIGH S2 + S3 closed)

First of four post-CP10-review fix clusters (C1 → C4). C1 was
prioritized first per the trajectory recommendation: must land
before Phase 4 begins because Phase 4 is by definition a stream of
asset-hash-bumping cicchetto deploys that would trigger the
original cache bug on the operator's pocket.

**Worktree:** `c1-sw-pwa` at `/home/vjt/code/IRC/grappa-c1-sw-pwa`.
Branched from local main, merged via `--no-ff` to preserve the
cluster boundary, deployed via `scripts/deploy.sh`, removed after.

**Two commits on the branch:**

- `24e9378` fix(cicchetto): swap home-rolled SW for vite-plugin-pwa
- `0381d98` fix(cicchetto): apply C1 review feedback (M1 + L2 + L1 doc)

**Approach (b) of the C1 design call:** vite-plugin-pwa@1.2.0 +
workbox-window@7.4.0 in `generateSW` mode. Workbox embeds the
precache manifest (every emitted asset, hashed) into the SW bytes
at build time — so any deploy that bumps an asset hash bumps the
SW byte content, which forces re-install + activate-time precache
eviction. Both bugs (S2 hardcoded cache name + S3 hashed assets
not pre-cached) fall to the same fix mechanically.

**Configuration choices:**

- `registerType: "autoUpdate"` — shell-only cache, stale assets
  never useful, no opt-in prompt.
- `injectRegister: false` — explicit `registerSW()` from
  `virtual:pwa-register` in main.tsx (deterministic vs.
  plugin-internal heuristic; `'auto'` would resolve to `false` here
  anyway because main.tsx imports the virtual module).
- `globPatterns: ["**/*.{js,css,html,svg,png,webmanifest,ico}"]` —
  precache the build's hashed JS/CSS + shell + manifest + icons.
- `navigateFallbackDenylist` for `/auth /me /networks /socket` —
  covers SPA-routing edge cases (e.g. a navigation-mode request to
  `/auth/oauth-redirect`); REST + WS protection is architectural,
  not denylist-driven (Workbox's NavigationRoute only matches
  `request.mode === "navigate"`).
- Manifest moved from `public/manifest.json` into `vite.config.ts`
  — single source of truth.

**Files removed:** `cicchetto/public/sw.js`,
`cicchetto/public/manifest.json` (both generated by plugin into
`dist/`).

**Code review (superpowers:code-reviewer agent) findings,
addressed in commit 0381d98:**

- **M1 (must fix):** original `navigateFallbackDenylist` comment
  misrepresented Workbox semantics — the denylist only matches
  `request.mode === "navigate"`, so it does NOT (and is not the
  mechanism that would) protect the REST + WS surface. Comment
  rewritten to state what the list actually does + clarify the
  REST/WS architectural protection.
- **L2:** dropped `immediate: true` from `registerSW()`. Workbox
  author flags it as not-recommended (registering before
  `window.load` splits asset-fetch bandwidth with first-paint
  critical-path bytes). For a 93 KiB shell on iPhone the delta is
  noise either way, but defaulting matches plugin recommendation.
- **L1 (doc-only):** legacy `cicchetto-shell-v1` cache from CP09
  home-rolled SW won't be evicted by Workbox's
  `cleanupOutdatedCaches` (only deletes caches with `-precache-`
  substring in name). Intentional: leak is bounded (~few KB,
  one-shot per device); cleanup mechanism (injectManifest custom
  SW) costs more than the leak. Documented in DESIGN_NOTES under
  the new "vite-plugin-pwa swap-in" section.
- **L3 (peer dep on `vite ^7` vs installed `^8`):** noise; runtime
  uses only stable Vite plugin hooks; build + virtual-module
  integration verified working. Re-evaluate when vite-plugin-pwa
  ships a release with `^8` peer.

**Verification (worktree):**

- `scripts/bun.sh run check` green — biome + tsc strict, 16 files
- `scripts/bun.sh run test` green — 44/44 vitest
- `scripts/bun.sh run build` green — PWA generateSW, 12 precache
  entries (93.10 KiB), `dist/sw.js` confirmed to contain hashed
  asset filenames embedded in `precacheAndRoute([...])`

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

- `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
- `curl http://grappa.bad.ass/sw.js` → workbox-generated SW; embedded
  precache list contains the production-build hashes (`index-BohVUIaa.js`,
  `index-BEtZsWUQ.css`); `cleanupOutdatedCaches()` present
- `curl http://grappa.bad.ass/manifest.webmanifest` → auto-generated
  manifest with `lang: "en"` + `scope: "/"` + the original fields
- `curl http://grappa.bad.ass/` → auto-injected
  `<link rel="manifest" href="/manifest.webmanifest">`

**Cluster status:** C1 closed. C2 (blocking init pair: irc S1 +
lifecycle S12) next, C3 (MessageKind widen + exhaustive switch:
S4 + S15) after, C4 (hygiene cluster) last. Each cluster gets its
own worktree per the cluster-execution pattern; `/clear` between
clusters to keep context warm.

**DESIGN_NOTES:** new "2026-04-27 — vite-plugin-pwa swap-in" entry
records the cache-busting fix + the legacy-cache-leak intentional
non-cleanup so a future operator-support session knows why DevTools
shows two caches on legacy installs.

---

## S3 — 2026-04-27 — C2 cluster: blocking init pair closed (HIGH S1 + S12)

CP10 codebase review HIGH irc/S1 + lifecycle/S12 closed: both
`Grappa.IRC.Client.init/1` and `Grappa.Session.Server.init/1` defer
their blocking work into `handle_continue/2`. Connect timeout pinned
to 30_000 ms explicitly on both `:gen_tcp.connect/4` and
`:ssl.connect/4` (was `:infinity` default — a SYN-dropped router
deadlocked init forever).

**Bootstrap inversion of meaning:** pre-fix, an upstream connect
refusal made `Session.start_session/3` return `{:error,
{:client_start_failed, _}}` synchronously and Bootstrap counted it
under `failed`. Post-fix, the failure is async — Bootstrap reports
`started=N failed=0` for any row that passed `Client.init/1`'s
validation; the per-Session `:transient` policy retries up to
`max_restarts: 3` and the `DynamicSupervisor` then terminates the
child permanently. Operators grep `(stop) {:connect_failed, _}`
from the Session crash trace under the new semantic. Phase 5
reconnect/backoff replaces the exhaust-and-give-up shape.

**Bonus root-cause fix:** pre-existing `Session.stop_session/2` race
(~1-in-5 flake on the existing teardown test). `DynamicSupervisor.
terminate_child/2` returns when the child is dead, but
`Grappa.SessionRegistry`'s OWN process-monitor on the dying pid runs
in the Registry process and may not have processed its DOWN yet.
`stop_session/2` now monitors the pid itself, awaits the DOWN with a
5s `Logger.error`-on-timeout budget, then polls `Registry.lookup`
until the entry is gone (5ms × 100). The race surfaced reliably
while iterating on the C2 test; chained `stop_session/2` →
`start_session/3` would otherwise occasionally fire `:already_started`
against a zombie pid.

**Test-side discipline pinned:** the Session-level non-blocking-init
test uses `Grappa.Session.Server.start_link/1` directly (linked to
the test pid), NOT `Session.start_session/3` via DynamicSupervisor.
The latter would trigger the connect-refused crash → `:transient`
restart cycle, which exhausts `SessionSupervisor`'s `max_restarts: 3`
budget in <100 ms and crashes the supervisor — torching every other
Session in the test run. Pinning the GenServer init contract
directly is the right surface; the supervisor path is the wrong
unit-of-test.

**Commits (in order, on `c2-blocking-init` worktree):**

- `0c1af4a` fix(otp): defer blocking connect/handshake to
  handle_continue (CP10 S3, C2)
- `d0a4d0f` fix(otp): apply C2 review feedback (L1 + L2)
- `307b327` docs: DESIGN_NOTES — C2 init/1 → handle_continue
  (CP10 S3)
- `1e6f063` merge: C2 cluster — init/1 non-blocking via
  handle_continue (CP10 S3) — `--no-ff` to main

**Code review verdict:** SHIP IT. Three LOW findings; L1 (named
constants) + L2 (`Logger.error` on stop-budget timeout) applied in
`d0a4d0f`. L3 (test-helper `pick_unused_port/0` duplicated across two
test files) deferred — extracting to `test/support/test_ports.ex` is
a separate test-infrastructure concern, not in C2's scope.

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

- `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
- `scripts/monitor.sh` boot trace: `phase 1 TLS posture: verify_none`
  warning fires synchronously in init (per the test contract);
  `bootstrap done credentials=1 started=1 failed=0` reports the
  Azzurra session as started; subsequent `irc event` lines for
  `mode` and `join #grappa` confirm the upstream connection
  completed via `handle_continue` and the session is processing
  inbound traffic.

**HIGH-finding tally:** 3 of 4 CP10 HIGH closed (cicchetto S2 + S3 in
C1; irc S1 + lifecycle S12 in C2). Remaining: cicchetto S4
(MessageKind union narrower than server `Message.kind()`) → C3.

**Cluster status:** C1 + C2 closed. C3 (MessageKind widen +
exhaustive switch: cicchetto S4 + S15) next, C4 (hygiene cluster)
last. Each cluster gets its own worktree; `/clear` between clusters
to keep context warm.

**DESIGN_NOTES:** new "2026-04-27 — `init/1` defers connect via
`handle_continue` (CP10 S3, C2)" entry records the cluster's five
load-bearing apply rules — sealed-struct continue term, OTP-safe
nil window, TLS-warning placement, Bootstrap semantic shift, and
the test-side don't-trigger-supervisor-restart-cycle discipline.

---

## S4 — 2026-04-27 — C3 cluster: MessageKind widen + exhaustive switch (HIGH S4 + MEDIUM S15)

Third of four post-CP10-review fix clusters. Closes the last CP10 HIGH
finding (cicchetto/S4 — `MessageKind` union 3-of-10 narrower than the
server's `Grappa.Scrollback.Message.kind()` enum) AND its paired
MEDIUM (S15 — non-exhaustive switch in `ScrollbackLine`). Pre-fix the
TS union carried `privmsg | notice | action`; the renderer's
`<Show>` fallback rendered every non-action/non-notice kind with
PRIVMSG `<sender>` framing. Phase 5 presence-event capture would
have shipped JOIN/PART/etc. silently mis-framed.

**Worktree:** `c3-messagekind-widen` at
`/home/vjt/code/IRC/grappa-c3-messagekind-widen`. Branched from local
main, merged via `--no-ff` to preserve the cluster boundary, deployed
via `scripts/deploy.sh`, removed after.

**Two commits on the branch:**

- `e3a1d03` fix(cicchetto): widen MessageKind to all 10 server kinds
  + exhaustive switch
- `1a7e6ae` fix(cicchetto): apply C3 review feedback (S-1 + S-2 + C-1)

**Type contract:** `MessageKind` mirrors `Scrollback.Message.@kinds`
verbatim — ten atoms in their snake_case wire forms (Jason's
`Atom.to_string/1` on `:nick_change` lands as `"nick_change"`, no
kebab/camel transform). The renderer's `renderBody/1` switch is
exhaustive over the union; the `default` arm
`const _exhaustive: never = msg.kind` makes any future addition to
the union a compile error here. No `as` cast, no runtime fallback.

**Framing:** irssi convention. PRIVMSG `<nick> body`, NOTICE
`-nick- body`, ACTION + presence/op kinds `* nick <verb> [target]`.
Presence kinds carry their event-specific fields in `meta` (mirror of
`Scrollback.Meta` allowlist: `target`, `new_nick`, `modes`, `args`,
`reason`); each access narrows defensively against the wire-side
`Record<string, unknown>`. Added `.scrollback-presence` CSS class
(muted + italic) so non-message kinds visually disambiguate from
PRIVMSG flow.

**Defense-in-depth (`reasonOf/1`):** `:part` / `:quit` / `:kick`
carry their reason in `body` per `Scrollback.Meta`'s per-kind shape
table. The Meta allowlist still includes `:reason` (review S29 dead
key). The renderer reads `body` first, falls back to `meta.reason` —
so if the server ever shifts reason into the meta payload (S29 fix
path), the client doesn't silently drop it.

**TDD pin:** new test "renders all ten server kinds without falling
through to PRIVMSG framing" — pre-fix, TS rejected `kind: "join"` at
compile time (TS2322, "Type '\"join\"' is not assignable to type
'MessageKind'"); post-fix the union accepts and runtime assertions
pin that presence/op rows NEVER render `<sender>` PRIVMSG framing.
Both layers — type-system gate AND runtime contract — must move in
lockstep when extending the kind set.

**Code review verdict:** SHIP-IT. Two SHOULDs (`reasonOf/1`
defense + Phase 4 single-channel-buffer note) and one CONSIDER (mode
test adjacency: `+o heidi`, not `+o` and `heidi` separately) applied
in `1a7e6ae`. Two CONSIDERs deferred (`console.warn` on `?`
fallbacks; collapse `PRESENCE_KINDS` Set + union into a tagged
record) — both reviewer-flagged as "optimization-of-an-edge-case",
Phase 4 will rework the kind-class mapping when the visual model
evolves.

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

- `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
- `scripts/monitor.sh` boot trace: `bootstrap done credentials=1
  started=1 failed=0` reports the Azzurra session as started; mode +
  join `#grappa` `irc event` lines confirm upstream connection
  re-established post-deploy
- Browser snapshot of `http://grappa.bad.ass/login` via Chrome DevTools
  MCP: shell renders, console clean (zero errors / warnings)

**HIGH-finding tally: 4 of 4 CP10 HIGH closed.** cicchetto S2 + S3 in
C1 (vite-plugin-pwa swap), irc S1 + lifecycle S12 in C2 (init/1 →
handle_continue), cicchetto S4 + cicchetto S15 in C3 (this cluster).

**Cluster status:** C1 + C2 + C3 closed. Only C4 (post-Phase-2 hygiene
cluster carried since CP07: M3 nick-regex consolidation, M5
error-string-casing, H11 central User wire shape, M2 web→Repo dep
cleanup, M12 `Application.put_env` 6× duplication, plus this review's
S54 reinforcement of M12) remains. Worktree-per-cluster pattern + 
`/clear` between clusters held throughout — execution shape is now a
documented norm, not a one-off.

**DESIGN_NOTES:** new "2026-04-27 — `MessageKind` mirrors server enum,
exhaustive switch enforces drift (CP10 S4, C3)" entry records the
cluster's five load-bearing apply rules — wire-shape source-of-truth,
atom-as-wire-form, irssi framing convention, `reasonOf/1`
defense-in-depth, and TDD-pin-first-exhaust-both-layers.

## S5 — 2026-04-27 — C4 cluster: post-Phase-2 hygiene close-out

Final post-CP10-review fix cluster. Closes the post-Phase-2 hygiene
ledger carried since CP08-era (M3, M5, H11, M2, M12) plus this review's
S54 (reinforcement of M12) plus S8 (same anti-pattern class as M12/S54,
folded in). After this cluster all CP10 review trajectory items are
resolved; only the Phase 4 brainstorm (superpowers:brainstorming
mandatory before creative work) remains as the open CP10 pending.

**Worktree:** `c4-hygiene` at `/home/vjt/code/IRC/grappa-c4-hygiene`.
Branched from local main (`03c03a5`), merged via `--no-ff` to preserve
the cluster boundary, deployed via `scripts/deploy.sh`, removed after.

**Per-item disposition** (decision criterion: does the original
CP08-review failure rationale still apply against the post-Phase-3
codebase, or has the Phase 2 + Phase 3 work organically resolved it?):

| Item | Disposition | Rationale |
|------|-------------|-----------|
| M3 nick-regex consolidation | WON'T-FIX | CP09 F2 dropped `\-` from `Identifier.@nick_regex`; `Credential.changeset` already delegates to `Identifier.valid_nick?/1`. `User.@name_format` is grappa-account-name (different domain — character class excludes IRC-nick chars `[`, `]`, `\`, `{`, `}`). |
| H11 central User wire | WON'T-FIX | `Grappa.Accounts.Wire` exists; both `MeJSON` and `AuthJSON` delegate via `user_to_json/1` and `user_to_credential_json/1`. |
| M2 web→Repo dep | WON'T-FIX | `MessagesController` has zero `Repo.` calls; `Grappa.Repo` not in `GrappaWeb` Boundary deps. |
| M12 `Application.put_env` 6× | WON'T-FIX | Already consolidated to `Mix.Tasks.Grappa.Boot.start_app_silent/0` (single site). |
| M5 error-string-casing | FIX (cf53841) | snake_case envelope across `FallbackController` already landed; `Plugs.Authn.unauthorized/1` was hand-rolling 401 body, bypassing encoder. Funneled through `FallbackController.call(conn, {:error, :unauthorized})`; new clause + spec extension; cross-emitter shape pin test. |
| S8 Release.repos | FIX (c36b454) | `Application.fetch_env!(@app, :ecto_repos)` replaced with hardcoded `@repos = [Grappa.Repo]` per `Grappa.Repo` moduledoc (single shared Repo). Iteration was dead generality; mirror-`mix.exs` comment makes drift loud. |
| S54 boot.ex put_env | FIX-BY-DOC (8ca60b7) | Refined CLAUDE.md rule to spell out boot-time-vs-runtime distinction. `Application.{put,get}_env/2` allowed at boot-time configuration boundaries (config/*.exs, application.ex start/2, mix-task helpers BEFORE `ensure_all_started/1`); banned at runtime (callbacks, controllers, contexts, plugs, release tasks). DESIGN_NOTES new entry pins the principle. |

The two CP10-review-proposed S54 code paths both lost: option (a) "add
exemption" reduces to a growing list (Total-consistency violation);
option (b) "refactor `Application.start/2` to accept `:bootstrap?` at
start-time" requires either a compile-time `mod`-args change (same
shape, different name) OR hand-rolling a child-list duplicate in the
mix task (don't-duplicate-state violation, heavier than the 5 lines).
The principle that crystallized — "TIMING is the discriminator, not
the call site" — is durable across future Application-env questions.

**Four commits on the branch:**

- `cf53841` fix(web): funnel Authn 401 through FallbackController
  (CP10 S5, C4 / M5)
- `c36b454` fix(release): hardcode [Grappa.Repo] in Release migrate
  (CP10 S5, C4 / S8)
- `8ca60b7` docs: pin Application.env boot-time-vs-runtime rule
  (CP10 S5, C4 / S54)
- `c2c1b59` fix(c4): apply review feedback (S-1 + S-2 + C-4)

Stats: 4 commits / 6 files / +137 / −9 / +1 test (426 → 427).
ci.check green: 8 properties, 0 failures, Dialyzer 0, Credo 0,
Sobelow 0, format clean.

**Code review verdict:** SHIP-IT. Two SHOULDs (S-1 rule wording
generalization from `start_app_silent/0` name-pin to
`ensure_all_started/1` boundary-pin; S-2 drop asymmetric `accepts/2`
preprocessing in cross-module shape pin test) and one CONSIDER (C-4
release.ex mirror-`mix.exs` comment) applied in `c2c1b59`. Two skipped:
CONSIDER-3 `@spec` clause-order alphabetization (file's existing
pattern isn't strictly sorted), CONSIDER-5 DESIGN_NOTES tightening
(the "Why the distinction matters" section is load-bearing per the
review's own framing).

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

- `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
- `scripts/monitor.sh` boot trace: `bootstrap done credentials=1
  started=1 failed=0` — Azzurra session re-established post-deploy;
  `irc event` line for JOIN `#grappa` confirms upstream connection
  back up
- Browser-shell verification skipped — C4 hygiene changes don't touch
  the client surface (no `H11` User wire change in scope; the change
  is server-side wire bytes for an existing `:unauthorized` response
  that already matched bit-for-bit)

**Cluster status (post-merge):** C1 + C2 + C3 + C4 closed. All four
post-CP10-review clusters done. Worktree-per-cluster pattern + `/clear`
between clusters held cleanly across all four — execution shape is
now a documented norm, not a one-off. Per-cluster commit-then-merge-
`--no-ff` rhythm produced four self-documenting `git log` entries
each with the cluster's per-item disposition spelled out, the
DESIGN_NOTES selectively updated only when load-bearing principles
crystallized (C2 init/1 → handle_continue; C3 MessageKind atom-as-wire;
C4 Application.env timing rule), and the CP10 entries kept short
("see DESIGN_NOTES + commits").

**DESIGN_NOTES:** new "2026-04-27 — `Application.{put,get}_env/2`:
boot-time vs runtime (CP10 S5, C4)" entry records the unifying
principle that future plans/reviews lean on instead of relitigating
each Application-env site case-by-case. The CLAUDE.md OTP-patterns
line was rewritten in lockstep so the shorter form is greppable from
the rules-of-the-road document and the longer derivation is in the
chronological log.

**CP10 trajectory complete (clusters):** C1 + C2 + C3 + C4 closed.
**CP10 trajectory open (Phase 4 brainstorm):** still pending — Phase
4 starts with `superpowers:brainstorming` before creative work.

## S6 — 2026-04-27 — `/review architecture` (post-Phase-3 structural review)

Architecture review dispatched per CP10 trajectory recommendation
("Architecture review not run alongside this codebase review. Should
be queued — Phase 3 introduced the entire `cicchetto/` subsystem and
the server↔client wire-shape contract; structural review of the
cross-boundary concerns is overdue."). 6 parallel concern agents:
abstraction boundaries, responsibility & cohesion, duplication,
dependency architecture, type system leverage, extension &
maintainability.

**Output:** `docs/reviews/architecture/2026-04-27-architecture-review.md`
— 53 raw findings → 30 deduped. Tally: 0 CRITICAL, 4 HIGH, 13 MEDIUM,
13 LOW.

**Five cross-cutting themes:**

1. **Cross-language wire-shape drift (the cicchetto seam)** — 9
   findings. Server-side single-source-of-truth discipline is strong;
   client-side mirrors are by-hand without compile-time link. Six
   cross-language enums (MessageKind, auth_method, meta keys, error
   tokens, topic strings, REST URL prefixes) all hand-mirrored. JSON
   decoding bypasses `tsconfig` strict via `as` casts. Phase 5
   `mix grappa.gen_ts_types` codegen is the structural answer; until
   then each enum needs a contract test.
2. **Phase 5 presence-event capture is half-built** — 4 findings.
   Wire contract advertises 10 MessageKinds; producer writes 1.
   `Scrollback.persist_privmsg/5` hardcoded `:privmsg`; Phase 5 needs
   `persist_event/1`. `Session.Server.handle_info` will balloon —
   needs `Grappa.Session.EventRouter` (pure dispatcher) before the
   first presence kind ships.
3. **Cohesion drift in three load-bearing modules** — 3 HIGH findings.
   `Grappa.Networks` (4 concerns, 7 deps), `Grappa.IRC.Client` (4
   concerns, 569 lines), `cicchetto/src/lib/networks.ts` (9 concerns,
   245 lines). Each absorbed Phase 2 + Phase 3 responsibilities
   without splitting. Phase 5/6 surface lands here unless split first.
4. **cicchetto module-singleton state without lifecycle** — 1 HIGH
   finding (A1). `joined`/`loadedChannels` Sets at module scope, no
   token-rotation cleanup. Logout-then-login-as-different-user shows
   empty scrollback + dropped WS messages. Pair with CP10 review S17
   line-level item.
5. **Extension paths are uneven** — REST endpoint addition (~6 files,
   smooth pattern) and mix task addition (1 file, helper triad) are
   well-rehearsed; adding a `MessageKind` is 9-10 files across two
   languages with no contract test; adding a context has no written
   recipe (pattern propagation will entrench the first accident).

**HIGH findings (4):**
- A1 cicchetto Set leak on token rotation
- A2 `Grappa.Networks` god-context (7 deps)
- A3 `Grappa.IRC.Client` god-module (transport+FSM+policy+crypto)
- A4 `cicchetto/src/lib/networks.ts` god-module (9 concerns)

**Direction recommendation (verbatim from review):** the user's stated
next move — fix all CP10 review correctness carryovers (C5 security +
C6 IRC-state + C7 channel-test flake + C8 omnibus) — is the right
immediate path; the review's MEDIUM findings are structural debt, not
correctness bugs, so they don't displace the correctness campaign.
The HIGH findings cluster into "D-series" architectural mini-clusters
that should land between the correctness campaign and Phase 4
brainstorm — A1 folds naturally into C7 (token-rotation cleanup is
half the S17 fix), A2/A3/A4 are each a half-session split-and-test
refactor that should NOT be deferred to "naturally surface during
Phase 4" (the same anti-pattern that produced this review). Phase 4
brainstorm begins AFTER C5-C8 correctness campaign closes AND after
at minimum A4 (cicchetto/lib/networks.ts split) lands, so the
brainstorm operates on clean modules instead of recreating the
god-module shape in components.

**Updated cluster sequence (this session):**
- C5: security correctness (S14 probing oracle + S18 token rotation)
- C6: IRC-state correctness (S5 + S6 + S7 + S13)
- C7: S17 channel-test flake — fold A1 cicchetto Set leak in scope
  (token-rotation cleanup is the same lifecycle question)
- C8: S29 dead-key + non-Phase-5 LOW omnibus + todo.md sweep
- D1 (architectural HIGHs, post-correctness, pre-Phase-4): A2 Networks
  split, A3 IRC.Client FSM extraction, A4 cicchetto/lib/networks.ts
  split. Each ~half-session. D1 may fold into Phase 4 brainstorm if
  the brainstorm itself surfaces a cleaner refactoring sequence.

## S7 — 2026-04-27 — C5 in progress (S14 LANDED on branch, S18 + merge pending)

C5 cluster started + paused mid-flight at user request ("context is
huge, stop soon"). S14 (probing-oracle close) committed to the
`c5-security` worktree branch + pushed to origin; S18 (cicchetto
socket token rotation) still pending. NOT merged to main — the cluster
is incomplete.

**Branch:** `c5-security` at `/home/vjt/code/IRC/grappa-c5-security`,
pushed to `origin/c5-security`. One commit:

- `e78ce24` fix(web): close S14 probing oracle via ResolveNetwork plug
  (CP10 S7, C5 / S14)

**S14 LANDED on branch:**
- New `GrappaWeb.Plugs.ResolveNetwork` running after `Plugs.Authn` —
  resolves `:network_id` slug + per-user credential check; failure
  collapses to 404 `:not_found` (uniform body). On success assigns
  `:network` schema struct.
- Router restructured: `/networks/:network_id` scope pipes through
  `[:api, :authn, :resolve_network]`. `/me`, `/networks` index,
  `/auth/logout` keep `[:api, :authn]`.
- MessagesController + ChannelsController actions read
  `conn.assigns.network` instead of re-resolving.
- FallbackController `:no_session` clause renders `{"error":
  "not_found"}` body. Internal `:no_session` tag preserved in Session
  boundary @specs + operator log tracing.
- 4 new oracle-close tests + 3 updated assertions. Stats: 8
  properties, 431 tests (was 427), 0 failures.

**S18 PENDING (next session resume point):** `cicchetto/src/lib/socket.ts`
token rotation. Currently:
```ts
createEffect(() => {
  const t = token();
  if (t && !s.isConnected()) s.connect();
  if (!t && s.isConnected()) s.disconnect();
});
```
Token rotation A → B (both non-null) keeps the live connection up
with token A pinned because phoenix.js's `params: () => ({ token:
token() ?? "" })` is invoked at handshake time only. Phase 5
token-refresh would silently route under wrong identity.

**Fix shape:** track previous token; on `prev → curr` transition where
both non-null AND different, force `s.disconnect(); s.connect();` so
the new bearer is on the next handshake. TDD-first test in
`cicchetto/src/__tests__/socket.test.ts` pinning "token A → token B
forces reconnect" before the implementation. Code review subagent
afterward, then merge --no-ff to main, deploy, verify, CP10 S8 entry.

**Resume protocol next session:**
1. `/start` to read this entry + CP10 trajectory
2. `cd /home/vjt/code/IRC/grappa-c5-security` (worktree still in place)
3. Read `cicchetto/src/lib/socket.ts` lines 39-49 + `socket.test.ts`
   for current token-rotation surface
4. Read CP10 review S18 verbatim from
   `docs/reviews/codebase/2026-04-27-codebase-review.md:321-332`
5. Write failing test pinning A→B forces reconnect
6. Implement
7. Run `scripts/bun.sh run check` + `scripts/bun.sh run test`
8. Commit S18 to `c5-security`
9. Run `scripts/check.sh` (Elixir gate) + cicchetto gates
10. Code review subagent → apply feedback → merge --no-ff to main
11. Deploy + verify + CP10 S8 closure entry

After C5 closes: C6 (IRC-state correctness — S5/S6/S7/S13), C7 (S17
flake + folds A1 cicchetto Set leak), C8 (S29 + LOW omnibus + todo.md),
D1 (architectural HIGHs A2/A3/A4 splits). Phase 4 brainstorm AFTER
all four close.

**Live deploy state:** main HEAD is `6231526` (CP10 S6 architecture
review). Production at `http://grappa.bad.ass` is running C4-era code
(post-`1bad0b0`). The C5/S14 fix is NOT yet deployed — it lives on
the `c5-security` branch only. Next session deploys after merging the
complete C5 cluster (S14 + S18 + review feedback).

## S8 — 2026-04-27 — C5 cluster: security correctness LANDED

Resumed mid-flight from the S7 hand-off via the orchestrator file-handoff
flow (sibling Claude wrote `/tmp/orchestrate-next.txt` carrying full
context, orchestrator `/clear`'d the parent, this session re-entered with
zero memory and read the hand-off as its first action). Cluster closed
clean — TDD-first on S18, code-review subagent on the full
`1bad0b0..HEAD` range (3 commits at review time, 4 after applied
feedback), `--no-ff` merge with cluster-summary, deploy + verification.

**Worktree:** `c5-security` at `/home/vjt/code/IRC/grappa-c5-security`,
rebased onto main (which had advanced to `fc99bab` since the original
branch point). Four commits:

- `9a623fe` fix(web): close S14 probing oracle via ResolveNetwork plug
  (CP10 S7, C5 / S14)
- `97782da` fix(web): bare `_` for unused vars (CP10 S8, C5 hygiene)
- `110d0fd` fix(cicchetto): force WS reconnect on token rotation
  (CP10 S8, C5 / S18)
- `ea42ffa` docs(c5): apply review feedback — moduledoc post-S14 + S18
  (CP10 S8)

Cluster-summary merge `ab13085` to main with full per-item disposition
in the body.

**Per-item disposition:**

| Item | Status | Approach |
|------|--------|----------|
| S14 probing oracle | LANDED | New `GrappaWeb.Plugs.ResolveNetwork` runs after `Plugs.Authn`; resolves `:network_id` slug + per-user credential check; failure collapses to uniform 404 `{"error": "not_found"}` body. Router restructured: `/networks/:network_id` scope pipes through `[:api, :authn, :resolve_network]`. Controllers read `conn.assigns.network`. `FallbackController` `:no_session` clause renders `not_found` body but preserves the internal `:no_session` tag for operator-log tracing. 4 new oracle-close tests + 3 updated assertions. Behaviorally pinned (status code + body), not structurally. |
| S18 socket token rotation | LANDED | `createEffect(on(token, (t, prev) => ...))` from solid-js gives prev/curr semantics; rotation arm detects `prev != null && prev !== t` and force-disconnects+reconnects so phoenix.js's handshake-time `params` callback re-evaluates against the new bearer. Transition matrix exhaustive (cold/login/logout/rotation/no-op/re-login); test pins behavior (WS-level effect) not implementation (`params` callback invocation). |
| Branch hygiene | LANDED | Three Credo `Consistency.UnusedVariableNames` warnings introduced by S14 (`_opts`, `_credential`, `_params`) → bare `_` per project convention (`Plugs.Authn.call(conn, _)`). Separate atomic commit, not folded into S14 (CLAUDE.md bans `--amend`). |
| Doc fixes (post-review) | LANDED | Two stale moduledoc fragments in `channels_controller.ex` + `messages_controller.ex` claimed `error: "no_session"` on the wire; corrected to uniform `not_found` body to keep the docs honest. `socket.ts` moduledoc gained a paragraph noting the rotation tear-down + rejoin loop side-effect (phoenix.js auto-rejoin behavior) for Phase 5 telemetry. |

**Stats:** 4 commits / +85 / −22 / +5 tests (Elixir 427 → 431; cicchetto
44 → 46). Gates green at HEAD: 8 properties, 0 failures, Dialyzer 0
errors, Credo 0 issues, Sobelow 0, doctor 100%, format clean. Cicchetto:
46 vitest pass, Biome + TypeScript strict clean.

**Code review verdict (`superpowers:code-reviewer` agent):** SHIP-IT.
Zero MUST-fixes, one SHOULD-fix (stale moduledocs) applied as the 4th
cluster commit. Five defers logged for Phase 5 / future clusters:

- Telemetry counter on `network resolve rejected` (Phase 5 PromEx).
- Rate-limit hardening on the probe path (Phase 5 security pass).
- Factor a `Plug.fallback/2` helper if a third such plug arrives.
- Drop redundant `else` clause in `ResolveNetwork.resolve/2` (style).
- `Accounts.get_user!/1` raises on deleted-but-not-revoked-session
  user — defensible (let-it-crash on impossible state since `Authn`
  validated the session) but worth a moduledoc note in a future touch.

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

- `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
- `scripts/monitor.sh` boot trace: `bootstrap done credentials=1
  started=1 failed=0` — Azzurra session re-established post-deploy;
  `irc event command=join channel=#grappa` line confirms upstream
  connection back up and joined
- Browser-shell verification (`/login` via Chrome DevTools MCP): shell
  renders, console clean (zero errors / warnings), new bundle hash
  `index-Bus1Mh10.js` confirms the post-S18 build is being served.
  Full login-then-rotation dance not exercised — the only prod user is
  `vjt` and the password is not in the repo (correctly: env-only per
  CLAUDE.md security section). The S18 rotation arm (non-null A →
  non-null B) is unit-tested via the new vitest pin and the transition
  matrix is walked in the code review; it does not trigger naturally
  in the current single-user prod (no token-refresh, no admin-driven
  re-issue) until Phase 5 lands those flows.

**Cluster status (post-merge):** C1 + C2 + C3 + C4 + C5 closed. Five
post-CP10-review clusters done. Worktree-per-cluster pattern + `/clear`
between clusters held cleanly across all five; the file-handoff resume
flow (sibling-writes-context-then-orchestrator-clears) worked
end-to-end on its first cross-cluster test. Per-cluster commit-then-
merge-`--no-ff` rhythm produced five self-documenting `git log` entries
each with the cluster's per-item disposition spelled out.

**DESIGN_NOTES:** no new entry. The S14 fix is a domain-specific
oracle close (general principle "uniform body on auth failures across
all routing classes"); the S18 fix is a token-lifecycle correctness
patch with no new cross-cutting principle (the rotation handling is
a phoenix.js behavior workaround, not a Solid pattern worth pinning).
DESIGN_NOTES selectivity is the C-cluster norm: only C2 and C3 added
entries (init/1 contract + wire-shape source-of-truth); C4 added one
(boot-time-vs-runtime Application.env timing rule). C1 and C5 added
none.

**CP10 trajectory complete (correctness clusters):** C1 + C2 + C3 +
C4 + C5 closed. Remaining correctness work: C6 (IRC-state — S5/S6/S7/
S13), C7 (S17 channel-test flake + folds A1 cicchetto Set leak), C8
(S29 dead-key + LOW omnibus + todo.md sweep), then D1 (architectural
HIGHs A2/A3/A4 splits — post-correctness, pre-Phase-4).
**CP10 trajectory open (Phase 4 brainstorm):** still pending — Phase 4
starts with `superpowers:brainstorming` AFTER all correctness
clusters close.

**Live deploy state:** main HEAD is `ab13085` (C5 cluster merge).
Production at `http://grappa.bad.ass` is running C5-era code with both
the probing-oracle close AND the token-rotation force-reconnect
deployed. Next cluster session: C6 (IRC-state correctness) starting
fresh.

## S9 — 2026-04-27 — C6 cluster: IRC-state correctness LANDED

Sixth post-CP10-review fix cluster. Resumed cleanly from CP10 S8 via
the orchestrator file-handoff flow (sibling Claude wrote
`/tmp/orchestrate-next.txt`, this session re-entered with zero memory
and read the hand-off as first action — second cross-cluster test of
the resume mechanism, worked end-to-end without intervention).

**Worktree:** `c6-irc-state` at `/home/vjt/code/IRC/grappa-c6-irc-state`,
branched from local main (`a3ec61c`), merged via `--no-ff` to
preserve the cluster boundary, deployed via `scripts/deploy.sh`,
removed after.

**Per-item disposition:**

| Item | Status | Approach |
|------|--------|----------|
| S5 send_pong contract drift | LANDED | Two-sided fix. (1) `Grappa.IRC.Parser.strip_crlf/1` strips ALL `\r`/`\n` (not just trailing) via single-pass `:binary.replace/4` global — produced `params`/`command`/`prefix` tokens guaranteed CR/LF-free, RFC 2812 invariant pinned. (2) `Client.send_pong/2` drops `safe_line_token?/1` guard; spec narrows to `:ok` from `:ok | {:error, :invalid_line}`. Other helpers (send_privmsg/join/part/quit) keep guards — those accept user input via REST + Channels surface, where CR/LF injection IS a real attack. Test surface: 5 unit tests in `parser_test.exs` + 1 property test in `parser_property_test.exs` (review MUST) generating adversarial input with random CR/LF insertions at arbitrary positions. |
| S6 caps_buffer phase exit | LANDED | New `leave_cap_negotiation/2` private helper owns BOTH `:phase` AND `:caps_buffer` — single source of truth for any phase change that should clear the accumulator. Routed every escape point through it: numeric 1, numeric 903, maybe_send_cap_end, finalize_cap_ls (review SHOULD aligned the LS → AWAIT_ACK transition with the helper for consistency, even though inline reset was functionally equivalent). Today's residue is harmless; Phase 5 reconnect-with-backoff would have inherited stale chunks. TDD pin: 001-during-awaiting_cap_ls test where server emits welcome direct from continuation. |
| S7 Scrollback.insert/1 surface | LANDED | New `test/support/scrollback_helpers.ex` (with `use Boundary, top_level?: true, deps: [Grappa.Repo, Grappa.Scrollback]` matching `auth_fixtures.ex` pattern) exposes `insert/1` calling `Message.changeset/2 \|> Repo.insert()` directly. Public `Scrollback.insert/1` removed; `persist_privmsg/5` no longer routes through it (inlines the changeset+insert pair). Migration: every test caller (35+ across 5 files) swapped to ScrollbackHelpers + alias updated, single commit so the suite stays green at every revision boundary. CLAUDE.md "Total consistency or nothing" — half-migrated would create two patterns. Boundary contract: Grappa.Scrollback's public surface now matches its moduledoc invariant. |
| S13 nick mutation tracking | LANDED | `Session.Server.handle_info` gains two new clauses. (1) 001 RPL_WELCOME extracts the welcomed nick from params[0] and unconditionally writes it to state.nick (logs `from:`/`to:` on mismatch). (2) self-NICK (`Message.sender_nick(msg) == state.nick`) updates state.nick in place; other-user NICK falls through to the existing @logged_event_commands clause for logging. Pre-001 hole comment names the Phase 5 reopen + fix shape (review SHOULD) — today fatal-on-433 keeps it theoretical. New `:from`/`:to` Logger metadata keys added to the allowlist per project memory rule. Three TDD tests with PING/PONG flush idiom for cross-process pipeline sync — `:sys.get_state` alone serializes only the destination GenServer, not TCP buffer + Client mailbox. |

**Five commits on the branch:**

  * `cefe620` fix(irc): pin parser CR/LF invariant + drop send_pong guard
  * `9113c42` fix(irc): clear caps_buffer on every CAP-phase exit
  * `ae82af3` refactor(persistence): drop Scrollback.insert/1 from public surface
  * `7d5a6f1` fix(session): track upstream nick mutations in state.nick
  * `c8316c6` fix(c6): apply review feedback (MUST + 2 SHOULDs)

Cluster-summary merge `dd417d3` to main with full per-item
disposition in the body.

**Stats:** +361 / −98, 9 properties (was 8), 439 tests (was 431),
0 failures. Dialyzer 0 errors, Credo 0, Sobelow 0, format clean,
doctor 100%.

**Code review verdict (`superpowers:code-reviewer` agent):** SHIP-IT.
One MUST (CR/LF property test — coverage gap not pinned by example
tests) + two SHOULDs (finalize_cap_ls helper symmetry + S13 pre-001
hole comment) applied as commit `c8316c6`. Five CONSIDERs deferred
(generic `:from`/`:to` log key collision risk on future use,
malformed-NICK redundancy in @logged_event_commands, scrollback_test
↔ message_test changeset-validation overlap post-S7, persist_privmsg
inline shape vs `do_insert/1` extraction, PING/PONG flush idiom
helper extraction) — all "functionally fine, revisit when third call
site appears" shape.

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

  * `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
  * `scripts/monitor.sh` boot trace: `phase 1 TLS posture: verify_none`
    warning fires synchronously in init; `bootstrap done credentials=1
    started=1 failed=0` reports the Azzurra session as started; `irc
    event command=mode` and `irc event command=join channel=#grappa
    sender=grappa` lines confirm upstream connection back up + autojoin
    completed
  * Browser-shell verification skipped — C6 fixes are server-side
    IRC-state changes; cicchetto bundle unchanged (same `index-Bus1Mh10.js`
    hash from C5 deploy), no rendering or WS-protocol surface change

**Cluster status (post-merge):** C1 + C2 + C3 + C4 + C5 + C6 closed.
Six post-CP10-review clusters done. Worktree-per-cluster pattern +
`/clear` between clusters held cleanly across all six; the
file-handoff resume flow (sibling-writes-context-then-orchestrator-
clears) tested its second cross-cluster run end-to-end. Per-cluster
commit-then-merge-`--no-ff` rhythm produced six self-documenting
`git log` entries each with the cluster's per-item disposition spelled
out.

**DESIGN_NOTES:** new entry candidate — "exiting a phase clears all
phase-local state in one place" (S6 `leave_cap_negotiation/2` is the
example; the rule applies to any state-machine GenServer with
phase-locked fields). Phase 5 reconnect/backoff and the A3 IRC.Client
FSM extraction will benefit. Decision deferred to next D-cluster
session — the principle is general but its second use case (the
"second similar exit shape" CLAUDE.md design discipline calls for)
hasn't materialized yet. Logging a TODO; if Phase 5 surfaces a
clean second instance, pin then.

**CP10 trajectory complete (correctness clusters):** C1 + C2 + C3 +
C4 + C5 + C6 closed. Remaining correctness work: C7 (S17 channel-test
flake + folds A1 cicchetto Set leak), C8 (S29 dead-key + LOW omnibus
+ todo.md sweep), then D1 (architectural HIGHs A2/A3/A4 splits —
post-correctness, pre-Phase-4).

**CP10 trajectory open (Phase 4 brainstorm):** still pending — Phase 4
starts with `superpowers:brainstorming` AFTER all correctness
clusters close.

**Live deploy state:** main HEAD is `dd417d3` (C6 cluster merge).
Production at `http://grappa.bad.ass` is running C6-era code with the
parser CR/LF invariant + caps_buffer phase-exit cleanup + nick
mutation tracking deployed. Next cluster session: C7 (S17 channel-test
flake + folds A1 cicchetto Set leak) starting fresh.

## S10 — 2026-04-27 — C7 cluster: channel-lifecycle correctness LANDED (collapsed to A1)

Seventh post-CP10-codebase-review fix cluster. Resumed cleanly from CP10
S9 via the orchestrator file-handoff flow (third cross-cluster test of
the resume mechanism — sibling Claude wrote `/tmp/orchestrate-next.txt`
carrying full context, this session re-entered with zero memory and
read the hand-off as first action; worked end-to-end without
intervention).

**Cluster scope evolved.** The brief paired two findings under one
lifecycle theme: S17 (codebase review carryover, server-side
`grappa_channel_test:76` flake) and A1 (2026-04-27 architecture
review HIGH, cicchetto module-scoped Sets leaking across token
rotation). After investigation the cluster collapsed to **A1 only**.

**Worktree:** `c7-channel-lifecycle` at
`/home/vjt/code/IRC/grappa-c7-channel-lifecycle`. Branched from local
main (`fea85e0`), merged via `--no-ff` to preserve the cluster
boundary, deployed via `scripts/deploy.sh`, removed after.

**Per-item disposition:**

| Item | Status | Approach |
|------|--------|----------|
| S17 grappa_channel_test:76 flake | RESOLVED-UPSTREAM | 15 consecutive runs of `mix test` (full suite, container's `max_cases: 2`) showed zero `grappa_channel_test` failures. The CP10 carryover description (lines 115-120) is stale; the C2 cluster's `stop_session/2` race fix in the prior campaign almost certainly closed it upstream. Other flakes did surface during the verification — Grappa.BootstrapTest:80 (`SessionSupervisor.terminate_child` no-process when supervisor exhausted `max_restarts: 3` in another test), Networks.WireTest sqlite-busy, AccountsTest sqlite-busy — those are pre-existing per CP10 carryover documentation and out of C7 scope. Logged for D-cluster triage. |
| A1 cicchetto Set leak on token rotation | LANDED | Two module-scoped Sets (`joined`, `loadedChannels`) and three signals (`scrollbackByChannel`, `unreadCounts`, `selectedChannel`) inside `createRoot` were leaking across identity transitions. Composite key `${slug} ${name}` is user-agnostic, so logout-then-login-as-different-user inherited the previous user's scrollback AND silently skipped re-installing the WS event handler under the new identity (the join effect short-circuited at `joined.has(key)`). Fix: move both Sets inside the `createRoot` closure alongside the signals, then add `createEffect(on(token, (t, prev) => …))` registered BEFORE the join effect so cleanup runs first on the same flush. The `prev != null` loose-equality guard filters BOTH the initial-run (prev === undefined) and the cold-start login (prev === null) — both are no-ops; only logout (prev → null) and rotation (prev → t, both non-null distinct) clear state. Two TDD vitest cases pin both transitions. |

**Two commits on the branch + cluster-summary merge:**

  * `056ad05` fix(cicchetto): clear identity-scoped state on token
    rotation/logout
  * `5a8a4b0` merge: C7 cluster — channel-lifecycle correctness LANDED
    (CP10 S10) — `--no-ff` to main

**Stats:** 1 commit / +120 / −7 / 2 files. Cicchetto gates: 48 vitest
pass (was 46), Biome + tsc strict clean. Server gates unchanged: 9
properties, 439 tests, 0 failures, Dialyzer 0 errors, Credo 0,
Sobelow 0, format clean, doctor 100%.

**Code review verdict (`superpowers:code-reviewer` agent):** SHIP-IT.
Zero MUSTs, zero SHOULDs. Three CONSIDERs deferred to Phase 5+:

  * Transient stale-resource window during rotation (one rejected
    `phx_join` per channel before the `me` resource resolves the new
    user) — bounded, server authz rejects, defer to Phase 5 telemetry.
  * Stale phoenix.js channel handles after rotation (auto-rejoin
    chatter on doomed handles) — bounded memory leak, freed on tab
    close. Don't couple `networks.ts` to `socket.ts` internals; the
    C5/S18 split chose the cleaner layering.
  * `fireMessageEvent` test helper accumulates handler refs across
    rotations — not a bug today (no rotation test fires post-rotation
    events through it); one-line note if helper grows.

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

  * `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
  * `scripts/monitor.sh` boot trace: `phase 1 TLS posture: verify_none`
    warning; `bootstrap done credentials=1 started=1 failed=0`;
    `irc event command=mode pid=… sender=grappa` and
    `irc event command=join channel=#grappa pid=… sender=grappa`
    confirm upstream connection back up + autojoin completed
  * Bundle hash bumped to `index-DFEoTCDC.js` (was `index-Bus1Mh10.js`
    from C5/S18 deploy — confirms the cicchetto-side change is being
    served). SW precache manifest references the same hashes (12
    entries, 95.26 KiB).
  * Browser-shell `/login` via Chrome DevTools MCP: shell renders
    (a11y tree confirms heading `cicchetto`, Name/Password fields,
    Log in button), console clean (only host browser-extension
    chatter). Full login → logout → re-login dance not exercised —
    only one prod user (vjt), password env-only per CLAUDE.md
    security rule. The rotation/logout cleanup arm is unit-tested
    via the two new vitest pins; the transition matrix is walked in
    code review; it does not trigger naturally in the current
    single-user prod (no token-refresh, no admin-driven re-issue)
    until Phase 5 lands those flows.

**Cluster status (post-merge):** C1 + C2 + C3 + C4 + C5 + C6 + C7
closed. Seven post-CP10-review correctness clusters done. Worktree-
per-cluster pattern + `/clear` between clusters held cleanly across
all seven; the file-handoff resume flow tested its third cross-
cluster run end-to-end.

**DESIGN_NOTES:** no new entry. The "identity-scoped state must be
cleared on identity transitions" principle is pinned in the
`networks.ts` comment block and mirrors C5/S18's `socket.ts` rotation
arm; a single second use case (the cleanup arm here mirrors the
disconnect+reconnect arm there) suggests the rule is worth lifting to
DESIGN_NOTES if a third client-side identity-bound holder appears in
Phase 4/5.

**CP10 trajectory complete (correctness clusters):** C1 + C2 + C3 +
C4 + C5 + C6 + C7 closed. Remaining correctness work: C8 (S29 dead-key
+ LOW omnibus + todo.md sweep — also includes the BootstrapTest /
WireTest / AccountsTest flake observations surfaced during S17
verification, for D-cluster triage). Then D1 (architectural HIGHs
A2/A3/A4 splits — post-correctness, pre-Phase-4).

**CP10 trajectory open (Phase 4 brainstorm):** still pending — Phase 4
starts with `superpowers:brainstorming` AFTER all correctness
clusters close.

**Live deploy state:** main HEAD is `5a8a4b0` (C7 cluster merge).
Production at `http://grappa.bad.ass` is running C7-era code with the
cicchetto identity-scoped state cleanup deployed; bundle hash
`index-DFEoTCDC.js` confirms post-A1 build is being served. Next
cluster session: C8 (S29 dead-key + LOW omnibus + todo.md sweep)
starting fresh.


## S11 — 2026-04-27 — C8 cluster: omnibus housekeeping LANDED (CP10 review correctness campaign closed)

Eighth post-CP10-codebase-review fix cluster — the omnibus close-out
covering all FIX-NOW LOW findings (S22-S55), the dead `:reason` key
centerpiece (S29), and the todo.md sweep with C7-surfaced flake
observations parked for D-cluster triage. Resumed cleanly from CP10
S10 via the orchestrator file-handoff flow (fourth cross-cluster test
of the resume mechanism — sibling Claude wrote `/tmp/orchestrate-next.txt`
carrying full context, this session re-entered with zero memory and
read the hand-off as first action; worked end-to-end without
intervention).

**Worktree:** `c8-omnibus-cleanup` at
`/home/vjt/code/IRC/grappa-c8-omnibus-cleanup`. Branched from local main
(`ddb5b62`), merged via `--no-ff` to preserve the cluster boundary,
deployed via `scripts/deploy.sh`, removed after.

**Per-LOW disposition (33 findings triaged, 22 FIX-NOW landed across 11 commits):**

| Category | IDs | Outcome |
|----------|-----|---------|
| FIX-NOW landed | S22, S23, S25, S27, S28, S29, S30, S34, S37, S38, S40, S41, S42, S43, S45, S47, S48, S49, S50, S51, S52, S53 | 11 thematic commits |
| WON'T-FIX (rationale in commit msgs) | S26, S31, S44, S46 | See cluster-summary merge commit body for each |
| DEFER-PHASE-5 | S24, S32, S33, S35, S36 | Phase-5-class — pre-flagged in brief |
| ALREADY-CLOSED | S39, S54, S55 | Closed in earlier clusters; verified at `ddb5b62` |

**Centerpiece (S29):** `:reason` was in the `Grappa.Scrollback.Meta`
`@known_keys` allowlist but the moduledoc per-kind shape table places
it nowhere — `:quit` and `:kick` both explicitly state "body carries
reason." No producer wrote it. Dropped from server allowlist + spec;
also dropped the defensive `meta.reason` fallback in cicchetto's
`ScrollbackPane.reasonOf/1` (originally added in C3 as forward-compat
insurance for the case S29 closes today). Per CLAUDE.md "Total
consistency or nothing" — server can't ship the field, client mustn't
read it. The `:reason` Logger metadata key in `config/config.exs` is
unrelated (logs GenServer crash reasons like `:tcp_closed`) and stays.

**Notable WON'T-FIX dispositions:**

  * **S26** — Session-side `send_*` line-token guards. The reviewer
    wanted them dropped as duplicates of Client-side guards; they are
    NOT duplicates. The Session-side `send_privmsg` guard preserves
    the S14 oracle property: `:invalid_line` precedes `:no_session`
    so a probing attacker submitting a malformed line cannot infer
    network existence. The cast paths' `send_join` / `send_part`
    guards are also load-bearing because `:invalid_line` from Client
    cannot propagate through cast back to the controller.
  * **S31** — `@spec changeset(t() | %__MODULE__{}, map())`.
    Reviewer claimed redundant; Dialyzer surfaced `invalid_contract`
    on collapse, proving the union is meaningful: `t()` declares
    non-nilable fields (`channel: String.t()` etc.), `%__MODULE__{}`
    is the bare struct with all-nil. Pre-insert callers really do
    pass bare structs that are NOT a `t()`. Per CLAUDE.md "Dialyzer
    warnings are design signals" — original spec stays.
  * **S44** — `def join(topic, _, socket)` → `_payload`. Universal
    Phoenix convention is `_payload`; Credo's
    Consistency.UnusedVariableNames flagged it as inconsistent with
    THIS codebase's universal `_` convention. Per CLAUDE.md "Total
    consistency or nothing" — local convention wins.
  * **S46** — `createRoot` dispose for cicchetto tests. Tests don't
    currently invoke `vi.resetModules()` on the relevant modules; the
    fix is purely preventive. Per CLAUDE.md "Don't add for
    hypothetical future requirements."

**Eleven commits on the branch + cluster-summary merge:**

  * `4fc8e23` fix(scrollback): drop dead :reason key (S29)
  * `9337c86` docs: doc/comment-only sweep (S22 S25 S34 S52 S53)
  * `638ace7` refactor: rename placeholders parse_/1 + _ join (S23 S44)
  * `c960a7f` refactor: tighten IRC numeric type + Meta typing
    (S27 S28 S30; S31 WON'T-FIX)
  * `bc6738b` fix: tighten Topic.parse user_name guard +
    pattern-match terminate_child (S37 S38)
  * `d97f615` refactor(web): controller hygiene + plug user-load +
    S44 revert (S40 S41 S42 S43)
  * `c6aeff9` fix(cicchetto): channel-join error surface + Login
    strict-equality + drop dead exports (S47 S48 S49)
  * `7adf783` refactor(cicchetto): drop fromEntries cast + real Solid
    signal in ScrollbackPane test (S50 S51)
  * `d0c053b` fix(cicchetto): iOS PWA meta tags for standalone (S45)
  * `23f3328` docs(todo): C1-C8 closed sweep + D-cluster flake triage
  * `8896e36` fix: code-review follow-ups (S42 plug dedupe, S29 doc,
    S40 test, todo dup)
  * cluster-summary merge to main — `--no-ff`

**Stats:** 11 commits / +374 / −179 / 33 files. Server gates: 9
properties, 442 tests (was 439 — added S38 + S40 regression + S42
plug-load contract pin), 0 failures, Dialyzer 0 errors, Credo 0
issues, Sobelow 0, format clean, doctor 100%. Cicchetto gates: 49
vitest pass (was 48 — added S51 reactivity pin), Biome + tsc strict
clean.

**Code review verdict (`superpowers:code-reviewer` agent):** SHIP-IT.
Zero MUSTs. Five SHOULDs landed in `8896e36` before merge:

  1. S42 ResolveNetwork still re-fetched the user — d97f615 plug
     change incomplete, only Me/Networks/Channels-index dropped the
     re-fetch but `/networks/:network_id/*` still paid the duplicate.
  2. Authn plug test missing `:current_user` assertion.
  3. ScrollbackPane.tsx doc still listed `reason` in meta-allowlist
     mirror comment (same finding S29 closed, just upstream in file).
  4. todo.md sweep accidentally created duplicate `## High` header
     with truncated Phase-5 entry.
  5. S40 missing GET-side regression test.

Six CONSIDERs noted for future passes (none ship-blocking): S45
forward-compat `mobile-web-app-capable` tag, PNG `apple-touch-icon`
fidelity, S26/S31/S44/S46 disposition validation (all confirmed
correct), Meta.t() shape question (current shape correct), S38
coverage sufficient, S48 chain mocks correct, S51 cross-test leak
risk only emerges with hypothetical `vi.resetModules()`.

**Verification (live `http://grappa.bad.ass` after `scripts/deploy.sh`):**

  * `GRAPPA_PROD=1 scripts/healthcheck.sh` → `ok`
  * `scripts/monitor.sh` boot trace: `phase 1 TLS posture: verify_none`
    warning; `bootstrap done credentials=1 started=1 failed=0`;
    `irc event command=mode pid=… sender=grappa` and
    `irc event command=join channel=#grappa pid=… sender=grappa`
    confirm upstream connection back up + autojoin completed
  * Bundle hash bumped to `index-yiUejGMf.js` (was `index-DFEoTCDC.js`
    from C7/A1 deploy — confirms cicchetto-side changes are being
    served). SW precache manifest 12 entries / 96.06 KiB.
  * Browser-shell `/login` via Chrome DevTools MCP: shell renders
    (a11y tree confirms heading `cicchetto`, Name/Password fields,
    Log in button), console clean (only host browser-extension
    chatter + page-load metrics; no app errors).

**Cluster status (post-merge):** C1 + C2 + C3 + C4 + C5 + C6 + C7 + C8
closed. Eight post-CP10-review correctness/hygiene clusters done. The
worktree-per-cluster pattern + `/clear` between clusters held cleanly
across all eight; the file-handoff resume flow tested its fourth
cross-cluster run end-to-end.

**DESIGN_NOTES:** no new entry. C8 by definition is "many small
fixes, no central principle." The S26 + S31 dispositions both
crystallize the same underlying rule ("layered guards / spec unions
are design signals, not redundancy"), but a single second use case
isn't enough to lift to DESIGN_NOTES — re-evaluate if a third instance
appears.

**CP10 trajectory complete (correctness + hygiene clusters):** C1 + C2
+ C3 + C4 + C5 + C6 + C7 + C8 closed. The CP10 codebase-review fix
campaign is CLOSED. Remaining work surfaced from the CP10 review:
- D-cluster: architectural HIGHs (A2 Networks split, A3 IRC.Client
  FSM extraction, A4 cicchetto/lib/networks.ts split). Each
  ~half-session. Pre-Phase-4. May fold into Phase 4 brainstorm.
- D-cluster triage: BootstrapTest:80 max_restarts flake, WireTest +
  AccountsTest sqlite-busy. Each needs investigation pass larger than
  housekeeping (sqlite WAL-mode-in-test, sandbox shared-mode review,
  per-test SessionSupervisor isolation flag).
- Phase 4 brainstorm (irssi-shape UI redesign) — after D1 lands.

**Live deploy state:** main HEAD is the C8 cluster-summary merge
commit. Production at `http://grappa.bad.ass` is running C8-era code;
bundle hash `index-yiUejGMf.js` confirms post-S45/S47/S48/S50/S51
build is being served. Next cluster session: D1 (architectural HIGHs)
starting fresh via the same orchestrator file-handoff flow.

