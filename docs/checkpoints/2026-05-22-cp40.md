# CP40 — REV cluster mid-flight (2026-05-22)

## Status

**REV cluster bucket 6 of 11 — REV-F LANDED.** CP39 rotated at the
REV-D LANDED threshold (770 lines) per orchestrator plan; CP40
carries REV-E + REV-F + likely REV-G before next rotation.

Branch: `main`. Pushed to origin (`6574f0e` — REV-F LANDED head).
Container: HOT-deployed 2026-05-22 ~11:55 UTC (Phoenix.CodeReloader
swap, sessions preserved, container ID unchanged); healthcheck `ok`
via substrate-deep `/healthz`. Verified post-deploy that the new
`:awaiting_cap_ack_combined` phase + `dispatch_subject_verb`
catch-all source ARE visible in the live BEAM's bind-mount
(9 occurrences of `awaiting_cap_ack_combined` in auth_fsm.ex;
`"subject verb: upstream send failed"` Logger message present).

## REV cluster context

Per `project_post_tmu_full_review_scheduled` (vjt 2026-05-16 night
mandate: post-iOS-cluster-Z, fix ALL CRIT/HIGH + most-important
MED from the comprehensive review wave). Full review at
`docs/reviews/codebase/2026-05-22-codebase-review.md` (113 findings:
4 CRIT, 29 HIGH after dedup, 50 MED, 27 LOW). 11-bucket plan:

- ~~REV-A~~ — LANDED (`ad7565f`, CP38) — cross-surface wire arms (C1, C2, H1)
- ~~REV-B~~ — LANDED (`e21c299`, CP39) — persistence (C3, H6, H17, H18)
- ~~REV-C~~ — LANDED (`84ccc68`, CP39) — substrate preflight + healthcheck depth (C4, H20, H21, H26)
- ~~REV-D~~ — LANDED (`fc5d221`, CP39) — silent-swallow at boundaries (H12, H13, H14, H15, H16, M16, M17)
- ~~REV-E~~ — LANDED (`1980035`, CP40) — `:ok = Client.send_*` regression sweep (H11) — server, HOT
- ~~REV-F~~ — LANDED (`6574f0e`, CP40) — IRC SASL combined-REQ fallback + dispatch_subject_verb catch-all (H9, H10) — server, HOT
- **REV-G** — cic SW denylist + adminEvents narrower + markerRef leak (H22, H24, H23) — cic, HOT cic-only
- **REV-H** — server-side type tightening Theme A continued (H2, H3, H4, H5, H7, H8, H25) — both, COLD
- **REV-I** — infra simplification (nginx snippets + bin/grappa table + docker dedup) (H19, H27, M1-M6) — infra, COLD
- **REV-J** — cross-cutting smells (cross-module + lifecycle + persistence MEDs)
- **REV-K** — cross-surface naming pay-down (sender_nick → sender + error envelope unify) (M19, M20) — both, COLD
- **REV-Z** — docs sweep + README closed-clusters entry + LOW liquidation that fits — docs only

Standing autopilot mandate (vjt asleep): orchestrator dispatched.
Per-bucket reviewer-loop mandatory. Per-bucket deploy + healthcheck.
LANDED requires literal gate-tail paste. Push autonomy once green.

## What's deployed RIGHT NOW (REV-F close state)

- **Server**: `6574f0e` (REV-F LANDED). HOT-deployed 2026-05-22
  ~11:55 UTC via `Phoenix.CodeReloader.reload/1` — sessions preserved,
  container ID unchanged.
- **cic bundle**: `D5kebe2p` (REV-A — unchanged through REV-B + REV-C
  + REV-D + REV-E + REV-F; all REV buckets so far have been server-only).
- **Healthcheck**: `ok` via substrate-deep `/healthz`.
- New live since REV-F:
  - `Grappa.IRC.AuthFSM`'s `@type phase` widened with two new phases:
    `:awaiting_cap_ack_combined` (combined `CAP REQ :sasl
    labeled-response` in flight) + `:awaiting_cap_ack_sasl_only`
    (post-combined-NAK fallback `CAP REQ :sasl` in flight).
  - `finalize_cap_ls/2` now transitions to `:awaiting_cap_ack_combined`
    for the both-caps shape; `:awaiting_cap_ack` reserved for the
    standalone-cap shapes (`:sasl` only OR `:labeled-response` only).
  - `handle_cap/2` NAK clause split per-phase: combined NAK → fallback
    REQ; standalone OR fallback NAK → existing `cap_unavailable/1`.
  - `handle_cap/2` ACK clause guard widened to match all three
    awaiting-ACK phases (semantics identical).
  - `maybe_send_cap_end/1` recognises both new phases for the explicit
    teardown path.
  - `GrappaWeb.GrappaChannel.dispatch_subject_verb/3` gains catch-all
    `{:error, reason}` arm — verbatim mirror of REV-E HIGH-1's
    catch-all on the sibling `dispatch_ops_verb/3`. Maps any
    uncategorised upstream-send failure from `Session.send_whois`,
    `send_who`, `send_names`, `send_banlist` to typed
    `upstream_unavailable` cic reply + Logger.warning. Prevents the
    WithClauseError crash class REV-E HIGH-1 closed at the ops sibling,
    relocated to the subject-verb path.

## What was live at CP40 open (REV-D close state, historical)

- **Server**: `fc5d221` (REV-D — silent-swallow + boot-pin VAPID
  + monotonic visitor TTL + schema-cap last_joined + race-safe
  visitor mutators + funneled Backoff bookkeeping). Container
  COLD-recreated 2026-05-22 ~10:11 UTC. **Superseded by REV-E
  HOT-deploy 11:23 UTC — see below.**
- **cic bundle**: `D5kebe2p` (REV-A — still live; all REV buckets
  through REV-E have been server-only).
- New live in REV-D:
  - `Grappa.Push.boot/0` + `vapid_public_key/0` — boot-pin VAPID
    in `:persistent_term`
  - `Grappa.Session.Server.terminate/2` — abnormal-reason clause
    is the single funnel for `Backoff.record_failure`
  - `Visitors.Visitor.expire_changeset/2` (forced-expiry,
    bypasses monotonicity guard) + guarded `touch_changeset/2`
  - `Credential.last_joined_channels_max/0` (SoT for the 200-cap)

## What's deployed RIGHT NOW (REV-E close state)

- **Server**: `1980035` (REV-E reviewer round-1 fix on top of base
  `b457efc` REV-E sweep). HOT-deployed 2026-05-22 ~11:23 UTC via
  `Phoenix.CodeReloader.reload/1` — sessions preserved, container ID
  unchanged.
- **cic bundle**: `D5kebe2p` (REV-A — unchanged through REV-B + REV-C
  + REV-D + REV-E; all REV buckets so far have been server-only).
- **Healthcheck**: `ok` via substrate-deep `/healthz`.
- New live since REV-E:
  - `Grappa.Session.send_transport_error/0` — typedoc'd public type
    for the dead-socket / closed-mid-write error shape (`:no_socket
    | :closed | :inet.posix()`).
  - All 22 `Session.send_*` wrappers widened to include
    `send_transport_error()`.
  - `Grappa.Session.Server.flush_mode_chunks/3` — new recursive
    halt-on-first-error helper replacing `send_chunked_mode`'s
    Enum.each (matches CLAUDE.md collect-or-bail pattern).
  - `Grappa.Session.Server.maybe_log_send_failure/2` — single
    fire-and-forget Logger helper consolidating 5 AWAY-internal
    sites.
  - `GrappaWeb.GrappaChannel.dispatch_ops_verb/3` gains catch-all
    `{:error, reason}` arm — maps any uncategorised upstream-send
    failure to typed `upstream_unavailable` cic reply +
    Logger.warning. Prevents WithClauseError crash class that the
    REV-E spec widening would have surfaced.

## REV-E LANDED 2026-05-22

**`:ok = Client.send_*` strict-bind regression sweep (H11)** — closes
the lone HIGH from the 2026-05-22 codebase review tagged with the
post-U-cluster boundary-fix drift class.

**Root cause:** U-cluster boundary fix (commit `7bb3caa`, 2026-05-17,
per `feedback_no_silent_drops_closed`) changed `Grappa.IRC.Client`'s
send handling — `transport_send/2` got a nil-socket guard returning
`{:error, :no_socket}` cleanly instead of raising
`FunctionClauseError`. Every `:ok = Client.send_*` strict-bind site
in `Session.Server` became a `MatchError` landmine that would crash
the calling Session.Server the next time a dead-socket SEND fired
(operator's autojoin sequence racing a k-line, NickServ ghost-recovery
mid-flow disconnect, AWAY issued during socket RST, etc.). Crashes
funnel via REV-D's `terminate/2` bookkeeping → Backoff bump →
`:transient` respawn → re-crash on next send — exact crash-loop class
REV-D defends against, just with a different trigger.

**Fix shape (9 sites in `lib/grappa/session/server.ex`):**

Two patterns split by caller intent (per CLAUDE.md "No silent-swallow
at boundaries"):

1. **Propagate path** (caller can surface error):
   - `handle_call({:send_mode, ...})` raw MODE (line ~1037) — switched
     to `{:reply, Client.send_line(...), state}` match-arm reply
     (mirrors the existing `send_nick`/`send_quit` precedent at lines
     838-855).
   - `send_chunked_mode/4` (/op /deop /voice /devoice /ban /unban) —
     `Enum.each` → recursive `flush_mode_chunks/3` collect-or-bail per
     CLAUDE.md. Halts on first `{:error, _}` and propagates the typed
     error to the GrappaChannel caller.

2. **Fire-and-forget path** (in-process state mutation, dead-socket
   means supervisor restart owns reconnect):
   - `apply_effects([{:reply, line} | _], _)` (EventRouter outbound,
     e.g. CTCP VERSION NOTICE reply) — paired `:persist` effect
     runs on the next recursion regardless of reply outcome, so the
     scrollback row lands either way.
   - `flush_lines/2` (ghost-recovery NickServ dance) — `pending_auth`
     stage discarded harmlessly on terminate; next reconnect retries
     GHOST + IDENTIFY from scratch.
   - 5 AWAY-internal sites consolidated into a single
     `maybe_log_send_failure/2` helper covering
     `set_explicit_away_internal/3` (2 clauses),
     `set_auto_away_internal/1`, `unset_away_internal/2` (2 clauses).
     Honest about the limitation: AwayState mutator flips local
     state for the LIVE process (so `away_confirmed` broadcast +
     Mentions aggregation track the window), but a Session crash +
     supervisor restart wipes AwayState — operator must re-issue
     `/away` post-reconnect (UX-7 follow-up could surface a hint).

**Reviewer round-1 caught HIGH-1 + 3 MED + 2 LOW:**

- **HIGH-1**: `dispatch_ops_verb/3`'s `with`/`else` was non-exhaustive
  for the new error shapes. Same crash class, relocated one process
  upward (Channel crash instead of Session.Server crash). Fix: catch-
  all `{:error, reason} -> upstream_unavailable` arm with
  Logger.warning. Regression test added at
  `grappa_channel_test.exs:959`.
- **MED-1**: Session.send_* spec drift — all 22 wrappers widened to
  include the new `send_transport_error()` type. Dialyzer's success-
  typing validated the new shape end-to-end through the
  `dispatch_ops_verb` catch-all (without the catch-all, Dialyzer would
  have flagged HIGH-1 automatically).
- **MED-2**: `apply_effects {:reply, line}` comment lied about reply/
  persist ordering — fixed to describe actual `[reply, persist]`
  effect-list ordering.
- **MED-3**: AwayState recovery story overstated ("next reconnect
  re-issues AWAY") — fixed to be honest about Session restart wiping
  AwayState.
- **LOW-1** (CTCP source-grep test) + **LOW-2** (helper takes
  String.t() label) — deferred as stylistic follow-ups.

Reviewer round-2: **APPROVE clean** — 0/0/0/0 findings, gates
verified literally.

**Regression coverage (6 + 1 = 7 ExUnit cases):**

- 6 cases in `test/grappa/session/server_test.exs` "REV-E (H11) —
  `:ok = Client.send_*` strict-bind regression sweep" describe block
  exercise each fixed site against a real Session.Server with the
  linked Client's socket nilled via `:sys.replace_state` (same trick
  as `Grappa.IRC.ClientTest`'s existing dead-socket coverage). Each
  asserts (a) Session pid stays alive, (b) Logger emits the honest
  signal, (c) state mutation is correct per call site.
- 1 case in `test/grappa_web/channels/grappa_channel_test.exs`
  ("op (REV-E HIGH-1): dead Session.Server socket → typed
  upstream_unavailable reply, Channel survives") at line 959
  exercises the new `dispatch_ops_verb` catch-all end-to-end.
- The CTCP `{:reply, line}` apply_effects test is a code-shape grep
  (not a runtime exercise) — the IRC.Client recv-loop's
  `:prim_inet.setopts(nil, ...)` crash blocks the runtime path post-
  socket-nil. That's a SEPARATE silent-swallow class outside REV-E
  scope (handle_info `{:tcp, _, _}` post-socket-nil); the AWAY tests
  exercise the same `Client.send_line/send_away/send_away_unset`
  return-shape paths the apply_effects `:reply` arm consumes.

**Gates (final, third clean check.sh run):**

```
8 doctests, 33 properties, 2404 tests, 0 failures      (ExUnit)
Total errors: 0, Skipped: 0, Unnecessary Skips: 0      (Dialyzer)
... SCAN COMPLETE ...                                    (Sobelow)
Doctor validation has passed!
ok 23 list-visitors invokes docker exec -T grappa ...   (bats 23/23)
```

`scripts/dialyzer.sh` standalone: `Total errors: 0 / done (passed
successfully)` — fresh-PLT signal confirmed.

Intermittent baseline flakes observed across 4 check.sh runs (1 or
9 failures across 2 runs, 0 in the others): `AdmissionTest`
ETS-singleton-leak class (parallel async tests don't reset
`NetworkCircuit` ETS between cases) + `AdminEventsTest:197`
assert_receive flake. Both pre-date REV-E + documented in
MEMORY (`feedback_bahamut_load_flake` + the singleton-class
backlog).

**Deploy:** **HOT** via `Phoenix.CodeReloader.reload/1` (no
defstruct/init/application.ex/migration touched). Manual preflight
verification via `Grappa.Deploy.Preflight.cli(["4b33ae6", "HEAD"])`
(Path 2 from the orchestrator briefing) confirmed HOT classification
BEFORE running `scripts/deploy.sh` — defuses the FALSE-HOT empty-
diff trap from `feedback_deploy_preflight_empty_diff_after_merge`.
Live-verified post-deploy: container healthy + healthcheck `ok` +
`grep upstream_unavailable` against `/app/lib/...` confirms the new
catch-all source is loaded.

## Carry-forwards into REV-F

### MED-2 carry-forward from REV-B reviewer

`ArchiveController.delete/2` runs `validate_target_name/1` on the
PRE-canonical target. Bytes-equivalent today; minor drift risk if
the validator tightens. Out-of-scope for REV-E (REV-J or REV-Z).

### REV-D reviewer LOW-1 carry-forward

H14 narrow-window test name vs. behavior. The test labeled
`"rescue maps Ecto.StaleEntryError to {:error, :not_found}"` only
asserts that `Repo.update` on a stale struct raises — it does NOT
route through `Visitors.commit_password/2`'s rescue clause. The
rescue mapping is two lines, both nil-get + rescue paths return
`{:error, :not_found}`. Documented; not fixed inline (would
require Mox-injected stale Repo.get stub, weight > value for the
two-line rescue). REV-Z polish if motivated.

### Preflight empty-diff FALSE-HOT trap

`feedback_deploy_preflight_empty_diff_after_merge` reproduced
during REV-D deploy. Local `git merge --ff-only` advanced HEAD to
the deploy target BEFORE `scripts/deploy.sh`'s `git pull --ff-only`
ran → pull returned "Already up to date" → `prev_sha == HEAD` →
preflight's "same SHA = nothing to deploy" shortcircuit returned
HOT. The HOT reload silently no-op'd Application.start, leaving
new `:persistent_term`-pinning module Grappa.Push.boot/0
uncalled. Caught immediately by the H16 smoke; cleaned
`_build/prod` + `--force-cold` recovered.

**Mitigation candidate (REV-J or REV-Z):** preflight should compare
against the LIVE container's deployed SHA, not local `prev_sha ==
HEAD`. Operator runbook also needs the cleanup recipe documented.

## Anti-patterns to keep watching

- **`_build/prod` corruption from prior HOT** —
  `feedback_hot_deploy_corrupts_build_prod` reproduced FOURTH
  time now (REV-A + REV-B + REV-C + REV-D). The recipe
  (`docker compose run --rm -T grappa rm -rf /app/_build/prod`
  THEN `--force-cold`) is mechanical but still undocumented in
  the operator runbook. REV-Z target.
- **HOT silent no-op for application.ex changes** — even though
  the preflight catches `lib/grappa/application.ex` in Class 2,
  the empty-diff trap can short-circuit BEFORE classification.
  Any new `:persistent_term`-pinning module added at boot is a
  flashpoint.
- **Reviewer-loop earns its keep even on small buckets** — REV-D
  reviewer's LOW-2 caught a dead spec branch in `Visitors.touch/1`
  post-H13. Even single-round APPROVE has value beyond gating;
  it's the only thing checking spec-vs-impl after a refactor.
  REV-F was reviewer-round-1-APPROVE-clean (no rounds of fix),
  but the brief specifically asked verifier to paste literal
  gate-tail (per `feedback_reviewer_gate_evidence`); reviewer
  ran check.sh + dialyzer.sh directly and confirmed the commit-
  message claim — gate-evidence discipline is the only thing
  that distinguishes "real APPROVE" from "implied APPROVE on
  trust." Keep the literal-paste mandate in every brief.
- **CP rotation discipline holds** — CP38→CP39 at REV-A LANDED,
  CP39→CP40 at REV-D LANDED. Both rotations happened at the
  END of a bucket (per CLAUDE.md "Do NOT rotate mid-bucket"),
  flushing gate evidence first. CP40 now ~430 lines post-REV-F;
  CP40→CP41 at REV-G LANDED is the next likely rotation point.

## S1 — REV-F LANDED (2026-05-22 ~11:55 UTC)

**Commit**: `6574f0e` — `fix(rev-f): IRC SASL combined-REQ fallback +
dispatch_subject_verb catch-all (H9 + H10)`.

**Closes**: 2 HIGH from `docs/reviews/codebase/2026-05-22-codebase-review.md`.

### H9 (irc/S1) — AuthFSM combined CAP REQ fallback on NAK

`Grappa.IRC.AuthFSM`'s combined `CAP REQ :sasl labeled-response` blob
had no fallback when the server NAK'd. Bahamut + some Solanum
variants advertise `labeled-response` in CAP LS but NAK the combined
REQ while ACKing `:sasl` alone. Pre-fix, a `:sasl`-required
credential restart-looped permanently against the exponential
backoff ladder.

Fix: split post-REQ wait into per-shape phases.
`:awaiting_cap_ack` unchanged for standalone `:sasl` or
`:labeled-response` REQs (NAK still declares `:sasl_unavailable`
immediately — nothing to fall back from). New
`:awaiting_cap_ack_combined` for the combined REQ; NAK there falls
back to `CAP REQ :sasl` alone and transitions to
`:awaiting_cap_ack_sasl_only`. NAK on the fallback phase declares
`:sasl_unavailable` genuinely. ACK clause guard widened to match
all three phases (semantics identical: SASL ACK → AUTHENTICATE
PLAIN; non-SASL ACK → cap_unavailable). `maybe_send_cap_end/1`
extended to recognise both new phases for explicit teardown.

`:auto` benefits too — pre-fix `:auto` combined-NAK fell through
to `cap_unavailable/1`'s non-`:sasl` clause (PASS-handoff, no
`:stop`), losing SASL entirely even when the server supported it.
Post-fix `:auto` also exercises the fallback before PASS-handoff.

Tests: 7 new ExUnit cases in `auth_fsm_test.exs`:
1. finalize_cap_ls with sasl + labeled-response → combined REQ
2. Combined-NAK → fallback REQ + transition to sasl_only phase
3. Fallback-ACK → AUTHENTICATE PLAIN + sasl_pending
4. Fallback-NAK → :stop :sasl_unavailable (genuine no-SASL)
5. :auto combined-NAK → fallback (SASL eligibility preserved)
6. :auto fallback-NAK → :cont PASS-handoff (unchanged behavior)
7. Invariant preservation: standalone :sasl REQ NAK on
   :awaiting_cap_ack STILL declares :sasl_unavailable immediately

### H10 (web/S1) — dispatch_subject_verb catch-all

`GrappaWeb.GrappaChannel.dispatch_subject_verb/3` (sister of
`dispatch_ops_verb/3`) was missing the catch-all `{:error, reason}`
arm REV-E HIGH-1 added to its sibling. `Session.send_whois/3`,
`send_who/3`, `send_names/3`, `send_banlist/3` post-U-cluster CAN
return `Session.send_transport_error/0` shapes (`:no_socket
| :closed | :inet.posix()`) when the underlying client socket is
dead. Pre-REV-F those crashed the channel pid with
`WithClauseError` — same crash class as REV-E HIGH-1 relocated to
the sibling helper. Consistency drift was the root cause.

Fix: catch-all arm verbatim mirror of REV-E HIGH-1 —
`Logger.warning("subject verb: upstream send failed", reason: inspect(reason))`
+ typed `{:reply, {:error, %{reason: "upstream_unavailable"}}, socket}`.
Operator + user both surfaced per `feedback_no_silent_drops_closed`.

Tests: 1 new ExUnit case in `grappa_channel_test.exs` mirroring
the REV-E HIGH-1 regression test at line 959 — replaces `op` push
with `whois` push to exercise the subject-verb path. Same
`:sys.replace_state` socket-nil shape, same `Process.alive?`
survival assertions on both Channel and Session.Server pids.

### Gates (literal tail evidence)

`scripts/check.sh` final clean run:
```
Finished in 53.4 seconds (22.2s async, 31.2s sync)
8 doctests, 33 properties, 2412 tests, 0 failures
...
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.56s
done (passed successfully)
```

First-run hit baseline `AdminEventsTest:197` flake (1 failure of
2412) — known `feedback_bahamut_load_flake` class, NOT REV-F-caused.
Re-run validated clean per the documented up-to-3-runs allowance.

`scripts/dialyzer.sh` standalone:
```
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.69s
done (passed successfully)
```

Bats 23/23. Sobelow done. Doctor passed.

### Reviewer round-1 APPROVE clean

General-purpose agent dispatched with literal gate-tail evidence
mandate. Reviewer ran `check.sh` + `dialyzer.sh` directly,
confirmed the commit-message gate claims, and verified every
invariant the brief named:
- Phase union update + moduledoc accuracy
- New phases reach existing ACK clause via guarded match (no SASL
  credential leak via C1 phase pin which remains exclusively
  `:sasl_pending`)
- `maybe_send_cap_end/1` recognises both new phases
- `leave_cap_negotiation/2` docstring accurate re:
  `AWAIT_COMBINED → AWAIT_SASL_ONLY` direct transition (correct
  to bypass `leave_cap_negotiation/2` because `caps_buffer` was
  already cleared at the LS boundary)
- `:auto` parity tested both NAK paths
- Standalone `:sasl` REQ NAK invariant preserved + test-pinned
- H10 verbatim mirror of REV-E HIGH-1 sibling
- Test mirrors REV-E HIGH-1 line 959 exactly modulo verb swap
- `Session.send_whois/3` spec confirms `send_transport_error()`
  IS a real return shape — no over-engineering

Verdict: **APPROVE.** LOW-1 (optional symmetry test for
labeled-response-only NAK path) + LOW-2 (vertical phase list
style nit) deferred as opportunistic polish — out-of-scope for
REV-F minimal surface.

### Deploy procedure (Path 2 manual preflight — proven safe)

```
git checkout main
git merge --ff-only rev-f                           # a4d4b22..6574f0e
scripts/mix.sh run --no-start -e \
  'Grappa.Deploy.Preflight.cli(["a4d4b22", "HEAD"])'  # → HOT
scripts/deploy.sh                                    # HOT
scripts/healthcheck.sh                               # ok
docker compose exec -T grappa grep -c \
  'awaiting_cap_ack_combined' /app/lib/grappa/irc/auth_fsm.ex
  # → 9 (live source verified)
git push origin main                                 # a4d4b22..6574f0e
```

Preflight returned `→ no unsafe markers → HOT`. HOT reload via
`Phoenix.CodeReloader.reload/1` completed cleanly — sessions
preserved, container ID unchanged. Path-2 manual preflight against
deployed SHA (REV-D introduced; REV-E proven; REV-F third
consecutive clean use) — defuses the FALSE-HOT empty-diff trap.

### Carry-forwards into REV-G

- **HOT-deploy Path 2 procedure** continues to work. Real
  mitigation (preflight compares against LIVE container SHA, not
  local `prev_sha == HEAD`) still belongs in REV-J or REV-Z.
- **AdminEventsTest:197 baseline flake** repeated again — surface
  for the post-REV-Z flake-triage cluster per
  `project_post_review_ordering_2026_05_22` step (1).
- **REV-F LOW-1 + LOW-2** stylistic follow-ups deferred for REV-Z
  polish:
  - LOW-1: labeled-response-only NAK symmetry test (path is
    exercised indirectly by existing S4.2 tests; explicit test
    would close the matrix)
  - LOW-2: `maybe_send_cap_end/1` 5-element vertical phase list
    style nit (purely cosmetic)
- **Carry-forwards from REV-D and REV-E remain** (AwayState
  reconnect re-issue, CTCP apply_effects runtime test, `_build/prod`
  cleanup operator-runbook entry, preflight LIVE-SHA comparison).

### What changes about REV-G

REV-G is **cic-only HOT**. Closes 3 HIGH from the 2026-05-22 review:
H22 (PWA SW navigation-route denylist missing `/api`, `/uploads`,
`/admin`), H24 (admin-channel snapshot/event handlers cast WS
payloads without runtime narrower), H23 (`markerRef` `let`-bound
ref in ScrollbackPane.tsx leaks across `<For>` mid-channel
re-renders). All cic-side; deploy via `scripts/deploy-cic.sh`
(vite build + bundle_hash broadcast for refresh banner). No server
restart. cic-only e2e + vitest gates required (`scripts/bun.sh
run check` + relevant Playwright specs).
