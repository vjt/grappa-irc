---
status: active
---

# CP07

CP06 closed at S20 / 3 sessions / ~440 lines — covered Tasks 9 + 10
(Phase 1 walking-skeleton CLOSURE) and the Phase 2 design pass +
plan-write. CP05 closed at S17 covering Phase 1.5 + line-level review
fix. CP04 closed at S13 (walking-skeleton complete + live on Pi).
CP03 closed at S11 (Task 8). CP02 covered Tasks 5-7. CP01 covered
Phase 0 + Tasks 1-4. See prior checkpoints for back-story.

CP07 opens with **Phase 1 walking-skeleton COMPLETE + live on Pi**
(connected to Azzurra as `vjt-grappa`, autojoined `#grappa`,
end-to-end PRIVMSG round-trip via REST + PubSub confirmed against
real upstream). **Phase 2 design DONE; plan written**
(`docs/plans/2026-04-25-phase2-auth.md`, 1316 lines, 12 sub-tasks).
CP07's job: implement Phase 2 — auth, multi-user, per-network
credentials, real upstream auth state machine.

---

## State at checkpoint creation

Frozen at main commit `b1a2904` (`docs: Phase 2 auth plan — A-G
decisions locked + 12 sub-tasks`). Working tree clean, origin in sync.

**Phase 1 walking-skeleton:** all 10 tasks done + live on Pi. Phase
1.5 (architecture review fix-up + codebase review) merged + deployed.
Boundary library enforced via `mix compile --warnings-as-errors` in
ci.check.

**Phase 2 status:** plan written and committed. Design decisions A–G
all locked in conversation (see CP06 S20 entry for the full
decision-by-decision walkthrough). Implementation begins next
session in a fresh `phase2-auth` worktree.

**Codebase shape (frozen at S19):**
- ~2,800 LOC of Elixir under `lib/`
- 189 tests + 5 properties green (with one known flake at
  `grappa_channel_test.exs:76`)
- ci.check ~30s on Pi (worktree, PLT hot)
- 67 commits on main since project init (b1a2904 = #67)
- 11 top-level Boundary annotations enforced

**Tooling baseline:** Dialyxir (`:underspecs`), Credo (`--strict`),
Sobelow, mix_audit, doctor (100% module health), Boundary (enforced
via warnings-as-errors), ExUnit, StreamData, Mox, Bypass, ExMachina,
excoveralls. All green.

**Live on Pi:** bouncer up at http://192.168.53.11:4000, connected to
azzurra as `vjt-grappa`, autojoined `#grappa`. End-to-end PRIVMSG →
Scrollback.insert → REST + PubSub round-trip confirmed against real
upstream traffic.

**Phase 2 implementation roadmap** (12 sub-tasks, ~43h, 4-6 sessions):

1. **2a-pre** — deps + Cloak.Vault setup
2. **2a** — Accounts context (User + Argon2 + create_user mix task)
3. **2b** — Sessions + sliding-7d bearer tokens + Authn plug
4. **2c** — REST auth endpoints + authn pipeline
5. **2d** — Networks/Server/Credential schemas + 5 mix tasks
6. **2e** — Per-user iso on messages
7. **2h** — PubSub topic shape change + Grappa.PubSub.Topic builder
8. **2f** — IRC.Client SASL + PASS state machine
9. **2g** — Session.Server uses credentials from DB
10. **2i** — Channel connect/3 + topic authz
11. **2j** — Bootstrap rewrite; toml deletion
12. **2k** — Pi deploy + smoke (real Azzurra PASS + Libera SASL)

Code-review round-trips planned at 2a, 2b, 2d, 2f, 2g.

**Phase 2 deps to add (sub-task 2a-pre):**
- `argon2_elixir ~> 4.0`
- `cloak ~> 1.1`
- `cloak_ecto ~> 1.3`

**Phase 2 schema additions:**
- `users (id binary_id pk, name unique, password_hash, timestamps)`
- `sessions (id binary_id pk = bearer token, user_id, created_at, last_seen_at, revoked_at, ip, user_agent)`
- `networks (id integer pk, slug unique, timestamps)`
- `network_servers (id integer pk, network_id FK, host, port, tls, priority, enabled)`
- `network_credentials (composite pk user_id+network_id, nick, realname, sasl_user, password_encrypted bytea, auth_method enum, auth_command_template, autojoin_channels json, timestamps)`
- `messages` migration: add `user_id binary_id FK`; change `network_id` from `text` → `integer FK`; new index `(user_id, network_id, channel, server_time DESC)`; wipe table

**Phase 2 wire-shape break:** PubSub topics gain user discriminator —
`grappa:user:{user}/network:{net}/channel:{chan}` (was
`grappa:network:{net}/channel:{chan}`). Built via single-source
`Grappa.PubSub.Topic` module.

**Phase 2 file deletions:**
- `lib/grappa/config.ex` (and `lib/grappa/config/` directory)
- `grappa.toml.example`
- `test/grappa/config_test.exs`
- `compose.yaml` grappa.toml bind-mount

**Phase 2 first-deploy operator UX** (documented in plan + README):
1. `scripts/deploy.sh`
2. `scripts/mix.sh grappa.gen_encryption_key` → save into `.env` as `GRAPPA_ENCRYPTION_KEY`
3. `scripts/deploy.sh` (restart with key)
4. `scripts/mix.sh grappa.create_user --name vjt --password '...'`
5. `scripts/mix.sh grappa.bind_network --user vjt --network azzurra ...`
6. `curl -X POST /auth/login -d '{"name":"vjt","password":"..."}'`

**Outstanding from CP06 / pre-CP07:**
- Channel test flake at `grappa_channel_test.exs:76` — race between
  channel join and PubSub subscribe before broadcast lands. Hit ~1-in-5
  under `mix ci.check` parallelism. Will likely need refactor in 2h
  (PubSub topic shape change) or 2i (Channel auth).
- Sqlite "Database busy" intermittent flake hit once during S19
  ci.check (`Repo` / `Scrollback` / `Wire` simultaneous fail). Likely
  contention with live Pi container writing to shared dev.db. Promote
  to S20 observation.
- Phase 5 hardening pile (post-Phase 2):
  - Session.Server `terminate/2` cleanup with QUIT
  - Bootstrap warning UX polish (originally A20)
  - `verify_none` → CA chain (TLS hardening)
  - async `IRC.Client.connect` via `{:continue, _}` (CONSIDER #7)
  - NickServ REGISTER proxy + reply correlation machinery
  - Multi-server failover logic (schema ready in Phase 2; logic in Phase 5)
  - Post-registration `+r` umode check + IDENTIFY-as-fallback retry
  - NickServ NOTICE reply parsing (success/fail, GHOST/RECOVER)
  - HSM-keyed Cloak.Vault (operator escape from "env on disk")
  - PromEx exporter
  - Reconnect/backoff policy
  - Scrollback eviction policy
- **Note:** Phase 1.5 hardening item "signing_salt → runtime.exs" is
  ABSORBED into Phase 2 plan (sub-task 2b: session-token signing salt
  lives in the Sessions implementation). Removed from todo.

**Codebase review gate:** not due. Last 2026-04-25 (S15), 5 sessions
ago. Threshold: ≥12 sessions OR >2 weeks since last. Next due ~S27 or
2026-05-09, whichever first. Phase 2 implementation will likely span
sessions S21-S26ish; review window aligns with Phase 2 closeout.

---

## S21 — 2a-pre + 2a (Cloak.Vault + Accounts.User + Argon2)

**Date:** 2026-04-26 **Commits:** `a2e9a7b` `68ac842` `8f88b3a` `b562ea9`

- `a2e9a7b` worktree tooling: `WRITABLE_LOCK=1` escape hatch in
  `scripts/mix.sh deps.get` so worktree-side mix.lock mutation works
  (named volume default is read-only).
- `68ac842` (2a-pre): cloak ~> 1.1 + cloak_ecto ~> 1.3 deps; new
  `Grappa.Vault` GenServer + `Grappa.EncryptedBinary` Cloak.Ecto type;
  `Mix.Tasks.Grappa.GenEncryptionKey` task. **Vault BEFORE Repo in
  supervision tree** — Cloak Ecto types reach Vault at schema dump/load,
  reordering crashes `:noproc`. AES.GCM cipher in runtime/dev/test.
- `8f88b3a` (2a): `Grappa.Accounts` top-level Boundary, deps `[Repo]`,
  exports `[User]`. User schema: binary_id PK + unique name format
  `^[a-zA-Z][a-zA-Z0-9_-]$` length 1..64 + Argon2 hash on
  `valid?: true`. `get_user_by_credentials/2` uniform
  `:invalid_credentials` for both branches with `Argon2.no_user_verify/0`
  on the nil branch (timing oracle defense).
  `Mix.Tasks.Grappa.CreateUser` sets `Application.put_env(:grappa,
  :start_bootstrap, false)` BEFORE `ensure_all_started` — pattern reused
  for every Phase 2 mix task.
- `b562ea9` (2a review nits): `errors_on/1` lifted to `Grappa.DataCase`;
  redundant `t() | %MODULE{}` dropped from User.changeset @spec.

**Validation:** ci.check green. New deps add ~100 LOC; no production
behavior change beyond Accounts boundary surface.

---

## S22 — sqlite-contention infra + 2b (bearer tokens + Authn plug)

**Date:** 2026-04-26 **Commits:** `c7413be` `6f2fdc1` `93f7333`

- `c7413be` (infra): `max_cases: 2` cap in `test_helper.exs` + bump
  `busy_timeout` `5_000 → 30_000` in test config. Argon2 (~100ms) +
  Cloak + concurrent Sessions cascaded "Database busy" — pin
  parallelism + give writer-lock more headroom. CP07-documented
  intermittent flake hit zero recurrences post-fix.
- `6f2fdc1` (2b): migration `20260426000001_create_sessions.exs` —
  binary_id PK that **IS** the bearer token + user_id FK
  `on_delete: :delete_all`; `Grappa.Accounts.Session` schema; Accounts
  adds `create_session/3` + `authenticate/1` + `revoke_session/1` with
  `@idle_timeout_seconds 7*24*3600` + `@last_seen_bump_threshold_seconds 60`
  sliding-7d-idle. Failure modes
  `:invalid_token | :not_found | :revoked | :expired`. `GrappaWeb.Plugs.Authn`
  parses `Authorization: Bearer`, assigns `:current_user_id` +
  `:current_session_id`, halts uniform 401
  `{"error":"unauthorized"}` on every failure. User `t()` widened to
  `String.t() | nil` because fresh `%User{}` has nil name and
  Dialyzer surfaced the gap.
- `93f7333` (2b review nits): `revoke_session/1` logs affected-row
  count; Authn plug typed-case on `{:error, reason}` so each failure
  reason lands in `Logger.info("authn rejected", authn_failure: ...)`;
  config metadata allowlist gains `:session_id` `:affected`
  `:authn_failure`.

**Validation:** ci.check green. ~250 LOC + 137 new test lines.

---

## S23 — 2c (REST auth endpoints + authn pipeline)

**Date:** 2026-04-26 **Commits:** `79f6146` `2eaaa0d`

- `79f6146` (2c): `POST /auth/login` → 200 `{token, user}` | 401
  `{error: "invalid_credentials"}` | 400; `DELETE /auth/logout` → 204 |
  401; `GET /me` → 200 | 401. `AuthController` + `MeController` with
  matching JSON modules. `FallbackController` gains
  `{:error, :invalid_credentials}` → 401 with **distinct body** from
  plug's "unauthorized" so operator log analysis splits wrong-creds
  from session-state failures while clients collapse both to same UX.
  Router gains `:authn` pipeline three scopes — `/healthz` no-pipeline,
  `/auth/login` `:api`, everything else `:api+:authn`. Login records
  `remote_ip` via `:inet.ntoa/1` + user-agent for audit but
  **deliberately ignores `x-forwarded-for`** — pre-allowlist it's a
  forge-able audit field; trusted-proxy allowlist deferred to Phase 5
  (plan inline-amended). Existing channels/messages controller tests
  gain setup creating user + session + Authorization header + a
  no-bearer-401 regression test each. New `test/support/auth_fixtures.ex`
  centralizes `user_fixture/1` (bypasses Argon2 with
  `password_hash: "x"`); slow `user_fixture_with_password/1`
  reserved for the two login tests.
- `2eaaa0d` (2c review nits): `AuthController.login/2` @spec dropped
  `Ecto.Changeset.t()` since user_id+ip+user_agent are server-derived
  so create_session can never return changeset (pattern-matched
  `{:ok, session} = ...`); `AuthFixtures.session_count/0` helper
  centralizes the no-row assertion.

**Validation:** ci.check green. 10 + 5 new tests (auth + me).

---

## S24 — 2d (Networks/Servers/Credential schemas + 5 mix tasks)

**Date:** 2026-04-26 **Commits:** `23e4415` `9b96235`

- `23e4415` (2d): migration `20260426000002_create_networks.exs` —
  networks autoincrement INTEGER PK + unique slug; **drops Phase 1
  init.exs orphan networks/channels stub tables**; network_servers
  many-per-network with `UNIQUE(network_id, host, port)` for fail-over
  fan-out + `priority asc, id asc` ordering hint; network_credentials
  composite PK `(user_id, network_id)` FK `:delete_all` on users +
  `:restrict` on networks because **cascade-on-empty lives in
  `Networks.unbind_credential/2` not the FK** — last binding's removal
  explicitly tears down network + servers; `password_encrypted` BLOB
  via `Grappa.EncryptedBinary` Cloak.Ecto AES-GCM. `Grappa.Networks`
  context Boundary `[Accounts, EncryptedBinary, Repo, Vault]` exports
  `[Network, Server, Credential]`. `Credential.auth_method` allowlist
  `:auto | :sasl | :server_pass | :nickserv_identify | :none` as
  Ecto.Enum + `autojoin_channels` array + virtual `:password`
  `redact: true`. IRC nick format
  `^[a-zA-Z[]^{|}][a-zA-Z0-9[]^{|}-]$` length 1..30. 5 mix tasks
  `Grappa.{BindNetwork, UnbindNetwork, AddServer, RemoveServer,
  UpdateNetworkCredential}` all using `start_bootstrap=false`.
- `9b96235` (2d review nits): `Networks.host_port_collision?/1`
  matches by `constraint_name` not just `:unique` (future second
  unique constraint stays distinct); `unbind_credential/2` wrapped in
  `Repo.transaction/1`; `Credential.validate_password_for_auth_method/1`
  rejects auth_method changes that don't supply fresh `:password`;
  extracted `Mix.Tasks.Grappa.OptionParsing` top-level Boundary for
  `parse_server / parse_auth / parse_autojoin`.

**Validation:** ci.check green. ~600 LOC + 359 test lines + 5 mix tasks.

---

## S25 — 2e (per-user iso on messages + scrollback rebuild)

**Date:** 2026-04-26 **Commits:** `6f8b307` `35dc86a`

- `6f8b307` (2e): migration `20260426000003_messages_per_user_iso.exs`
  **wipe-and-rebuild per decision G2** — `DELETE FROM messages` first,
  then **drops the messages_network_id_channel_server_time_index BEFORE
  the column-type swap** (sqlite ALTER TABLE table-recreate fails to
  rebuild the index across a column type change with "no such column:
  network_id"), then alter table adds `user_id BLOB references users
  on_delete: :delete_all NOT NULL` + removes `network_id :string` +
  adds `network_id references networks on_delete: :delete_all NOT NULL`,
  then creates `messages_user_id_network_id_channel_server_time_index`
  (per-user partition predicate is single index scan).
  `Grappa.Scrollback.Message` belongs_to user/network with
  assoc_constraint. `Grappa.Scrollback` Boundary deps += Accounts +
  Networks; `fetch/4 → fetch/5` with `(user_id, network_id integer,
  channel, cursor, limit)` guards. `Grappa.Scrollback.Wire`
  pattern-matches `%Message{network: %Network{slug: slug}} = m` so
  unloaded `:network` assoc raises `FunctionClauseError` per "let it
  crash"; emits `:network → slug`; **REMOVES `:user_id` per decision
  G3** (user identity is topic discriminator not payload field).
  `Grappa.Networks.get_network_by_slug/1` for controller URL slug →
  integer FK. `Session.Server` state adds `user_id` + `network_id`
  integer FK + `network_slug`; init resolves slug via
  `find_or_create_network`; `persist_and_broadcast` preloads `:network`
  before broadcast.
- `35dc86a` (2e review nits): **M1** `find_or_create_network/1` retries
  `Repo.get_by/2` once on changeset error via
  `insert_or_recover/2`; **M2** Bootstrap summary line splits skipped
  from failed counter; **S1** Session.Server.state RENAMED
  `network_db_id → network_id` (integer FK) + `network_id slug →
  network_slug` (eliminates half-migrated naming); **S6** `Wire.t`
  tightens `id integer not integer | nil` + `kind not nil`.

**Validation:** ci.check green. Scrollback round-trip survives across
new FK shape; tests bridge to Session via `network.id` integer.

---

## S26 — 2h (PubSub topic shape break + per-user routing iso)

**Date:** 2026-04-26 **Commits:** `533b4e2` `899c446`

- `533b4e2` (2h): `Grappa.PubSub.Topic` reshape: `user/1` unchanged but
  `network/2` + `channel/3` take `user_name` as first segment so every
  Grappa topic is **rooted in user discriminator**. `parse/1` returns
  `{:ok, {:user, name}} | {:network, name, slug} | {:channel, name,
  slug, chan}` via `"grappa:user:" <> rest` split on `"/"`, parts
  guards. New `Topic.user_of/1` predicate is the **authz contract every
  join uses**. **OLD `grappa:network:{net}/channel:{chan}` shape from
  Phase 1 is now un-parseable AND un-routable** — UserSocket dropped
  the `channel "grappa:network:"` macro, leaving only
  `channel "grappa:user:"`. `Session.Server.persist_and_broadcast` now
  broadcasts on `Topic.channel(state.user_name, state.network_slug,
  target)` closing the **S2 leak from 2e code review** where wire
  PAYLOAD already dropped user_id but DELIVERY ROUTE still leaked
  (shared topic across users). `GrappaChannel.join/3` `with`-chain
  parses via `Topic.parse/1` (Phase 1 shape returns `:error` →
  "unknown topic"); `authorize/2` checks
  `Topic.user_of(parsed) == socket.assigns.user_name` → mismatch
  "forbidden". UserSocket still hardcodes `user_name: "vjt"` in
  `connect/3` — token-bound socket auth is **2i deferred** — but
  authz wired so 2i flip is one-line zero-channel-work.
- `899c446` (2h review nit): **S1** single fix — replaced fragile
  `assert_raise RuntimeError ~r/no channel/i` Phase 1-shape regression
  with structural assertion at `UserSocket.channel/1` returning nil
  for old prefix + sanity check that new shape returns
  `{GrappaWeb.GrappaChannel, _}`. Same coverage, no Phoenix-version
  brittleness. **Three-prong Phase 1 rejection**: router (UserSocket
  channel macro), parser (Topic.parse), authz (user_of/socket
  comparison) — defense in depth.

**Validation:** ci.check green. Cross-user broadcast leak closed at
both PAYLOAD (G3) and ROUTE (2h) levels.

---

## S27 — 2f (IRC.Client SASL + PASS + IDENTIFY state machine)

**Date:** 2026-04-26 **Commits:** `687f157` `f56a0f3`

- `687f157` (2f): `Grappa.IRC.Client.init/1` now drives the **FULL
  upstream registration handshake** PASS / CAP LS 302 / NICK / USER /
  AUTHENTICATE / CAP END branched per `:auth_method` ∈ `{:auto, :sasl,
  :server_pass, :nickserv_identify, :none}` matching Credential enum.
  State adds `phase ∈ :pre_register | :awaiting_cap_ls |
  :awaiting_cap_ack | :sasl_pending | :registered` + `caps_buffer` for
  IRCv3.2 multi-line CAP LS continuation. `:auto` is default — modern
  ircd "just works" via CAP REQ + AUTHENTICATE PLAIN +
  `base64(\0sasl_user\0sasl_user\0pw)`, **legacy ircd Bahamut/Azzurra
  gets PASS at register-time which the server hands off to NickServ
  internally per `~/code/IRC/bahamut-azzurra/src/s_user.c:1273-1278`**
  (source-code-verified) — `421 :Unknown CAP` from legacy doesn't
  stall, 001 unconditionally promotes to `:registered`. Only
  `:nickserv_identify` fires client-side `PRIVMSG NickServ :IDENTIFY`
  post-001. **904/905 from upstream stops Client `{:sasl_failed, code}`**
  → propagates via link to Session → DynamicSupervisor `:transient` →
  same wall — credential is wrong, operator must intervene; **no
  silent fallback to PASS-handoff** (would weaken security posture for
  `:sasl` which is operator explicitly saying "must SASL"). Parser
  gains `"AUTHENTICATE" → :authenticate`; Message command type
  widened. Session.Server drops `Client.send_handshake/2` and now
  passes `nick + realname: opts.nick + sasl_user: opts.nick +
  auth_method: :none` (2g sources from Credential row). 8 new
  client_test.exs tests scripted via per-test sasl_handler /
  bahamut_handler / rfc_handler.
- `f56a0f3` (2f review fixes): **C1** split `:awaiting_cap_ack` phase
  between `:awaiting_cap_ls` and `:sasl_pending` so AUTHENTICATE PLAIN
  only fires AFTER server CAP * ACK :sasl arrives (IRCv3 spec REQUIRES
  this; strict ircd Solanum/Ergo would reject otherwise; lenient fake
  server tolerated the bug); **C2** multi-line CAP LS handling — new
  `state.caps_buffer` accumulates from `[_, "LS", "*", chunk]` and
  finalizes on `[_, "LS", chunk]` (without this any modern ircd
  advertising >8 caps would land "sasl" on line 2); **C3+I1** opts
  shape now requires `:realname + :sasl_user` (was `|| opts.nick`
  fallback violating CLAUDE.md "no defaults via \\\\") + init
  validates `auth_method != :none` implies non-empty password
  returning `{:stop, {:missing_password, m}}`; **I3** `@derive
  {Inspect, except: [:password]}` on Client struct (no plaintext in
  inspect/1 / `:sys.get_state/1` / SASL crash reports); **I4**
  `handle_cap` LS/ACK/NAK guarded by phase so CAP NEW post-registration
  cannot re-enter SASL chain; **I2** Logger.error for SASL failure
  includes `:sasl_user` alongside `:numeric`; **T5** 432/433 NICK
  rejection during registration stops `{:nick_rejected, code,
  attempted_nick}` (Phase 1 would stall forever in `:pre_register`);
  **T1+T2** new tests for CAP NAK :sasl crash + multi-line CAP LS;
  **T4** 4 instances of `Process.sleep(20)` replaced with new
  `await_handshake/1` test helper using `IRCServer.wait_for_line` on
  USER + bahamut test's `Process.sleep(100)` replaced with
  `assert_receive {:irc, %Message{command: {:numeric, 421}}}` for
  deterministic synchronisation via dispatch_to mailbox (mid-line
  coalesce test's intentional `Process.sleep(50)` preserved — time
  gap IS the test); plus 3 init/1 contract tests pinning
  `{:missing_password, m}` shape.

**Validation:** ci.check green. 332 tests, 0 failures deterministic at
`max_cases:2`. Reviewer SHIP after fixup.

---

## S28 — 2g (Session.Server loads credentials from DB)

**Date:** 2026-04-26 **Commits:** `7395fdb` `387a7e0`

- `7395fdb` (2g): `Session.start_session/2` takes `(user_id, network_id
  integer)`; Server's `init/1` resolves user/network/credential/server
  from DB. Flat opts map gone — DB is single source of truth. Registry
  key `{:session, user_id, network_id}` (UUID + integer); pre-2g
  `(user_name, network_id_string)` shape gone everywhere.
  `Session.placeholder_user/0` + `Session.spawn_batch/1` deleted.
  Controllers (`MessagesController`, `ChannelsController`) shifted to
  `conn.assigns.current_user_id` + `Networks.get_network_by_slug` for
  slug→integer FK resolution. **Unknown slug returns 404 `:not_found`**
  (distinct from `:no_session` for known-slug-no-session) — operator
  debug benefit. `Session.Server.NoServerError` exception raised when
  resolved network has zero enabled servers (Exception not tuple:
  human-readable network identity in supervisor restart-loop log;
  symmetric with `Ecto.NoResultsError` from `get_credential!/2`).
  `Networks.get_network!/1` added; `Credential.effective_realname/1` +
  `effective_sasl_user/1` added (resolves 2f I1 `|| nick` nil-fallback
  pattern). Bootstrap kept TOML-as-spawn-list during 2g (full TOML
  drop is 2j); missing DB credential counts on `failed`, missing DB
  user counts on `skipped` — distinct operator actions
  (`mix grappa.bind_network` vs `mix grappa.create_user`). New
  `AuthFixtures.network_with_server/1` + `credential_fixture/3` test
  helpers. Outbound controller test fixed to bear the SAME user's
  session that Server spawns for (pre-2g routed via placeholder_user
  so bearer mismatch was invisible; post-2g it would 404 every POST).
- `387a7e0` (2g review fix): **I1** `Networks.Credential.password_encrypted`
  marked `redact: true`. Cloak `:load` decrypts on read so the field
  IN MEMORY carries plaintext; without redact, `inspect/1` over a
  fetched credential leaks cleartext (concrete leak surfaces:
  `Logger.error("session start failed", error: inspect(reason))`,
  IEx debugging, future crash-report telemetry). Symmetric with 2f I3
  `@derive {Inspect, except: [:password]}` on IRC.Client. **S6** test
  rename "transient supervisor would retry" → "(start_child returns
  error tuple)" — DynamicSupervisor doesn't restart on init failure.
  **S3** test rename "PER-USER ROUTING ISO: alice's subscribe..." →
  "topic discriminator scopes by user_name — bare-slug subscriber gets
  nothing" — old name overpromised cross-session iso semantic that 2h
  owns. **S1** (NoServerError doc clarification) skipped as cosmetic.
  **S2** (Bootstrap test TOML/DB duplication) deferred to 2j.

**Validation:** 6 properties / 340 tests / 0 failures; dialyzer 0;
credo 0; sobelow 0; doctor 100%/100%; ci.check fully green. Reviewer
SHIP verdict.

**Branch state:** `phase2-auth` 19 ahead of unpushed local main.
Sub-tasks 2a-pre, 2a, 2b, 2c, 2d, 2e, 2h, 2f, 2g all done. 2i + 2j +
2k remain.

---

## Pending at flush

**Next sub-task (per plan order):** 2i — one-line `UserSocket.connect/3`
flip from hardcoded `user_name: "vjt"` to `Accounts.authenticate(token)`.
Authz wire-up at `GrappaChannel.join/3` already done in 2h.

**Then:** 2j (Bootstrap reads from DB + grappa.toml deletion + Config
schema deletion + compose.yaml bind-mount removal + README rewrite —
big-delete sub-task). Then 2k (live deploy + smoke against real
Azzurra PASS path + Libera SASL path + DESIGN_NOTES Phase 2 close
pass).

**DESIGN_NOTES Phase 2 close pass entries owed (do at 2k):**
- Name format trade-off `^[a-zA-Z][a-zA-Z0-9_-]$` rejects
  `[^]{}|\` so same string flows through IRC nicks + PubSub topic
  segments without escaping
- Network slug `^[a-z0-9_-]+$` URL/topic-safety
- Decision G2 wipe-and-rebuild migration choice for messages.network_id
  text → integer FK
- "no `delete_network/1`; cascade-on-empty is the only network-drop path"
- IRCv3 CAP ACK gate decision + Bahamut `s_user.c:1273-1278`
  PASS-handoff verification + `sasl_unavailable` crashes (no fallback
  to PASS-handoff for `:sasl`) rationale

**Phase 5 deferred items (reviewer-noted, not for Phase 2):**
- Nick-mangling fallback for `{:nick_rejected, ...}` (append `_` + retry)
- Reconnect/backoff inside Client on socket close
- Server-side `authzid` distinction for stricter SASL implementations
  (currently `authzid == authcid == sasl_user` matches
  irssi/weechat/hexchat)
- CAP NEW post-registration SASL re-auth (currently I4-guarded no-op)
- x-forwarded-for trusted-proxy allowlist + RealIP plug
- `build_unauthenticated_conn/0` conn-helper consolidation
  (now 7+ inline `Phoenix.ConnTest.build_conn()` sites)
- Human-readable changeset error formatting in mix tasks

**Codebase review gate:** still not due. Last 2026-04-25 (S15), 13
sessions ago. **Threshold trip: due at S28 = NOW.** Recommend running
codebase review BEFORE 2i — Phase 2 implementation has been heavy
(19 commits, 5491 insertions / 501 deletions on branch); a broad
review catches drift before 2j's big-delete pass.

**CP07 line count:** ~370 lines after this flush — ROTATE TO CP08
recommended. CP08 should open at S29 (codebase review or 2i,
operator's call).

---
