# CP35 — M-11 mid-reviewer flush (2026-05-16)

## Status

M-11 (real-time `grappa:admin:events` topic + cic Events tab) WIP on
worktree `cluster/admin` at `.worktrees/admin`. Server + cic surfaces
complete (2028 ExUnit + 1161 vitest green BEFORE reviewer-driven
changes); reviewer findings being folded; **WIP commit `16f3405`** is
the resume point.

## What's done

### Server-side (full check.sh green at HEAD@{1} = `c86d8d8`)

- `lib/grappa/pubsub/topic.ex` — `admin_events/0` + `:admin_events`
  parser arm; `user_of/1` deliberately fails on it.
- `lib/grappa/admin_events.ex` — singleton GenServer with ring
  buffer (cap 200) + telemetry adapter + `record/1` synthetic path.
- `lib/grappa/admin_events/wire.ex` — 10 typed event kinds +
  `from_telemetry/3` closed union (`:skip` arm for `:operator_reset`).
- `lib/grappa_web/channels/admin_channel.ex` — NEW. Gates on
  `socket.assigns.is_admin == true` (post-CRIT-1 fix).
- `lib/grappa_web/channels/user_socket.ex` — adds channel route +
  assigns `:is_admin` at connect time (CRIT-1 fix).
- `lib/grappa/application.ex` — supervised AdminEvents child +
  `attach_admin_telemetry?/0` boot-time gate.
- `lib/grappa/hot_reload/long_lived_modules.ex` — added
  Grappa.AdminEvents to @modules.
- `lib/grappa_web.ex` — Grappa.AdminEvents in deps.
- `config/config.exs` — Logger metadata `:topic` + `:kind`.
- `config/test.exs` — `:attach_admin_telemetry, false`.
- `lib/grappa/operator.ex` — extended `delete_visitor`,
  `disconnect_session`, `terminate_session`, `reap_visitors`,
  `reset_circuit` with optional `actor` arg.
- `lib/grappa/visitors/reaper.ex` — per-row `:visitor_reaped`;
  `:reaper_swept` moved to caller (Operator + handle_info).
- `lib/grappa_web/admin/auth_plug.ex` — added
  `actor_from_conn/1` (single source).
- 4 admin controllers (visitors/sessions/networks/circuit) thread
  `AuthPlug.actor_from_conn/1`.

### Tests (server)

- `test/grappa/pubsub/topic_test.exs` — admin_events round-trip.
- `test/grappa/admin_events/wire_test.exs` — 25 cases.
- `test/grappa/admin_events_test.exs` — 6 cases (telemetry adapter
  + ring buffer + cap).
- `test/grappa_web/channels/admin_channel_test.exs` — rewritten
  post-CRIT-1: 11 cases + 2 end-to-end UserSocket.connect tests.
- `test/support/auth_fixtures.ex` — `user_fixture/1` accepts
  `is_admin: bool`.

### Cic-side (1161 vitest + biome green AT WIP COMMIT)

- `cicchetto/src/lib/api.ts` — WireAdminEvent discriminated union
  (10 arms).
- `cicchetto/src/lib/socket.ts` — `joinAdminEvents()`.
- `cicchetto/src/lib/adminEvents.ts` — module-singleton store +
  `install/uninstall/startAdminEventsSubscription` (production
  wrapper + test seam — comment corrected per NIT-1).
- `cicchetto/src/AdminEventsTab.tsx` — renders ring buffer
  newest-first with exhaustive renderEvent switch.
- `cicchetto/src/AdminPane.tsx` — 4th tab + subscribe lifecycle
  (onMount/onCleanup).
- `cicchetto/src/__tests__/AdminPane.test.tsx` — extended for 4th
  tab + lifecycle assert.
- `cicchetto/src/__tests__/AdminEventsTab.test.tsx` — 15 cases.
- `cicchetto/src/__tests__/adminEvents.test.ts` — 8 cases.

## Reviewer findings — all SERVER + CIC findings applied in WIP

CRIT-1, CRIT-2, HIGH-1..HIGH-4, MED-4, NIT-2-server, NIT-1-cic all
folded into the WIP commit. See commit body for the changeset map.

## What's left (post-/clear resume order)

1. **Verify gates after reviewer-driven changes**: `scripts/check.sh`
   full server suite (must pass; the channel test was rewritten),
   `scripts/bun.sh run check` + `run test` for cic.
2. **NIT-2 cic** (optional defense-in-depth): add
   `if (installed !== channel) return;` inside the `.on snapshot/event`
   callbacks at `cicchetto/src/lib/adminEvents.ts:65,69`.
3. **Playwright e2e** at `cicchetto/e2e/tests/m11-admin-events-live.spec.ts`
   per scripts/integration.sh (mandatory per
   `feedback_ux_e2e_mandatory`).
4. **Browser smoke** at `https://irc.sindro.me` per
   `feedback_cicchetto_browser_smoke` (WS subscription lifecycle is
   not vitest-testable).
5. **Squash WIP commit** into a clean feature commit before merge
   (`sl/git rebase -i HEAD~1` to fold the M-11 commit on top of
   `c86d8d8`).
6. **Rebase + ff-merge to main**.
7. **Deploy COLD** per `feedback_hot_deploy_preflight` HIGH-29 etc.
   (touched: `application.ex` + `user_socket.ex` + `long_lived_modules.ex`
   + new `Grappa.AdminEvents` defstruct).
   `scripts/deploy.sh --force-cold` if preflight misclassifies
   post-merge.
8. **Deploy cic bundle**: `scripts/deploy-cic.sh`.
9. **Smoke browser via chrome-devtools**: vjt admin → admin pane →
   Events tab → trigger visitor delete in another tab → assert row
   lands within 100ms.
10. **Update**: M cluster ledger (now 12/12 if M-11 lands cleanly;
    M-12 docs sweep + M-Z close still pending).

## Carried-forward project state

- **vjt is PERMANENT ADMIN** — `is_admin: true` on live BEAM. Memory
  `feedback_vjt_is_permanent_admin` — NEVER demote.
- **S\`grappa visitor** — vjt-blessed alive on azzurra. DO NOT
  DELETE.
- **nginx admin allowlist** (`infra/nginx.conf` + `cicchetto/e2e/nginx-test.conf`):
  M-11 adds NO new `/admin/<resource>` HTTP endpoint (channel uses
  `/socket`). Allowlist unchanged.
- **Deploy class for M-11**: **COLD** (supervision tree edit +
  new channel registration + new long-lived module — all in
  preflight). Per `feedback_deploy_preflight_empty_diff_after_merge`,
  use `--force-cold` after `git merge --ff-only` if preflight diff
  empty.

## Plan ref

`docs/plans/2026-05-16-tmu-cluster-arc.md` §M-11 (lines 1007-1018,
deliberately scarno — design lived in the plan-agent output captured
in this conversation; commit body has the full reviewer-fix changeset).
