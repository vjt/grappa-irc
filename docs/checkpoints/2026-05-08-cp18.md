---
status: active
---

# CP18 — bnd-A2 close + scroll-on-window-switch

Opened 2026-05-08, immediately after CP17 (server-side-pending) closed
+ DEPLOYED at `15d2034`. Two clusters this session:

1. **`cluster/cic-network-id-store`** (cluster #16) — single-target
   commission to close the LAST remaining HIGH OPEN architecture audit
   row (bnd-A2). After this, **HIGH OPEN count = 0** across both
   audits.
2. **`cluster/scroll-on-window-switch`** — user-reported bug fix:
   re-selecting a populated channel after opening an empty query window
   left scroll pinned at the top.

Previous: CP17 — server-side-pending — LANDED 2026-05-08 at `a37b199`
+ DEPLOYED + browser-smoke verified. CLAUDE.md hard-invariant "cic
NEVER originates state" restored.

## Buckets

- **B0 — bnd-A2** LANDED — extracted `networkBySlug` +
  `networkIdBySlug` Map-keyed `createMemo` helpers in
  `cicchetto/src/lib/networks.ts`; collapsed all 14
  `networks()?.find((n) => n.slug === networkSlug)?.id` literal call
  sites in `compose.ts` (channel-ops verbs /op /deop /voice /devoice
  /kick /ban /unban /banlist /invite /umode /mode /topic-clear + DM
  verbs /msg /query) to `networkIdBySlug(networkSlug)`. The /quit
  handler at L292 keeps its `networks() ?? []` enumeration (full-list
  iter, not slug-keyed). Commits `4c6a1ad` + `ec9843a`, merged at
  `51e8bb3`.
- **B1 — scroll-on-window-switch** LANDED — extended on-key effect in
  `ScrollbackPane.tsx` to snap scroll on every window switch, branching
  on unread-marker presence: marker → `scrollIntoView({ block: "center" })`,
  no marker → snap to tail. Length-effect's marker branch ALSO moved
  `block: "start"` → `block: "center"` so the marker-centered UX is
  symmetric across both mount paths (initial-focus vs switch-back).
  `markerScrolled` reset on key change so a delayed REST page can still
  re-fire the length-effect's marker branch. `atBottom` set per branch
  so the floating "scroll to bottom" button doesn't flash mid-switch.
  Commit `2a803c9`, merged at `4ba244d`.

## Design summary

### bnd-A2 (Option B per vjt)

Three options weighed at design time: (A) pure helper function, (B)
Map-keyed `createMemo` + helper, (C) bigger refactor pushing
`network_id` resolution UP into `slashCommands.ts` dispatch. **Option B
chosen** — mirror-symmetric with cluster #13 M4 (`networkKey` /
`decodeChannelKey` extraction) + M7 (`target_kind/1` public-helper
promotion). +6 LOC of memo wiring vs Option A; O(1) lookup vs the 14×
O(n) scan (n is small in practice — wins are single-source-of-truth
and futureproofing, not perf).

Memo invalidates whenever the underlying `networks` resource updates
(post-/connect, post-/disconnect, bearer rotation), so callers see new
entries without manual cache management. Helper signatures take
`slug: string` (NOT `string | null`) because the 14 call sites all
hand a guaranteed-string from `submit(_, networkSlug: string, _)` —
per CLAUDE.md "Don't add error handling for scenarios that can't
happen," no nullable widening.

`compose.test.ts` mock fixture extended: mock `../lib/networks` now
exposes `networkBySlug` + `networkIdBySlug` derived from the existing
`mockNetworksData` fixture. The "/msg without active network returns
inline error" test stubs `networkIdBySlug` (was: `networks`) to
undefined — same contract under test (inline error when helper returns
undefined), updated to the new dispatch surface.

### scroll-on-window-switch (root cause + fix)

Root cause: the `[data-testid="scrollback"]` `<div>` is the SAME DOM
node across `selectedChannel` changes (Solid's `<Show>` in `Shell.tsx`
is non-keyed, so the element is reused, not rebuilt). Pre-fix the
length-effect at `ScrollbackPane.tsx:583` only fired when
`messages().length` changed, so re-selecting a previously-loaded
channel never re-snapped to its tail. The query window left scrollTop
= 0; switching back to the channel preserved that scrollTop on the
shared `<div>`.

Fix design discipline: the on-key effect already existed (resets
banner + markerScrolled); extend it rather than add a parallel effect
on the same key. Snap behavior branches on marker presence so the same
UX rule applies to both mount paths (initial focus + switch-back):
unreads → marker centered, no unreads → tail. The user's spec was
literal: "more or less in the middle of the screen, and if no unreads
then scroll to bottom."

The companion change — length-effect's `block: "start"` → `block: "center"`
— is the OTHER mount path: a window opened with unreads where the REST
page lands AFTER focus. Without this, switch-back centers the marker
but initial-focus pinned it to the top edge. Asymmetric UX is worse
than no fix at all.

## Audit closures

After this session:

| Surface | Total | LANDED | PARTIAL | OPEN | STALE |
|---|---|---|---|---|---|
| Codebase review | 88 | 54 | 0 | 32 | 2 |
| Architecture review | 75 | 31 | 3 | 40 | 0 |
| **TOTAL** | **163** | **85** | **3** | **72** | **2** |

**HIGH OPEN count = 0** across both audits. Codebase HIGH count went
to 0 in cluster #15; architecture HIGH count went to 0 in this
session's cluster #16. The 72 OPEN remaining are all MEDIUM / LOW —
candidates for future `*-omnibus` clusters, no urgency.

## Tests

- `scripts/check.sh` EXIT=0 (full ci.check) — both clusters
- `scripts/dialyzer.sh` EXIT=0 (Total errors: 0, Skipped: 0)
- `scripts/integration.sh` EXIT=0:
  - bnd-A2 cluster: 33 passed + 1 retry-passed flaky (cp13-server-window
    S5 caveat — pre-existing race, not a regression)
  - scroll-on-window-switch cluster: **35 passed** (+2 new specs in
    `scroll-on-window-switch.spec.ts`) + 1 retry-passed flaky (same
    pre-existing S5 caveat)
- `bun run check` (biome + tsc) clean across 85 files
- `bun run test` 684/684 passed (40/40 files; +6 new for
  `networkBySlug`/`networkIdBySlug` reactivity + lookup contracts)

## Browser smoke (cic-touching, MANDATORY)

Both clusters, headed Chrome via CDP against deployed prod
(`voygrappa.bad.ass`):

- bnd-A2: sidebar populated (8 azzurra entries), channel-click →
  scrollback render OK, `/topic` and `/op vjt` typed cleanly into
  compose box (helper-resolved `networkId` path exercised at parser
  boundary, no JS error, focus retained), clientId still valid v4
  UUID, `errors` array length 0, all 7 bound-channel `rc:azzurra:*`
  read-cursor keys present in localStorage.
- scroll-on-window-switch: 4-window switch sequence (vjt → grappa →
  vjt-claude → Server) executes without errors (0 console errors), all
  clicks landed; sidebar populated; geometry probe confirmed
  `distFromBottom = 0` on Server window. The e2e specs covered the
  geometry contract (305ms + 269ms first-try pass).

## Pending for next session

- **Cluster `event-driven-windows`** — brainstorm + impl plan docs
  exist (`docs/plans/2026-05-07-event-driven-windows.md` +
  `-impl.md`). Largest unfinished design item before MVP. 7 buckets
  (B1–B7), sized 4–6 sessions. Closes 3 todo items + the members race
  + the channel-not-connected ghost-window bug + the
  optimistic-state-assumption class. Confirmed by user this session as
  the next priority.
- **Open via /clear → /start → open worktree
  `cluster/event-driven-windows`** — the brainstorm doc was written to
  survive /clear so the next session reads it fresh.
- 72 MEDIUM/LOW audit rows remain — bundle into future omnibus clusters
  ad-hoc.
