# CP37 — U cluster bucket U-3 LANDED (2026-05-16)

> **CLOSED 2026-05-22** — rotated to CP38. Eight clusters shipped
> under this checkpoint without rotation discipline during the
> autopilot dogfooding wave (U-4..U-Z, UX, UX-1..3, iOS, UX-4, UX-5,
> UX-6); rotation deferred at the bucket level got recovered at the
> UX-6-Z cluster close. Read CP38 for the current state.

## Status

**U cluster — 4/8 buckets shipped, U-3 CLOSED**. U-0/U-1/U-2 LANDED +
pushed; U-3 LANDED + HOT-deployed + cic bundle deployed + live smoke
matrix green; pushed to origin/main. U-4 next (device-identity-change
UD5.A+B+C; larger; recommended for a fresh session).

Branch: `main`. Pushed to origin.

## Commit graph (HEAD-side)

```
<feat-u-3-sha>  feat(u-3): FC 503 too_many_sessions + admin live_counts + cic friendly mapping
<docs-cp37-sha> docs(cp37): U-3 LANDED — deployed + smoked + pushed
a68bc19         feat(u-2): admission split + login timeout phase split + honest log
```

Three WIP commits (`b7965a5`, `be568ad`, `f460f79`) + one e2e-fix
WIP (`6f74be0`) collapsed into single `feat(u-3)` at rebase.

## What shipped (full commit body in `feat(u-3)`)

- **UD3** — FallbackController `:client_cap_exceeded` 429 → 503
  with envelope `too_many_sessions` + exhaustive enum matrix test
  cross-checked against `Admission.capacity_error_atoms/0` canary.
- **UD4** — admin `live_counts` projection on `GET /admin/networks`
  (per-row + bulk variants; one feature one code path via private
  `count_live_sessions/2`) + AdminSessionsTab per-network cap-count
  summary block (visitors N/cap, users N/cap, per-client cap).
- **Cic friendlyApiError** — single shared typed-error → human copy
  module with literal-union `KnownApiErrorCode` + `assertNever`
  exhaustiveness; 19+ arms covered by vitest; Login + compose
  delegate to the shared module.
- **E2E** — `cicchetto/e2e/tests/u-3-cap-honesty-mapping.spec.ts`
  parameterized over (UD3 wire-flip + UD4 admin pane render).
- **Test isolation** — synthetic-id + `register_for_test/2` helper
  in `admission_test.exs`; structural `live_counts` asserts in
  controller test.

## Gate evidence

`scripts/check.sh` exit-0 twice in a row at `be568ad` (pre-rebase
snapshot — equivalent code in post-rebase commit):
```
8 doctests, 29 properties, 2053 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
```

bats 23/23; bun check (biome + tsc) clean; vitest 1164+/1164+
across all files; Sobelow no medium-or-above; mix audit + hex.audit
green. Playwright `u-3-cap-honesty-mapping.spec.ts` both arms green
(177ms + 821ms in chromium).

## Reviewer outcomes

| Round | Scope | Verdict | Findings |
|-------|-------|---------|----------|
| R1 web | server-side | NEEDS_CHANGES → FIXED | M2 exhaustiveness canary + M3 N+1 collapse + L1+L2 minor (M1 wire-shape pinning deferred non-blocking) |
| R1 cic | cic-side | NEEDS_CHANGES → FIXED | M1 error-collapse + M2 closed-union exhaustiveness + M3 missing arms + L1+L2+L3 minor |
| R1 cross-surface | both | APPROVED | none |
| R2 cumulative | full surface | APPROVED | L1+L2 parallel-canon drift hazard (non-blocking polish; future cluster) |

## Deploy + smoke

- `scripts/deploy.sh` — auto-classified HOT (no migrations, no config
  additions, no long_lived_modules struct changes); `Phoenix.CodeReloader`
  reload OK; sessions preserved, container ID unchanged.
- `scripts/deploy-cic.sh` — cic bundle built (hash `mSEM__jO`) and
  broadcast to all live user-topics via `/admin/cic-bundle-changed`.
- **U-3.1 wire-flip evidence** (live BEAM, mint-session):
  `PATCH /networks/azzurra {connection_state: "connected"}` under
  saturated cap returned `503 {"error":"too_many_sessions"}` ✓.
- **U-3.2 projection evidence** (live BEAM, admin token):
  `GET /admin/networks` returned `live_counts: {visitors: N, users: M}`
  per row ✓.

vjt remained `is_admin: true` throughout per
`feedback_vjt_is_permanent_admin`. Cleanup deleted 3 minted smoke
session rows; vjt user record + admin flag preserved.

## U cluster progress (post-U-3)

- U-0 LANDED (`f5a1d8e`) — stop-swallow
- U-1 LANDED (`84388a7` + `313501f`) — schema split (cold)
- U-2 LANDED (`a68bc19`) — admission split + login phase-timeout
  split + honest log (cold)
- **U-3 LANDED** — FC 503 + admin live_counts + cic friendly mapping (hot)
- U-4 NEXT — device-identity-change UD5.A+B+C (own session)
- U-5..U-8 + U-Z pending per plan §U

## What's next

Open `/tmp/orchestrate-next.txt` at the start of the next session.
U-4 is the next bucket — device disconnect-then-reconnect-with-
different-identity (UD5.A subject-aware client-cap reset + UD5.B
visitor↔user identity-change parity + UD5.C user-identity-change
parity). Larger scope than U-3; recommended for a fresh session.
