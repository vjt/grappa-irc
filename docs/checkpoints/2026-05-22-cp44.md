---
status: closed
---

# CP44 — FLAKE-C per-spec collaborative triage (2026-05-22)

## Status

**FLAKE cluster bucket C in flight.** CP43 closed at 307 lines (3
sessions: FLAKE-A manifest + FLAKE-B Part 1 fixture fix + FLAKE-B
Part 2 true-isolation triage). CP44 opens with FLAKE-C: per-spec
collaborative triage with vjt for the 7 real-bug candidates
surfaced by Part 2.

Standing autopilot mandate continues per
`project_post_tmu_full_review_scheduled` +
`feedback_orchestrator_autonomy` + `feedback_push_autonomy`.
Per-spec full-stack cycle is the ONLY reliable isolation primitive
(`feedback_flake_b_batched_isolation_leaks`).

## Inputs from CP43 / FLAKE-B Part 2

Manifest: `docs/reviews/flake-triage-2026-05-22.md` § "FLAKE-B Part 2".

**7 real-bug candidates** (confirmed-fail in true isolation):

1. `i2-image-upload` — **vjt 2026-05-22: "uploads work in prod →
   spec is wrong"** (start here as confirmed-spec-rot, workflow
   validator).
2. `m9-cicchetto-part-x-click`
3. `members-prefix-regression` — page snapshot showed `.members-pane`
   absent from DOM at assertion time in Part 2.
4. `names-ux-n3-cold-load-auto-select`
5. `nick-case-sensitivity`
6. `p0d-lusers`
7. `p0e-invite-ack`

**4 FLAKE files** (Pass-1 mixed; not yet revalidated true-iso):
`cp14-b3-dm-history-bidirectional`, `ios-z-cluster-journey`,
`m9b-admin-sessions-actions`, `ux-6-k-pm-unread-cursor`. Likely
re-classify as SPEC-ROT under true isolation; defer until 7
candidates triaged.

Most likely outcome per FLAKE-A's wrongness pattern: most are SPEC
ROT (UX-4/5/6/7 sweeps moved DOM; specs assert on stale selectors).

## FLAKE-C workflow (per-spec)

For each of the 7 candidates, one at a time:

1. Bring up fresh testnet stack (`scripts/testnet.sh up` once for
   the session; per-spec cycle between runs if a spec leaks state
   per `feedback_flake_b_batched_isolation_leaks`).
2. Iso-run the spec headed if possible (debug mode); else capture
   error-context + page snapshot from Playwright HTML report.
3. Present diagnosis to vjt with options: SPEC ROT (selector drift,
   timing assumption) vs REAL BUG.
4. On SPEC-ROT verdict: fix the spec in the FLAKE-C bucket
   (per-fix commit); on REAL BUG verdict: split into a separate
   FLAKE-D/E/etc bucket sized by class.

## Pending after FLAKE-C

- **FLAKE-D..G** — per-class fix buckets, sized after FLAKE-C
  triage confirms which (if any) are real bugs.
- **FLAKE-Z** — cluster closer. Reconciliation; remaining
  quarantines with inline justification; DESIGN_NOTES cluster-close
  entry; README closed-clusters bullet; cluster summary memory.

## What's deployed RIGHT NOW (unchanged from CP43 close)

- **Server**: `bf3ba3a` on `origin/main` base; HEAD now `074e48f`
  (vjt's CLAUDE.md/todo sweep + FLAKE-B docs). Pure docs commits
  over REV-K `8070551` server base. COLD-deployed 2026-05-22
  (REV-K). Healthcheck: `ok`.
- **cic bundle**: `34TrT3jr` (REV-K) — `sender:` + `error:`
  unified wire shape.

## Carry-forwards from CP43 (unchanged)

### REV-J.5 (M1+M5) still deferred
Dockerfile UID prep prerequisite for M1+M5 anonymous-volumes
refactor; standalone bucket between flakes and codegen if bandwidth
permits, else future infra-polish cluster.

### Baseline flakes (do NOT block LANDED)
- `Grappa.AdmissionTest` ETS-singleton-leak class
- `AdminEventsTest:197` assert_receive class
- cic biome 2 baseline errors + 21 baseline warnings
- Per-spec testnet load behavior — FLAKE-B Part 1 fixture fix
  closed the 6 desktop-Server-window specs; broader load-class is
  the 27-spec SPEC-ROT (load class) population already classified.

### POST-FLAKES roadmap (vjt 2026-05-22 evening, locked)
1. UX-8 scroll cluster (read-cursor on scroll = new server contract).
2. wireTypes.ts codegen from server `Grappa.*.Wire` typespecs.
3. Bastille deploy workstream (GitHub issue #8).

See `docs/todo.md` ★ block for full rationale. Memory pointer:
`project_post_rev_roadmap.md`.
