---
status: active
---

# CP02

The active checkpoint. Append a `## Sn` section per session. Rotate (mark
this `status: done`, create `cp03`) when this file hits ~200 lines or
8 sessions.

CP01 closed at S5 / 392 lines / 22 commits — covered Phase 0 + Phase 1
Tasks 1–4. See `docs/checkpoints/2026-04-25-cp01.md` for the back-story.

---

## State at checkpoint creation

Frozen at commit `09f65a3` (`docs: S5 checkpoint + project-story
episode + todo flush`). Working tree clean, in sync with origin/main.

**Phase 1 progress (walking-skeleton plan):**
- Task 1 ✅ — sqlite + Ecto wiring + dev/prod DBs
- Task 2 ✅ — `Grappa.Repo` + worktree-aware scaffold, named volumes
  shared across worktrees so PLT cache + deps survive
- Task 3 ✅ — `Grappa.Scrollback` context (`insert/1`, paginated
  `fetch/4` by `(network_id, channel, server_time DESC)`)
- Task 4 ✅ — Bandit-backed `Phoenix.Endpoint`, `GrappaWeb.Router`,
  `HealthController`, `FallbackController`, `ErrorJSON`,
  `GrappaWeb.ConnCase`. `/healthz` returns `text/plain ok` outside
  the `:api` pipeline. Endpoint mounts last in supervision tree.
- Tasks 5–10 pending.

**Codebase shape:**
- ~620 LOC of Elixir under `lib/`
- 21 tests, ci.check ~17s on Pi (worktree, PLT hot)
- 22 commits on main since project init

**Tooling baseline:** Dialyxir (`:underspecs`), Credo (`--strict`),
Sobelow, mix_audit, doctor (doc + @spec coverage gate), Boundary,
ExUnit, StreamData, Mox, Bypass, ExMachina, excoveralls. All green.

**Outstanding from CP01:**
- Phase 1 Task 5 — `GET /networks/:net/channels/:chan/messages` per
  `docs/plans/2026-04-25-walking-skeleton.md` line 1450. First read
  resource that exercises `Grappa.Scrollback.fetch/4` over HTTP.
- Worktree `grappa-task4` cleanup ✅ (closed at CP02 open: `git
  worktree remove` + branch deleted).
- Live deploy + healthz round-trip on Pi — deferred to Task 8 when
  `Grappa.Bootstrap` wires `grappa.toml` into the supervision tree.
- Phase 5 hardening: lift `signing_salt` (currently `"rotate-me"` in
  `lib/grappa_web/endpoint.ex`) to `runtime.exs`; same for the
  `verify: :verify_none` TLS posture noted in CLAUDE.md.

**Codebase review gate:** not due. First review threshold is
2026-05-09 or S12, whichever comes first. Currently S5.

---

## S6 — 2026-04-25 — Phase 1 Task 5, GET /messages

First read resource lands. `MessagesController.index/2` exposes
`Grappa.Scrollback.fetch/4` over HTTP at
`/networks/:network_id/channels/:channel_id/messages` with
`?before=` (cursor) and `?limit=` (page size, default 50).
`MessagesJSON` view renders the flat array — using
`render(conn, :index, ...)` instead of inline `json/2` keeps the
surface aligned with Phoenix 1.8 `formats: [:json]` and gives Task 6
a place to add `:create` without a controller refactor (this was a
plan-body deviation from the plan-body's inline serialization; the
plan's own Files list mentioned `messages_json.ex`, so the body was
out of sync with itself).

### Code review surfaced a real bug

Reviewer flagged `?limit=banana` falling back silently to the default
as a CLAUDE.md violation ("validate at boundary, reject unknown
values"). Initial impl matched the plan, which permitted any int (and
had its own bug — `?limit=0` would be passed to
`Scrollback.fetch/4`, crashing on the `when limit > 0` guard, which
in turn would 500). Correct shape is to distinguish *absent* (use
default) from *present-and-unparseable* (return 400). Helpers now
return `{:ok, n}` / `{:ok, default}` / `{:error, :bad_request}`;
controller uses `with`; `FallbackController` gets a `:bad_request`
clause and the spec widens to match.

This is the second time a plan deviation flagged a plan bug rather
than a code bug — a healthy pattern. Plans aren't sacred; CLAUDE.md is.

### Reviewer was wrong about Jason atom encoding

The "MUST FIX" claim that `:privmsg` would crash Jason was based on
an incorrect premise — Jason's default behavior IS to encode any
atom as its string name. Verified at REPL:
`Jason.encode!(%{kind: :foo}) == ~s|{"kind":"foo"}|`. Tests passing
were the actual evidence (they round-trip through the Phoenix JSON
pipeline).

But the *related* point — that the contract wasn't asserted in tests
— was correct. Added `assert Enum.at(body, 0)["kind"] == "privmsg"`
to one of the tests so the contract is now load-bearing in the test
suite, not just a moduledoc claim.

Pattern: when a reviewer flags a MUST FIX based on assumed behavior,
verify the mechanism before changing code. The reviewer can be wrong
about the cause and right about the gap.

### Test coverage tightened

Added cross-channel and cross-network isolation test — would have
caught a `Scrollback.fetch/4` regression that dropped its `WHERE
channel == ^channel` clause. Previous tests would have shipped that
bug green because every seed row used the same `(network_id,
channel)` pair. This is the "if the implementation were wrong, would
this test catch it?" CLAUDE.md test-quality bar. Plus three 400-input
tests for the boundary-validation tightening.

Stats:
- 28 tests (+7 from Task 5: 4 happy + 1 isolation + 3 bad-input)
- ~750 LOC of Elixir under `lib/` (+130)
- 24 commits on main (+2 from Task 5: 6ac4456, 01c0a92)
- ci.check still ~17s on Pi (PLT hot)

### Worktree cleanup carried forward

`grappa-task5` worktree + `phase1-task5-messages` branch outstanding
once S6 closes — same pattern as task4 cleanup. Live deploy still
deferred to Task 8 (Bootstrap reads `grappa.toml`).

Pending for next session:
- Phase 1 Task 6 — `POST /networks/:net/channels/:chan/messages`.
  First write resource; introduces `Phoenix.PubSub.broadcast/3` over
  `grappa:network:{net}/channel:{chan}` topic. Per
  `docs/plans/2026-04-25-walking-skeleton.md` line 1572.
- Worktree cleanup: `git worktree remove
  /home/vjt/code/IRC/grappa-task5` and `git branch -D
  phase1-task5-messages` once S6 fully closed.
