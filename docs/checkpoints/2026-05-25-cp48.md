---
status: active
---

# CP48 — BUGHUNT-3 cursor sub-cluster CLOSED (2026-05-25)

## Status

**BUGHUNT-3 cursor cluster CLOSED 2026-05-25 at `47be86e`.**
One commit, test-code-only. cp14-b1 + BUGHUNT-2 cursor-* trio's
cursor-mutation residue on `(vjt, bahamut-test, #bofh)` is the
poisoner for marker-target T2, r6-own-action, scroll-settle-cursor
+ ux-6-k cascade victims. Root cause: `Grappa.ReadCursor.set/4` is
last-write-wins with direction NOT enforced (lib/grappa/
read_cursor.ex:113-119); shared seeded vjt user persists the
mid-pane cursor across spec boundaries.

**Bahamut state-pollution sub-cluster — CLOSED later same day**
by the `rows().length` prod fix; see the BUGHUNT-3 sub-cluster B
block below. The cluster carried into the same checkpoint because
the bisect+fix landed in one continuous session.

## Bisect summary

7 bisect iterations, each one `scripts/integration.sh --project
chromium -- <half-list> tests/marker-target-window-regression.
spec.ts`.

```
Iter 1: half1 (32 specs) + anchor               → anchor ✘ — poisoner in 1-32
Iter 2: half1a (1-16)    + anchor               → anchor ✘ — poisoner in 1-16
Iter 3: half1aa (1-8)    + anchor               → anchor ✓ — poisoner in 9-16
Iter 4: half1ab (9-12)   + anchor               → anchor ✘ — poisoner in 9-12
Iter 5: half1ac (9-10)   + anchor               → anchor ✓ — poisoner in 11-12
Iter 6: half1ad (11-12)  + anchor               → anchor ✘ — poisoner in 11-12
Iter 7: cp14-b1 alone    + anchor               → anchor ✘ — POISONER = cp14-b1
```

cp14-b1's `seedCursor` POSTs to `/networks/:slug/channels/:chan/
read-cursor` for Scenario 2's mid-pane assertion. Server accepts
the mid-pane id; never reset. Anchor T2's `dist <= 50` assertion
on `#bofh` post-switch lands on the injected unread-marker
(scrollIntoView path) instead of the bottom.

Second bisect (post-cp14-fix) identified the BUGHUNT-2 cursor-*
trio + scroll-settle-cursor as siblings of the same class —
real-wheel-scroll-up + 500ms settle POSTs cursor mid-pane via
the production code path under test, same persistence problem.
Same fix shape applied.

## Fix shape

`cicchetto/e2e/fixtures/grappaApi.ts` — added shared
`restoreReadCursorToTail(token, networkSlug, channel)` helper:
GET /messages → take `rows[0]` (DESC-ordered tail) → POST
`message_id` to `/read-cursor`. Idempotent (cursor is forward-
only at the cic side; at the server side `do_set/4` is last-
write-wins so re-POSTing tail is a no-op when already at tail).

Wired into:
- `cp14-b1-scroll-marker-vs-bottom.spec.ts` — afterAll (covers
  both scenarios' cursor advances).
- `cursor-advances-on-switch.spec.ts` — afterAll.
- `cursor-no-advance-on-open.spec.ts` — afterAll.
- `cursor-walks-with-scroll.spec.ts` — afterAll.
- `scroll-settle-cursor.spec.ts` — afterAll (test 1 leaves
  cursor mid-pane).

Plus defensive **beforeEach** restore (cumulative row arrivals
between cursor-* afterAll + victim spec start can push tail past
the restored cursor → marker re-injects):
- `marker-target-window-regression.spec.ts` — beforeEach (BOTH
  tests).
- `r6-own-action-no-events-badge.spec.ts` — beforeEach (kept
  existing afterEach joinChannel restore).

## Verification

```
scripts/integration.sh --project chromium    # v1: pre-fix     4 ✘
scripts/integration.sh --project chromium    # v2: after cp14   2 ✘
scripts/integration.sh --project chromium    # v3: + cursor-*   3 ✘ (different set)
scripts/integration.sh --project chromium    # v4: rerun        3 ✘ (rotating bahamut)
```

The 4 v1 fails (m2, marker-target, r6, ux-5-bk) → cursor-cluster
3 of those cleared, the 4th (ux-5-bk) is in the bahamut cluster.

Remaining v3+v4 fails = bahamut cluster: cp15-b6-pending-to-
failed-invite-only + p0e-invite-ack + ux-5-bk-join-fail-dupe +
intermittent ux-5-bs. 3/3 ✓ iso, separate cascade, separate
session.

### Gate evidence

`scripts/check.sh` exit 0:
- format ✓
- credo ✓
- test (2455 tests, 0 failures) ✓
- dialyzer (0 errors) ✓
- sobelow ✓
- deps.audit / hex.audit ✓
- doctor ✓
- bun check / bun test ✓
- bats (1..24 ok) ✓

## Filed for follow-up

`docs/todo.md` BUGHUNT-3 entry now carries only Sub-cluster C
(webkit-iphone-15). Sub-clusters A (cursor) + B (bahamut/prod
scroll race) both CLOSED in this checkpoint.

## Carry-forward memory candidates

- `feedback_e2e_shared_fixture_cursor_pollution`: when a spec
  writes server state on the shared seeded user, an
  afterAll/beforeEach restore is mandatory. Cursor, autojoin,
  parked credentials, channel modes all qualify. Pattern:
  `restoreReadCursorToTail` shape.
- `feedback_cascade_bisect_skips_per_run_stack`: full-suite
  bisect requires `scripts/integration.sh` per iteration (testnet
  tears down between runs); ~90s overhead per iteration but
  necessary for clean state. 7 iterations = ~12min for log₂(64).

---

# BUGHUNT-3 sub-cluster B (bahamut cascade) — PARTIAL CLOSURE 2026-05-25

`origin/main` `4cb0c40` — prod-side fix to `cicchetto/src/ScrollbackPane.tsx`.

## What "bahamut state pollution" actually was

NOT bahamut testnet cumulative state. NOT `join_failed` WS race.
A scroll-position race in `ScrollbackPane.tsx`:

- `applyMeEnvelope` (cold-load cursor hydrate from `/me`) is
  async via the `me()` createResource in `networks.ts:47-59`.
- `loadInitialScrollback` (per-channel REST fetch) is also async.
- When scrollback resolves BEFORE the cursor signal updates,
  `rows()` memo computes `unreadCount = 0` → no marker → the
  `scrollToActivation` rAF×2 takes the no-marker branch and
  snaps to tail with `lastElementChild.scrollIntoView({block:"end"})`.
- Cursor hydrates LATER → `rows()` re-runs → marker injects in
  the row list → marker DOM appears.
- BUT: key-effect doesn't re-fire (key unchanged) AND length-effect
  doesn't re-fire (it tracked `messages().length` which is unchanged
  — the marker lives INSIDE the rows() memo, not the messages array).
- Net result: marker DOM present, scroll glued to tail.

Symptom rotated per run because the `me` vs scrollback REST
ordering is non-deterministic and sensitive to upstream test
load. The race always existed but `bughunt-1-b-archive-mobile-seed`
(PART+JOIN cycle on `#bofh` in afterEach) perturbs the cold-load
timing reliably enough to make the bug visible from spec position
14 onwards. CP48's `restoreReadCursorToTail` fix cleared the
cursor sub-cluster but the rotating cascade family kept hitting
because the underlying scroll race was untouched.

## Fix

`ScrollbackPane.tsx:1284` — track `rows().length` instead of
`messages().length`:

```diff
   createEffect(
     on(
-      () => messages()?.length ?? 0,
+      () => rows()?.length ?? 0,
       () => {
```

When the cursor hydrates and the marker injects, `rows().length`
grows by 1 → the length-effect fires → existing `!markerScrolled()
&& marker` branch runs → `scrollIntoView(marker, {block: "center"})`.
Idempotent: `markerScrolled` latch + atBottom gate keep
subsequent appends on their existing paths.

## Bisect path

1. Phase 0 baseline (`scripts/integration.sh --project chromium`)
   `132 passed, 1 failed = cp14-b1-scroll-marker-vs-bottom:171`.
2. iso 3/3 ✓ for cp14-b1 alone → cascade confirmed.
3. Binary chop: h1 (specs 1-6 + anchor) ✓; h2 (7-13 + anchor) ✘;
   h3 (7-9 + anchor) ✘; h4 (7 alone + anchor) ✘ →
   `bughunt-1-b-archive-mobile-seed` is the trigger.
4. First test-side fix attempt (joinChannel awaits `joined:true`)
   was insufficient — 1/3 pass. Reverted.
5. Inspected failed cic state via `KEEP_STACK=1` + sqlite:
   marker DOM present, scroll glued to tail. Identified the
   cursor-hydration vs scrollback-REST ordering race.
6. First prod fix attempt (third activation trigger watching
   `markerRef()`) cleared cp14-b1 but full suite still 5 fails.
   Reverted.
7. Second prod fix attempt (`rows().length` track) — cp14-b1
   stable + bahamut cluster cleared. Landed.

## Validation

- iso `bughunt-1-b + cp14-b1`: 3/3 ✓
- iso cursor sentinels (marker-target + r6-own-action + scroll-
  settle + ux-6-k): 7/7 ✓
- full chromium runs (2): 131-132 / 134 (pre-fix 128-132 / 134).
  Bahamut trio (cp15-b6-pending + p0e + ux-5-bk) consistently ✓.
- cic vitest: 1641 ✓
- `scripts/check.sh` exit 0 (format / credo / mix test 2455 ✓ /
  dialyzer / sobelow / deps.audit / hex.audit / doctor /
  wire-types drift / bats 1..24)
- `scripts/deploy-cic.sh` + healthcheck ok
- CI integration run `26395056218`: **187 passed, 0 chromium ✘**
  (5 ✘ = pre-existing webkit-iphone-15 cluster: ux-6-c, ux-6-d,
  ux-6-g × 3 — Sub-cluster C per the brief). cp14-b1 ✓,
  cp15-b6-pending ✓, p0e-invite-ack ✓, scroll-on-window-switch:225 ✓.
- CI ci run `26395056248`: ✘ 10 fails ALL in
  `Grappa.AdminEventsTest` with "SessionRegistry never drained"
  setup-failure (same visitor id `e31d5c5f-...` across all 10).
  Pre-existing known-flaky test (a9e0c24, b17fd71, ee20035 all
  raised the drain budget over the past months); the docs-only
  commit `e3db8e4d` on identical HEAD passed CI ci ✓. Per
  `feedback_no_ci_retries_on_first_failure`, no rerun.

## Residual

`scroll-on-window-switch:225` ✓ on CI integration run
26395056218 under chromium. Locally it rotates as a victim
under specific timing (1-2/3 local full chromium runs); iso
2/2 ✓. Observation-only; not blocking. If it surfaces on CI
in a future run, separate bisect needed (anchor: position 89
in lex order).

## Carry-forward memory candidates

- `feedback_solid_memo_length_tracking`: when an effect tracks
  `signal().length` to fire on row append, the signal must be
  the same one whose RENDER drives the DOM. Tracking the upstream
  source (messages) misses commits triggered by downstream-only
  memo updates (rows = messages + cursor → marker injection).
  Trace from DOM back through memos to find the true reactive
  source for the effect's intent.
- `feedback_prod_race_disguised_as_cascade`: a rotating cascade
  across `(spec_a, spec_b, spec_c, ...)` with different victims
  per run that all share a common UI surface (here: ScrollbackPane
  initial-mount scroll) is more likely a prod-side race surfaced
  by varying test ordering than test-side pollution. Test-side
  fixes will be insufficient and rotate the failure to a different
  victim. Inspect what's COMMON about the UI surface those specs
  exercise.

## BUGHUNT-3 Sub-cluster D — webkit-iphone-15 prod-bug investigation HALTED (2026-05-25)

Worktree: `/private/tmp/grappa-bughunt3-c` on branch `bughunt-3-webkit`
(local, NOT pushed; clean working tree at halt time, no commits).

### The 5 specs

Same set as cp47 Category B + cp48 Sub-cluster C carry-forward
(NEVER passed iso 0/3, per cp47):

```
[webkit-iphone-15] tests/ux-6-c-mobile-admin-launcher.spec.ts:97
  → @webkit admin on mobile — drawer launcher footer hosts admin
    button; tap opens AdminPane
[webkit-iphone-15] tests/ux-6-d-keyboard-pattern.spec.ts:153
  → (f) Admin → Debug tab renders diag panel + DiagFloat toggle
[webkit-iphone-15] tests/ux-6-g-admin-mobile-h-scroll.spec.ts:104
  → wide admin tables permit pan-x via touch-action
[webkit-iphone-15] tests/ux-6-g-admin-mobile-h-scroll.spec.ts:169
  → pan-x gesture actually scrolls the wide table
[webkit-iphone-15] tests/ux-6-g-admin-mobile-h-scroll.spec.ts:204
  → vertical scroll inside the pane still works
```

All 5 share ONE failure mode: tap on `mobile-panel-admin` button
inside the mobile members drawer → `getByTestId("admin-pane")
.toBeVisible()` times out.

### Root cause IDENTIFIED — actual prod bug

**Solid runtime exception** during `setSelectedChannel({kind:"admin"})`
propagation:

```
TypeError: null is not an object (evaluating 'e.owned[t]')
  at je (https://nginx-test/assets/index-*.js:1:10585)
  at be (...:1:8185)
  at Op (...:4:5686)   ← openAdminPanel
  at s (...:1:16626)   ← Solid Listener
  at tt (...:1:17029)  ← Solid effect runner
```

`je` is Solid's `cleanNode` (`node.owned[i]` access on a null
`owned` array). The exception breaks the Show flip mid-render:
`AdminPane` factory IS invoked (children-factory runs once), but
the DOM mount step never completes — `onMount` doesn't fire, the
`data-testid="admin-pane"` never lands in DOM. Yet the underlying
signal IS set to admin (probe via `window.__BUGHUNT3C_GET_SEL__`
reads `{kind:"admin"}`; `__BUGHUNT3C_GET_ADMIN__` reads `true`;
`__BUGHUNT3C_GET_USER__` reads `{...is_admin:true}`). Subsequent
`createEffect(() => selectedChannel())` subscribers also stop
re-firing post-throw (Solid batch interrupted).

The likely shape: an `onCleanup` somewhere during the
channel→admin Show transition tears down a parent owner whose
`owned` array was already nulled in this update cycle —
concurrent modification of `node.owned` during
`for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i])`
in `cleanNode` (`solid-js/dist/solid.js:959`).

### Why it's a real prod bug, NOT synthetic-tap

Probe inside `page.evaluate` after the tap proves selection IS
admin AND user IS admin AND isAdmin() returns true. The Solid
exception is reproducible 3/3 iso, fires on the SAME stack
frame every run. This is NOT a synthetic-tap timing issue —
it's a Solid render race triggered by the specific JSX shape
of Shell.tsx's mobile branch.

vjt reports successful iPhone PWA admin usage in dogfooding —
which means the exception either (a) doesn't fire on real iOS
Safari touch (different gesture-event sequencing), or (b) it
DOES fire but the spec's exact tap-sequence-then-immediately-
read-DOM is what surfaces it (real user tap → delay → click ≠
Playwright's instant synthetic tap).

### 3 fix attempts that DIDN'T land

All discarded (worktree reverted to clean):

1. **`createMemo` for the Show predicate** — `const
   isAdminPaneVisible = createMemo(() => selectedChannel()?.kind
   === "admin" && isAdmin())` — Show still throws.
2. **`batch()` wrap of `openAdminPanel`'s 4 setters** — drawer
   close + 3 mutex setters + navigate coalesced into one
   reactive batch. Throw still fires at the END of the batch,
   when Solid flushes.
3. **Reorder navigate FIRST** then drawer-close — same throw,
   moved one step earlier in the stack (`s → Op → l → be → je`
   becomes `s → Op → l → be → je` — `l` = setSelectedChannel
   wrapper).

The throw is inside Solid's internal `cleanNode` during the
recursive teardown of the kind=channel branch's owner subtree.
The fix needs to identify WHICH `onCleanup` in the
ScrollbackPane / TopicBar / ComposeBox / fallback-arm subtree
is mutating its grandparent's `owned` array during disposal.

### Evidence captured (not in repo — local-only)

- Probe via `window.__BUGHUNT3C_GET_SEL__` confirms signal value
  IS `{kind:"admin"}` post-tap
- Probe via `window.__BUGHUNT3C_GET_ADMIN__` confirms isAdmin()
  returns true
- Page snapshot at failure shows `complementary` (members drawer)
  still in DOM + ScrollbackPane (#bofh fallback content) still
  rendered → confirms Show DIDN'T flip
- `pageError` event in Playwright trace fires at the exact
  moment of `openAdminPanel(navigate)` execution

### Next-session pickup

Bisect strategy:
1. Temporarily remove each `onCleanup` site in ScrollbackPane,
   TopicBar, ComposeBox one at a time + re-run webkit spec to
   identify the culprit teardown.
2. Likely candidates: `ScrollbackPane.tsx:979` (focus-leave
   cursor write), `ScrollbackPane.tsx:1010` (resize listener
   cleanup), `ScrollbackPane.tsx:1545` (marker-ref onCleanup).
3. Once identified, the fix is likely to defer the cleanup
   side-effect via `queueMicrotask` so it runs AFTER the parent's
   `cleanNode` loop completes.

Alternative: replace the Show predicate with a `Switch`/`Match`
that gates kind=admin and kind=channel as siblings instead of
nested fallback — may avoid the recursive disposal.

DO NOT quarantine the specs — the bug is real even if the
production impact is dormant. If Playwright's tap-timing shifts
in a future browser-bundle bump, the bug could surface in real
user flows.

### Why halt

3 fix attempts didn't land. The bug is inside Solid's internal
reactivity tree management; isolating which onCleanup triggers
the concurrent modification requires bisect work that exceeds
this session's remaining context budget. Halt + push current
evidence + dispatch a fresh session with the bisect plan.

## BUGHUNT-3 Sub-cluster D CLOSED 2026-05-25 — Switch/Match refactor lands

vjt dogfood-confirmed prod-broken on desktop AND mobile (tap
admin → UI freezes, controls unresponsive) — so the bug is
NOT dormant after all. Halt reverted, dug deeper, landed the
fix in 4th attempt.

### Root cause (confirmed)

Solid's `<Show>` reactivity races during channel→admin
transition. Three Shows in Shell.tsx all subscribed to
`selectedChannel()` independently:
1. Outer admin gate: `<Show when={kind==="admin" && isAdmin()}>`
2. Inner sel-truthy gate: `<Show when={selectedChannel()}>{(sel)=>...}>`
3. Inner kind dispatches: `<Show when={sel().kind === "channel"}>` etc.

When admin tap fires `setSelectedChannel({kind:"admin"})`:
- Outer Show flips TRUE → begins disposing fallback subtree
- Inner Show re-evaluates `sel().kind === "channel"` on the
  NEW admin value mid-disposal
- Concurrent owner-tree walks in Solid's `cleanNode` collide:
  `for(i=node.owned.length-1...cleanNode(node.owned[i]))` reads
  a now-nulled `node.owned[t]` → throws
  `TypeError: null is not an object (evaluating 'e.owned[t]')`
- Throw halts the AdminPane Show mount cycle AND ALL downstream
  reactivity — every signal subscriber stops firing → UI freeze

### Fix (commit `515920d`)

Replace nested `<Show>` chains with `<Switch>/<Match>` in both
desktop + mobile Shell branches. ONE memo edge for all kind
branches (admin, channel/query/server, mentions, home) ensures
disposal is ATOMIC — previous subtree fully tears down before
the new branch begins mounting. `channel + query + server`
share ONE Match so ScrollbackPane stays mounted across
scrollback-bearing kind transitions (channel↔query↔server) —
the pane's `on(key, prevKey)` leave-arm cursor write at
ScrollbackPane.tsx:1142 depends on prop-driven reactivity, not
remount. TopicBar gated by an inner `<Show when={selKind() ===
"channel"}>` inside the combined Match.

### Validation

- webkit-iphone-15 full project: **59/59 ✓** (pre-fix 54/59;
  Sub-cluster C/D specs that NEVER passed iso are all green)
- ux-6-c-mobile-admin-launcher webkit iso 3/3 ✓
- `scripts/check.sh` exit 0 (format / credo / mix test 2455 ✓
  / dialyzer / sobelow / deps.audit / hex.audit / doctor /
  wire-types drift / bats 1..24)
- `scripts/bun.sh run check` exit 0
- `scripts/bun.sh run test` 1641 ✓
- chromium full: 131/133 (2 fails are pre-existing flakes
  ux-6-k-pm-unread-cursor + scroll-on-window-switch:225;
  baseline-without-fix shows same fail-rate)

### Deployed

`scripts/deploy-cic.sh` → bundle hash `CWyliz7O` broadcast
to all live user-topics. Server-side unchanged (cic-only fix);
no `deploy.sh` server restart needed.

### Memories

- New: `feedback_solid_show_nested_clearmode_race.md` — nested
  `<Show>` chains subscribed to the same signal race during
  transitions; use `<Switch>/<Match>` siblings to make disposal
  atomic. Linked from MEMORY.md.
- Updated: `feedback_solid_cleannode_owned_null.md` — added
  "Fixed via Switch/Match refactor" note pointing at commit
  `515920d`.

### Why the 3 earlier attempts didn't land

- `createMemo` for the Show predicate: didn't help because each
  Show still subscribed independently to selectedChannel
- `batch()` wrap in openAdminPanel: throw fires AT the batch
  flush, not during the individual setters
- Reorder navigate-first: same throw, just shifted one step
  earlier in the call stack

Only structural elimination of the nested-Show race fixes the
underlying class.


