---
status: active
---

# CP03

The active checkpoint. Append a `## Sn` section per session. Rotate (mark
this `status: done`, create `cp04`) when this file hits ~200 lines or
8 sessions.

CP02 closed at S8 / 295+ lines / 3 sessions — covered Phase 1 Tasks
5–7 (first read resource, first write resource + PubSub, first WS
surface). Before that CP01 closed at S5 / 392 lines / 22 commits —
covered Phase 0 + Phase 1 Tasks 1–4. See the prior checkpoints for
the back-story.

---

## State at checkpoint creation

Frozen at commit `70d9605` (`web: tighten Task 7 surfaces from code
review`). Working tree clean (CP02-close + CP03-open docs commit
about to land), in sync with origin/main.

**Phase 1 progress (walking-skeleton plan):**
- Task 1 ✅ — sqlite + Ecto wiring + dev/prod DBs
- Task 2 ✅ — `Grappa.Repo` + worktree-aware scaffold, named volumes
  shared across worktrees
- Task 3 ✅ — `Grappa.Scrollback` context (`insert/1`, paginated
  `fetch/4`)
- Task 4 ✅ — Bandit-backed `Phoenix.Endpoint`, `GrappaWeb.Router`,
  `HealthController`, `FallbackController`, `ErrorJSON`
- Task 5 ✅ — `GET /networks/:net/channels/:chan/messages`
  (pagination + boundary validation)
- Task 6 ✅ — `POST /networks/:net/channels/:chan/messages` +
  `Phoenix.PubSub.broadcast/3` on
  `grappa:network:{net}/channel:{chan}`
- Task 7 ✅ — `GrappaWeb.UserSocket` at `/socket/websocket` +
  `GrappaWeb.GrappaChannel` (single channel, `grappa:user:*` and
  `grappa:network:*` topics, payload-verbatim push)
- Tasks 8–10 pending.

**Codebase shape:**
- 810 LOC of Elixir under `lib/`
- 41 tests, ci.check ~17s on Pi (worktree, PLT hot)
- 45 commits on main since project init

**Tooling baseline:** Dialyxir (`:underspecs`), Credo (`--strict`),
Sobelow, mix_audit, doctor (doc + @spec coverage gate), Boundary,
ExUnit, StreamData, Mox, Bypass, ExMachina, excoveralls. All green.

**Outstanding from CP02:**
- Phase 1 Task 8 — `Grappa.IRC.Client` + `Grappa.Session` GenServer
  + `Grappa.Bootstrap` per
  `docs/plans/2026-04-25-walking-skeleton.md` line 1800. Largest
  task in Phase 1; introduces the IRC parser, the per-(user,network)
  session GenServer under `DynamicSupervisor`, and the bootstrap
  module that reads `grappa.toml` and spawns sessions. Live deploy
  + healthz round-trip on the Pi unblocks here too (deferred from
  every prior task).
- Phase 5 hardening: lift `signing_salt` (currently `"rotate-me"` in
  `lib/grappa_web/endpoint.ex`) to `runtime.exs`; same for the
  `verify: :verify_none` TLS posture noted in CLAUDE.md.

**Codebase review gate:** not due. First review threshold is
2026-05-09 or S12, whichever comes first. Currently S9.

---

## S9 (2026-04-25) — Task 8 replan + sub-task 8a-pre

**Pre-work**: re-planned Task 8 fresh against current code (per S8
lesson on climbing plan-deviation count). Plan written to
`~/.claude/plans/toasty-twirling-creek.md`; supersedes
`docs/plans/2026-04-25-walking-skeleton.md` lines 1829-2470. Plan
agent (`Plan` subagent) gave second-opinion review surfacing 14
findings; 11 accepted into plan, 3 rationalized. The most important
catch: schema extension to fit ALL IRC event types now (CLAUDE.md
"future CHATHISTORY listener facade is mechanical query translation,
not redesign") — original plan had `kind: :privmsg | :notice |
:action`, too narrow. Plan adds `:join | :part | :quit |
:nick_change | :mode | :topic | :kick`, nullable body, typed `meta`.

**Sub-task 8a-pre done** (4 commits on worktree
`phase1-task8-session`, branched from main `f5bb5dd`):

1. `1d39310` — sqlite `busy_timeout: 5_000` test config. Fixes
   pre-existing flake on baseline check (`Database busy` from
   ScrollbackTest's 505-row insert loop competing with concurrent
   `async: true` test writers). Per CLAUDE.md "fix pre-existing
   errors first" + "fix root causes."
2. `f0fdfb9` — drop `:ro` from worktree source mounts in
   `scripts/_lib.sh`. Elixir 1.19's incremental compiler does
   `File.touch!` on changed sources for staleness tracking; RO
   blocks it. Worked for Tasks 3-7 by accident (their compile
   cycles didn't touch the modified files); broke immediately when
   modifying `lib/grappa/scrollback/message.ex` in Task 8.
   `mix.exs`/`mix.lock`/`.formatter.exs`/`.credo.exs`/`.sobelow-conf`
   keep `:ro` (compiler doesn't touch them; `deps.get` could
   mutate `mix.lock`).
3. `ee5a85f` — `Message` schema: extend `kind` enum to 10 atoms,
   `body` nullable, `meta` column added. Per-kind body validation
   in changeset (PRIVMSG/NOTICE/ACTION/TOPIC require body; presence
   events accept `nil`). `to_wire/1` moved to domain; `MessagesJSON`
   becomes thin Phoenix view delegating to it; controller's
   `broadcast_message/3` uses it directly. Init migration rewritten
   in place — last edit-in-place; discipline ("never edit applied
   migrations") starts AFTER first deploy (Task 8e).
4. `5c978eb` — typed atom-keyed Meta via custom `Ecto.Type` +
   allowlist. Fixed mid-cycle when user caught my "string keys
   mandatory" rule as a CLAUDE.md "atoms or @type — never untyped
   strings" deviation. Custom `Grappa.Scrollback.Meta` type with
   `cast/load/dump` callbacks atomizes allowlisted keys
   (`@known_keys ~w[target new_nick modes args reason]a`),
   preserves atom shape across all access paths
   (`Repo.insert` return AND `Repo.all` fetch). Security boundary:
   uses lookup-against-allowlist instead of `String.to_atom` —
   unbounded atom table DoS vector closed. 15 unit tests in
   `Grappa.Scrollback.MetaTest` pin the type at the boundary.

**Stats**:
- 63 tests (was 41, +22: 7 schema tests + 15 Meta unit tests)
- 8 modified + 2 created files; ~600 lines net added
- All gates green every commit (format, credo strict, sobelow,
  deps.audit, hex.audit, doctor, dialyzer 0 errors, docs)

**Engineering rigor lessons (S9 add-ons to project-story)**:
- Plan-deviation catalogue can hide design holes that aren't even in
  the original plan. Original Task 8 plan didn't address schema
  forward-compat at all; only by reading CLAUDE.md "mechanical query
  translation" carefully against the schema scope did the gap
  surface.
- User catches > self-review. The "string keys mandatory" rule was
  a self-justified shortcut that violated CLAUDE.md plainly. Took
  one user question ("why strings for atoms??") to flip it.
- Custom Ecto.Type for typed JSON columns is the disciplined pattern,
  not `:map` + key-shape conventions in callers.
- Compile-time mtime touches by Elixir 1.19 mean RO bind-mounts of
  source dirs are incompatible with worktree dev. Fix infra, don't
  work around.

**Pending after this session**:
- Sub-tasks 8a (parser) → 8b (client + IRCServer) → 8c (session) →
  8d (bootstrap) → 8e (smoke + Pi deploy) → review → docs.
- Compact triggered at S9 end; resume at sub-task 8a fresh.
