---
status: active
---

# CP54

Previous: cp53 (scroll-to-bottom iOS fix live; e2e runnable locally on
the RPi; post-bastille roadmap). Closed mid-session — unread-badge UX
work split across two days.

## What shipped this session (DONE + DEPLOYED)

**RC1 — sidebar unread badge now clears on window select.** This was
vjt's original ask: decouple the badge from the read cursor. The
unread-badges-from-cursor cluster derived the badge purely from the
cursor and had NO mechanism to clear the focused window's badge → the
badge stayed until you scrolled/left.

- Fix: `cicchetto/src/lib/selection.ts` `perChannelUnread` memo now
  zeros the focused-AND-visible window's `{messages, events}` as a
  final overwrite, derived from `selectedChannel` + `isDocumentVisible`.
  Cursor is NOT advanced → the in-pane unread marker survives the
  select (clears only on scroll/defocus/send, per spec). Backgrounded-
  but-selected tab keeps accruing its badge.
- Spec: `docs/superpowers/specs/2026-06-02-decouple-unread-badge-design.md`
  Plan: `docs/superpowers/plans/2026-06-02-decouple-unread-badge.md`
- Commit `4766f38` on main (origin pushed). 5 unit tests added
  (`selection.test.ts`, 45 pass). **Deployed to m42 via
  `scripts/deploy-m42.sh --cic`** — hash `Cww_t3Gq`, hot, no BEAM
  restart, refresh banner broadcast.
- **Validated locally: RC1 flips 9 of the 10 red unread-cluster e2e
  specs green** (m1, m4, m5, m10, m11, r6, cp13, cursor-forward-only +
  focus-clear path). Confirmed on the RPi local e2e stack.

## Integration was RED before this session (cluster shipped broken)

The `integration` (Playwright) workflow has been red since the
unread-cursor cluster landed ~2026-06-01 — 10 specs, NOT stale tests
(they were updated by the cluster in `cf24cfa` to encode intended
behavior the impl never delivered). vitest (1740) stayed green because
it fully mocks the read cursor; only full-stack e2e exercises the real
POST→broadcast→WS-echo→DOM chain. RC1 fixed 9. One remains:

## RC2 — SHIPPED + DEPLOYED (2026-06-03)

**DONE.** m2 green, `integration` CI green (10m16s, was red since the
cluster landed). Deployed to m42 hot — cic hash `CvBDwAyg`, no BEAM
restart, refresh banner broadcast. Prod healthz 200 (sindro + sniffo);
live bundle `index-CvBDwAyg.js` confirmed.

Commits on main: `388b1b1` (biome-format pre-existing subscribe.ts
drift), `dfc60ec` (the fix + 3 unit tests + selection.ts/ScrollbackPane
doc refresh), `19114fe` (DESIGN_NOTES entry).

**Fix:** `scrollback.ts` `loadInitialScrollback` — after merging the
loaded page, baseline the cursor to the page's MAX id when
`getReadCursor === null`. Tail from the REST page (NOT store-after-merge)
so a WS PRIVMSG arriving during the load stays unread. Gated on null so
an existing read position + its in-pane marker survive a re-open. Fires
on load-completion → beats the leave-race; fires only on focus → m4
unfocused DMs stay unmarked. DESIGN_NOTES records the load-bearing
coupling: the baseline is only honest because fresh-open auto-scrolls
to the newest row — revisit if a jump-to-first-unread lands.

Validated on RPi local e2e: m2 green + full chromium+webkit 215 passed,
0 failed. vitest 1748 green (3 new tests pin null-gate / tail-from-page
/ empty-page). Adversarial code review: no substantive defects on 5
scrutinized axes (WS-race, TOCTOU, m4, catch-rollback, monotonicity).

**FINDING (worth a story episode):** `bun run check` is
`biome check src && tsc --noEmit`. A pre-existing biome FAILURE
short-circuits the `&&` so **tsc never runs** — the check gate silently
stops type-checking. The subscribe.ts format drift (from B2 cluster
`4dae97e`) had masked tsc this way; my RC2 code shipped a real TS2532
(`page[0].id` under `noUncheckedIndexedAccess`) that `check` waved
through — only the e2e's `tsc --noEmit && vite build` caught it. Lesson:
cic biome + vitest are LOCAL-ONLY gates (`ci.yml` is pure Elixir), and
a red biome hides type errors. Keep biome green or the type gate lies.
Candidate: split check into `biome check; tsc` (`;` not `&&`) so both
always run. Also: `cmd; echo EXIT=$?` masks the real exit code in
background-task notifications — check the log, not just the exit.

--- historical root-cause record below ---

**Spec:** `m2-irssi-to-chan-defocused` — visit `#bofh` (200 seeded
backlog), defocus to $server, peer sends 1 PRIVMSG → expect badge "1",
**got "201"** (200 backlog + 1).

**Root cause (confirmed via isolated m2 + grappa server logs):** the
read cursor is NEVER written for `#bofh` during m2 — zero
`read_cursors` INSERT/UPDATE, no `POST /read-cursor`. The leave-arm
(`ScrollbackPane.tsx` `on(key)` ~1190) computes
`id = visibleTailSnapshot.get(prevKey) ?? storeTail` and skips when both
are null. m2 switches away right after the own-JOIN line renders —
BEFORE the 200-msg REST backlog hydrates into `scrollbackByChannel` —
so `storeTail` is null and no snapshot exists → no cursor write → cursor
stays nil → server's nil-cursor `unread_count` = full backlog (200) +
the new msg = 201. `selectChannel`'s `awaitWsReady` only waits for the
JOIN line, not the backlog GET. (Contrast: `cursor-forward-only` waits
for rows, so its leave-arm has a tail → passes.)

**Planned fix (NOT yet implemented):** in
`cicchetto/src/lib/scrollback.ts` `loadInitialScrollback`, after merging
the loaded page, if `getReadCursor(slug, name) === null` advance the
cursor to the loaded tail (max id) via `setReadCursor` (forward-only
gate inline, as bucket D did). Rationale:
- "Opening a fresh channel marks its existing backlog as your read
  baseline" — a freshly-opened channel auto-scrolls to the bottom
  (newest), so cursor=tail is the honest "you've seen the newest."
- Gated on `cursor === null` → NEVER touches a channel that already has
  a read position, so the marker is preserved (consistent with vjt's
  "cursor-honest / leftover re-badges" answer; that scenario has a
  non-null cursor).
- Fires on load-COMPLETION → robust to the leave-race (the load was
  triggered by focus; completing after you leave still marks read).
- `loadInitialScrollback` only fires on focus, so unfocused new DMs
  (m4 "shows unread") are NOT auto-marked. Verify this holds.
- `scrollback.ts` already imports `get/setReadCursor` — no new cycle.

**Must validate after implementing:** local e2e — m2 green AND no
regression across the full chromium+webkit suite (RC1 only validated
the 10 formerly-red specs, not the other ~180). Then merge + cic deploy.

## Local e2e on the RPi (working)

- `git submodule update --init cicchetto/e2e/infra` (azzurra testnet) —
  DONE in the worktree. docker compose v5.0.2 present.
- `KEEP_STACK=1 ./scripts/integration.sh --project=chromium --grep "..."`
  for iso runs. NOTE: `--project` is variadic — use `--project=chromium`
  (= form) or it eats positional file filters as project names.
- `testnet up` kills+re-ups (rebuilds cic dist from worktree source), so
  re-running integration picks up cic edits. Stack torn down at session
  end.

## Deferred doc cleanups (do with RC2) — DONE

- `selection.ts` moduledoc — updated: now documents RC1's explicit
  focused-AND-visible suppression in `perChannelUnread` (no longer
  claims badges drop purely via cursor advance).
- `ScrollbackPane.tsx` cursor-advance comment — updated: lists the
  settle triggers (focus-leave, blur, scroll-settle, send) + the RC2
  fresh-channel load-baseline, no longer "only navigates AWAY."

## Where things are

- Worktree `feat/decouple-unread-badge` REMOVED + branch deleted (was
  merged ff to main at `19114fe`). Needed `sudo rm -rf` first — the
  local e2e left root-owned artifacts (dist + e2e node_modules/certs/
  results); `git worktree remove` had already unregistered it but
  couldn't delete the dir. The whole decouple-unread-badge cluster
  (RC1 + RC2) is CLOSED.
- main @ origin clean at `19114fe`.
- `integration` CI: GREEN. `ci`: GREEN.

## Next session

decouple-unread-badge cluster CLOSED + deployed. Back to cp53's
post-bastille roadmap (Voice TTS/STT → UI polish cluster → PUBLIC OPEN).
**Codebase review due ~2026-06-05** — likely the next gate before new
feature work. Consider splitting `cic` `check` into `biome check; tsc`
(decouple the gates) per the RC2 finding above.

## 2026-06-04 — per-server source: vjt dedicated `::42` PROD DEPLOYED

First production use of the per-server `source_address` feature
(shipped prior session). **No code changes — pure prod ops + docs.**

**What was done (prod, on m42 jail):**
- New network `azzurra-vjt` (network_id 2) → `irc.azzurra.chat:6697`,
  `source_address=2a03:4000:2:33c::42`. vjt rebound to it (nick
  `grappa`→`vjt`, `nickserv_identify`, realname `https://sindro.me`,
  autojoin = his 5 `last_joined_channels`). Visitors stay on `azzurra`
  (net 1, pool) — compile-pinned `:visitor_network`, untouched.
- vjt's scrollback migrated net 1→2 via `Repo.update_all` on
  `user_id` (17,237 msgs / 47 cursors / 1 window); visitor rows on
  net 1 untouched. Credential removed via direct row delete (NOT
  `unbind_credential` — it rolls back `:scrollback_present` on the
  last-user-with-scrollback path; latent gap, see DESIGN_NOTES).
- `::42` (host primary, rDNS `m42.openssl.it`) added to the shared-IP
  jail as `vtnet0|::42/64` (jail.conf + live `jail -m`). **Validated
  empirically** that a shared-IP jail can bind a host-owned addr and
  jail teardown won't strip it. Verified: vjt outbound from `::42`,
  visitors from pool, host `::42/64` + pool intact, backups kept.

**Decisions / lessons (full detail in DESIGN_NOTES 2026-06-04):**
- per-server source + single-server picker ⇒ operator & visitors need
  separate `networks` rows; the operator's is the one that moves.
- `unbind_credential/2` can't detach the last user from a
  visitor-only-but-scrollback'd network.
- shared-IP jail can safely share the host's primary v6.

**Incidents (both resolved, no data loss):**
- `service grappa restart` node-name race aborted boot → ~1–2 min
  outage; recovered with plain `service grappa start`. Runbook updated
  to prefer stop→verify→start.
- vjt rotated his account password mid-session → dead cic token →
  client loop tripped host fail2ban `http-404` → IP ban looked like a
  "hung BEAM." Unbanned via `fail2ban-client unban`; fix = clear cic
  `localStorage["grappa-token"]` + re-login.

**Docs updated:** OPERATIONS (`--source` verbs + per-server-source
section + prod-jail operator/rpc/source-IP/fail2ban notes),
DESIGN_NOTES (2026-06-04 entry), this checkpoint, todo, project-story.
Private memory: `project_prod_deployment.md`.

**Pending:** issue #42 filed — make `:visitor_network` env-driven (drop
the `compile_env!` pin). Optional: a "detach user, keep network" verb /
teach unbind that visitor presence counts (also in todo).
