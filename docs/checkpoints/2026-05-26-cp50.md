---
status: active
---

# CP50 — bastille deploy workstream open (2026-05-26)

## Status

Fresh rotation from cp49 (closed E2E-ROBUSTNESS + spec-audit, archived).

origin/main HEAD: `a1b8b9f` (chore(ci): empty commit to trigger CI on HEAD).
Working tree clean. Container UP healthy.

GitHub Actions: degraded service all afternoon (codeload.github.com
404 on `erlef/setup-beam` action archive). Local
`scripts/check.sh` + `scripts/integration.sh` both exit 0 on HEAD:
`8 doctests, 34 properties, 2482 tests, 0 failures`. Watch
githubstatus.com for recovery + verify CI green on the unverified
5 commits (R6 `87fe1b2`, R5 `e683645`, R1 `e5aedc4`, R3 `1acd06f`,
rename `1451d29`).

## Cluster brief — bastille deploy

GitHub issue #8. FreeBSD bastille jail target prod runtime. Current
deploy = docker-compose. Likely parallel target
(`scripts/deploy-bastille.sh` sibling to `scripts/deploy.sh`), not a
Docker → Bastille rewrite. **Verify scope from #8 before assuming.**

Pre-bastille gates all met per todo.md ★ ROADMAP block — the
substrate is green-suite + structurally-typed-boundary +
no-known-regressions.

### First actions on session resume

1. Read GH issue #8 in full — scope, prior comments, related issues.
2. Survey current deploy script shape (`scripts/deploy.sh` + the hot/
   cold preflight + `_lib.sh`).
3. Survey existing FreeBSD bastille tooling on the target host (if
   any prior work already shipped).
4. Decide: parallel target (preferred per ★ ROADMAP guess) vs.
   rewrite. Capture rationale + open a plan doc at
   `docs/plans/2026-MM-DD-bastille-deploy.md` (or pick a different
   path if a sibling doc already exists).

## Carry-forwards into bastille work

* `_build/prod` cleanup procedure — undocumented in operator
  runbook (REV-C/D carry-forward; HOT path stopped recurrences but
  did not solve). Bastille runtime may surface this differently.
* TLS `verify: :verify_none` → CA chain verification (Phase 5
  hardening). Bastille might be the right window to flip this if
  the new runtime needs a fresh TLS-trust-store config strategy
  anyway.

(All other open carry-forwards remain in `docs/todo.md` —
"Carry-forwards from REV cluster (open)" section. Bastille work
should NOT scope-creep into those unless they block.)

## Open observations

* GH Actions infra fragility — both this session's
  `codeload.github.com` 404 and previous CI flakes (cancelled runs,
  webhook drops) suggest the integration suite's CI execution path
  has multiple non-code failure modes. If bastille work is
  CI-heavy, factor in the GH-side variability.
* Spec audit deferred R2 (mobile CSS-shape consolidation 5→3) +
  R4 (bug7 fold + webkit-iphone-15 CI matrix extension). Neither
  blocks bastille. Park.
