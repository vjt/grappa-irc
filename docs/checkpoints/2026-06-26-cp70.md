# CP70 — Channel directory `/list` (#84): COMPLETE, green, merge+deploy gated

**Date:** 2026-06-26
**Branch:** `channel-directory` (worktree `/srv/grappa`) — all phases done,
NOT merged, NOT pushed, NOT deployed. Merge+deploy intentionally gated on vjt
(see "Why deploy is gated").
**Plan:** `docs/plans/2026-06-26-channel-directory-plan.md` (executed via
subagent-driven-development — fresh implementer + spec-review + quality-review
per task; tasks #12–#19 in the harness TaskList all `completed`).
**Supersedes:** cp69 (server-complete + cic D1). This is the feature-complete
checkpoint.

## Status: FEATURE COMPLETE, ALL GATES GREEN

Full server (Phases A–C) + full cic (Phases D–E) implemented, reviewed,
and verified end-to-end against the real bahamut testnet.

- **`scripts/check.sh` (Elixir):** GREEN — `CHECK_EXIT=0` (2949 tests,
  dialyzer 0, credo --strict clean, sobelow, doctor, format,
  `gen_wire_types --check` in sync, deploy-script bats 0–43).
- **cic `scripts/bun.sh run build`:** clean (full tsc + vite).
- **cic `scripts/bun.sh run test`:** 2139 passed. (One transient
  `Keyboard.test.tsx` emoji-layer 5s TIMEOUT during the combined run was
  resource-contention flake — Elixir check.sh compiling concurrently on the
  Pi pushed the vitest `environment` phase to 456s vs 22s solo. Iso-rerun
  `scripts/bun.sh run test Keyboard` = 60/60 green. NOT a regression, NOT in
  any touched file.)
- **e2e `scripts/integration.sh tests/channel-directory.spec.ts`:**
  `1 passed (11.8s)`, first run, no retries — real bahamut: peer creates a
  channel → grappa LISTs it → cic 📇 `$list` opens with NO `/messages` fetch
  (#81 guard) → search filters → one-click join lands it in the sidebar.

## Commits on branch (cp69 HEAD `72c8800` → now), oldest→newest
Server (pre-cp69): A1 `44621a3` · A2 `7e30efe` · A3 `f0b367a` · C1 `df4834f`
· C2+C4 `5e38fc3` · dialyzer `a386441` · C3 `e0cac85` · credo `998f11c` ·
wireTypes regen `4abcf3b` · B1 `8a91c02` · DESIGN_NOTES `086db90` ·
D1 `9f6fe6a` · named-Wire-entry `93e63cc` · cp69 `72c8800`.
cic (this session, 7 commits):
- `541dc6d` D3 channelDirectory store
- `88a1e9f` D2+D4 narrow + dispatch directory pings (merged — union growth
  forces both switches via `assertNever`)
- `f43c9a9` E1 `$list` window-name constant
- `284b14e` E2 📇 channels row under each network
- `79c3aac` E3 DirectoryPane + Shell pane router (desktop + mobile Switch)
- `d3202ba` E4 `/list` executor + HomePane "Browse channels" link
- `8ada162` E5 e2e channel directory browse + join
(+ docs: this cp + todo follow-up, committed after.)

## Review outcomes (all tasks APPROVED)
Every cic task got spec-compliance + code-quality review (combined for the
trivial ones). No [Critical]/[Important] findings survived. Notable:
- D2+D4: dispatch tests proven to catch a kind→hook swap; tsc `assertNever`
  proves dispatch exhaustiveness.
- E3: all 8 spec items met; `list` Match in BOTH Shell Switch blocks
  (desktop ~629 + mobile ~783 — missing the mobile one = blank pane on phones).
- E5: all 4 e2e assertions verified REAL (would fail if the feature broke),
  not vacuous; idempotent cleanup (afterEach PART + peer disconnect + unique
  per-run channel).

## One follow-up recorded (NOT a blocker)
todo.md Low/Observation: **channel directory search abort-and-replace** — D3
store `setQuery`/`fetchInto` is last-arrival-wins, so rapid typing + out-of-
order responses could show stale results. Low risk (cached server snapshot →
stable ordering). Fix = `AbortController` keyed by slug in `fetchInto`. v1
ships without it deliberately.

## Why deploy is GATED on vjt (do NOT auto-merge+deploy)
1. **COLD deploy.** `config/config.exs` gained the `Grappa.ChannelDirectory`
   block + there's a new migration (`20260626120000_create_channel_directory`).
   Per `feedback_minimize_cold_deploys`: ANY `config/*.exs` change forces a
   COLD deploy, which DROPS ALL LIVE IRC SESSIONS on prod (m42 bastille jail).
   That's outward-facing + operator-timing-sensitive — vjt picks the window.
2. **Red gate on feature work.** todo.md Immediate: "further FEATURE work
   formally sits on a red gate" pending the deferred codebase review
   (~30d/~36 sessions overdue). The channel directory is a feature. vjt
   controls when feature work ships past that gate.

## Closeout sequence WHEN vjt approves (the dev cycle, absolute paths)
1. `cd /srv/grappa` (this worktree). `git checkout main` is NOT needed — merge
   from here.
2. Rebase branch onto local main FIRST: `git rebase main` (from worktree).
   (Already verified clean against main at cp70 time — re-verify, other
   sessions may have moved main.)
3. `git checkout main && git merge channel-directory` (local main).
4. `git push origin main` (primes the m42 pull-deploy).
5. `/srv/grappa/scripts/deploy-m42.sh` — auto hot/cold; this WILL go COLD
   (config + migration). Invoke by ABSOLUTE path. Redirect output to a file
   (no truncating pipes — SIGPIPE kills remote deploy mid-flight, per
   `feedback_no_pipes_on_deploy_output`). The migration runs in-deploy
   (expand-only `create table` — hot-migratable in principle, but the config
   change forces cold regardless).
6. Health check: hit `/healthz` through nginx; confirm sessions respawn +
   directory endpoint live (`GET /networks/<slug>/directory`).

## Architecture (as built — matches DESIGN_NOTES 13240)
- Server: `channel_directory` table (subject-XOR FK), `Grappa.ChannelDirectory`
  context (`replace_start`→`ingest`→`finalize` + paginated `list/3` + `ttl_ms/0`
  via `compile_env`), `.Wire` (named `entry` type → flat
  `ChannelDirectoryWireEntry[]` to dodge the codegen↔biome nested-object
  conflict), `DirectoryController` (GET index + POST refresh), `Session.Server`
  in-flight `directory_refresh` tracker intercepting 321/322/323 ABOVE the
  generic numeric handler, watchdog timeout → `directory_failed`.
- cic: `api.ts` `listDirectory`/`refreshDirectory`; `channelDirectory.ts`
  identity-scoped store (re-GETs on each ping); `WireUserEvent` +
  `narrowUserEvent` + userTopic dispatch for the 3 pings; `$list` WindowKind
  (no scrollback); Sidebar 📇 row; `DirectoryPane` (search/refresh/sort/total/
  last-refreshed-stale-CTA/rows/one-click-join/scroll-preserve) wired into both
  Shell Switch blocks; `/list` compose executor + HomePane browse link.
