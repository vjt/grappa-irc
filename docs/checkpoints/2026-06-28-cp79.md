# CP79 — #121 rfc1459 nick casemapping SHIPPED

**Date:** 2026-06-28
**Repo state:** `main` = `origin/main` = `bbec36c`, pushed. Tree clean.
**Supersedes:** cp78 as the active-state pointer.

## Done this session (orchestrator autopilot, feature pack CYCLE 1)

### GH #121 (P0) — visitor/session nick matching was case-sensitive

Bug: a visitor reconnecting with a different-case nick (`Mezmerize` →
`mezmerize`) was NOT recognised as the same identity — the case-SENSITIVE
`Repo.get_by(Visitor, nick: ...)` missed, provisioned a SECOND
visitor/session, and the orphan kept holding the nick (later `/nick
Mezmerize` → 433). Requested on channel.

Fix unifies EVERY server-side nick compare under ONE rfc1459 casemapper
(azzurra = bahamut: folds `A-Z` **plus** `[ ] \ ~` → `{ } | ^`).

**6 commits on `vjt-claude/nick-casefold-rfc1459` (merged FF → main):**
- `43bb7b6` `Identifier.canonical_nick/1` — ASCII-only byte fold (NOT
  Unicode downcase; matches bahamut's byte casemapping AND the SQL
  `lower()` backfill). Full property tests.
- `4becfe7` visitors: UNIQUE **expression index** on the fold (NOT a
  denormalised column — vjt rejected the column mid-build; derive,
  don't duplicate). All 4 lookups fold via `Identifier.nick_fold/1`.
- `80fb84c` query_windows: upgraded its `lower(target_nick)` expression
  index → rfc1459 (conflict_target + close + fetch_existing).
- `640ace4` in-memory sweep: userhost/whois/whowas caches
  (EventRouter.normalize_nick ↔ Session.Server key sites ↔ PartCleanup),
  numeric_router.nick_eq?, scrollback dm_peer/self-DM/delete_for_dm,
  ghost_recovery/chanserv, event_router self-detection (new nil-safe
  `nick_eq?/2`; self-nick MODE clause de-guarded + merged into the
  channel-MODE clause).
- `c838553` review fixes: folded `maybe_dispatch_push` self-echo guard
  (the one nick compare the sweep missed — latent if echo-message ever
  enabled); narrowed nick_eq? spec (dialyzer); renamed migration
  `FoldVisitorsNickUniqueIndex`.
- `bbec36c` docs: DESIGN_NOTES entry + CLAUDE.md "Nicks are case-folded
  under rfc1459" invariant.

**Scope boundary (deliberate, documented):** the in-memory members-map
keys + `state.nick`-as-identity stay raw-cased — server-consistent
identity, not a fold-MATCH site. Folding them is a separate members-map
restructure → FOLLOW-UP candidate.

**SQL-fold identity invariant:** the rfc1459 fold appears in 4 forms
(2 migrations, `nick_fold/1` macro, `@nick_fold_sql` conflict target) that
MUST stay character-identical or SQLite drops the index. Code review
verified all 4 byte-identical.

**#117 reattach:** folded `lookup_visitor` → same `visitor.id` → existing
attach-to-existing-session path reattaches (no new code).

## Gate / deploy
- Full `check.sh` GREEN: 3005 tests / 39 properties / 8 doctests, 0
  failures; dialyzer 0 errors; format/credo/sobelow/doctor/docs/wire/bats.
- Code review (general-purpose subagent): core correct & well-tested;
  one real finding (maybe_dispatch_push) fixed; nits addressed/noted.
- **COLD-deployed to m42** (2 migrations force cold): both migrations ran,
  `/healthz` green (0 retries), `DEPLOY_EXIT=0`. Fix is live.

## NOT yet done
- **#it-opers announce** — ORCHESTRATOR fires it (not the sibling).
- **Live smoke-test** — the different-case-reconnect behavior wants a real
  IRC reconnect; await vjt dogfood confirmation.
- Codebase-review gate STILL DUE (carried cp73→cp79; this was a P0 bugfix,
  exempt — but the gate persists for the next feature).

## Next (orchestrator CHAIN)
Feature pack CYCLE 1 → #105 (unbind cascade-drop) next.
