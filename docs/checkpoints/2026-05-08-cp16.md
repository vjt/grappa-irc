---
status: active
---

# CP16 — wire-discipline-sweep

Cluster opened 2026-05-08, immediately after `codebase-review-fixes`
LANDED + DEPLOYED at `0a2e52a`. Implements Theme 1 of the 2026-05-08
architecture review: "CP15 B7 wire-module rule shipped as
documentation, not enforcement."

Plan: [`docs/plans/2026-05-08-wire-discipline-sweep.md`](../plans/2026-05-08-wire-discipline-sweep.md).

## Buckets

- **B1** — `Grappa.Session.Wire` extraction (9 event payloads, 1
  Wire fn per `kind:`, snapshot path collapses to same fn).
- **B2** — `Grappa.Visitors.Wire` extraction (closes
  `password_encrypted` leak risk + LoginResponse/MeResponse drift).
- **B3** — `Networks.broadcast_state_change` inline payload → Wire fn.
- **B4** — Stale typespec fixes (3 sites) + `kind:` atom-vs-string
  consistency (`Wire.message_payload/1` switches to string).
- **B5** — cic `WireUserEvent` discriminated union + `QueryWindowEntry`
  typed export; `userTopic.ts` handler exhaustive via `assertNever`.
- **B6** — Cluster close: full gates + deploy + browser smoke.

## Gate evidence (per bucket)

To be filled in as buckets close. Per `feedback_landed_claim_evidence`,
literal tail paste mandatory at LANDED claim.

