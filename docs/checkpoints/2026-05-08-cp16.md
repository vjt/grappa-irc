---
status: complete
---

# CP16 — wire-discipline-sweep

Cluster opened 2026-05-08, immediately after `codebase-review-fixes`
LANDED + DEPLOYED at `0a2e52a`. Implemented Theme 1 of the
2026-05-08 architecture review: "CP15 B7 wire-module rule shipped
as documentation, not enforcement."

Plan: [`docs/plans/2026-05-08-wire-discipline-sweep.md`](../plans/2026-05-08-wire-discipline-sweep.md).

## Buckets

- **B1** LANDED — `Grappa.Session.Wire` extraction (commit `427b427`).
  9 event payload verbs + snapshot-path collapse to one-liners.
- **B2** LANDED — `Grappa.Visitors.Wire` extraction (commit `b7e9353`).
  `visitor_to_json/1` + `visitor_to_credential_json/1`; MeJSON +
  AuthJSON delegate; `:password_encrypted` leak risk closed.
- **B3** LANDED — `Networks.broadcast_state_change` inline payload
  → Wire (commit `fe2e781`). Consistency follow-through to
  codebase-review-fixes H1.
- **B4** LANDED — three stale typespecs fixed + `kind:` atom-vs-string
  alignment (commit `1a3bdcc`). Server-side discriminator type
  consistent across every Wire fn.
- **B5** LANDED — cic `WireUserEvent` discriminated union +
  `QueryWindowEntry` + `MentionsBundleMessage` typed exports +
  `assertNever` exhaustiveness (commit `e4ecd19`).
- **B6** LANDED — cluster close + DESIGN_NOTES + CP16 close + deploy.

## Gate evidence (per `feedback_landed_claim_evidence`)

### B1 close
```
scripts/test.sh test/grappa/session/wire_test.exs
  13 tests, 0 failures
scripts/test.sh test/grappa/session test/grappa_web/channels
  1 property, 216 tests, 0 failures
scripts/format.sh — clean
scripts/credo.sh — 1359 mods/funs, no issues
scripts/dialyzer.sh — Total errors: 0
```

### B2 close
```
scripts/test.sh test/grappa/visitors test/grappa_web/controllers/me_json_test.exs
  test/grappa_web/controllers/auth_json_test.exs
  test/grappa_web/controllers/me_controller_test.exs
  test/grappa_web/controllers/auth_controller_test.exs
  68 tests, 0 failures
scripts/credo.sh — 1364 mods/funs, no issues
scripts/dialyzer.sh — Total errors: 0
```

### B3 close
```
scripts/test.sh test/grappa/networks
  36 tests, 0 failures
scripts/credo.sh — 1365 mods/funs, no issues
scripts/dialyzer.sh — Total errors: 0
```

### B4 close
```
scripts/test.sh
  7 doctests, 26 properties, 1280 tests, 0 failures
scripts/credo.sh — 1365 mods/funs, no issues
scripts/dialyzer.sh — Total errors: 0
```

### B5 close
```
scripts/bun.sh run check — biome + tsc clean (82 files, 0 errors)
scripts/bun.sh run test — 632 passed (632), 38 test files
```

### B6 cluster close
```
scripts/check.sh — EXIT 0
  7 doctests, 26 properties, 1280 tests, 0 failures
  Total errors: 0
  No vulnerabilities found.
  No retired packages found
  Sobelow SCAN COMPLETE
  ExDoc generated (no new doc warnings)
scripts/dialyzer.sh standalone — Total errors: 0
scripts/bun.sh run check — clean
scripts/bun.sh run test — 632 passed (632)
scripts/integration.sh — 30 passed (41.9s)
```

## Architecture-review findings closed

  * **A1 (HIGH abstraction-boundaries)** — Session.Server's 9 inline
    payloads now go through Wire.
  * **A2 (HIGH responsibility)** — Three event-broadcast paths
    (Session, Networks, GrappaChannel after-join) now delegate.
  * **A3 (HIGH abstraction-boundaries)** — Visitors.Wire exists,
    `:password_encrypted` allowlist enforced.
  * **A4 (HIGH abstraction-boundaries)** — three stale typespecs
    caught up to `Wire.windows_map()`.
  * **A7 (MEDIUM abstraction-boundaries)** — cic WireUserEvent
    discriminated union; `as` casts gone.
  * **A8 (MEDIUM abstraction-boundaries)** — mentions_bundle
    per-message shape divergence documented in Wire moduledoc.
  * **Type-system A1 (HIGH)** — kind: as string consistent everywhere.
  * **Type-system A2 (HIGH)** — `wire_event_kind` enum pinned;
    discriminated union mirrored cic-side.
  * **Duplication A1+A5+A6 (HIGH/MEDIUM)** — inline-mirror gone.
  * **Extensibility A2 (HIGH)** — byte-identicality enforced by
    function (snapshot calls same Wire fn as event-time arm), not
    by code-review prose.

## Pending follow-ups (NOT done in this cluster)

  * Theme 2 — `server-side-pending` cluster (~quarter session): move
    `:pending` window-state origination to the server; drop cic's
    `compose.ts:210 setPending(...)` workaround.
  * Theme 3 — `Session.Server.WindowState` extraction (~half session):
    pull `window_states` + `window_failure_*` + `window_kicked_meta`
    triplet maps into a sub-context. Adjacent: `AwayState`,
    `ModeChunker`.
  * Arch A2 — `network_id`-by-`slug` cic refactor (14 sites). Bigger
    surface change; deferred.
  * Arch A6 — `channels_changed` typed-delta refactor. Bigger
    conceptual change with cic store implications; deferred.
  * T32 parked-spec design pass. DESIGN-BLOCKED, requires brainstorm.
