---
status: complete
---

# CP17 — server-side-pending

Cluster opened 2026-05-08, immediately after CP16 wire-discipline-sweep
LANDED + DEPLOYED at `a096cf0`. Implements Theme 2 of the 2026-05-08
architecture review: move `:pending` window-state origination from cic
(`compose.ts:210 setPending(...)` workaround) to the server, closing
the CLAUDE.md hard-invariant violation "cic NEVER originates state".

Plan: [`docs/plans/2026-05-08-server-side-pending.md`](../plans/2026-05-08-server-side-pending.md).

## Buckets

- **B0** LANDED — worktree + plan + CP (commit `920a805`).
- **B1** LANDED — server-side: `Grappa.Session.Wire.window_pending/2`
  + `record_in_flight_join/2` writes `:pending` + broadcasts on
  user-topic (commit `7858452`). Idempotency fix (re-JOIN of an
  already-`:joined` channel does NOT downgrade state) shipped in
  follow-up commit `8631418` after integration suite m11-peer-nick
  failure surfaced the race.
- **B2** LANDED — cic: drop `compose.ts:210` workaround + dispatch
  typed `window_pending` event in `userTopic.ts` + `WireUserEvent`
  union arm (commit `a37b199`).
- **B3** LANDED — cluster close + DESIGN_NOTES + deploy + browser smoke.

## Design summary

* Broadcast on `Topic.user(...)`, NOT per-channel topic — chicken-and-
  egg: cic only subscribes to per-channel after seeing `:pending`.
  User-topic is joined from boot (`userTopic.ts` createRoot effect).
* New verb: `Grappa.Session.Wire.window_pending(network_slug, channel)`
  → `%{kind: "window_pending", network: ..., channel: ..., state: "pending"}`.
* Snapshot push for `:pending` is INTENTIONALLY a no-op (per-channel
  after_join can't deliver new info; the user-topic broadcast already
  reached cic).
* Idempotency rule: re-JOIN of an already-`:joined` channel does NOT
  downgrade state to `:pending` and does NOT broadcast. The in-flight
  entry IS still recorded for failure-numeric correlation.
* cic-side: `setPending` export STAYS but is now fed by the user-
  topic dispatcher. The pre-subscribe loop in `subscribe.ts:425`
  re-runs on the signal change regardless of who calls setPending.

## Architecture-review findings closed

* **Theme 2 (load-bearing)** — server-side-pending bucket. Move
  `:pending` window-state origination from cic to the server. Drops
  the `compose.ts:210 setPending(...)` workaround for the
  Phoenix-PubSub late-subscriber race. Closes the CLAUDE.md hard
  invariant "cic NEVER originates state — no parallel client-side
  state machine."

## Gate evidence (per `feedback_landed_claim_evidence`)

### B1 close (incl. idempotency fix)

```
scripts/test.sh test/grappa/session/wire_test.exs
  14 tests, 0 failures
scripts/test.sh test/grappa/session/server_test.exs --only "describe:CP15 B2 — in_flight_joins map"
  11 tests, 0 failures (146 excluded)
scripts/credo.sh — 1366 mods/funs, no issues
scripts/dialyzer.sh — Total errors: 0
```

### B2 close

```
scripts/bun.sh run check — biome + tsc clean (82 files, 0 errors)
scripts/bun.sh run test — 634 passed (634), 38 test files
```

### B3 cluster close

```
scripts/check.sh — EXIT 0
  7 doctests, 26 properties, 1285 tests, 0 failures
  Total errors: 0
  No vulnerabilities found.
  No retired packages found
  Sobelow SCAN COMPLETE
  ExDoc generated (no new doc warnings)
scripts/dialyzer.sh standalone — Total errors: 0
scripts/bun.sh run check — clean
scripts/bun.sh run test — 634 passed (634)
scripts/integration.sh — EXIT 0
  28 passed; 2 pre-existing flakes (cp13 S10, m9) pass on retry
```

## Pending follow-ups (NOT done in this cluster)

* Theme 3 — `Session.Server.WindowState` extraction (~half session):
  pull `window_states` + `window_failure_*` + `window_kicked_meta`
  triplet maps into a sub-context. Adjacent: `AwayState`. Mechanical
  now that Wire modules + `:pending` are pervasive on the server.
* Arch A6 — `channels_changed` typed-delta refactor. EXPLICITLY out of
  scope per cluster brief (tight scope, no bundling). Bigger
  conceptual change with cic store implications; deferred.
* T32 parked-spec design pass. DESIGN-BLOCKED, separate cluster.

## Sessions

### S1 (2026-05-08, single session)

Full cluster start-to-finish in one orchestrated session. Brief
arrived as `/tmp/orchestrate-next.txt`; vjt blessed Theme 2 with
autonomy delegation. Key incident: B1 first ship broke
m11-peer-nick e2e via the idempotent re-JOIN race; fix shipped
in B1 follow-up commit + new test asserting the contract.
