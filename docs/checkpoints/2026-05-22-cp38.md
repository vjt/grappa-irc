# CP38 — UX-6 cluster CLOSED + UX-5 backfill (2026-05-22)

## Status

**UX-6 cluster CLOSED** (11 buckets A→L + Z) + **UX-5 backfill** into
README's `Closed clusters` section (per-bucket-update miss recovered
under safety-net Z sweep, per `feedback_readme_currency`). CP37
rotated to CP38 — the previous checkpoint had been frozen at U-3
LANDED while EIGHT clusters shipped under it (U-4..U-Z, UX,
UX-1..3, iOS, UX-4, UX-5, UX-6) under the autopilot dogfooding
mandate without rotation discipline. CP38 opens the post-cluster-
close state.

Branch: `main`. Pushed to origin.

## What shipped this session (UX-6-Z, autopilot)

Docs-only sweep, no Elixir / no cic-bundle changes, no deploy:

- **README `Closed clusters`** — two new entries:
  - **UX-6** (closed 2026-05-22) — 11 buckets A→L + Z, autopilot
    wave from iPhone PWA bug-list; full per-bucket inventory with
    commit hashes; meta-lessons inline (CSS-only iOS rubber-band
    broken; Telegram pattern atomic-only; B5 deep-link carry-debt;
    server-side predicate divergence; APNs SW-suppress tradeoff).
  - **UX-5** (closed 2026-05-20) — 15 buckets A→BD, mobile-polish
    wave preceding UX-6. Per-bucket inventory with commit hashes.
- **DESIGN_NOTES.md** — `## 2026-05-22 — UX-6 cluster CLOSED +
  UX-5 backfill (UX-6-Z docs sweep)` entry: bucket-inventory
  table, 5 meta-lessons, carry-forwards (UX-6-M + UX-6-I.2 +
  baseline e2e fails), 2 accepted residuals.
- **docs/todo.md** — UX-6-Z flipped to LANDED; UX-6 cluster
  marked CLOSED in line with the bucket list.
- **CP37 → CP38 rotation** — this file.

## Cluster carry-forwards

- **UX-6-M** (channel scroll position interference on switch) —
  BLOCKED on vjt repro pattern. Likely related to ScrollbackPane
  being reused via Solid `<Show>` non-keyed across selectedChannel
  changes — `listRef.scrollTop` survives switch (intentional per
  UX-4-K's `scrollToActivation`), per-channel scroll position
  isn't persisted/restored.
- **UX-6-I.2** (real-bundle-swap e2e fixture) — current e2e stubs
  `getRegistration` + `caches` and uses the `__refreshProbe`
  seam; proves WIRING but not REAL SW + REAL precache behavior.
  Meaningful e2e would mid-session `compose run cicchetto-build`
  + `POST /admin/cic-bundle-changed` + assert single-press
  convergence. MEDIUM effort; risk: docker-compose oneshot timing
  in e2e is fiddly.

## Pre-existing baseline e2e fails (NOT regressions, surfaced via UX-6-E smoke)

- `cicchetto/e2e/tests/ux-4-z-cluster-journey.spec.ts:141` —
  members-pane from `aside.shell-members.open` subtree intercepts
  pointer events when test taps `.shell-drawer-backdrop.open`;
  drawer doesn't actually close on backdrop tap on
  webkit-iphone-15.
- `cicchetto/e2e/tests/ux-z-cluster-journey.spec.ts:86` — archive
  modal `#bofh` row never renders (`toHaveCount(1)` got 0 after
  5s).

Both reproduce on `e53000c` baseline. Park for a dedicated
investigation cluster — flagged in todo.md.

## Two accepted residuals (do NOT chase)

1. Visible iOS keyboard slide-in animation (~250ms) — WKWebView
   compositor below JS, unfixable in pure PWA. Capacitor escape
   if priority rises.
2. UX-6-M as parked above.

## Gate evidence (UX-6-Z = docs-only)

Pure-docs cluster close — `scripts/check.sh` not in the critical
path for prose-only changes, BUT per `feedback_landed_claim_evidence`
the LANDED claim still requires evidence. Run captured pre-commit.

## Anti-patterns recorded cluster-wide

- DO NOT ship without reviewer-loop on cluster buckets — UX-6-J
  M1/M2/L1/L2 all caught by reviewer pass post-impl-pre-commit
  (M1 was silent `createEffect`-outside-`createRoot` Solid
  footgun that e2e tolerated).
- DO NOT trust that "e2e passed" means "the right code path ran"
  — discriminating probes (window flag, etc.) mandatory for
  boot-time effects (UX-6-J `__cicPushTargetApplied`).
- DO NOT silent-swallow rejections in recovery actions (UX-6-J
  L1 same lesson as UX-6-I H2: every best-effort chain needs a
  `console.warn` on parse-fail).
- DO NOT treat e2e stub-everything tests as proof of behavior
  (UX-6-I.2 parked for this reason).

## What's next

UX-6-I.2 LANDED 2026-05-22 — real-bundle-swap e2e fixture closing
the UX-6-I M2 follow-up. See docs/DESIGN_NOTES.md UX-6-I.2 entry.

UX-6-M still blocked on vjt repro pattern — wait for vjt to
surface the precise reproduction.

The two baseline e2e fails (ux-4-z:141 + ux-z:86) remain pending
a dedicated investigation cluster — surfaced again post-UX-6-I.2
baseline-parity run.

`/tmp/orchestrate-next.txt` staged at session close for the
post-clear pickup.
