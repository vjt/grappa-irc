---
status: done
---

# CP09

CP08 closed at S33 / 13 sessions / ~340 lines. CP08 covered Phase 2
close + S29 codebase review + 2j cluster review + the architecture
review (S31) + ALL THREE pre-Phase-3 cleanup clusters:

* **Cluster 1 (S31, 8 commits):** wire-shape contract — closed
  CRITICAL A1 (Credential password JSON-leak via `Jason.encode!`)
  + A4+A5+A7+A8+A18+A23+A26. New surface: `Networks.Wire`,
  `Accounts.Wire`. Single source for nick + slug rules. REST
  error-string convention pinned to snake_case. `lib/grappa_web/`
  Repo-free.
* **Cluster 2 (S32, 3 commits):** Networks↔Session cycle inversion —
  A2+A6+A10+A22. `Session.Server.init/1` ~25 LOC pure data
  consumer; deps shrink 7→4. `Networks.session_plan/1` is the
  canonical resolved-plan producer. Reverse `Networks→Session`
  edge legalised. `pick_server!` + `NoServerError` move to
  Networks. `Scrollback.has_messages_for_network?/1` replaces raw
  `messages` table bypass. Networks↔Scrollback structural cycle
  broken via Boundary `dirty_xrefs` (real fix queued Phase 5+).
* **Cluster 3 (S33, 2 commits):** operator surface — A11+M12+A20+A30.
  `Mix.Tasks.Grappa.Boot.start_app_silent/0` + `Output.halt_changeset/2`
  helpers extracted; 6 mix tasks de-duplicate boot ceremony +
  changeset-error halt. New no-suffix `mix grappa` help-index task
  reads `Mix.Task.all_modules/0` for self-syncing operator
  discovery.

CP07 closed at S30 / 10 sessions covering Phase 2 (sub-tasks
2a-pre through 2k). CP06 closed at S20 (Phase 1 Tasks 9+10 + Phase
2 design pass). CP05 closed at S17 (Phase 1.5). CP04 closed at S13
(walking-skeleton complete + live on Pi). See prior checkpoints for
back-story.

CP09 opens with **Phase 3 unblocked** — bouncer live in prod
(192.168.53.11:4000), connected to Azzurra as `grappa`, joined
`#grappa`. Architecture review's 12 pre-Phase-3 findings all
landed. Wire shape stable, boundaries clean, operator surface
de-duplicated. **Phase 3 = client walking skeleton (cicchetto PWA
repo bootstrap)** starts here.

---

## State at checkpoint creation

Frozen at main commit `32569fe`
(`operator(A30): mix grappa — help-index task lists every grappa.*
subtask`). Working tree clean, origin/main in sync. No worktrees
beyond the main repo (cleanup1/cleanup2/cleanup3 all merged + their
worktrees removed).

**Codebase shape (post-Cluster-3):**
- ~7,000 LOC of Elixir under `lib/` (estimate, post-Cluster-3)
- 6 properties + 411 tests + 0 failures
- ci.check ~25s on Pi (worktree, PLT hot)
- 14 top-level Boundary annotations enforced + 3 internal-only
  (`Mix.Tasks.Grappa.{Boot,Output,OptionParsing}`)
- Architecture review action plan: 12/12 pre-Phase-3 findings
  closed; 18 findings deferred to Phase 5+

**Tooling baseline:** unchanged from CP08. Dialyxir
(`:underspecs` + `:unmatched_returns`), Credo (`--strict`),
Sobelow, mix_audit, doctor (99.2% module health — pre-existing
Grappa.Application hidden-doc warning, not from this work),
Boundary (enforced + 1 documented `dirty_xrefs` exception),
ExUnit, StreamData, Mox, Bypass, ExMachina, excoveralls. All
green.

**Live in prod:** bouncer at http://192.168.53.11:4000, connected
to Azzurra as `grappa`, joined `#grappa`. End-to-end PRIVMSG →
Scrollback → REST + PubSub round-trip working (no PWA client
connected yet — that's Phase 3's job). `mix grappa` help-index
listing 7 operator subtasks alphabetically (bare task itself
correctly excluded by the `String.starts_with?(name, "grappa.")`
filter).

---

## Pending at checkpoint open

### Phase 3 — cicchetto PWA bootstrap (in-monorepo)

CP09 S2.5 (2026-04-26) corrected a misreading from CP08 + the
opening of CP09: cicchetto is **NOT** a separate repository.
README line 1 of "What" reads "Two components, one monorepo." The
"two facades, one store" DESIGN_NOTES decision is about REST+WS
vs IRCv3-listener facades over a single scrollback DB — orthogonal
to the repo-organization question, which the README answered as
monorepo from the start.

Phase 3 lives at `cicchetto/` as a sibling of `lib/` + `test/` in
this repo.

Per Roadmap:
- [ ] PWA shell, manifest, service worker (install-eligible day 1)
- [ ] Login flow → token → connect `/socket/websocket`
- [ ] Channel list + scrollback fetch on select
- [ ] Send message

**Client-stack decision (LANDED 2026-04-26 in conversation, to be
written into DESIGN_NOTES at Phase 3 start):**

| Slot | Pick | Why |
|------|------|-----|
| Framework | **SolidJS** | Fine-grained reactivity → high-frequency WS updates (every PRIVMSG = state update) only re-render the affected DOM nodes, no virtual-DOM diff, no component re-runs. ~7kb runtime. |
| Language | **TypeScript** | Wire-shape contract on JS side — mirrors `@spec` + Dialyzer discipline server-side. SolidJS is TS-native. |
| Build / dev | **Vite** | Universal default; Solid plugin first-class. |
| Tooling runtime | **Bun** | Single binary (install + dev + TS + JSX + tests + bundler), ~10× faster than pnpm/npm. Build-only — nginx serves static `dist/` in prod, so runtime risk surface = zero. |
| Router | **`@solidjs/router`** | First-party, file-based routes, lazy loading. |
| Linter / formatter | **Biome** | Single fast tool replacing ESLint + Prettier. |
| Tests | **`@solidjs/testing-library` + Bun's built-in test runner** | Component + integration; deferred until first non-trivial component. |
| Theming | **CSS variables + theme files in `cicchetto/src/themes/`** | Day-1 barebones (system mono, fixed line height, no chrome). Future: irssi / mIRC / xchat theme packs. Theming infrastructure is designed-in from the start; only one theme exists at walking-skeleton landing. |

**Auth model:** `POST /auth/login` → `{token}`, store in
localStorage, attach as WS `connect` query param. Phase 2 already
has `Grappa.Accounts.Session` (DB-backed token sessions) — Phase 3
is wiring the existing surface, no server-side auth changes.

**Deploy shape (single hostname, no CORS):** new DNS record
`grappa.bad.ass A 192.168.53.11` via Technitium API
(token at `/srv/dns/.env`). nginx terminates HTTPS, serves
`cicchetto/dist/` static, proxies `/api` + `/socket` to the
grappa container. Two new compose services:

- `cicchetto-build` — slim Bun image, one-shot, runs `bun run build`
  into a named volume on container start, exits.
- `nginx` — mounts the named volume read-only; proxies `/api` +
  `/socket` to grappa; serves SPA fallback (`index.html`) for
  client-side routes.

**Cross-stack coupling:** wire-shape contracts grappa ships are
the cicchetto contract. Phase 3 will surface gaps in grappa's
REST surface (notably **missing `GET /networks` + `GET /networks/:nid/channels`
listing endpoints** — the channel-list view needs them). Land
those on `main` directly when small; bigger ones in a
`phase3-server-gaps` worktree.

### Codebase review gate — CP09 S1 RUN

**Codebase review ran 2026-04-26 (CP09 S1)** — `docs/reviews/codebase/2026-04-26-codebase-review-cluster3-post.md`.

5 parallel scope agents (irc/, persistence/, lifecycle/, web/,
cross-module + infra). Results post-dedupe:

| Severity | Count | Notes |
|----------|------:|-------|
| CRITICAL | 0 | (S29's CRITICAL closed by Cluster 1 A1) |
| HIGH | 1 | F1 — CAP LS continuation phase guard (S29 M1 carryover, claimed fixed but never landed) |
| MEDIUM | 4 | F2 leading-`-` nick regex, F3 Bootstrap `:already_started` misclassification, F4 spec lies (×2) |
| LOW | ~28 | Mix of S29 carryovers + new line-level drifts |

Net trend vs S29 baseline: 51 → ~33 findings, no new CRITICAL,
all 4 MEDIUMs are existing carryovers (the 3 cleanup clusters
demonstrably narrowed the surface; residual findings are the ones
that escaped scope). Two findings were "claimed fixed in CP07 but
never actually landed" — the kind of erosion CLAUDE.md "Total
consistency or nothing" warns against.

**Direction recommendation (from review trajectory section):**
Land F1 as a one-commit follow-up (5 min, 2-line fix) before
starting Phase 3. Optionally bundle F2 + F3 + F4 into a "S29
carryover sweep" half-session commit (~10 findings, mechanical).
The carryover sweep closes the S29 ledger before Phase 3 layers
on top — cleaner baseline. Or defer and fold into Phase 3 work
naturally.

### S29 carryover sweep — CP09 S2 LANDED

**Sweep ran 2026-04-26 (CP09 S2)** — 4 commits on
`cleanup4-s29-carryover-sweep`, ff-merged to main at `906dd46`,
deployed to prod (192.168.53.11:4000), bouncer reconnected to
Azzurra + joined `#grappa`. F1 + F2 + F3 + F4 all closed; the S29
ledger that's been carrying forward since CP06 is now empty.

| Commit | Finding | What landed |
|--------|---------|-------------|
| cb66e4d | F1 HIGH | `client.ex:421-426` LS continuation clauses get `%{phase: :awaiting_cap_ls} = state` guard; `++` flipped to `parse_(chunk) ++ state.caps_buffer` (O(chunk) not O(N²)); now-unreachable `finalize_cap_ls/2` second clause dropped. New "stray CAP LS post-registration is absorbed and does not grow caps_buffer" test feeds 50 strays + asserts `:sys.get_state(client).caps_buffer == []`. |
| fde5208 | F2 MEDIUM | `identifier.ex:26` `@nick_regex` first-char class drops `\-` (RFC 2812 §2.3.1: dash is tail-only). Property test "rejects any nick with a leading dash, regardless of tail" + property "accepts a one-char nick for every legal first-char". Credential ↔ Identifier parity fixtures updated (`-vjt` moved acceptance → rejection list; `v-jt` added to acceptance). |
| 2b96eb0 | F3 MEDIUM | `bootstrap.ex:121-134` adds explicit `{:error, {:already_started, _}}` clause to `else` that logs `:debug` + increments `started`. New "second run finds existing sessions and counts them as started, not failed" test pins idempotency on Bootstrap restart. Moduledoc updated to spell out the dual-success contract. |
| 906dd46 | F4 MEDIUM ×2 | `networks.ex:259` rename `update_credential/3` → `update_credential!/3` (admits the `Ecto.NoResultsError` raise via `!` suffix); spec, moduledoc, mix-task callsite, networks_test.exs `describe` + 4 tests + 1 NEW `assert_raise` test. `add_server/2` + `bind_credential/3` drop `Map.new/1` + add `is_map(attrs)` guard (specs already declared `map()`-only). |

Stats: 4 commits / 10 files / +207 / −49 / +4 tests (411 → 415).
ci.check green: 8 properties, 0 failures, Dialyzer 0, Credo 0,
Sobelow 0. Reviewer signoff: "Ship it. Cristo cane, the F1
carryover from S29 → CP07 → CP09 finally has a test that would
have caught the never-landed claim — that's the real win here."

After the sweep: **start Phase 3 cicchetto repo bootstrap**.
Architecture review's pre-Phase-3 plan is 12/12 closed; S29
carryover ledger is empty; remaining 18 A* findings are Phase 5+.

### S3: Phase 3 cicchetto walking-skeleton wrap — LANDED 2026-04-27

Phase 3 walking skeleton is live in prod at `http://grappa.bad.ass`
(LAN-side via vlan53 192.168.53.11 → nginx → grappa:4000). 16 commits
landed on main; iPhone PWA install + login + scrollback + send round-
trip green. Operator-readiness criteria for "the walking skeleton can
talk to the bouncer over an installable PWA" are met.

**Commits, in order (main, post-merge):**

| SHA | Sub-task | What landed |
|-----|----------|-------------|
| 523dc20 | 1 | `GET /networks` + `GET /networks/:nid/channels` REST gaps. Wire mirrors `Grappa.Networks.Wire` shape; per-user iso (404 on known-slug-no-credential, same shape as unknown-slug — prevents network enumeration). |
| 1333e66 | 2 | `cicchetto/` SolidJS + TS + Vite + Bun + Biome scaffold + day-1 PWA shell (manifest + sw.js + icon set). |
| ac92bcc | 3 | Login form + `auth.ts` token signal store + protected route guard. |
| 69eea69 | 4 | Channel list + Phoenix Channels client (`socket.ts`, lazy connect, token-driven (re)auth, topic vocabulary mirrors `Grappa.PubSub.Topic`). |
| ccdc367 | 5 | Scrollback fetch on channel select + compose textarea + send (REST POST + WS push dedupe by id, ASC normalization on REST DESC pages). |
| 29ec512 | 6 | `compose.prod.yaml` restructure: grappa drops `vlan53` → `grappa_internal` Docker bridge; nginx joins `vlan53 .53.11` + bridge; `infra/nginx.conf` SPA `try_files` + reverse-proxy `/auth /me /networks /healthz /socket` to `grappa:4000` with WS upgrade headers + HTTP/1.1; `cicchetto-build` oneshot `oven/bun:1` produces `dist/`; `scripts/deploy.sh` + `scripts/healthcheck.sh` + new `scripts/register-dns.sh` (Technitium API). |
| 22bf457 | 7 | DESIGN_NOTES SolidJS+TS+Vite+Bun+Biome stack decision; README cicchetto bullet; Phase 0 client-framework tick + Phase 3 walking-skeleton box ticks; todo.md sub-task 8 runbook. |
| 700aa3d | 8a — B1 fix | `runtime.exs` reads `PHX_HOST` (default `grappa.bad.ass`) and sets `url:` + `check_origin: ["//#{phx_host}"]`. Without this, every Channels WS connect from `http://grappa.bad.ass` was rejected (Phoenix default = check Origin against endpoint URL host = `localhost`). Caught by code review as a hard prod blocker. DESIGN_NOTES paragraph "WS check_origin is the defense-in-depth on bearer-in-querystring" wires the rationale. |
| c257197 | 8b — B2 fix | `config/config.exs` sets `:filter_parameters, ["password", "token"]` so Phoenix.Logger redacts the bearer from `[info] CONNECTED TO ...` lines; `infra/nginx.conf` `access_log off` inside `location /socket` so the request URI (which carries `?token=…` per phoenix.js Socket transport) doesn't surface in nginx stdout. Both layers required; either alone leaves the bearer in a different log file. |
| a2b81be | 8c — S1+S2 fix | `infra/nginx.conf` adds CSP + X-Content-Type-Options + Referrer-Policy + X-Frame-Options + Permissions-Policy via `add_header … always` at server scope. CSP allowlist tuned for the SolidJS + PWA shell (connect-src self+ws+wss, style-src self+'unsafe-inline', worker-src self, manifest-src self, frame-ancestors none). `location = /sw.js` adds `Cache-Control: no-cache` + re-asserts the security header set (nginx add_header inheritance stops at the first inner add_header). |
| defa6a5 | 8d — S3 fix | `cicchetto/src/lib/api.ts` adds `setOn401Handler` registry; `readError` invokes the handler before building ApiError. `auth.ts` registers `() => setToken(null)` at module load. Closes the dead-token reconnect loop: any 401 → token cleared → socket disconnects via existing `createEffect` → RequireAuth bounces. New `api.test.ts` (6 tests) + extended `auth.test.ts` (1 new test). 37 → 44 vitest. |
| 59beac0 | 8e — S4 fix | `scripts/register-dns.sh` rewritten for strong idempotency: pre-condition `dig` skips API call if record already correct; otherwise delete-then-add (corrects drift); post-condition `dig` asserts authoritative resolution. New `api_call` helper extracts only `status` + `errorMessage` from Technitium responses (token never surfaces in operator stdout/CI logs). |
| 56e4ae8 | 8f — S5+S6 fix | `cicchetto/src/lib/networks.ts` `ChannelKey` properly opaque-branded via `unique symbol` (the prior `${string} ${string}` template-literal type erased to plain `string`); separator-comment fixed (matches the literal-space code; both NUL and space are RFC 2812-forbidden, picked space for debugger readability). |
| ff36278 | 8g | todo.md updated: sub-task 8 progress (gates + review + fixes done); 6 reviewer CONSIDER items logged across High (Phase 5 hardening), Medium (image-tag-pin), Low (nginx keepalive perf nit). |
| bb64c18 | 8h — deploy fixup | First prod deploy hit `EACCES` on Vite's prepare-out-dir step: fresh Docker named volume is root:root but `cicchetto-build` runs as UID 1000 (so node_modules writes don't land as root on the host bind). Replaced `cicchetto_dist` named volume with `./runtime/cicchetto-dist` host bind-mount; `deploy.sh` `mkdir -p`s the dir before invoking the build (operator UID = 1000 = container user). Bonus: dist/ now inspectable from host without docker exec. |
| 6601a93 | 8i — deploy fixup | `scripts/register-dns.sh` references TECHNITIUM_API_TOKEN; reality on the canonical deployment is `/srv/dns/.env` exporting `TECHNITIUM_TOKEN`. Renamed all references in the script to match (env file is the existing single source of truth). |

**Gates landed green:**

- Server: `scripts/check.sh` clean — 424/0 ExUnit (8 properties, 0 failures), Dialyzer 0, Credo 0, Sobelow 0, format clean, audits clean, doctor 100%/100% (only pre-existing lib/grappa.ex:24 `Grappa.Application` hidden-doc warning, not from this branch).
- Cicchetto: `scripts/bun.sh run check` clean — Biome lint + format + tsc strict, 16 files. `scripts/bun.sh run test` 44/44 vitest (was 37 pre-S3 fix).
- One sqlite "Database busy" flake hit during the first server check.sh re-run on `Grappa.Networks.WireTest` setup (write-heavy `max_cases: 2` parallelism); re-run was clean. Pre-existing flake documented in CP08.

**Deploy timestamp:** 2026-04-26 23:30 UTC (first attempt failed on the volume permissions issue at 23:26; recovery deploy with bb64c18 fixup landed at 23:30). Bundle size: 62.13 KB JS / 21.45 KB gzip — well under the 200 KB pre-Vosk/piper budget.

**DNS:** `grappa.bad.ass A 192.168.53.11 TTL 300` registered via `scripts/register-dns.sh` against the home Technitium instance at `ns1.bad.ass`. Pre-condition dig confirmed no prior record; delete returned absence; add succeeded; post-condition dig confirmed authoritative resolution.

**Operator credentials reset:** `vjt` user password reset to operator's chosen value via `bin/grappa rpc` against the live release: `User.changeset(%{password: "..."}) |> Repo.update!()`. The original Phase 2 password was forgotten between deploys; min-8-char validation (User changeset) enforced. No production code weakened to bypass — the `:password` field flows through the existing changeset path that hashes via Argon2.

**iPhone PWA acceptance — verbatim user confirmation:**
> "works. can log in and see scrillback and send messages. vidual layout is messy but i guess we do that later. verified also session persists app clisure"

That covers: (1) Add to Home Screen → installable PWA, (2) launch from home screen, (3) login round-trip with the freshly-reset bearer, (4) scrollback fetch on channel select, (5) live PRIVMSG receive (the channel had upstream traffic during the session), (6) PRIVMSG send round-trip, (7) bearer persistence across PWA cold-start (localStorage survives app closure on iOS).

**What works (Phase 3 walking skeleton, verified end-to-end):**

- REST: `/auth/login`, `/me`, `/networks`, `/networks/:nid/channels`, `/networks/:nid/channels/:name/messages` (GET + POST). All gated by `Plugs.Authn` bearer. FallbackController maps `{:error, _}` → JSON error envelope.
- WS: `/socket/websocket` with bearer auth in `?token=…`; `check_origin` validates against `grappa.bad.ass`; topic vocabulary `grappa:user:{name}/network:{slug}/channel:{name}`; `event` push handler appends scrollback rows + bumps unread counts.
- PWA: installable on iOS Safari (manifest-driven Add to Home Screen). Service Worker registers as a no-op for offline shell cache (will cache with proper effect once Phase 5 TLS lands — secure-context requirement). Token persists in localStorage across cold-starts.
- nginx: SPA `try_files`, reverse-proxy with WS upgrade headers, security headers verified on the wire (CSP + X-Content-Type-Options + Referrer-Policy + X-Frame-Options + Permissions-Policy), `access_log off` on `/socket`.
- Operator UX: `scripts/deploy.sh` + `scripts/register-dns.sh` end-to-end deploy + DNS in two commands; healthcheck via nginx :80 confirms the full path is up.

**What's deferred to Phase 4 (irssi-shape UI):**

User confirmed the visual layout is "messy" — expected. Phase 4 owns:

- Keyboard-first layout (Ctrl-N / Ctrl-P / Alt-1..9 channel switching, no mouse needed).
- Theme system (single global theme, irssi-shape colour palette + extension hooks).
- Nick list + mode indicators + topic bar + presence per channel.
- Mobile ergonomics: tap-target sizing, swipe-to-switch-channel, full-screen compose modal.
- Voice I/O optional drop-in (Vosk/piper, must fit ≤200 KB gzip excluding the model).

See README "Roadmap" section for the full Phase 4 backlog.

**What's deferred to Phase 5 (hardening):** logged in `docs/todo.md`
under High/Phase-5-hardening cluster:

- TLS rollout — secure-context requirement for the SW to actually cache the shell on iOS.
- Move bearer off the WS query string (current redaction is on the log surface, not the wire).
- `loadMore` concurrency guard (REST request dedupe).
- Accessibility audit (channel sidebar tree semantics, tap targets, focus contrast).
- New WS subprotocols inherit `check_origin` (can't relax in-place).

Plus Medium (Docker tag pinning to digests for reproducible builds) and Low (nginx upstream keepalive perf nit on the API allowlist block).

**CLAUDE.md health check:** all rules upheld through Phase 3:

- Bouncer-owned scrollback, sqlite Ecto, no client-side IRC parsing.
- `CAP LS + SASL` only required upstream IRCv3 feature; everything else opportunistic.
- Phoenix Channels = streaming surface; topic vocabulary mirrors `Grappa.PubSub.Topic`.
- `Boundary` annotations clean (no new cycles).
- One feature, one code path, every door — `Networks.list_for_user/1` powers the REST endpoint that the PWA consumes; same context call.
- Bite-sized commits; messages explain WHY (the 8a-8i sub-task numbering plus the BLOCKERS-vs-SHOULD-FIX provenance lets someone reading `git log` later understand the order of operations).

### S4: /review skill extended for cicchetto/ — LANDED 2026-04-27

Phase 3 landed cicchetto as a first-class subsystem (~1100 LOC TS +
SolidJS PWA), but `docs/reviewing.md` + `.claude/skills/review/SKILL.md`
were Elixir/Phoenix-only — a `/review codebase` run would have skipped
the cicchetto/ tree entirely. Spotted at the natural review moment
("PWA review time?") just after the iPhone acceptance. Caught up the
review skill before running.

**Changes (single commit, b5b35ec):**

- Line-level review gains a 6th agent: `cicchetto/`. Scope:
  `cicchetto/src/**` + `tsconfig.json` + `vite.config.ts` +
  `vitest.config.ts` + `biome.json` + `package.json` + `index.html`
  + `public/{manifest.json, sw.js, icon*}`. The agent additionally
  reads server-side wire-shape counterparts
  (`Grappa.{Accounts,Networks,Scrollback}.Wire` +
  `GrappaWeb.*JSON`) so wire drift is caught against the source of
  truth.
- Cross-module + infra agent extended: `compose.prod.yaml` env
  vars vs `runtime.exs` reads; `infra/nginx.conf` reverse-proxy
  allowlist vs `lib/grappa_web/router.ex` routes (any new server
  route must land in nginx OR be intentionally bouncer-internal);
  `cicchetto/biome.json` rule consistency, `vite.config.ts` outDir
  vs `compose.prod.yaml` `cicchetto-build` expectations,
  `package.json` ↔ `bun.lock` sync, `scripts/bun.sh` UID/cache
  layout vs compose.
- Cicchetto agent's lookup bullets cover: SolidJS reactivity
  bugs (`setSignal` inside tracked scope, `createResource`
  invalidation, `untrack` misuse, `createRoot` ownership leaks);
  TS strictness (`any` escapes, exhaustive switches on closed
  unions like `MessageKind`, optional-chain holes,
  `noUncheckedIndexedAccess` violations); wire-shape drift; XSS /
  token leakage / CSP-incompatible patterns; a11y baseline (semantic
  HTML, ARIA, keyboard, focus, tap targets ≥44pt, lang attr); test
  quality (outcome assertions, module-boundary mocks, realistic mock
  data, `vi.resetModules` for module-singleton signals); PWA shell
  correctness (manifest required fields, SW caching strategy,
  cache-bump on deploy); build/tooling drift.
- Architecture review concerns broadened to span server↔client.
  Abstraction-boundaries includes `api.ts` ↔ server `Wire`.
  Responsibility-cohesion includes "components holding domain state
  instead of consuming from `lib/networks.ts` store." Duplication
  names cicchetto api.ts ↔ server Wire as the canonical paired-
  shape case. Dependency architecture covers TS module cycles
  (`auth ↔ api`) + module-level mutable state on the client. Type
  system leverage adds TS `strict` + `noUncheckedIndexedAccess` +
  branded/opaque types (the `ChannelKey` pattern). Extension &
  maintainability includes "adding a new IRC kind → 5 places
  including cicchetto MessageKind + ScrollbackPane render."
- "What agents IGNORE" extended: Phase 4 UI scope (irssi-shape
  redesign — keyboard layout, theme system, nick lists, mobile
  ergonomics, voice I/O). Walking-skeleton UI is intentionally rough
  until Phase 4; flag bugs, not "the layout is messy."

**Why this matters:** the `/review` skill is institutional memory
for "what counts as a problem." Phase 3 added a major subsystem with
its own idioms (SolidJS reactivity, TS strict, PWA shell, a11y
baseline) that the prior version of the skill couldn't articulate as
findings. Without this update, the next codebase review gate would
have produced a server-side-clean report while client-side bugs
quietly accumulated. Same shape as CLAUDE.md "Total consistency or
nothing" — half-coverage is worse than no coverage because it lulls
you into thinking the review ran.

The /review codebase + architecture runs themselves are deferred to
the next session — the skill update was the gate before running, and
this session's context is heavy enough that running 6 parallel agents
on top would have forced a compaction mid-review.

### Phase 5+ deferred items (from architecture review)

NOT in cleanup scope; queued for Phase 5+ hardening:
- A3 IRC.Client god-module split
- A9 IRC kind catalog
- A12 PubSub wrapper
- A13 Client behaviour
- A14 auth_method enum unification
- A15 per-kind Wire meta typing
- A16 single safe_line_token? owner
- A17 Accounts.AuthContext struct
- A19 IRC.Numerics table
- A21 Boundary deps tightening
- A24 Session.Server defstruct
- A25 Wire.event union
- A27/A28 IRC.Identifier split + speculative-validator removal
- A29 sandbox setup helper

### Architectural debt acknowledged (carried from CP08)

- **Networks ↔ Scrollback structural cycle** still exists (broken
  via Boundary `dirty_xrefs: [Grappa.Networks.Network]` on
  Scrollback boundary). Closing requires lifting `Network` schema
  struct out of `Networks` into a leaf boundary — major
  restructure, queued for Phase 5+ when wire-shape contract
  evolution makes the workaround painful.
- **Session cached-opts restart regression** — `:transient`
  restart replays opts captured at first `start_session/3`.
  Live credential changes only propagate when
  `Networks.unbind_credential/2` runs INSIDE the prod BEAM (via
  `bin/grappa rpc`). Phase 5 may add `Session.refresh/2`.
- **`Grappa.Application` hidden-doc warning** at lib/grappa.ex:24
  — pre-Phase-1 inheritance, low priority.
- **Server export from Session boundary** (S32 reviewer F6) — kept
  for test-only `Logger.put_module_level/2`. Phase 5 trajectory
  item: lift the per-module log helper into a test helper, drop
  the export.
- **Phase 5+ deferred from S29 codebase review** (already
  documented across CP07 + CP08): reconnect/backoff in
  IRC.Client, Session.Server clean `terminate/2`, TLS cert chain,
  pre-deploy lint cross-checking runtime.exs vs compose env,
  Logger NOTICE handling, `Grappa.Operator` CLI lift, NickServ
  REGISTER proxy.

### Other open items (carried from CP08)

- `test/grappa_web/channels/grappa_channel_test.exs:76` flake
  (`assert_receive %Phoenix.Socket.Message{}` intermittent
  timeout). Race between channel join + PubSub subscribe before
  broadcast lands. ~1-in-5 hit rate under `mix ci.check`
  parallelism. Hit S17. **Did NOT resolve naturally during
  Phase 2 or pre-Phase-3 cleanup** — re-check at next codebase
  review.
- `Database busy` flake on `Grappa.Networks.WireTest` setup under
  `max_cases:2` write-heavy parallelism. Hit during S33 ci.check
  re-runs. Re-run was clean. Pre-existing under sqlite contention,
  documented in DataCase comments.

---
