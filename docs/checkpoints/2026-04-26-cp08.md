---
status: active
---

# CP08

CP07 closed at S30 / 10 sessions / ~870 lines — covered Phase 2
multi-user auth end-to-end (sub-tasks 2a-pre through 2k) + the S29
codebase review + S29 followup cluster + 2j review followups + the
2k live deploy with two real bug fixes (compose env passthrough,
README rpc-not-eval). Phase 2 closed: bouncer live on Azzurra as
`grappa` in `#grappa`, NickServ IDENTIFY accepted, all 12 plan
sub-tasks done, DESIGN_NOTES Phase 2 close pass landed (7 entries
dated 2026-04-26).

CP06 closed at S20 / 3 sessions / ~440 lines — Phase 1 Tasks 9 + 10
+ Phase 2 design pass + plan-write. CP05 closed at S17 covering
Phase 1.5 + line-level review fix. CP04 closed at S13 (walking-
skeleton complete + live on Pi). CP03 closed at S11 (Task 8). CP02
covered Tasks 5-7. CP01 covered Phase 0 + Tasks 1-4. See prior
checkpoints for back-story.

CP08 opens with **Phase 2 COMPLETE + live in prod** (192.168.53.11:4000,
bouncer connected to Azzurra as `grappa`, joined `#grappa`, NickServ
IDENTIFY validated). **Phase 3 (client walking skeleton — PWA shell,
login → token → WS, channel list + scrollback, send message) starts
next.**

Phase 3 belongs in a separate repository (`cicchetto`, the PWA per
README "two facades, one store" — see DESIGN_NOTES 2026-04-20
"decision: two facades, one store"). CP08's job: cicchetto repo
init + first walking-skeleton implementation.

---

## State at checkpoint creation

Frozen at main commit `f3972fc` (`docs: README — bin/grappa rpc, not
eval, for operator-side context calls`). Working tree clean,
origin/main in sync (35 commits pushed from the Phase 2 close).

**Phase 2 walking-shape:** 12 plan sub-tasks done + S29 codebase
review acted on + 2j review acted on + 2 deploy-time bug fixes.
Bouncer live in prod against real Azzurra IRC traffic.

**Codebase shape (frozen at S30):**
- ~6,000 LOC of Elixir under `lib/` (estimate, post-Phase-2)
- 6 properties + 378 tests + 0 failures
- ci.check ~50s on Pi (worktree, PLT hot)
- 100 commits on `main` since project init (`f3972fc` = #100)
- 13 top-level Boundary annotations enforced (Accounts + Networks
  added during Phase 2)

**Tooling baseline:** unchanged from CP07 — Dialyxir
(`:underspecs`), Credo (`--strict`), Sobelow, mix_audit, doctor
(99.2% module health — pre-existing Grappa.Application hidden-doc
warning, not from Phase 2), Boundary (enforced), ExUnit, StreamData,
Mox, Bypass, ExMachina, excoveralls. All green.

**Live in prod:** bouncer at http://192.168.53.11:4000, connected
to Azzurra as `grappa`, joined `#grappa`. End-to-end PRIVMSG →
Scrollback → REST + PubSub round-trip ready (no PWA client
connected yet — that's Phase 3's job).

**Phase 2 ledger:**
- 35 commits on `main` from `phase2-auth` push
- 12/12 plan sub-tasks complete (2a-pre, 2a, 2b, 2c, 2d, 2e, 2f,
  2g, 2h, 2i, 2j, 2k)
- 1 codebase review (S29, 51 findings → 5-bug followup cluster)
- 1 cluster review (2j, 2 critical + 4 high → 2 followup commits)
- 2 deploy-time bug fixes (compose env, README rpc)
- 7 DESIGN_NOTES Phase 2 close entries

---

## Pending at checkpoint open

### Carried forward from CP07 (post-Phase-2 cleanup cluster)

These were explicitly deferred from S29 / 2j review to a dedicated
cleanup cluster, NOT into Phase 5 hardening:

- **M3** — three nick-regex implementations drift; consolidate.
- **M5** — error-string-casing inconsistency
  (`"not found"` vs `"invalid_credentials"`).
- **H11** — central User wire shape
  (`MeJSON` vs `AuthJSON` drift).
- **M2** — web→Repo dep cleanup (the M1 review fix lives in
  `messages_controller.ex` `Repo.preload(:network)` — possibly land
  as part of the M3/M5/H11 cluster commit).
- **M12** — `Application.put_env` 6× duplication across Mix tasks +
  Bootstrap; extract a `Grappa.MixTaskBoot` helper or similar.

Combined budget: ~half a session. Could land as a single
post-Phase-2 hygiene commit ("cleanup: consolidate Phase 2
wire-shape + Boundary fixups") before Phase 3 starts, OR ride
naturally as Phase 3 grows the surface and naturally invokes
the same patterns.

### Phase 3 prep

Phase 3 = client walking skeleton (cicchetto PWA). Per Roadmap:
- [ ] PWA shell, manifest, service worker
- [ ] Login flow → token → connect `/socket/websocket`
- [ ] Channel list + scrollback fetch on select
- [ ] Send message

**Open client-stack decision:** README Phase 0 list still has
"[ ] Pick a client framework (Svelte vs SolidJS vs plain
lit-html)" unchecked. Needs to land before Phase 3 starts in
earnest. Add to DESIGN_NOTES as a decision when made; the
framework choice frames every subsequent Phase 3 sub-task.

**Cicchetto repo bootstrap:** new repository (the README is clear:
two-store, two-repo split). Will need its own `CLAUDE.md`,
`docs/`, build/deploy scripts mirroring the grappa container model
where it makes sense (Docker-based dev, named volumes for
node_modules cache, etc.).

**Cross-repo coupling surface:** the REST + WS contracts grappa
ships are the cicchetto contract. Phase 3 may surface gaps in
grappa's wire shape (missing fields, awkward pagination, etc.)
that need grappa-side fixes. Land those on `main` directly
(small) or in a `phase3-feedback` worktree (large).

### Codebase review gate

Last codebase review: 2026-04-26 (S29). Phase 2 cluster review
(2j) on the same date. Threshold: ≥12 sessions OR >2 weeks.
S30 is session 1 since the last review; not yet due. Next due
trip estimated around CP08 S5 (2-3 sessions into Phase 3).

### Architecture review (NEW — S31)

Architecture review landed 2026-04-26 at
`docs/reviews/architecture/2026-04-26-architecture-review.md`.
6 parallel concern-based agents → 83 raw findings → 30 deduped
(1 CRITICAL, 9 HIGH, 13 MEDIUM, 7 LOW). Five cross-cutting themes:
1. **Wire contract** about to be frozen by cicchetto Phase 3 (A1, A4, A5, A7, A8, A15, A18)
2. **Networks ↔ Session cycle** metastasizing (A2, A6, A10, A22) — folds into one fix
3. **IRC subsystem** library-extraction-hostile (A3, A9, A13, A14, A19, A28) — Phase 5
4. **Operator surface** sprawling without abstraction (A11, A20, A30)
5. **Type leverage** strong but not total (A14, A15, A17, A23, A24, A25)

**A1 is CRITICAL**: `Credential.password_encrypted` carries plaintext after
Cloak load AND no `Networks.Wire` exists — the first naïve Phase 3
`GET /networks` controller leaks the upstream NickServ password to JSON.
`redact: true` only protects `inspect/1`, not `Jason.encode!/1`.

**Pre-Phase-3 action plan** (architecture review ## Action plan):
- Cleanup cluster A1 + A4 + A5 + A7 + A8 + A18 + A23 + A26 (~1.5 sessions)
- Cycle inversion A2 + A6 + A10 + A22 (~1 session)
- Operator surface A11 + A20 + A30 (~0.5 session)
Total: ~3 sessions BEFORE Phase 3 starts.

The carried-forward post-Phase-2 hygiene cluster (M2, M3, M5, H11, M12)
is fully absorbed into the pre-Phase-3 cleanup cluster.

### Worktree state

`phase2-auth` worktree at `/home/vjt/code/IRC/grappa-phase2` —
fully merged + pushed; the worktree itself is dead weight now.
**Action: `git worktree remove ~/code/IRC/grappa-phase2`** at
start of next session (if not already done at S30 close).

### Deferred to Phase 5 hardening (NOT Phase 3)

- Reconnect/backoff inside IRC.Client on socket close (currently
  `:tcp_closed` crashes Session.Server, supervisor restarts cold)
- Session.Server clean `terminate/2` (send QUIT to upstream +
  close socket cleanly — current `:normal` exit kills via link)
- TLS cert chain verification (currently `verify: :verify_none`
  at IRC.Client transport layer)
- Pre-deploy lint cross-checking `runtime.exs` env reads against
  `compose.prod.yaml` env passthroughs (would have caught the
  GRAPPA_ENCRYPTION_KEY oversight)
- Logger NOTICE message handling (currently filtered out of
  `irc event` metadata logging — bites handshake debugging)
- Thin `Grappa.Operator` CLI lift (extract the operator surface
  from `Mix.Tasks.Grappa.*` so prod operators don't need
  `bin/grappa rpc` with verbose context-function calls)
- Boundary cycle Networks↔Session — invert Session.Server.init
  to take credential data via opts instead of reading Networks
  at init time; lifts the inlined `stop_session_for_unbind/2`
  duplication in Networks back into `Session.stop_session/2`.
- NickServ REGISTER proxy (Roadmap Phase 2 unticked box; Phase 5
  catch-all per CP07 S30 close)

### Other open items (carried from CP07)

- Investigate flake in `test/grappa_web/channels/grappa_channel_test.exs:76`
  — `assert_receive %Phoenix.Socket.Message{}` intermittently
  times out. Race between channel join and PubSub subscribe before
  broadcast lands. ~1-in-5 hit rate under `mix ci.check`
  parallelism. Hit S17. **Did NOT resolve naturally during 2h or
  2i** — re-check at next codebase review.

---

## S31 — Cluster 1 (wire-shape contract): A1+A4+A5+A7+A8+A18+A23+A26 LANDED + DEPLOYED

8 commits on `cleanup1-wire-contract` worktree, ff-merged to `main`
(190b7d4..94dd8a9), 24 files +776/-111. superpowers:code-reviewer
verdict: APPROVE with 7 LOW findings (0 CRITICAL/HIGH/MEDIUM, all
defer-OK to Cluster 2/Phase 3). Live-deployed; bouncer reconnected
to Azzurra at 18:09:42, joined #grappa.

Closed CRITICAL A1 (Credential password JSON-leak) + 7 architecture
findings absorbing carried-forward post-Phase-2 hygiene (M2/M3/M5/H11
folded into A4/A8/A5 fixes). New surface: `Grappa.Networks.Wire`,
`Grappa.Accounts.Wire`. Single source for nick rule
(`Identifier.valid_nick?/1`), single source for slug rule
(`Identifier.valid_network_slug?/1`, cap unified to 32). REST
error-string convention pinned to snake_case across 6 controllers.
`lib/grappa_web/` is now Repo-free.

## S32 — Cluster 2 (cycle inversion): A2+A6+A10+A22 LANDED + DEPLOYED

3 commits on `cleanup2-cycle-inversion` worktree, ff-merged to `main`
(cdb719c..04eaafc), 15 files +546/-241. superpowers:code-reviewer
verdict: APPROVE-WITH-NITS (1 HIGH doc-honesty F1, 1 MEDIUM type
leverage F2, 1 LOW defensive-rescue rationale F3 — all folded into
the 04eaafc follow-up commit). Live-deployed; bouncer respawned + sent
NickServ IDENTIFY, joined #grappa at 18:55:23.

Mechanism: `Session.start_session/3` takes pre-resolved primitive
opts (`Session.start_opts/0`); `Networks.session_plan/1` is the
canonical producer; `Session.Server.init/1` is ~25 LOC of pure data
consumption. Boundary deps shrink: Session 7→4 (drop Accounts,
Networks, Repo). Reverse `Networks → Session` edge becomes legal —
`Networks.unbind_credential/2` calls `Session.stop_session/2`
directly, dropping the 22-line inlined `stop_session_for_unbind/2` +
duplicated registry-key tuple. `pick_server!/1` + `NoServerError`
move from Session to Networks (server-list policy belongs with the
data). `Scrollback.has_messages_for_network?/1` added; Networks'
cascade-on-empty path calls it instead of the raw
`from(m in "messages")` bypass. Networks ↔ Scrollback structural
cycle (Scrollback's `belongs_to :network, Network`) broken via
`dirty_xrefs: [Grappa.Networks.Network]` on Scrollback boundary —
declarative + visible at the boundary annotation, vs. the
raw-string-table-name workaround that hid the dep.

Trade-off documented: `:transient` restart replays cached opts. The
operator path that re-spawns must hit the LIVE BEAM
(`bin/grappa rpc 'Networks.unbind_credential(...)'`) — bare
`mix grappa.unbind_network` runs in a separate BEAM with empty
registry, so its `stop_session/2` is a no-op against prod.
F1 fix (04eaafc) made the Session moduledoc honest about this.

A6 sub-commit: 4 mix tasks
(`add_server`/`remove_server`/`unbind_network`/`update_network_credential`)
drop `Grappa.Repo` + `Grappa.Networks.Network` aliases, call
`Networks.get_network_by_slug/1` (or `!` variant) instead of
`Repo.get_by!`. Operator surface goes through the boundary now;
slug-lookup semantics are evolvable in one place.

ci.check exit 0; 6 properties, 409 tests, 0 failures (was 401 in S30
baseline — net +8: 11 new Networks tests for session_plan/pick_server/
get_network_by_slug + 3 deleted tests in server_test.exs whose
failure surface moved to session_plan).

## Pending after S32

**Cluster 3** = operator surface ~0.5 session, 3 sub-tasks:
- A11/M12: extract `Mix.Tasks.Grappa.Boot.start_app_silent/0` for the
  6× `Application.put_env(:start_bootstrap, false) + ensure_all_started`
  dance across all six grappa.* mix tasks excluding gen_encryption_key.
- A20: lift `halt_changeset/2` from bind_network.ex to shared
  `Mix.Tasks.Grappa.Output` for 4 sites.
- A30: add `Mix.Tasks.Grappa` no-suffix help-index task printing all
  subcommands with `@shortdoc` lines (~30 LOC).

After Cluster 3 lands → ALL THREE CLUSTERS LANDED → start Phase 3
(cicchetto PWA repo bootstrap). DO NOT START PHASE 3 BEFORE
CLUSTER 3 COMPLETES.

**Phase 5+ deferred** (NOT in cleanup scope): A3, A9, A12, A13,
A14, A15, A16, A17, A19, A21, A24, A25, A27, A28, A29. Plus
trajectory items from S31 reviewer F4 (Server export rationale
review), S32 reviewer F4 (commit-boundary blur — defer-OK), S32
reviewer F6 (Server export pre-existing).

**Architectural debt acknowledged:**
- Networks ↔ Scrollback structural cycle still exists (broken via
  Boundary `dirty_xrefs`, not via real architectural fix). Closing
  it requires lifting `Network` schema struct out of `Networks`
  into a leaf boundary — major restructure, queued for Phase 5+
  when wire-shape contract gets harder to keep in sync.
- The cached-opts restart regression: documented but real.
  Phase 5 may add `Session.refresh/2` if hot-reload becomes
  painful in practice.

**CP08 status:** ~280 lines after S32. Approaching 200-line
rotation threshold (already past actually). Rotate to CP09 after
Cluster 3 lands so Phase 3 starts with a fresh checkpoint.

---
