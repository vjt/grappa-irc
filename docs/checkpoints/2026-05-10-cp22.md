---
status: active
---

# CP22 — channel-client-polish next-up: cluster B (/who + /names)

Opened 2026-05-10 immediately after CP21 closeout (whois-and-nickclick
LANDED + deployed + live-smoked at http://voygrappa.bad.ass).

## S1 — three prod regression fixes shipped (2026-05-10 afternoon)

Cluster B was DEFERRED before any code: the orchestrator brief copy-
pasted the WHOIS card shape onto `/who` + `/names`, but vjt's UX call
("in scrollback no fucking card porco dio") rescopes both verbs as
scrollback rows + MembersPane refresh, NOT cards. New cluster B brief
needed before re-opening `.worktrees/who-names`.

While Cluster B was re-scoping, three prod regressions surfaced and
shipped same-session, all on main, all deployed, all browser-verified
at voygrappa.bad.ass:

### S1.1 — MembersPane prefix clip (commit `2ffc29f`)

Symptom: members panel showed only `@` / `+` characters, full nicks
invisible. Root cause: Spec #5 click-button (`width: 100%` block-level)
+ pre-existing `::before { content: "@" | "+" }` inline prefix → button
wrapped to a new line below prefix → li `overflow: hidden` clipped the
wrapped button. Plain rows masked it (prefix is just space).

Fix: render prefix as first character of button's text content via
new `lib/memberSigil.ts` (lifted from `ScrollbackPane.tsx`'s former
module-private helper). Drop CSS `::before` rules. Inline flow,
no wrap, no clip. ScrollbackPane reuses the same helper (one source
of truth per CLAUDE.md "implement once, reuse everywhere").

Tests: vitest extended (`toContain("@vjt")` style — assertion now
catches the regression class because prefix lives in DOM textContent
not CSS). New Playwright e2e `members-prefix-regression.spec.ts`
asserts `.member-op .member-name` text equals `@<nick>` AND
boundingBox width > 30px (catches the wrap+clip; jsdom is blind to
`::before` and to layout). 713 vitest pass, 1411 mix tests pass,
scripts/check.sh EXIT=0.

### S1.2 — Focused-window marker + switch-target scroll (commit `dc3042b`)

Symptom A (marker): operator types `/msg <bot> body`, bot replies a
beat later in the focused query window — yet "1 unread message" marker
spawns between own-msg and reply.

Symptom B (scroll): switch from a DM (where own-msg + peer-reply just
landed) to a TALL channel — viewport stuck at top instead of bottom.
Second visit works.

Root cause (BOTH bugs): `markerRef` retained the disposed DOM pointer
across window key changes. Solid's let-bound refs assigned via JSX
`<div ref={markerRef}>` inside `<For>`-rendered subtrees DON'T auto-
null on unmount — the variable retains the stale node. Key createEffect
took the marker branch on the wrong window, called `scrollIntoView`
on the disposed node (no-op), never fell through to scrollTop=
scrollHeight. Marker bug shared the leak: focused-window arrivals had
`server_time > cursor` (cursor only advances on own-msg) so the rows
memo's filter included peer reply → marker injected.

Fix (3 parts, ScrollbackPane.tsx only):
1. `markerRef = undefined` synchronously at key change in createEffect
   — closes the leak before scroll branch evaluates.
2. `queueMicrotask` defer of the scroll decision — Solid commits the
   new window's rows BEFORE we read markerRef. New marker (if any)
   reassigns ref via `<div ref={markerRef}>`; otherwise scroll-to-bottom.
3. New `sessionStart` signal: ms timestamp captured at window mount
   (key change). Marker injection requires `cursor < server_time
   <= sessionStart` — anything arriving DURING focus session is
   live-read by definition, never spawns a new marker. Pre-existing
   markers (from before mount) survive intact.

Tests: TDD red → green. New Playwright e2e
`marker-target-window-regression.spec.ts` with two test cases mirroring
vjt's exact prod repro flow. Both failed pre-fix with literal evidence
(`Expected: 0, Received: 1` for marker count; dist > 1000 for scroll).
Both pass post-fix. 713/0 vitest, 1411/0 mix, scripts/check.sh EXIT=0.

### S1.3 — Own-nick DM filter leaks every inbound DM (commit `178bd5b`)

Symptom: vjt opened `grappa` (own-nick) query window post-deploy and
saw EVERY inbound DM (CristoBOT replies, prior peer conversations)
leaked into it.

Root cause: `Scrollback.fetch/5` → `channel_or_dm_where/2` applied
uniform `WHERE channel == ^name OR dm_with == ^name` filter. For
peer-DM windows this is correct (outbound at `channel = peer`,
inbound at `channel = own_nick AND dm_with = peer`). But for the
OWN-NICK window, the `channel == own_nick` clause matched every
inbound DM the server canonically stores at `channel = own_nick,
dm_with = peer`. CP14-B3 regression (commit 47866bc, 2026-05-07) —
latent for 3 days because cic's in-memory scrollback masked it
until S1.1 deploy invalidated the SW cache.

Fix: new `Scrollback.fetch/6` takes explicit `own_nick`. When `name
== own_nick` (case-insensitive — IRC nicks are CI), filter restricts
to `channel == ^name AND dm_with == ^name` (self-msgs only).
Controller resolves own_nick via `Session.current_nick/2` (already
exists). `fetch/5` kept as nil-passing wrapper for back-compat.

Tests: TDD — 4 new cases in `scrollback_test.exs` describe "fetch/5
— :dm_with bidirectional DM": own-nick narrowing, peer DM unchanged,
case-insensitivity, fetch/5 wrapper preserves legacy OR. 1411/0
failures, scripts/check.sh EXIT=0.

### Cumulative session ship-evidence (all three fixes)

```
git log --oneline f64dc6e..HEAD
178bd5b fix(scrollback): own-nick query window leaks every inbound DM (CP14-B3 regression)
dc3042b fix(cic): focused-window marker + switch-target scroll — kill stale markerRef
2ffc29f fix(cic): MembersPane prefix clipping — render @/+ in button text
```

All three: pushed to origin/main, deployed via `scripts/deploy.sh`,
/healthz ok, browser-smoked at http://voygrappa.bad.ass with literal
DOM-state evidence (members text, marker count, grappa-window sender
list). 5 SpawnOrchestrator TCP flakes during one ship-gate pass were
pre-existing load-related; pass in isolation + on retry.

### Memories saved (5 new)

- `feedback_card_vs_scrollback_ux.md` — cards = single-entity
  structured (whois OK); /who, /names, /list = scrollback rows or
  live-state refresh, NEVER cards. Re-scopes Cluster B brief.
- `feedback_css_block_button_wraps_inline_prefix.md` — `width:100%`
  block button + `::before` inline prefix wraps + clips; render
  prefix INSIDE button text + assert textContent in vitest +
  Playwright e2e mandatory.
- `feedback_target_window_ux_rule.md` — focused-window arrivals are
  live-read; markers/badges only describe pre-session state. SOURCE
  state must clear at switch BEFORE evaluating TARGET decisions.
- `feedback_solidjs_for_ref_leak.md` — Solid's let-bound refs in
  `<For>` children DON'T auto-null on unmount; manually reset in
  parent lifecycle; defer DOM reads via queueMicrotask.
- `feedback_dm_filter_own_nick_narrowing.md` — Scrollback OR-shape
  works for peer DMs but OVERMATCHES own-nick query window; pass
  own_nick + restrict to `channel==name AND dm_with==name`.

## Carry-forward from CP21

CP21 S6 shipped channel-client-polish **#5** (left-click on member
nick → DM auto-open + focus) and **#2** (`/whois <nick>` end-to-end:
delegated 311/312/313/317/319 accumulator → 318 RPL_ENDOFWHOIS bundle
broadcast → cic ephemeral WhoisCard inline render).

The WHOIS pipeline is now the **template** for the three NOT-STARTED
info-verb specs:

| Spec | Numerics | Pattern |
|------|----------|---------|
| #14 `/who` + `/names` | 352/315 + 353/366 | per-target accumulator → end-marker bundle → ephemeral card |
| #15 `/list` | 321/322/323 | dedicated `:list` pseudo-window (already declared in `windowKinds.ts:22`) + sortable rows + click-to-/join |
| #16 `/links` | 364/365 | tree-format render in `$server` window (no new window kind, vjt-blessed) |

`Grappa.Session.NumericRouter`'s `@delegated_numerics` set already
short-circuits all of these; the per-handler responsibility is to emit
the bundle effect rather than just stub the path (mirror of WHOIS pattern).

## Pending PARTIAL items left from CP21 S6 audit

None of #5/#2 left over — both fully shipped + e2e + live smoked.

## Recommended next bundle: cluster B — /who + /names

**Why first:** mechanical application of the WHOIS template just
shipped. Same shape, additive across `EventRouter` (4 new arms:
352/315 for /who; 353/366 for /names), `Wire.who_bundle/3` +
`Wire.names_bundle/3` (or one shared `Wire.user_list_bundle/4` with
discriminator), `Session.send_who/3` + `Session.send_names/3` facades,
`GrappaChannel.handle_in("who", ...)` + `("names", ...)` bridges,
`slashCommands.ts` `/who #channel` + `/names #channel` parsers,
`compose.ts` dispatch arms, `userTopic.ts` `narrowUserEvent` discriminator
+ store mutation, **shared rendering surface** with WhoisCard
(extend WhoisCard or sister `WhoCard` / `NamesCard` component).

**Estimated:** 8-10 commits matching the WHOIS bundle shape, ~600
LoC including tests + 2 e2e (one per verb).

## Cluster B prep checklist

Before opening worktree `cluster/who-names`:

- [ ] Re-verify NumericRouter pre-seeding: 352/315/353/366 are
      already in `@delegated_numerics` per CP21 S6 (if not, bundle
      that into commit 1).
- [ ] Decide rendering: shared `<UserListCard>` polymorphic on bundle
      kind vs. dedicated `WhoCard.tsx` + `NamesCard.tsx`. WHOIS
      already establishes the precedent for one-card-per-verb; same
      shape per /who + /names is reasonable, but a `<UserListCard>`
      that polymorphs over `{kind: who | names, rows: [...]}` is more
      reuse. Brainstorm with vjt at cluster open.
- [ ] /names rendering: list is a flat nick-with-prefix-sigils set —
      can render alongside MembersPane (same shape) or as its own
      ephemeral list. Suggest the latter: /names is a snapshot answer,
      not a live channel-state mirror.
- [ ] Server-side: per-target accumulator state goes on
      `state.who_pending` + `state.names_pending` (or one
      `state.user_list_pending` with `{kind, target}` key). Mirror the
      WHOIS layout in `Server.handle_call({:send_who, ...})`.

## Other clusters waiting (per CP21 S6 ordering)

- **C** /list dedicated window (LARGE, new `:list` window kind +
  `ListWindow.tsx`). Wait until B done — needs same numeric template
  + fresh window-kind machinery.
- **D** /links tree render in $server (SMALL/MEDIUM). After C, since
  $server window is more stable territory.
- **E** Polish closeouts (#3 ban-mask UserContextMenu, #19 regex
  watchlist, #4 lifecycle event verification).

After clusters B + C + D + E ship, channel-client-polish hits 21/21
SHIPPED — the next-cluster recommendation in `project_channel_client_polish`
memory becomes "MOVE ON: image-upload cluster" per memory
`project_image_upload`.

## Live state

- Branch `cluster/cp20-fix-server-unread` worktree at
  `.worktrees/cp20-fix-server-unread`: deletable (CP20 LANDED).
- Branch `cluster/whois-and-nickclick` worktree at
  `.worktrees/whois-and-nickclick`: deletable (CP21 LANDED).
- `main` at `4d97a74` (post-CP21 merge), pushed.
- Prod at http://voygrappa.bad.ass running on the post-merge image.
- CI runs in_progress on `4d97a74` at writeup time.

## Open

This checkpoint stays `status: active` until cluster B (or whichever
the next-up choice settles on) opens its worktree, at which point S1
of CP22 starts.
