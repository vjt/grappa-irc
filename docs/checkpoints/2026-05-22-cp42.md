---
status: closed
---

# CP42 — REV cluster final buckets, REV-K + REV-Z (2026-05-22)

**Closed 2026-05-22** at 225 lines after S1 (REV-K LANDED). CP43 opens
for REV-Z + flakes-cluster handoff per the rotate-at-200 discipline.
REV-Z carry-forwards (REV-J.5 still deferred; LOW set; baseline flakes)
move to CP43.

## Status

**REV cluster bucket 11 of 11 — REV-K cross-surface naming pay-down
in flight.** CP41 closed at 447 lines (4 sessions: REV-G + REV-H +
REV-I + REV-J). CP42 opens with REV-K (M19 + M20) + optional REV-J.5
bundle (M1+M5 anonymous-volumes with Dockerfile UID prep) and will
close with REV-Z docs sweep.

Standing autopilot mandate continues per `project_post_tmu_full_review_scheduled`
+ `feedback_orchestrator_autonomy` + `feedback_push_autonomy`.
Per-bucket reviewer-loop mandatory. Per-bucket deploy + healthcheck.
LANDED requires literal gate-tail paste (per `feedback_landed_claim_evidence`
+ `feedback_reviewer_gate_evidence`).

## What's deployed RIGHT NOW (REV-J close state)

- **Server**: `e0b8b27` on `origin/main` (REV-J base sweep) + docs
  follow-on `7c87545`. HOT-deployed 2026-05-22 (lib + test only —
  preflight classified HOT, modules reloaded in live BEAM, sessions
  preserved, container ID unchanged). Healthcheck: `ok`.
- **cic bundle**: `DpQoKo_g` (REV-H — unchanged through REV-I + REV-J).

## Carry-forwards from CP41

### REV-J.5 (M1+M5) — Dockerfile UID prep prerequisite
First REV-J attempt at anonymous-volumes hit the named-volume
root-init trap (`feedback_named_volume_uid_trap`). Fix path:
Dockerfile chown of `/app/_build` / `/app/deps` / `/app/.mix` /
`/app/.hex` / `/app/.cache` / `/app/.local` to 1000:1000 BEFORE
COPY layers. Decide at REV-K whether to bundle REV-J.5 here or
ship standalone between REV-K and REV-Z.

### `feedback_deploy_preflight_empty_diff_after_merge` discipline
REV-J was lib-only so the trap didn't bite, but REV-K is expected
COLD (cic bundle + server wire shape changes). Manual preflight
with explicit prev-SHA after local merge is mandatory:
```
scripts/mix.sh run --no-start -e \
  'Grappa.Deploy.Preflight.cli(["7c87545", "HEAD"])'
```

### Earlier REV-A through REV-J carry-forwards remain
See CP41 head of file + CP40 S3 carry-forward section + DESIGN_NOTES
chronological log.

### Baseline flakes (do NOT block LANDED)
- `Grappa.AdmissionTest` ETS-singleton-leak class
- `AdminEventsTest:197` assert_receive class
- cic biome 2 baseline errors + 21 baseline warnings
Triage in post-REV-Z flakes cluster per
`project_post_review_ordering_2026_05_22`.

## REV-K plan — cross-surface naming pay-down

**Closes:** 2 MEDIUM. Both surfaces; COLD (wire shape changes).

### M19 (cross-surface S15)
`mentions_bundle.messages[*].sender_nick:` while sibling
`ScrollbackMessage` uses `sender:`. Server moduledoc flagged this
as deferred drift; per "Total consistency or nothing" should be
paid. **Fix:** Rename `sender_nick` → `sender` everywhere (server
Wire + cic api.ts + cic narrowing + test fixtures); one-touch
breaking change.

### M20 (cross-surface S18)
REST error envelope uses `error:` key; WS Channel uses `reason:`
key for the same conceptual error. cic can't branch on WS error
tokens. **Fix:** Unify on `error:` in both surfaces. Cic-side:
update `reason:` consumers in Channel error narrowing arms;
server-side: emit `error:` from Channel error replies.

### Tests + gates required
- Per-finding ExUnit/Mox updates where applicable (mentions_bundle
  wire-shape test, Channel error reply test).
- `scripts/check.sh` full gate; `scripts/dialyzer.sh` standalone;
  `scripts/bun.sh run check` + `scripts/bun.sh run test`.

### Deploy — COLD (cic bundle + server wire shape)
Manual preflight with explicit prev-SHA after local merge;
`--force-cold` if needed; `scripts/deploy-cic.sh` after server-side
deploy; healthcheck; push.

## REV-Z plan (final bucket after REV-K)

Docs sweep + README closed-clusters entry + LOW liquidation. No
code changes. Final REV bucket. Per `project_post_review_ordering_2026_05_22`,
REV-Z LANDED close-out brief MUST hand off to the **flakes-cluster**
brief (NOT bastille).

★ Post-REV-Z bucket ordering — vjt mandate 2026-05-22 mid-REV-E ★

1. **E2e flake triage + fix** — 45 baseline-fail testnet specs +
   AdmissionTest ETS-leak class + AdminEventsTest:197 class.
2. **wireTypes.ts codegen** — generate cic TS wire types directly
   from server-side `Grappa.*.Wire` typespecs. Closes C1+C2+H1-H4+H6+M19+M20
   structurally; supersedes hand-edits.
3. **Bastille deploy workstream** — GitHub issue #8.

REV-K's M19+M20 manual rename is NOT wasted; the unified `sender:`
/ `error:` names become the server-side typespecs that codegen
consumes.

## REV-K session log

### S1 — REV-K LANDED 2026-05-22 (autopilot, base e4a08bc + reviewer-fix 8070551)

**REV cluster bucket 11 of 11.** Closes 2 MEDIUM from the
2026-05-22 codebase review — cross-surface naming pay-down (M19 +
M20). 17 lib + test + cic files in base sweep; +2 files in
reviewer-fix patch (LOW-1 unit test + LOW-2 docstring softening);
COLD-deployed.

### Findings closed

**M19 (cross-surface S15)** — `mentions_bundle.messages[*]`
wire-shape uses `sender_nick:` field while sibling
`ScrollbackMessage` uses `sender:`. One-touch rename across
server Wire + cic narrowing + cic render + tests. Note
`Message.sender_nick/1` (IRC parser helper for prefix extraction)
intentionally unchanged — different domain.

**M20 (cross-surface S18)** — WS Channel error envelope used
`%{reason: "<token>"}` while REST `FallbackController` uses
`%{error: "<token>"}`. cic push helpers couldn't branch on WS
error token (opaque `unknown` stringified to `[object Object]`).
Unified on `error:` key in both surfaces; 36 server error replies
+ 33 channel test assertions + cic typed `ChannelPushError` +
`channelPushError/1` extractor mirroring `ApiError`'s shape.
Push helpers (pushAwaySet, pushAwayUnset, pushWatchlist*) now
reject with the typed error class carrying the wire `code`.

Per `feedback_no_silent_drops_closed`: pushWatchlist's prior
`reject(err)` of bare unknown was effectively a silent-swallow at
the cic boundary — typed error class closes that gap.

### Reviewer round

Round-1: APPROVE with 3 LOW observations.
- LOW-1: ChannelPushError extractor lacked direct unit test.
  Addressed in reviewer-fix patch — 5 focused unit tests in
  `api.test.ts` covering well-formed, sibling fields, missing
  `error` key, non-object payloads, subclass identity.
- LOW-2: Docstring claim "callers can branch on code" outpaced
  consumer reality (`compose.ts:601` only branches on `ApiError`).
  Addressed in reviewer-fix patch — softened to "FUTURE consumer
  pattern" + explicit note current consumers fall through.
- LOW-3: `info` field duplicates `error` key. Deferred as
  cosmetic.

Round-2: APPROVE — mutate-tested all 5 new tests, all real
assertions. Ship REV-K.

### Gate evidence

`scripts/check.sh` exit-0 (second run; first hit 2 baseline
flakes per `feedback_bahamut_load_flake`; re-run clean):
- `8 doctests, 33 properties, 2424 tests, 0 failures`
- Dialyzer `Total errors: 0, Skipped: 0, Unnecessary Skips: 0`
- Sobelow + Doctor + Credo strict: all green
- Bats: 24/24

`scripts/dialyzer.sh` standalone (per `feedback_dialyzer_plt_staleness`):
- `Total errors: 0, Skipped: 0, Unnecessary Skips: 0`
- `done (passed successfully)`

`scripts/bun.sh run check`: baseline 2 errors + 21 warnings
(unchanged).
`scripts/bun.sh run test`: 89 files / **1640 tests** passing (+5
new from reviewer-fix; +0 vs base sweep).

### Deploy — COLD (--force-cold)

Preflight classified HOT (lib + cic only, no compose / mix /
struct / supervision / migration / nginx). BUT business rule
"wire-shape change to live connected cic sockets is risky" — server
hot-reload would emit NEW shape while connected browser tabs run
OLD bundle until refresh banner clicked. Conservative bias per
`feedback_hot_deploy_preflight` ("in doubt, COLD"): forced cold.

`scripts/deploy.sh --force-cold` → image rebuild + container
recreate. Sessions reset. Healthcheck `ok`. Container Up healthy.

`scripts/deploy-cic.sh` → cic bundle rebuilt, hash `34TrT3jr`
broadcast to live user-topics. Refresh banner auto-prompts on any
surviving tab with old bundle.

Push: `git push origin main` → `e412c17..8070551`.

### Carry-forwards into REV-Z

- **REV-J.5 still deferred** — Dockerfile UID prep prerequisite
  for M1+M5 anonymous-volumes refactor not bundled in REV-K
  (REV-K touched lib + cic only). Standalone bucket between REV-K
  and REV-Z if bandwidth permits, else future infra-polish cluster.
- **LOW-3 cosmetic** — `info` field duplicates `error` key in
  ChannelPushError. Polish opportunity.
- **compose.ts ChannelPushError branching consumer** — wire
  `compose.ts:601` to handle the typed class symmetrically with
  `ApiError`. Bucket-sized; REV-Z or future polish.
- **45 baseline e2e fails + AdmissionTest ETS-leak +
  AdminEventsTest:197 assert_receive** — for post-REV-Z flake-
  triage cluster per `project_post_review_ordering_2026_05_22`.

### Subsequent buckets

REV-Z is the FINAL REV bucket: docs sweep + README closed-clusters
entry + LOW liquidation that fits. No code changes (or trivial
spot-fixes only).

Per `project_post_review_ordering_2026_05_22` (vjt 2026-05-22
mandate), after REV-Z LANDED, next-cluster ordering is:
1. E2e flake triage + fix
2. wireTypes.ts codegen (supersedes hand-edits including REV-K's
   M19+M20 — but REV-K's typespec changes become the SOURCE for
   the codegen; not wasted work)
3. Bastille deploy workstream (GitHub issue #8)

REV-Z LANDED close-out brief MUST hand off to the flakes-cluster
brief (NOT bastille). Standing autopilot mandate continues.
