---
status: active
---

# CP53

Previous: cp52 (PWA manifest id + bastille cic-only deploy path +
XFF loopback-vs-reverse-proxy fix + visitor session sharing +
deploy pipeline incident + scroll-to-bottom button fix + m42 deploy
streamline). Closed at ~290 lines.

## State at checkpoint creation

- prod live on m42 bastille jail (irc.sniffo.org / irc.sindro.me);
  scroll-to-bottom iOS fix confirmed live + working on device.
- No server-side (Elixir) changes pending from cp52's close.
- cic vitest 1740 pass; e2e now runnable locally on the RPi
  (compose v5.0.2 + .dockerignore cert fixes).
- main @ origin clean.

## Next session

Pick from `docs/todo.md` ★ POST-BASTILLE ROADMAP: (1) Voice TTS+STT,
(2) UI polish cluster (mobile-first), (3) PUBLIC OPEN. Codebase
review last 2026-05-22 — due ~2026-06-05 (2wk) or at 12 sessions.
