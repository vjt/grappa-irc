---
status: done
---

# CP67

Previous: cp66 (rotation from the PWA-badge feature, then four bug-fix
sessions all FF-merged + deployed: empty-`/away` un-away fix; the #37/#40
issue sweep + cic biome gate; #27 duplicate NickServ IDENTIFY on login;
and the orphaned-PWA-badge foreground reconcile). Rotated 2026-06-21 —
CP66 crossed the ~200-line rotation threshold at 5 sessions.

## State at checkpoint creation

- **Date**: 2026-06-21
- **Main HEAD at rotation**: `6b7fd84` (OPERATIONS jail-name doc fix).
  The `docs: close session` commit for this rotation lands on top.
- **Tests**: full `scripts/check.sh` green — 8 doctests, 37 properties,
  **2914** Elixir tests, 0 failures; credo `--strict` clean; dialyzer
  0 errors; sobelow 0 Medium+. cic: biome `check` exit 0 (23 pre-existing
  CSS warnings only), **117** vitest files green, `bun run build` (tsc)
  green.
- **Prod (m42 jail, name `grappa`)**: server hot-reloaded `83e6b43`
  (`Grappa.Session.Server` + `Grappa.Visitors.Login`, sessions preserved)
  for #27; cic bundle `BrpmuHuj` (badge-reconcile) broadcast hot, no BEAM
  restart. Public root 200 on sindro.me + sniffo.org.

## Open scope (carried from CP66)

- **Codebase review STILL DUE** — deferred by vjt (token cost) across
  cp65/cp66; now ~30 days / ~36+ sessions past the last review
  (2026-05-22), well beyond both thresholds. Re-flag each `/start`; vjt
  decides when it runs. Bug/deploy fixes stay exempt; FEATURE work sits
  on a red gate.
- **Dogfood pending** — media-viewer round 2, long-press text selection,
  #39 EXIF/ICC-profile, portrait-orientation upload, AND (new this
  session) **PWA badge CLEARS on resume** (badge-orphan fix, bundle
  `BrpmuHuj` — see todo.md Immediate).
- **Issue #51** — deploy decision-lib extraction + docker parity (after
  the codebase review + a jail-deploy soak).
- **Post-bastille roadmap** (vjt 2026-05-27, canonical in `todo.md`):
  (1) Voice TTS+STT → (2) UI-polish cluster → (3) PUBLIC OPEN.

## Session 2026-06-21 (1) — duplicate NickServ IDENTIFY on login (#27) ✅ SHIPPED

Mezmerize (Azzurra) reported the NickServ "risulti identificato" NOTICE
showing **twice** on registered-visitor login. Not a cic render bug —
grappa put `PRIVMSG NickServ :IDENTIFY <pw>` on the wire twice: (1)
`IRC.AuthFSM.maybe_nickserv_identify/1` at 001 RPL_WELCOME (the canonical
site — fires for every `:nickserv_identify` spawn incl. Bootstrap
crash-respawn; staged for the +r MODE observer via
`Session.Server.maybe_stage_pending_password/1`, NOT NSInterceptor, since
the AuthFSM emission bypasses the `send_privmsg` call path), and (2) a
second, redundant `Login.send_post_login_identify/3` post-readiness. A
case-2 visitor is *always* `:nickserv_identify` (no SASL path in
`Visitors.SessionPlan`), so (1) already produced the same NOTICE + +r
MODE before (2) ran.

Fix: delete the post-readiness send + dead helpers
(`send_post_login_identify/3`, `error_tag/1`, `require Logger`). No new
state. Side benefit: that path was the lone site threading the cleartext
password through `Session.send_privmsg`. Strengthened `login_test` to
assert IDENTIFY appears on the wire **exactly once** via a TCP-order
barrier (push `PRIVMSG NickServ :HELP`, wait for it, then count —
`packet: :line` + `active: :once` guarantee earlier lines buffered);
fails on old code (`got 2`), passes after.

- **Commit**: `83e6b43` (fix + DESIGN_NOTES, FF-merged + pushed).
  `Closes #27`.
- **Deploy**: server **HOT** (`/admin/reload`, sessions preserved, 0
  retries).

## Session 2026-06-21 (2) — orphaned PWA icon badge (foreground reconcile) ✅ SHIPPED

vjt reported the home-screen icon badge sticking at "1 unread" after
reading everything; his instinct was "reset the badge on app open."
Pushed back (a client-only reset bounces off the `/me` seed; a blind
reset-to-0 wipes a legit background-arrived badge) and **debugged with
data first**: prod rpc returned `BadgeCount.count/1 == 0` — the server
count was correct, the drift was purely the OS icon-badge surface.

Root cause: two writers, no shared state. The service worker push
handler (`service-worker.ts applyIconBadge`, door #1) calls `setAppBadge`
directly off-signal while backgrounded; the in-page `mountBadgeSync`
effect only re-fires on a signal *change*. Warm foreground reading
0-over-0 → effect never fires → SW-set badge orphaned. Cold launch was
fine (`/me` seed + mount); warm resume had no reconcile point.

Fix (`f4f8012`): `mountBadgeReconcile` adds a `visibilitychange` listener
that re-pulls the authoritative `/me` `badge_count` and `reconcileBadge`
force-applies it, bypassing the signal-equality skip. Reconciles to the
SERVER count (not blind 0) so a genuine background-arrived badge
survives. TDD RED→GREEN unit tests in `badge.test.ts`. Two-agent review:
one comment added (app-lifetime listener rationale); race-jitter +
iOS-`visibilitychange` documented as accepted, self-healing tradeoffs.

- **Deploy**: cic bundle **HOT** (`deploy-m42.sh --cic`), hash
  `BrpmuHuj` broadcast. **DOGFOOD PENDING** — confirm on iOS device the
  badge clears on background→push→foreground→read (not Playwright-able).

## Session 2026-06-21 (3) — OPERATIONS jail-name doc fix ✅ SHIPPED

While running prod rpc for the badge debug, the doc'd `jexec 6` failed
`jail 6 not found` (the grappa jail was JID 3 — JIDs drift on restart).
Corrected `docs/OPERATIONS.md` to reference the jail by NAME
(`jexec grappa` / `pkg -j grappa`) everywhere + a note that `jls` lists
the current map. Docs-only (`6b7fd84`).

## Session 2026-06-21 (4) — own nick change feedback (#61) ✅ SHIPPED

Changing your own nick gave no visible confirmation in cic, worst with
zero channels joined. `EventRouter`'s `:nick` clause fans out a
`:nick_change` row per shared channel — empty for a no-channel
self-rename → no feedback (the `own_nick_changed` state event rotated the
nick silently). Fix: emit one `:nick_change` row on the synthetic
`"$server"` window for a self-rename (`old_nick == state.nick`),
independent of channel membership; cic renders it via the existing line,
no cic change. Reviewer-surfaced: the row counts as a cic "event" (not a
message) in cursor-derived unread until the server tab is viewed — same
as the per-channel self-rename rows already do; the OS/notify badge
ignores presence kinds. Kept on purpose — that indicator IS the
always-visible confirmation #61 wanted. TDD: the two existing tests
encoded the bug (one asserted zero effects, one exactly the per-channel
row); both rewritten.

- **Commit**: `8a46171` (fix + tests + DESIGN_NOTES, FF-merged + pushed).
  `Closes #61`.
- **Gates**: full `scripts/check.sh` green — 2914 tests, 0 failures;
  credo/sobelow/dialyzer clean. One-agent review (surfaced the unread
  comment inaccuracy → comment corrected, behavior kept).
- **Deploy**: server **HOT** (`/admin/reload` reloaded
  `Grappa.Session.EventRouter`, sessions preserved, 0 retries). No cic
  bundle. Deploy cadence for this session: deploy-each-fix-as-it-lands
  (vjt authorized).

## Session 2026-06-21 (5) — sender grade glyph snapshot (#25) ✅ SHIPPED

A user's `@`/`%`/`+` grade glyph was applied retroactively to their past
scrollback lines on a grade change — cic's `prefixFor` derived it live
at render. Fix (issue's direction a): server snapshots the sender's
grade into `meta.sender_prefix` at persist time; cic renders content-row
senders from the frozen value. Server: one capture in
`EventRouter.build_persist` (all inbound content kinds) + mirror in
`Session.Server` outbound (`own_sender_prefix_meta`), shared
`Identifier.member_prefix/1` precedence reducer; `Meta` allowlists the
key + `config/config.exs` Logger metadata (sync test). cic:
`prefixFor` → `nickColor.snapshotSenderPrefix`, keeping live derivation
for presence senders + kick target; absent snapshot → no glyph (old rows
lose their wrong glyph, never a live guess).

- **Commit**: `b24ff9c` (13 files, FF-merged + pushed). `Closes #25`.
- **Gates**: full `scripts/check.sh` green — 2923 Elixir tests, 0
  failures; dialyzer/credo/sobelow clean. cic: biome 0 err, 117 vitest
  files (2065 tests), tsc build. Two-agent review: zero code defects;
  one flagged coverage gap (outbound opped case) → closed with a
  server_test.
- **Deploy**: server **COLD** — the `config/config.exs` Logger-metadata
  edit (required by the known_keys↔metadata sync test) forces cold;
  preflight detected it, daemon respawned, sessions reset + bootstrapped,
  healthcheck passed, 0 retries. Public root 200 on sindro.me +
  sniffo.org. (vjt approved the cold drop.)
- **Behaviour note**: pre-deploy scrollback rows have no snapshot → they
  now render with NO sender glyph (neutral) until they age out; new rows
  carry the frozen glyph.

## Remaining tractable review-exempt bugs (next)

#38 (+k tab stuck after reconnect), #16 (MembersPane stuck loading after
+k JOIN — confirmed DISTINCT from #38, see investigation below), #12
(/msg to non-joined channel → unclose-able window). #63 (iOS keyboard
drops letters) + #46 (Android unread marker) need on-device evidence.

## Session 2026-06-23 — #38 + #16 deep investigation (NO code change)

Investigated both +k bugs with DATA (prod rpc repro + full code trace).
Confirmed they are DISTINCT (#38 = rejoin-failure path, #16 = join-
success-but-members-empty path). Key outcome: **both SERVER sides are
correct and the obvious races are ALREADY fixed in the tree** — which is
why neither could be reproduced from prod state. The residuals are cic-
runtime delivery/timing that static analysis could not pin.

- **#16**: prod repro (fresh keyed JOIN of a +k channel) seeds members
  server-side in <2s → bahamut DOES send 353/366; the issue's "omits the
  burst" hypothesis is FALSE. `members_seeded` rides the per-channel
  topic (server.ex:2519) — the one typed event F1 didn't move to
  Topic.user — but the cold-subscribe race is covered by CP15 B3's
  after_join `push_members_if_seeded` (grappa_channel.ex:1042, skips on
  `:uninitialized`). Topic casing ruled out (both sides canonicalize).
  #16 was reported AFTER those fixes → a residual remains, unpinned.
- **#38**: grappa never persists +k keys (server.ex:1629) so every
  reconnect re-JOINs without key → 475. GET /channels merges autojoin →
  returns the chan `joined:false` → cic renders it greyed-but-live →
  × → DELETE PART (442 is an async scrollback reply, not the HTTP
  resp). BUT the dismiss path is already hardened (send_part force-
  broadcasts channels_changed server.ex:1403; DELETE removes-from-
  autojoin-first + M16 propagate) → × should drop on first click. Could
  not repro stuck. DANGER: do not "fix" via the Sidebar dedup — the
  windowState-wins change was reverted for ghost rows (Sidebar.tsx:131).

**Next (path forward): deterministic e2e reproduction** against the real
testnet bahamut. Templates exist (cp15-b6-pending-to-failed-invite-only
for #38, refresh-on-join-ws-gap-recovery for #16). e2e PASS → bug already
fixed → close + keep as regression guard. e2e FAIL → real repro → fix.
Full brief (fixtures, spec skeletons, the open "which reconnect path
re-joins dynamic +k channels" mechanics question) in
`/tmp/grappa-issue-38-16-handoff.md`. Worktree
`.claude/worktrees/16-members-seeded` has NO code changes (investigation
only; its branch name's "usertopic" fix idea is superseded).

## Session 2026-06-23 (2) — #38 e2e repro + fix SHIPPED, #16 guarded ✅

Followed the handoff's e2e-first plan against the real testnet bahamut
(worktree `e2e/38-16-keyed-channel`, off local main).

- **#16 — already fixed; regression guard added.** The e2e
  (`issue16-keyed-join-members-seed.spec.ts`) PASSED on the untouched
  tree: a peer founds a +k channel, cic `/join`s with the key, members
  seed BOTH on the live JOIN and after a page reload (the deterministic
  cold WS resubscribe → `push_members_if_seeded`). Confirms the
  investigation; closed as already-fixed, spec kept as guard.
- **#38 — reproduced deterministically, then fixed.** Resolved the open
  mechanics: `GET /channels` merges ONLY `autojoin_channels ∪ live
  members` (`last_joined` is spawn-only), so the stuck row needs the +k
  chan in `autojoin_channels`. `resetSubject` can't stage it (its
  `await_autojoin` 504s on the perpetual 475); used the admin
  credentials PATCH (autojoin-only edit = `:left_alone`, no restart) +
  `/disconnect`+Reconnect to fire the 475 → both sidebar sources live.
  The e2e RED'd (row survives ×), confirming the bug. **Root cause:**
  `closeChannelWindow` only `postPart`'d, relying on the self-PART
  scrollback echo (the sole `setParted` caller) to clear `windowState`
  — but that echo never fires for a never-joined (442 no-op PART)
  channel, orphaning `windowState=:failed` → un-dismissable greyed
  pseudo-row once `channelsBySlug` drops the name. **Fix** (`4e455fe`):
  `closeChannelWindow` also `setParted`s — opposite direction from the
  reverted PHASE-1.1 ghost-row arm (clearing emits FEWER pseudo-rows).
  Shared helper → mobile BottomBar × fixed too. General class, not just
  +k. `Closes #38`.
- **Gates**: windowClose unit test (RED→GREEN), tsc clean, full vitest
  2066 green, e2e #38+#16 green — re-verified GREEN after rebasing onto
  origin/main's `07c7f69` (restore-last-window) so the #16 reload path
  is confirmed against the integrated tree. One-agent adversarial review:
  fix sound, dodges the ghost-row regression, only NITs (addressed).
- **Deploy**: cic bundle **HOT** (`deploy-m42.sh --cic`) — cic-only
  change, no server restart. Bundle hash `CivW5jDe` broadcast to all
  live user-topics; public root 200 on sindro.me + sniffo.org.
- **Follow-up (deferred, vjt 2026-06-23)**: +k key persistence so
  autojoin rejoins +k channels automatically (Cloak-encrypted key,
  captured on keyed `/join`). Separate design pass — stores channel
  passwords. Tracked in todo.md.

Remaining tractable bug: **#12** (/msg to non-joined channel →
unclose-able window) — LIKELY the same window-lifecycle class as #38;
re-check the query-close path against the #38 fix before reworking.
