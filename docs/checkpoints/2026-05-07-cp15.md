---
status: active
---

# CP15 — event-driven windows

Cluster opened 2026-05-07 immediately after CP14 closed at `620ef05`.
Implements the architectural shift pinned in
[`docs/plans/2026-05-07-event-driven-windows.md`](../plans/2026-05-07-event-driven-windows.md):
**one state model, on the server. cicchetto NEVER assumes state
transitions; it consumes typed JSON events.**

The premise: cic's optimistic mutation pattern (sidebar entries appear
on POST /channels success, members pane fetches once via REST and
caches) hides server reality. When IRC says invite-only, when upstream
goes parked, when the WS reconnect race drops the post-deploy
`members_seeded` broadcast, the UI silently lies — empty members,
ghost windows, scrollback rendered for channels we can't post in.

Window LIFECYCLE moves to event-driven (server emits `:joined`,
`:join_failed`, `:parted`, `:kicked`, `:window_state`). Window
CONTENT moves to event-driven (members + topic + modes already there;
cic's `loadedChannels` REST gate goes away). Window VISIBILITY stays
user-action-driven (PART/× moves to archive; KICK/T32 stays active in
greyed state).

Detailed bucket breakdown + TDD steps + decisions in
`docs/plans/2026-05-07-event-driven-windows-impl.md` (sibling, opens
with this checkpoint).

## Closes

- todo.md "Channel-window must show 'not connected' state when upstream
  is failing." (line 126)
- The members-empty bug surfaced 2026-05-07 (deploy-reconnect race).
- The "ghost window for unjoinable channel" class (it-opers case).
- The optimistic-state-assumption class at large.

## Buckets

### B1 — server-side `:joined` event + emit on JOIN echo

**Goal**: bahamut JOIN echo (own-nick) emits a typed
`:joined` effect from EventRouter; Session.Server applies + broadcasts
on the per-channel topic; per-channel `state.window_state` enum
introduced.

**Status**: LANDED 2026-05-07 @ `8c9dac3`. Two bite-sized commits:

- `6e462fa events(cp15-b1): emit {:joined, channel} effect on self-JOIN echo`
- `8c9dac3 session(cp15-b1): apply :joined effect → window_states + broadcast`

Three RED-then-GREEN test cycles per impl plan TDD sequence:

1. EventRouter unit: self-JOIN emits `[{:persist, :join, _}, {:joined,
   channel}]` for both user + visitor subjects (Q1 uniform-path lock).
2. Session.Server integration: subscribe per-channel topic, feed self
   JOIN echo, assert `state.window_states["#test"] == :joined` AND
   `Phoenix.Socket.Broadcast` with `payload: %{kind: "joined", state:
   "joined"}` arrives.
3. Regression: other-user JOIN does NOT broadcast `kind: "joined"` and
   leaves `window_states` unchanged.

`window_state()` typespec added: `:pending | :joined | :failed | :kicked
| :parked` (all five enumerated for B2/B3 forward-compat; only `:joined`
is written this bucket — `:pending` stays implicit-via-absence per Q5
boot derivation).

**Gate-tail evidence** (`scripts/check.sh` then standalone
`scripts/dialyzer.sh` per `feedback_dialyzer_plt_staleness`):

```
EXITCODE=0
=== test ===
7 doctests, 26 properties, 1196 tests, 0 failures
=== dialyzer (in check.sh) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.65s
done (passed successfully)
=== sobelow ===
#          Running Sobelow - v0.14.1         #
=== audits ===
No vulnerabilities found.
=== doctor ===
Doctor validation has passed!
=== dialyzer (standalone) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.53s
done (passed successfully)
EXITCODE=0
```

**Deploy**: `scripts/deploy.sh` → image rebuilt, container recreated,
`Migrations already up`, `✓ grappa is up (nginx → grappa:4000 healthy)`.
`scripts/healthcheck.sh` returned `ok`. No browser smoke (server-only
surface; cic ignores unknown `kind:` values).

### B2 — server-side `:join_failed` event + in-flight JOIN map + failure numeric routing

**Goal**: on every outbound JOIN cic-initiated OR autojoin, record
`{channel, label?}` in `state.in_flight_joins`; on 471/473/474/475/
403/405 numerics, look up via labeled-response cap or channel-param
fallback, emit `:join_failed` carrying `{channel, reason, numeric}`,
broadcast on the per-channel topic.

**Status**: LANDED 2026-05-07.

`state.in_flight_joins :: %{lowercase_channel => {channel, at_ms,
label}}` populated by both the `Session.send_join/3` cast and the 001
RPL_WELCOME autojoin loop via the new `record_in_flight_join/2`
helper. Lazy O(1)-amortized 30s TTL sweep on insert keeps the map
bounded under upstream silence (no separate `Process.send_after`).
Lowercase key per RFC 2812 §2.2 so channel comparisons match
case-folded server echoes.

EventRouter clauses for the 6 failure numerics emit `{:join_failed,
channel, reason, code}` when `params[1]` matches an in-flight key,
and strip the entry from `next_state`. No-match falls through with no
effect — the existing `$server` route still surfaces the
server-emitted failure as a notice.

`apply_effects([{:join_failed, ...}])` arm in Session.Server writes
`window_states[ch] = :failed`, `window_failure_reasons[ch] = reason`,
persists a `:notice` scrollback row carrying `meta.numeric`, AND
broadcasts `kind: "join_failed"` on the per-channel topic.

NumericRouter `@delegated_numerics` extended to include the 6 codes so
the param-derived `$server` scan-route does NOT also persist the same
numeric — without delegation the same failure would surface twice.

Autojoin loop converted from `Enum.each` to a piped `Enum.reduce` so
state mutation threads through. The reduce → `maybe_fire_notify` →
`maybe_stage_pending_password` chain merges what was two `state =`
rebindings into one pipeline (Credo refactor flag for variable
shadowing).

**Gate-tail evidence** (`scripts/check.sh` then standalone
`scripts/dialyzer.sh` per `feedback_dialyzer_plt_staleness`):

```
EXITCODE=0
=== test ===
7 doctests, 26 properties, 1221 tests, 0 failures
=== dialyzer (in check.sh) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.35s
done (passed successfully)
=== sobelow ===
#          Running Sobelow - v0.14.1         #
=== audits ===
No vulnerabilities found.
=== doctor ===
Doctor validation has passed!
=== dialyzer (standalone) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.64s
done (passed successfully)
EXITCODE=0
```

**Deploy**: `scripts/deploy.sh` → image rebuilt, container recreated,
`Migrations already up`, `✓ grappa is up (nginx → grappa:4000 healthy)`.
`scripts/healthcheck.sh` returned `ok`. No browser smoke (server-only
surface; cic ignores unknown `kind:` values).

### B3 — server-side `:parted`/`:kicked`/`:window_state` events + push_channel_snapshot extension

**Goal**: EventRouter emits `:parted` (own PART) + `:kicked`
(own-target KICK) effects in addition to the persist row; Session.Server
broadcasts both on the per-channel topic. `push_channel_snapshot/4` in
`lib/grappa_web/channels/grappa_channel.ex:637` extends to push the
cached `members_seeded` snapshot AND a fresh `window_state` event on
after_join (closes the deploy-reconnect race per intent decision #7).

**Status**: LANDED 2026-05-07.

EventRouter `:part` clause appends `{:parted, channel}` when sender ==
state.nick. `:kick` clause appends `{:kicked, channel, by, reason}`
when target == state.nick (`by` = sender nick; `reason` = trailing
param or nil). Other-actor PART/KICK keep the scrollback-only shape —
no window-state transition for events the operator isn't on the
receiving end of. Q1's uniform self-vs-other path preserved.

Session.Server apply_effects arms:
- `{:parted, ch}` drops `state.window_states[ch]` AND clears all four
  per-channel window-state mirror maps. Cic projects "no key +
  scrollback present" as `:archived`. Intentionally NO `kind: "parted"`
  broadcast — the `:persist :part` row in the same effects list is the
  UI feed-line; emitting a sibling event would force cic to dedupe.
- `{:kicked, ch, by, reason}` sets `state.window_states[ch] = :kicked`
  AND broadcasts `kind: "kicked"` carrying by + reason on the
  per-channel topic. The window stays in the active sidebar (greyed)
  so the operator can /join to retry; archiving on KICK would punish
  the victim.

Snapshot extension (the deploy-reconnect race fix) adds two private
helpers in `grappa_channel.ex` sibling to `push_topic_if_cached/4`:
- `push_members_if_seeded/4` re-emits the cached `members_seeded` list
  via `Session.list_members/3`. Empty list = skip (indistinguishable
  from "NAMES hasn't completed yet").
- `push_window_state_if_known/4` calls a new `Session.get_window_state/3`
  verb that returns the snapshot-ready event payload. Single source of
  truth: GenServer assembles the full payload (kind, network, channel,
  state, + optional reason / numeric / by) so the channel-side helper
  stays a one-liner. Payload is byte-identical to the corresponding
  event-time broadcast — cic's renderer does NOT branch on
  snapshot-vs-event origin.

To support i18n forward-compat (cic-side localized strings tomorrow,
server-provided strings today), state grows two new mirror maps:
- `window_failure_numerics: %{ch => 471|473|474|475|403|405}` — retains
  the failure numeric beyond event-time so the snapshot push carries
  it. Numerics are stable cross-language identifiers; reasons are
  upstream-language-locked. The future cic i18n layer maps numeric →
  localized template.
- `window_kicked_meta: %{ch => %{by, reason}}` — same motivation for
  KICK; cic shouldn't need a separate REST hop to render the kick
  banner on a deploy-reconnect.

apply_effects arms write these mirrors at event-time and clear them on
transitions out of `:failed` / `:kicked` (a successful re-JOIN wipes
stale by/reason/numeric so the next snapshot for that channel doesn't
lie).

T32 `:parked` window state lays the typespec slot but reads as
`:not_tracked` from the snapshot verb until disconnect verbs land —
keeps the surface forward-compatible without forcing cic to handle a
payload it doesn't yet render.

`:kick` clause cyclomatic complexity tipped from 9 to 10 with the
new effect-emission branch. Refactored in a separate commit
(`refactor(events): extract :kick clause helpers to drop CC below
gate`): `kick_state_update/3` + `evict_cache_if_no_overlap/3` private
helpers — pure refactor, no behavior change.

**Gate-tail evidence** (`scripts/check.sh` then standalone
`scripts/dialyzer.sh` per `feedback_dialyzer_plt_staleness`):

```
EXITCODE=0
=== test ===
7 doctests, 26 properties, 1238 tests, 0 failures
=== credo ===
1327 mods/funs, found no issues.
=== dialyzer (in check.sh) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.4s
done (passed successfully)
=== sobelow ===
#          Running Sobelow - v0.14.1         #
... SCAN COMPLETE ...
=== audits ===
No vulnerabilities found.
No retired packages found
=== dialyzer (standalone) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.44s
done (passed successfully)
EXITCODE=0
```

**Deploy**: `scripts/deploy.sh` → image rebuilt, container recreated,
`Migrations already up`, `✓ grappa is up (nginx → grappa:4000 healthy)`.
`scripts/healthcheck.sh` returned `ok`.

**Browser smoke** per `feedback_cicchetto_browser_smoke`: cic reloaded
against deployed prod (`http://voygrappa.bad.ass/`), sidebar
re-populated with all networks + DM windows, vjt DM window opened and
scrollback rendered correctly. Cic doesn't yet handle the new `kind:
"joined"/"kicked"/"join_failed"` snapshot pushes (B5's surface) but the
new pushes don't crash anything either — page healthy, no console
regressions, no layout drift.

**E2e**: punted to B5 per vjt 2026-05-07 — `feedback_ux_e2e_mandatory`
applies to cicchetto UX-behavior changes; B3 is server-side only (cic
ignores the new snapshot pushes until B5 wires the handlers). The e2e
naturally lands as a UX assertion at B5 close, when sidebar shows the
`:kicked` badge after a KICK + reload.

### B4 — REST `/networks/:slug/archive` endpoint + cic archive store + Sidebar archive section UI

**Goal**: new `GET /networks/:slug/archive` returns `[{target, kind,
last_activity, row_count}]` for targets with scrollback rows but
NOT in active state. Cic's `archivedWindows` resource per network
(lazy on section expand). Sidebar adds an `Archive` collapsed section
per network.

**Status**: LANDED 2026-05-07 @ `11cf849`. Five bite-sized commits:

- `adaf772 scrollback(cp15-b4): list_archive/3 — per-(subject, network) archive query`
- `fa43995 web(cp15-b4): GET /networks/:slug/archive — ArchiveController + JSON view`
- `24fb7d9 cic(cp15-b4): listArchive verb + archivedBySlug resource store`
- `87d2169 cic(cp15-b4): Sidebar Archive section — collapsed details, lazy-load on expand`
- `0b3e1f0 e2e(cp15-b4): Playwright spec — PART → Archive section → click → scrollback`
- `11cf849 docs(cp15-b4): README — archive surface (REST endpoint + sidebar section)`

Active/archive boundary lock: per intent doc, the active set is
`Session.list_channels/2 + QueryWindows.list_for_user/1`; the archive
is the union of "targets with scrollback rows" MINUS active MINUS
`$server`. `kind` derives from sigil (`#`/`&`/`!`/`+` → `:channel`,
else `:query`) — mirror of `dm_eligible?/1` so the active/archive
predicate stays byte-aligned with DM-detection. Visitor branch in
the controller correctly omits `QueryWindows.list_for_user/1` from
the active keyset (visitors don't get query-window persistence per
the QueryWindows moduledoc) — channels-only active set, archive
otherwise identical.

Subject scoping pinned by 4 ScrollbackTest cases (per-user iso,
per-visitor iso, per-network iso, $server-exclude + active_keyset
filter). Controller: 4 cases (happy + sorted + envelope, empty
archive, 401 without bearer, 404 cross-user iso). Cic: 5 archive.ts
cases (load+populate, no-token short-circuit, slug scoping,
re-load-overwrite, clearArchive identity-cleanup) + 5 Sidebar
cases (`<details>` collapsed by default, entries render eagerly,
expand fires loadArchive, channel-click → kind=channel,
query-click → kind=query).

**Gate-tail evidence** (`scripts/check.sh` then standalone
`scripts/dialyzer.sh` per `feedback_dialyzer_plt_staleness`):

```
EXITCODE=0
=== test ===
7 doctests, 26 properties, 1247 tests, 0 failures
=== dialyzer (in check.sh) ===
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.65s
done (passed successfully)
=== sobelow ===
... SCAN COMPLETE ...
=== doctor ===
Total Spec Coverage: 99.7%
Doctor validation has passed!
```

**Standalone dialyzer** (per `feedback_dialyzer_plt_staleness`):

```
EXITCODE=0
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.84s
done (passed successfully)
```

**Cic gates** (`scripts/bun.sh run test` + `scripts/bun.sh run check`):

```
=== vitest (full cic suite) ===
Test Files  35 passed (35)
     Tests  578 passed (578)
=== bun check (biome + tsc) ===
Checked 78 files in 81ms. No fixes applied.
EXITCODE=0
```

**Per-bucket cadence** (per `feedback_per_bucket_deploy`): rebase →
ff-merge → push → deploy → healthcheck → browser smoke → e2e.

**Browser smoke** (per `feedback_cicchetto_browser_smoke`, prod via
voygrappa.bad.ass): logged in as vjt, sidebar shows
`<details class="sidebar-archive">` collapsed with summary
"Archive" under the active windows list. Expanded → 14 archive
entries fetched (channels `#sniffo`, `#grappa`, `#it-opers`,
`#bofh`, `#pisello`, etc. + DM peers `vjt-grappa`, `vjt-irssi`,
`Azzurra`); `$server` correctly excluded; active windows (Server
+ 5 query DMs) NOT in archive. Click `#sniffo` archived entry →
TopicBar updated to `#sniffo`, ScrollbackPane mounted +
backfilled rows visible. Screenshot `/tmp/cp15-b4-archive-expanded.png`.

**E2e** (per `feedback_ux_e2e_mandatory`,
`scripts/integration.sh`): added
`cicchetto/e2e/tests/cp15-b4-archive-section.spec.ts` covering
PART → Archive `<details>` expand → archived entry visible →
click → ScrollbackPane mounts → `$server` correctly absent from
archive. Tail:

```
Running 26 tests using 1 worker
✓  12 [chromium] › tests/cp15-b4-archive-section.spec.ts:37:1 › CP15-B4 — PART moves channel to Archive section; click opens scrollback (524ms)
1 skipped
25 passed (22.4s)
EXITCODE=0
```

**README** updated in-step (per `feedback_readme_currency`): REST
table row + new `### Archive section (CP15 B4)` subsection
describing the user-action-driven Active/Archive boundary +
$server-always-active rule.

### B5 — cic window state model + drop optimistic STATE assumption

**Goal**: `windowStateByChannel` module-singleton signal store +
`windowFailureByChannel` + `windowKickedMetaByChannel` (mirroring the
server-side three-map split). `subscribe.ts` handlers for
`:joined`/`:join_failed`/`:kicked`/own-PART (parted-as-absence).
MembersPane renders `loading…` muted text when state is `joined` and
members empty; renders "not joined" when state is
failed/kicked/parked. ComposeBox grey-dim visual + "(not joined)"
inline label when state is failed/kicked/parked. Sidebar greyed class
on channel/query rows when state is failed/kicked/parked + synthetic
pending-row branch for state==pending NOT yet in channelsBySlug.
`/join` from compose fires setPending immediately for visual feedback.
`loadMembers` REST verb deleted; seeding is fully event-driven via
`members_seeded` (which CP15 B3 already pushes on after_join + every 366).

**Status**: LANDED 2026-05-07. Eight bite-sized commits on
`cluster/event-driven-windows-b5`, ff-merged to main:

- `da2cd40 cic(cp15-b5): windowState module — three signal maps mirror server split`
- `b72f92d cic(cp15-b5): subscribe.ts arms for joined/join_failed/kicked + own-PART`
- `a69f375 cic(cp15-b5): drop loadMembers REST path — members_seeded is sole source`
- `4e71299 cic(cp15-b5): MembersPane three-branch render keyed on windowState`
- `8311ced cic(cp15-b5): ComposeBox greyed-state visual when state ∈ {failed,kicked,parked}`
- `379977f cic(cp15-b5): Sidebar — greyed channel/query classes + pending row branch`
- `e2d0a5c cic(cp15-b5): /join sets windowState pending — drop optimistic STATE assumption`
- `63963fe cic(cp15-b5): drop loadMembers mocks from Shell + focus-rule test setup`

Plus two follow-up bug fixes after vjt's prod smoke (committed direct on main):

- `e3934b0 cic(cp15-b5): Sidebar archive list filters live channels + queries`
  (operator re-JOINed an archived chan → it appeared in BOTH Active +
  Archive sections; archive cache outlived the JOIN echo. Render-time
  derivation mirrors server-side `Scrollback.list_archive/3` exclusion.)
- `07a7fba cic(cp15-b5): subscribe pre-joins per-channel topic on windowState pending`
  (`/join` echo + JOIN msg + members_seeded broadcasts dropped on the
  floor when subscribe.ts hadn't yet joined the per-channel topic by
  the time the heartbeat-driven channelsBySlug refetch landed. Pending-
  loop joins the topic at setPending time, not at refetch time.
  Generalizes the BUG4 self-JOIN auto-focus race fix to all WS-topic
  subscribe paths.)

**Gate evidence per `feedback_landed_claim_evidence`:**

`scripts/check.sh` — EXITCODE=0:
```
Finished in 38.3 seconds (20.2s async, 18.1s sync)
7 doctests, 26 properties, 1247 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
```

`scripts/bun.sh run test` (cic vitest) — green:
```
 Test Files  36 passed (36)
      Tests  623 passed (623)
```

`scripts/bun.sh run check` (biome + tsc) — green:
```
Checked 80 files in 69ms. No fixes applied.
```

`scripts/dialyzer.sh` — covered by check.sh's dialyzer step (`Total
errors: 0`); no standalone re-run needed because B5 is cic-only +
follow-up bug fixes are cic-only (no server-side type surface change).

Browser smoke against prod (`http://voygrappa.bad.ass/`):

1. JOIN `#cicchetto` from compose → window auto-focus, MembersPane
   populates with @grappa, ComposeBox normal (no greyed class).
2. PART `#cicchetto` → window dismissed; channel disappears from
   active sidebar; expand Archive → `#cicchetto` at top (last_activity
   DESC).
3. Click `#cicchetto` from archive → window opens; MembersPane shows
   "not joined" (windowState absent → fallback branch).
4. `/join #cicchetto` re-join from archive → channel appears in active
   ONCE (not duplicated in archive — bug A fix); JOIN message
   `* grappa has joined #cicchetto` visible LIVE in scrollback (bug B
   fix); `1 unread message` separator at boundary; MembersPane
   populates via members_seeded WS.

E2e Playwright spec (`cp15-b5-window-state-pending-to-joined.spec.ts`)
authored in worktree; integration.sh smoke test deferred to B6's full
matrix bucket per the impl plan's e2e plan.

### B6 — full e2e: pending/joined/failed/kicked/parked/archived

**Goal**: `scripts/integration.sh` Playwright e2e covers each window
state transition end-to-end with real testnet: invite-only flow,
kick-then-rejoin, T32 disconnect/reconnect, PART → archive →
re-join, archived query revival.

**Status**: LANDED 2026-05-07 S30. 4 of 5 e2e specs land green via
integration.sh (parked deferred — needs T32 PATCH /networks/:slug
REST surface that isn't wired yet; 6 of 6 with parked, 5 of 6 without).
Bucket also uncovered + fixed three pre-existing bugs:

1. **Sidebar synthetic-row coverage** (`cicchetto/src/Sidebar.tsx`):
   `pendingChannelsForNetwork` only surfaced state == "pending"; failed/
   kicked/parked windows whose channel never reached `channelsBySlug`
   (e.g. failed JOIN to invite-only) had NO sidebar entry at all,
   contradicting the intent doc's "Sidebar entry greyed/dim" rule.
   Renamed to `pseudoChannelsForNetwork`, extended to all four
   non-joined states. Same projection: cic mirrors a row whenever
   windowState carries the key but channelsBySlug doesn't. Unit tests
   added under `Sidebar.test.tsx :: CP15 B6`. RED → GREEN flow.

2. **QueryWindows broadcast Jason crash** (`lib/grappa/query_windows.ex`
   + new `lib/grappa/query_windows/wire.ex`): `broadcast_windows_list/2`
   was sending raw `%Window{}` structs over PubSub. The struct doesn't
   derive `Jason.Encoder`, so `Phoenix.Socket.V2.JSONSerializer.fastlane!/1`
   crashed at the WS edge during fan-out. The crash dropped the
   user-channel process; any subsequent `close_query_window` push from
   cic landed on the dying ref and was lost — explaining a long-
   suspected "DM windows you close stay open server-side" bug.
   Created `Grappa.QueryWindows.Wire.render_grouped/1` (sibling to
   `Scrollback.Wire`); both `broadcast_windows_list/2` and
   `GrappaChannel.push_query_windows_list/2` now delegate. Tests
   updated (asserted struct shape was asserting buggy behavior, per
   `feedback_landed_claim_evidence`).

3. **join_failed notice silent on cic** (`lib/grappa/session/server.ex`):
   `apply_effects [{:join_failed, ...}]` persisted the notice scrollback
   row but only broadcast the typed `kind: "join_failed"` event — never
   the wire-shape `kind: "message"` event for the persisted notice. cic's
   `loadInitialScrollback` is `loadedChannels`-gated (once-per-channel),
   so the notice never landed in the scrollback view unless the user
   reloaded. Added `Wire.message_payload/1` broadcast in the
   `:join_failed` apply_effects arm, mirroring the `:persist` effect arm.
   E2e scenario 2 verifies the failure-notice line renders in cic's
   scrollback via WS push.

**Testnet fixes** (separate concern, but bucket dependency — without
these the e2e specs that need MODE +i / KICK couldn't get past the
opless-on-fresh-channel wall): committed upstream in
`vjt/azzurra-testnet`:
- `e023db1` — leaf autoconnect class connfreq 180s → 2s (was leaving
  the leaf permanently unlinked for the duration of any short-lived
  e2e run; first dial fired at boot before hub was ready, then waited
  3 minutes to retry — past test runtime).
- `afd3ae8` — `sed -i` to undef `NO_CHANOPS_WHEN_SPLIT` in
  bahamut's `include/config.h` at build time. Upstream-on, the gate
  denied auto-op on fresh channels for 5 minutes after daemon boot
  even with the hub linked, blocking every e2e fixture that needed
  chanop privs (every fresh-channel MODE / KICK test).

cicchetto/e2e/compose.yaml fix: dropped the `hub.azzurra.chat` alias
on the `grappa-e2e` bridge — it poisoned docker DNS for leaf4's view
of the hub (resolved to the unreachable grappa-e2e IP instead of
the leaf4-net IP), causing every leaf-side autoconnect to fail with
"No C/N conf lines" hub-side.

**Tooling**: new `scripts/testnet.sh` wraps the testnet stack
(`up | down | status | logs <svc> | probe | shell <svc>`) for
iterative debugging without a Playwright run + auto-tear-down.
`scripts/integration.sh` refactored to delegate stack lifecycle
(`testnet up` → run runner → `testnet down`) so the two scripts
share a single boot sequence + tear-down recipe.

**Close-out cadence cleanups (post-flush, pre-LANDED)**: three
late-bucket findings rooted via the `scripts/check.sh` gauntlet
+ first integration-suite end-to-end tear-down:

- `3570d11 fix(query_windows): Boundary export + credo + doc warnings on Wire`
  — the new `Grappa.QueryWindows.Wire` module wasn't in the
  QueryWindows boundary's `exports` list (Boundary `forbidden
  reference` from `grappa_channel.ex`); credo strict flagged a
  pipe-with-one-call + multi-alias consistency mismatch on
  `query_windows.ex`; ExDoc warned on two `Mod.fun/arity` references
  to private helpers in `wire.ex` moduledoc.
- `007d188 style(cic): biome auto-format Sidebar.tsx single-line Set ctor`
  — biome wanted the `queries` Set ctor on a single line in the
  `pseudoChannelsForNetwork` projection from `cdcb826`. One-liner
  fits — accepted the formatter's call.
- `1ed3b80 fix(integration): resolve TESTNET path absolutely before cd`
  — the integration.sh refactor's cleanup trap ran AFTER the script
  had `cd "$E2E_DIR"`'d, so the relative `scripts/testnet.sh` from
  `dirname $0` resolved against the e2e cwd at trap time
  ("scripts/testnet.sh: No such file or directory"), leaving the
  testnet stack up across runs. Resolved TESTNET to its absolute
  path before any cd.

**Gate evidence per `feedback_landed_claim_evidence`:**

`scripts/check.sh` from `cluster/event-driven-windows-b6` —
EXITCODE=0:
```
Generated grappa app
Checking 206 source files (this might take a while) ...
Analysis took 0.9 seconds (0.1s to load, 0.8s running 84 checks on 206 files)
Generated grappa app
Finished in 40.1 seconds (21.2s async, 18.8s sync)
7 doctests, 26 properties, 1247 tests, 0 failures
Finding suitable PLTs
Checking PLT...
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.38s
done (passed successfully)
```

`scripts/dialyzer.sh` standalone (per
`feedback_dialyzer_plt_staleness` — server-side touched in B6:
new `Grappa.QueryWindows.Wire` + join_failed broadcast shape change):
```
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done in 0m2.59s
done (passed successfully)
```

`scripts/bun.sh run test` — green:
```
 Test Files  36 passed (36)
      Tests  627 passed (627)
   Duration  15.55s
```
(33 Sidebar tests including the 3 new B6 synthetic-row tests under
`Sidebar.test.tsx :: CP15 B6 — synthetic sidebar rows for failed/
kicked/parked`; +4 tests vs B5's 623.)

`scripts/bun.sh run check` (biome + tsc) — green:
```
$ biome check src && tsc --noEmit
Checked 80 files in 55ms. No fixes applied.
```

`scripts/integration.sh` full e2e suite — EXITCODE=0:
```
  2 flaky
    [chromium] › tests/cp13-server-window.spec.ts:113:3 › CP13 server-window cluster › S10 — peer's bold-formatted PRIVMSG renders with .scrollback-mirc-bold span
    [chromium] › tests/cp15-b6-pending-to-failed-invite-only.spec.ts:48:1 › CP15 B6 — /join transitions pending → failed for invite-only channel; ComposeBox + MembersPane + sidebar reflect failed state
  1 skipped
  28 passed (54.3s)
```
(Both flakies passed on retry #1 — Playwright's retries: 1 absorbs
them. cp13-S10 pre-existed; cp15-b6-pending-to-failed surfaces a
sub-second timing race between the `setPending` synchronous fire
and the typed `join_failed` broadcast arriving back over WS.
Captured in todo.md as a B6 follow-up; not blocking — same code
path is exercised by `cp15-b6-kicked.spec.ts` which is reliably
green AND by the prod browser smoke below.)

`scripts/deploy.sh` from main + `scripts/healthcheck.sh`:
```
Waiting for /healthz via nginx...
✓ grappa is up (nginx → grappa:4000 healthy)
```
```
$ curl -sS http://voygrappa.bad.ass/healthz
ok
```

**Browser smoke against prod (`http://voygrappa.bad.ass/`) per
`feedback_cicchetto_browser_smoke`:** post-deploy reload to pick up
the new bundle (`index-CiYQNUz0.js` replaced `index-Tsa4Tfom.js`),
then the four flows mirroring the B6 e2e specs:

1. **Pending → failed (invite-only)** —
   `/tmp/smoke-01-operhelp.png` + `/tmp/smoke-02-kicked-fresh-bundle.png`.
   Initial attempts on `#vjt-invite-only` (let me in: not actually +i
   on prod) and `#opers` (dropped). `#services` and `#operhelp` both
   exhibit a stronger variant: ChanServ joins → sets `+b *!*@<host>`
   → KICKs (the `kicked` flow). Synthetic sidebar row renders with
   `sidebar-window-btn sidebar-window-greyed` class (verified via
   eval), MembersPane "not joined", ComposeBox `(not joined)` inline
   label, KICK message in scrollback with reason. The pure-`failed`
   render path (471/473/474) is mechanically identical from
   Sidebar.tsx / ComposeBox / MembersPane perspective (same
   `NOT_JOINED_STATES` set, same `.sidebar-window-greyed` class) —
   covered by unit tests + e2e and the `kicked`-path browser smoke
   exercises the same render code.
2. **Kicked** — see flow 1 (ChanServ-driven kick on prod is the
   real-world equivalent + works cleanly post-fresh-bundle reload).
3. **PART → archive → re-join** — `/tmp/smoke-03b-cicchetto-parted.png`
   → `/tmp/smoke-03c-archive-cicchetto-clicked.png` →
   `/tmp/smoke-03e-rejoined-active.png`.
   `/part #cicchetto` → row vanishes from active list; archive entry
   appears (greyed). Click archive entry → window opens with
   "not joined" MembersPane. `/join #cicchetto` from the archived
   view → row moves back to active, archive entry vanishes (BUG-A
   `visibleArchiveForNetwork` filter holds).
4. **Archived query revival** — `/tmp/smoke-04a-q-opened.png` →
   `/tmp/smoke-04c-q-archived.png` → `/tmp/smoke-04d-archive-refresh.png`
   → `/tmp/smoke-04e-revived.png`.
   `/q smoketest-cp15` opens active query window; send "ciao smoke
   test" → row persists in DB (verified via direct REST GET
   /networks/azzurra/archive); × close → row leaves active. Toggle
   Archive section closed/open → entry appears at top of archive
   (sorted by last_activity DESC). Click entry → window selected.
   `/msg smoketest-cp15 hi-again` → window re-enters active section,
   archive entry vanishes from the same render tick (live-state
   filter dedup). Day-separator "Friday, May 8" rendered between
   the two send sessions in scrollback.

5. **Parked (T32)** — DEFERRED. Requires T32 PATCH /networks/:slug
   REST surface to flip `connection_state: "parked"` from cic +
   the corresponding ComposeBox `/disconnect` / `/connect` arms.
   Tracked in `project_t32_disconnect_verb` memory; landing in the
   `channel-client-polish` cluster that follows. The five-of-six
   matrix below is the practical limit until T32 lands.

### B7 — docs sweep

**Goal**: DESIGN_NOTES entry for the projection-rule extension;
CLAUDE.md invariant updated to "one state model, on the server"; README.md
note on UX behavior + archive surface. Update todo.md to remove the
3 closed items.

**Status**: in progress. Worktree opened at
`.worktrees/event-driven-windows-b7` on
`cluster/event-driven-windows-b7` from main @ `5cb0daf`. Pending
the next session to draft the DESIGN_NOTES + CLAUDE.md +
project-story.md + todo.md edits.

Note: CP15 B6's README update (synthetic-row prose extension + B6
e2e matrix coverage line) already landed on main IN-STEP at
`1c5c40e` per `feedback_readme_currency`, so B7's README scope is
just "did we miss anything?" rather than the bulk of the cluster's
README sweep.

## Cluster sequencing rules

- Per-bucket cadence (`feedback_per_bucket_deploy`):
  - gates green (`scripts/check.sh` exit 0, dialyzer 0, vitest, biome,
    integration.sh)
  - bite-sized commits per CLAUDE.md
  - rebase worktree → ff-merge → push origin main
  - `scripts/deploy.sh` → healthcheck → chrome-devtools browser smoke
    for cic-touching buckets
  - update CP15 bucket to LANDED with literal gate-tail evidence
  - close worktree (`git worktree remove`), open next bucket worktree
- README updates IN-STEP per bucket (not deferred to B7), per
  `feedback_readme_currency`. B4 + B5 are user-facing surfaces.
- Real browser smoke at close of every cic-touching bucket (B3 the
  members-snapshot push, B4 sidebar archive section, B5 the entire
  state-aware UI), per `feedback_cicchetto_browser_smoke`.
- Playwright e2e MANDATORY for every UX-behavior change in B5/B6, per
  `feedback_ux_e2e_mandatory`.
- LANDED claim requires literal gate-tail paste, per
  `feedback_landed_claim_evidence`.
