---
status: active
---

# CP63

Previous: cp62 (metadata-strip cluster + playback saga, closed).
This session (2026-06-10 evening → 06-11): **the deploy-pipeline
honesty cluster** — what started as "substrate-aware preflight"
(vjt: "TOO MANY COLD DEPLOYS PORCO DIO") unravelled SIX stacked
deploy-machinery defects, each found by the previous fix's own
deployment. All shipped, live-verified, CI green. Main at `c0e37a5`,
pushed, no worktrees.

## Shipped (commits 766018f..c0e37a5, all on prod)

1. **Substrate-scoped preflight** (`766018f`): `classify_paths/2`,
   `classify/5`, `cli([from, to, "docker"|"jail"])` — explicit
   substrate, no default. Class 4a `:image_substrate` (Dockerfile,
   .dockerignore, root `compose.*` PREFIX, bin/start.sh, bin/grappa)
   colds docker only; new 4b `:rc_d` (rc.d/grappa wrapper) colds
   jail only. Deploy orchestrators stay HOT (d8f354c). Substrate
   matrix pinned in test/grappa/deploy/preflight_test.exs.
2. **Review hardening** (`d31fca1`): exit-code case in both
   orchestrators (was: every non-zero → COLD); jail re-exec guard
   was DEAD CODE (`cmp` path-vs-itself) → now diff-range-based +
   `DEPLOY_PREV_SHA` threading; rc.d install delegated to existing
   `jail_install_rcd.sh` between stop/start; `@substrate_strings`
   derived; keepalive wrapper deliberately NOT in 4b (different
   rc(8) service — BEAM restart wouldn't refresh it).
3. **Jail preflight had NEVER produced a verdict** (`c698f00`):
   `mix run` under MIX_ENV=prod evaluates runtime.exs → raised on
   missing DATABASE_PATH (su -l has no env) → exit 1 ≡ COLD on
   every auto deploy since the jail existed. Fixed: env file sourced
   (set -a, jail_release.sh flow); COLD verdict moved exit 1 → 3 so
   crashes can never read as verdicts (`Preflight.exit_code/1`).
4. **CI red since #39 strip** (`56a2914`, vjt flagged): ci.yml bare
   runner lacked exiftool/ffmpeg → apt step; e2e "1×1 PNG" constant
   had a CORRUPT IDAT CRC for months (nothing validated bytes until
   the fail-closed strip 422'd it) → valid PNG verified through real
   MetadataStrip, hoisted to e2e/fixtures/bytes.ts (4 copies → 1).
   Both workflows green from 56a2914 on.
5. **Half-deployed limbo killed** (`371d0fe` + `a5d76f1`): my own
   SIGPIPE (deploy piped through `head`) killed run between release
   and reload → "no commits — nothing to do" on re-run. Fixed:
   `runtime/last-deployed-sha` marker written as FINAL step;
   nothing-to-do requires same-HEAD AND marker match, else
   re-drives. Hot path now also aborts when reload response has
   `"failed":[...]` (HTTP 200 ≠ success). New `Grappa.HotReload`
   context: soft-purge (NOT hard — kills session-holding procs)
   before load; `:not_purged` double-reload live repro pinned.
6. **Uniform load_abs ebin walk** (`0615104` + `c0e37a5`):
   `:code.modified_modules/0` blind to ADDED modules (embedded mode
   → `:undef` 500); OTP 26+ cached code path makes `load_file`
   `:nofile` for ANY post-boot beam file, forever. Final shape: one
   walk of app ebin — not-loaded → load_abs; md5(disk) ≠
   md5(loaded) → soft-purge + load_abs. Deps out of scope (mix.lock
   → COLD by preflight). NO test calls `reload_modified/0`
   (coveralls cover-instruments loaded code — md5 never matches;
   walking real ebin would de-instrument mid-run).

**End-to-end acceptance achieved**: final deploy showed re-exec
guard firing on diff range, env-sourced preflight printing the
jail's FIRST-EVER real verdict ("no unsafe markers → HOT"), honest
reload check, marker re-drive recovering the aborted prior run,
sessions preserved throughout (zero cold restarts all session).

## Dispatch-1 follow-on session (2026-06-11, post-/clear)

**CI verify (TASK 1)**: `4ecadab` ci ✓, `c0e37a5` integration ✓
(10m24s). `c0e37a5` ci shows cancelled = concurrency supersede by
4ecadab's push 3min later — covered, not red.

**cic text-selection bug (orchestrate-queue Dispatch 1) — SHIPPED**
(`6ae5e15`..`dd684f0`, merged, pushed, deployed `--cic` hot, hash
BhVMIcil broadcast, healthz ok, policy selector verified in served
CSS). One symptom, TWO stacked root causes:

1. **Desktop**: `keepKeyboard.ts` document-capture mousedown
   preventDefault (UX-3 preserve-keyboard) fired on ALL platforms —
   mousedown's default action includes starting the selection drag,
   compose autofocus made it permanent. Header comment "No-op on
   desktop browsers" was a lie. Fix: `isIos()` gate in the handler
   (in-handler for test isolation: capture listener has no uninstall
   path). Pinned by `keepKeyboard.test.ts` (TDD red→green).
2. **iOS**: `html.is-ios { -webkit-user-select: none }` — Telegram
   Web K pattern (479b77d) copied WITHOUT Telegram's paired re-enable
   on message text. Fix: selectable-text POLICY BLOCK in default.css
   (`.scrollback`, `.topic-modal-text`, `input`, `textarea`); mentions
   rows + `[Join]` CTA deliberately excluded. Review caught the fix's
   own near-miss: separator/unread labels had only UNPREFIXED
   `user-select:none` (iOS Safari <18.4 parses only `-webkit-`) —
   both forms now mandatory for any user-select in the theme file.

Scoping decided consciously (DESIGN_NOTES 2026-06-11): Android
keyboard-preserve never validated → gate scopes to iOS, widens one
clause if Android dogfood complains; iPad-with-trackpad drag-select
still dead (isIos() deliberately true there) — waits for a complaint.
e2e guard `text-selection-restored.spec.ts` (chromium real drag +
webkit cascade assert). Full suite 225/225 green, vitest 1884, +2
tolerated noDescendingSpecificity biome warnings (same class as the
file's 4 pre-existing).

## Dispatch-2 session (2026-06-11, post-/clear): media-link viewer — SHIPPED

Orchestrate-queue Dispatch 2 (`8c78489`..`a18e7ef`, merged, pushed,
deployed `--cic` hot, hash **CWpQiDkF**, healthz ok). Root cause
VERIFIED before building: embedded upload URLs are same-origin,
manifest has no `scope` key → whole origin in-PWA-scope → iOS
standalone navigates in-scope links IN PLACE (`target=_blank`
ignored) → raw media doc, no chrome, return reloads cic. Cross-origin
links untouched (out-of-scope → Safari view, already fine).

Shipped: `lib/mediaLink.ts` pure classifier (own 📸/🎬 upload URL via
trailing emoji in preceding linkify segment, or same-host
media-extension URL → `{kind, href}` with page-origin-rooted href;
cross-host always null — ZERO CSP changes); `lib/mediaViewer.ts`
identity-scoped store; `MediaViewerModal.tsx` both Shell branches
(X-close, "open in browser", img/video/audio, Escape-while-open,
shared overlay lock); renderRun click intercept (modifier/aux clicks
keep browser semantics; href/copy/middle-click untouched).

**e2e first run caught a REAL prod defect**: `Endpoint.url()` =
`http://irc.sniffo.org` (runtime.exs `url:` had no scheme) — every
upload link ever minted is http:// on the https PWA. Fixed both ends:
runtime.exs PHX_HOST block mints `https://PHX_HOST:443` in ALL envs
when set (e2e compose sets `PHX_HOST: nginx-test`; prod raises when
missing — old grappa.bad.ass fallback minted equally-dead links);
cic matches on HOST and re-roots historical http:// hrefs. The
runtime.exs change is COLD (preflight class 7) — deliberately NOT
deployed; `--cic` doesn't move `runtime/last-deployed-sha` so it
rides the next server deploy's diff range automatically.

Review fixes (`a18e7ef`): `createOverlayLock` extracted (3rd verbatim
copy + latent same-task-close refcount leak fixed for ALL modals,
regression-pinned), identityScopedStore, Escape scoping, modifier
guard, `{kind, href}` API fold, e2e CSP-claim honesty. Full e2e
227/227 (10.2m), vitest 1921, check.sh green, biome 23 warnings =
baseline. Deferred to todo Low: `/uploads/<slug>.<ext>` minting
(emoji signal fails across mIRC-run splits — zero real surface
today), modal-chrome CSS dedup.

## Dogfood-fixes session (2026-06-11, post-/clear): media-viewer round-2 — SHIPPED

vjt's first device dogfood of the media viewer found two defects;
shipped `ede1672`..`3eac193` (merged, pushed, deployed `--cic` hot,
hash **CLK0_RL1**, healthz ok). CI for `a18e7ef`/`495f5a1` verified
green first (a18e7ef ci "cancelled" = concurrency supersede, covered).

1. **"Open in browser" navigated the PWA** — anchor relied on
   target=_blank, which iOS standalone IGNORES for in-scope links (the
   dispatch's own root cause; the shipped comment claiming otherwise
   was never device-verified). Only same-origin escape that exists:
   `x-safari-https://` scheme handoff to real Safari (iOS 17+, inert
   16). Mechanism via review: NEVER rewrite href (v1 did — breaks
   long-press Copy Link); plain-click intercept + same-window
   location.assign. Composed policy `escapePwaHref` + shared handler
   `maybeEscapePwaClick` in platform.ts; `isStandalonePwa()` extracted
   from main.tsx.
2. **No load feedback** — MediaViewerBody subcomponent (keyed remount
   = per-open reset), loading|ready|failed, spinner overlay, failure
   text on error.

**Review panel (7 angles) earned its keep**: (a) ALTITUDE: bug class =
"any same-host link in standalone PWA" — 📄 doc uploads + emoji-split
fallbacks had the identical unfixed defect → ScrollbackPane same-host
non-media clicks now escape too (`sameHostHref` extracted from
classifyMediaLink); (b) transitions gated to leave only `loading`
(mid-playback MEDIA_ERR_NETWORK no longer unmounts a playing element);
(c) `onSuspend` terminates the spinner (iOS Low Power Mode downgrades
preload=metadata → neither loadedmetadata nor error fires → v1 spun
forever); (d) `pointer-events: none` (spinner sat exactly on the
video's native play control, swallowing the tap that starts deferred
loading); (e) shared test platformStubs helper; .muted compose; e2e
load-state phases merged into one journey (abort phase FIRST — cache
hits bypass route interception).

jsdom boundary recorded: window.location unforgeable + unimplemented —
escape decision pinned pure, wiring via boundary mock, the assign line
is device-dogfood territory. x-safari handoff NOT e2e-able (gate false
in every Playwright project).

Gates: vitest 1952, full e2e 228/228 (10.3m), check 23 warnings =
baseline, build green. Todo: device dogfood rolled to ROUND 2 (4
verification points incl. 📄 + copy-link); e2e upload-journey 4-copy
dedup recorded against CSP-parity session. vjt's "use the link viewer
for media?" question answered: that's iOS out-of-scope treatment, not
requestable same-origin — would need a separate media origin; modal
stays, 📄 docs now get real-Safari handoff (the part of his instinct
that was right).

## EXIF-whitelist session (2026-06-11 afternoon): orientation fix + outage

vjt dogfood: #39 strip ate EXIF Orientation → portrait photos render
sideways. Shipped `33c4014`+`230c7c8` (merged, pushed, CI green):
`@kept_tags ~w(Orientation)` copy-back via exiftool
`-all= -tagsfromfile @` idiom, image/* ONLY (review: bare `-Tag`
resolves against QuickTime-embedded XMP/EXIF groups too — video keeps
blanket wipe; its rotation is tkhd structure, untouched). New
:oriented_jpeg fixture; GPS probed via exiftool read-back on stripped
output (byte markers can't see binary rationals). ICC_Profile =
named next candidate, OUT until a P3 fixture exists (todo Low).
Sideways already-stored uploads unfixable — re-upload.

**Deploying it caused a ~15 min prod outage** — defects #7–#9 of the
deploy saga, anatomy in DESIGN_NOTES 2026-06-11 "prod outage": (7)
preflight range base = pre-pull HEAD, not marker → cic pulls had
advanced jail HEAD past the runtime.exs commit → COLD silently
skipped, deploy went hot; (8) --force-cold swallowed by the
nothing-to-do fast path; (9) manual `service grappa restart` raced
the WS drain → new BEAM died on epmd name-in-use, rc.d silent. 
Recovery: `service grappa start` after old node exited. **Fix-up
dispatch handed off** (todo Immediate + /tmp/orchestrate-next.txt).

End state VERIFIED: BEAM restarted (fresh boot ~13:50), healthz ok,
`Endpoint.url()` = https://irc.sniffo.org (PHX_HOST debt CLEARED —
prod mints live upload links), 8/8 sessions respawned, marker
honest at `230c7c8`. Pending-COLD-on-main: NONE.

## Prod state

- m42 jail server HEAD = `3eac193`, marker = `c0e37a5` (BEAM untouched
  all session — pending runtime.exs cold change auto-applies on next
  server deploy via marker diff); cic bundle at `3eac193` (hot `--cic`
  deploy, hash CLK0_RL1), healthz ok.
- Bootstrap rpc recovery one-liner (node stuck pre-c0e37a5):
  DESIGN_NOTES "substrate-scoped preflight" entry, "Recovery
  one-liner" — `jail_release.sh rpc ':code.load_abs(~c"<ebin>/Elixir.Mod")'`.

## Still open (carried)

- vjt iPhone dogfood (ask, don't block): (a) refresh-banner tap +
  upload + exiftool stored URL shows no GPS (final #39 device
  verification); (b) NEW — long-press text selection in scrollback,
  including a SHORT channel (non-overflowing scrollback carries
  `touch-action: none`; emulation can't answer whether WebKit starts
  long-press selection inside it) + selection inside compose box.
- Android open question: keyboard-preserve now iOS-only; if Android
  PWA dogfood ever shows keyboard drops on chrome taps, widen the
  keepKeyboard gate by one clause.
- e2e CSP parity (todo High) + ranged-206-through-nginx spec.
- fail2ban /read-cursor exemption recheck ≥2026-06-16 (todo Low).
- Codebase review cadence check (/start) — was due-ish at cp62.
- vjt iPhone dogfood NEW ask: media-link viewer (tap 📸/🎬 link in
  standalone PWA → in-app modal, no navigation; todo Low).
- Pending COLD server change on main: runtime.exs PHX_HOST url block
  (prod still mints http:// upload links until next server deploy —
  cic normalizes client-side meanwhile, nothing urgent).
- Orchestrate queue: EMPTY after Dispatch 2.
