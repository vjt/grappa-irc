---
status: closed
---

# CP64

Previous: cp63 (deploy-honesty cluster + media-viewer/EXIF dispatches +
the 2026-06-11 outage, closed). This session (2026-06-11, post-/clear
fix-up dispatch): **deploy defects #7–#9 fixed** — the trio behind the
~15 min prod outage (anatomy: DESIGN_NOTES 2026-06-11 "prod outage";
fix shapes: the sibling "deploy defects #7–#9 fixed" entry).

## Shipped (`ee97b42` + `dedef93`, merged, pushed, deployed)

1. **#7 — preflight range base = `runtime/last-deployed-sha`**
   (`infra/freebsd/deploy.sh`): marker when present + valid (40-hex
   shape gate BEFORE the sha touches a `su -c` line, then
   `git cat-file -e`), pre-pull HEAD only when no marker exists.
   Garbage/non-commit marker → loud abort with fix-it hint (silent
   prev_sha fallback would re-open the cic-pull range hole). Re-exec
   guard deliberately KEEPS pre-pull range (running-bytes question —
   comment in file). Docker substrate NOT ported: scripts/deploy.sh
   has no marker infra at all; folded into REV-I todo entry (port the
   whole marker mechanism in one pass).
2. **#8 — nothing-to-do fast path is auto-mode only**: forced modes
   log observed state + overriding flag; "re-driving" message now
   names the benign cic-pull cause, not just died-mid-flight.
3. **#9 — `infra/freebsd/jail_beam_wait.sh`** (new, shared):
   `wait-stopped <node> <timeout>` (BEAM exit + epmd name release;
   escalates SIGKILL after timeout, epmd restart ONLY after BEAM
   confirmed dead — preserves the 2026-05-31 pkill-epmd-under-live-
   BEAM lesson) + `wait-name-free` (pre-start guard, NO escalation).
   rc.d `grappa_stop` blocks until stopped; `grappa_start` refuses a
   registered name + verifies boot (vanished beam.smp = immediate
   loud ERROR — "Starting grappa."-and-walk-away is dead). deploy.sh
   cold path = second call site (load-bearing forever: rc.d refresh
   runs BETWEEN stop and start, so rc.d-fix deploys stop via the
   PREVIOUS wrapper). New rc.conf.d knobs: grappa_node,
   grappa_{stop,start,name_wait}_timeout, grappa_beam_wait.
   Helper warns loudly if epmd binary missing (erlang pkg move ≠
   silent degradation).

**Tests**: `test/infra/{deploy_jail,jail_beam_wait}_test.bats` (19) —
marker-vs-fallback range, garbage + well-formed-non-commit marker
aborts, force-past-fast-path, re-exec range choice, cold-path
stop→wait→rcd→start ordering, helper escalation/no-escalation split.
Harness: throwaway upstream+clone, PATH-stubbed su/mix/curl/service.
`scripts/bats.sh` now scans `test/bin/ test/infra/`. check.sh green.

**Review**: subagent panel rate-limited (session cap) → single-
reviewer pass over the same 7 angles; 3 findings fixed in `dedef93`
(marker shape gate / epmd-missing warning / probes-vs-seconds log
honesty).

**Deploy + verification**: `--force-hot` (auto would COLD on the rc.d
diff; restart deliberately NOT burned — BEAM cold-booted once today
already). Re-exec guard fired on 230c7c8..dedef93, reload
`{"failed":[],"reloaded":[]}`, healthz ok, marker = `dedef93`.
Wrapper applied via manual `jail_install_rcd.sh` (touches nothing
live; status verb works through new wrapper, pid 37936 unchanged).
FreeBSD smoke: `sh -n` on installed rc.d OK; helper against REAL
epmd — free name instant 0, live `grappa` name loud exit 1.
**#9's stop/start behavior validates at the next real cold window** —
do NOT restart prod to test it. Pending-COLD-on-main: NONE (rc.d
change already applied via install; marker at HEAD).

## Prod state

- m42 jail: HEAD = marker = `dedef93`, BEAM pid 37936 (fresh boot
  ~13:50 today, untouched by this session), healthz ok, 8/8 sessions,
  cic bundle CLK0_RL1, `Endpoint.url()` = https://irc.sniffo.org.

## Session 2026-06-11/12 (post-/clear): CSP parity + badge design

**e2e CSP parity — premise was WRONG, work reshaped (worktree
`grappa-e2e-csp`, branch `e2e-csp-parity`, commits `d73228c`+`41944fb`,
NOT merged yet):**
- Evidence (curl inside nginx-test): the e2e nginx has served the FULL
  prod CSP since 2026-05-22 — nginx-test.conf → locations-api.conf →
  security-headers.conf, all from infra/snippets/ (same chain as
  infra/freebsd/nginx.conf prod). The todo High entry AND the
  media-link-modal-viewer spec header claiming "e2e carries no CSP"
  were both false. Real gap: a CSP block DEGRADES the media pipeline
  (probe fallback) instead of failing specs — nothing asserted
  zero violations. That's how 6f3327c shipped (video spec landed 4h
  before the fix, transcode-agnostic, stayed green).
- Shipped in worktree: `_cspGuard` auto fixture in fixtures/test.ts
  (securitypolicyviolation collector, teardown asserts ZERO — closes
  the class for every wrapped-import spec);
  nginx-csp-range-parity.spec.ts (header set pinned on :80+:443 +
  ranged GET 206/content-range through the proxy);
  fixtures/uploadJourney.ts hoists the 4-times-drifted picker→modal→
  POST journey, all call sites migrated; lying spec header fixed.
- Validated: targeted run 11/12 green; the 1 failure was the
  DELIBERATE negative-proof scratch spec failing on exactly the guard
  assert (then deleted, `41944fb`). check.sh baseline green in the
  worktree (bats submodule needed init — known worktree gotcha).
- REMAINING: full-suite run under the guard (latent violations may
  surface — that's the point), triage, code review, rebase+merge,
  push. e2e stack left UP (KEEP_STACK), main-repo compose project.

**PWA icon badge — design APPROVED by vjt (2026-06-12), impl NOT
started.** Spec at `docs/plans/2026-06-12-pwa-icon-badge.md`: badge =
prefs-matched push set counted server-side
(`Grappa.Push.BadgeCount.count/1`, capped SQL + capped LIKE-prefilter
mention verify), three doors (push payload `badge`, `/me`
`badge_count`, `read_cursor_set` `badge_count`); cic badge.ts effect
(setAppBadge + document.title mirror — vjt opted in); SW sets the
payload's number, no SW-local state; TS shouldNotify mirror with
shared truth-table JSON fixture for parity. iOS permission quirk →
dogfood.

**Dogfood checklist delivered to vjt** (media-viewer round 2,
text-selection, #39 EXIF, portrait orientation) — answers pending.

## Still open (carried)

- **Codebase review DUE — explicitly deferred by vjt 2026-06-11**
  (token cost). Stays due; re-flag at every session start, vjt
  decides when. Next session scope instead (vjt-set): e2e CSP parity
  + dogfood asks + PWA icon badge feature + #9 validation cold
  window (restart ORDERED — supersedes this session's
  do-not-restart note).
- vjt iPhone dogfood (ask, don't block): media-viewer ROUND 2 (4
  points incl. 📄 Safari handoff + copy-link); long-press text
  selection in scrollback (incl. short channel) + compose box; #39
  exiftool check on stored upload URL (no GPS). NEW: portrait photo
  upload should display upright (orientation whitelist live).
- Android open question: keyboard-preserve is iOS-only; widen
  keepKeyboard gate by one clause if Android dogfood complains.
- e2e CSP parity (todo High) + ranged-206-through-nginx spec; e2e
  upload-journey 4-copy dedup pairs with that session (todo Low).
- fail2ban /read-cursor exemption recheck ≥2026-06-16 (todo Low).
- Deploy decision-lib extraction + docker parity = **issue #51**
  (subsumes the REV-I guard port; spec + sequencing in the issue —
  after codebase review + jail-deploy soak).
- Orchestrate queue: EMPTY.

## 2026-06-14 — user@host on join/part/quit (ad-hoc, vjt request)

Shipped + LIVE in prod (`988cd96` feature, `56660f0` m7 e2e fix,
`6c2e3ff` scroll-cascade fix; merged, pushed, deployed). DESIGN_NOTES
2026-06-14 entry has the full why.

1. **Feature** (`988cd96`): join/part/quit render irssi-style
   `nick [user@host] has joined/left/quit`. `EventRouter.prefix_userhost/1`
   lifts user+host off `msg.prefix` into the persist `meta` (new
   `:sender_user`/`:sender_host` keys in `Scrollback.Meta` @known_keys +
   the Logger metadata allowlist the A18 sync test couples to it).
   Both-or-neither (cloaked +x prefix → `%{}`). cic
   `ScrollbackPane.userhostSuffix/1` renders it. NO migration (meta is a
   serialized column).
2. **Deploy was HOT, twice** (`--force-hot` server reload +
   `--cic` bundle), zero sessions dropped. The `config/config.exs`
   Logger-allowlist touch would classify COLD (Preflight Class 7) but
   is functionally hot-safe — the allowlist governs log printing only,
   not the feature path. Reload report: `failed:[]`, daemon pid
   unchanged.
3. **m7 e2e** (`56660f0`): the substring `"nick has joined"` broke once
   `[user@host]` split nick from verb → red integration CI post-merge.
   Fixed by asserting the FULL `nick [~nick@host] has joined` (proves
   the parser→meta→wire→cic path vs real Bahamut), not a tolerant regex.
   `scrollbackLine()` now takes `string | RegExp`.
4. **scroll-on-window-switch :142 cascade** (`6c2e3ff`): passed solo,
   failed mid-suite (3/3 iso → cascade). Root cause: the auto-reset
   re-seeds #bofh with fresh-timestamped rows + cleared cursor → cic
   shows all-200-unread → marker pinned to top → :142's "(no marker)"
   premise broke. NOT the reset's fault (its all-unread baseline is
   intentional, cp14-b1/:226 rely on it) and NOT the user@host change
   (this spec measures seeded-privmsg geometry). Fixed at the victim:
   seed #bofh cursor to HEAD before login, symmetric with :226.
   Verified full chromium 170/170; both `ci` + `integration` green.
5. **Tooling self-heal** (`988cd96`): `scripts/bun.sh` auto-`bun install`
   when `cicchetto/node_modules` absent; `scripts/bats.sh` auto-inits
   the bats-core submodule. Fresh worktrees stop biting.

Lesson logged ([[feedback_grep_e2e_on_render_change]]): changing cic
rendered text → grep `cicchetto/e2e/tests` (vitest doesn't cover e2e
text assertions). That gap is what red-CI'd the merge.

## 2026-06-19 — #62 visitor /away un-gated + PR#64 + resolved-issue sweep

Three threads, all shipped HOT to prod, zero sessions dropped.

1. **#62 — visitor `/away`** (`7a779b4` server, `928db52` cic; merged,
   pushed, deployed). Two defects:
   - **Server gate was bogus.** `GrappaChannel.handle_in("away",…)`
     rejected visitors with `visitor_no_away`, moduledoc justifying it
     as "the facade only routes to user sessions" — **factually wrong**.
     `Session.set_explicit_away/3,4` is `is_subject`-guarded and accepts
     `{:visitor,_}` identically; each visitor owns a private isolated
     `Session.Server` + IRC connection (unique nick), so `/away` is
     per-connection and safe. The comment conflated explicit `/away`
     with WSPresence AUTO-away (which legitimately stays user-only).
     Fix: delete the gate, route the subject via existing
     `resolve_subject/1` (same C3 WHOIS carve-out). Net simplification —
     one path replaces the `if visitor? … else` fork.
   - **Client swallowed the reason.** `compose.ts` only friendly-mapped
     `ApiError`; a `ChannelPushError` fell through to bare "Send failed".
     Added `friendlyChannelError.ts` (mirrors `friendlyApiError`:
     closed-union token→copy, loud fallback, exhaustive vitest matrix).
   - Coverage: there were ZERO channel-level `/away` tests (why it
     shipped). Added user+visitor set/unset + no-session. Deploy HOT
     (GrappaChannel reload `failed:[]`) + `--cic` bundle.
2. **PR #64** (vjt-claude, `74f6302`+`d74e6d3`): visitor home-pane copy —
   add `/join` guidance + correct the scrollback claim. The old copy
   ("scrollback stays archived on the bouncer") was a lie: visitor
   scrollback is CASCADE-wiped on expiry (`messages.visitor_id … ON
   DELETE CASCADE` + Reaper→`Visitors.delete`). Reviewed (verified the
   fact against code; grep confirmed no e2e/vitest asserts the copy),
   CI green, rebase-merged, deployed `--cic`.
3. **Resolved-but-open issue sweep** (vjt ask). Closed 5 with cited
   code+test evidence: **#62** (away), **#39** (EXIF strip —
   `Uploads.MetadataStrip`), **#21** (`/op` MODE), **#13**
   (`/mode`/`/umode`), **#15** ($server window routing). Audited 19
   open issues; left OPEN (#42/#30/#25/#31/#40/#61/#47/#29/#27/#28/#37)
   and UNCERTAIN (#32/#38/#16/#12/#46 — infra present but no test for
   the exact reported scenario; need human judgment / on-device).

Lesson ([[feedback_verify_comment_rationale]]): a guard's comment is a
claim, not evidence — a wrong WHY outlives the code and gets copied as
gospel. The `away` gate stood for months behind a one-line false
justification. Verify rationale against the code before trusting it.

### Pending for next session

- **Rotate to CP65** first thing — cp64 is ~225 lines / 4 sessions (over
  the ~200-line threshold).
- **#31 (visitor `/invite`) — easy win.** `/invite` still routes through
  `dispatch_ops_verb/2` (grappa_channel.ex), which rejects `{:visitor,_}`
  with `:visitor_not_allowed`. Migrate it to `dispatch_subject_verb/3`
  exactly like WHOIS/banlist already did (the C3 carve-out). Add a
  visitor-invite channel test mirroring the #62 away tests.
- **UNCERTAIN issues — need a test before closing:** #32 (password/
  nickserv sync, scope ambiguous), #38 (closeable +k failed-window —
  tested for manual join, NOT for reconnect-revoked-+k), #16 (members
  stuck 'loading…' — members_seeded infra exists, no +k seed/test), #12
  (`/msg #X` is now a closeable query window, restore-buffer layer
  untested), #46 (visibility-resume + server cursor exist, no Android
  long-background fix/test). Each is "infra present, exact reported
  scenario unverified" — write the failing test first, then decide.
- **Housekeeping:** the `kbfix` worktree is stale (its #62 work merged,
  now behind main) — `git worktree remove` it whenever.
