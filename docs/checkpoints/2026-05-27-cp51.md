---
status: done
---

# CP51 — bastille SHIPPED, log routing settled (2026-05-27)

## Status

Fresh rotation from cp50 (archived). Bastille deploy workstream
that's been blocking ★ ROADMAP since cp50 is **CLOSED**: m42 prod
live on irc.sniffo.org / irc.sindro.me, native Elixir release in a
FreeBSD bastille jail, Docker prod replaced.

origin/main HEAD: `7152827` (fix(rc.d): RELEASE_TMP=runtime, not
runtime/log). Working tree clean. m42 BEAM running, `/healthz` ok,
sessions live (vjt + M\Grappa).

## What landed this session

### Log routing under runtime/

After two passes, the final on-disk layout in prod:

```
runtime/
├── log/erlang.log.*  ← run_erl tee of BEAM stdout (RELEASE_TMP=runtime)
├── pipe/erlang.pipe.1.{r,w}  ← bin/grappa remote attach pipe
├── grappa_prod.db (+ -shm + -wal)
├── uploads/
├── bun-cache/
└── cicchetto-dist/
```

Single source of on-disk log: `run_erl`'s tee in prod, Docker
json-file driver in dev. No parallel Elixir-side file sink (first
pass tried that; reverted — produced same lines in two paths).

Driven by `RELEASE_TMP=/home/grappa/grappa/runtime` exported by
`infra/freebsd/rc.d/grappa`'s `grappa_runas/1`. RELEASE_TMP one
level UP from `runtime/log` because run_erl always creates its
own `log/` and `pipe/` subdirs.

### Bugs caught + landed even after the file-sink revert

- `mix release` CWD ≠ repo root → relative paths in `runtime.exs`
  `:eacces` under grappa user. `config/runtime.exs` defaults now
  derive from `Path.dirname(database_path)` so anything keyed on
  on-disk state inherits the already-absolute env-driven root.
- `RELEASE_TMP='...' . envfile && cmd` POSIX gotcha: the prefix
  binding dies with the `.` source builtin. Fixed by `export
  RELEASE_TMP;` as a separate statement.

### CI side-fix

`admin_events_test.exs` setup flunked 10 consecutive times on the
GHA runner (`SessionRegistry never drained — stale entries:
[...]`). Same rotating-cascade pattern as
`feedback_ci_cascade_rotating_set`: green locally, fails under
coveralls load. `Session.stop_session/2` returns once the worker
pid is dead but Registry's monitor-DOWN cleanup runs async. 50ms
single-sleep was too tight; replaced with 200×10ms poll matching
the upstream passive-wait shape (commit 1108808).

## Commits this session

```
7152827 fix(rc.d): RELEASE_TMP=runtime, not runtime/log
29810d5 revert(log): drop file sink, run_erl tee is enough
1108808 fix(test): admin_events_test — poll for Registry-monitor drain
76560d0 fix(rc.d): export RELEASE_TMP so it survives env-file source
1209479 fix(log): prod default LOG_DIR derives from DATABASE_PATH
51a161c feat(log): file sink under runtime/log/ + jail RELEASE_TMP
```

Docs updated: README "Trajectory" + `docs/todo.md` Immediate +
`docs/DESIGN_NOTES.md` `## 2026-05-27 — bastille deploy SHIPPED +
log routing under runtime/`. CLAUDE.md "Log file" section
simplified.

## Next session

Pick from ★ ROADMAP (`docs/todo.md`):

1. **Voice TTS+STT (Web Speech API on-device)** — per-channel TTS
   + STT toggle, browser-native, no third-party round-trip.
2. **UI polish cluster** — mobile-first, responsive breakpoints,
   touch targets, sidebar ergonomics, input bar density.
3. **PUBLIC OPEN** — self-hostable bouncer milestone.

Carry-forwards open in `docs/todo.md` "Carry-forwards from REV
cluster (open)" — opportunistic only.

## Open observations

- Push.SenderTest pre-existing flake (`req 0.5.18` surface,
  documented in `sender.ex:285`) still in CI. Not regression.
- `runtime/pid` file is declared in rc.d's `pidfile=...` but
  `service grappa status` actually delegates to `bin/grappa pid`
  (queries epmd) — the file is never created. Harmless but worth
  knowing if a future rc.d audit looks at it.

## S2 — 2026-05-27 — visitor rejoin + AdminEvents flake root cause + zombie respawn fix

Three bug fixes shipped, each chasing a deeper layer than the
previous. All commits LANDED on origin/main + cold-deployed to
bastille m42.

### Visitor rejoin on bouncer restart (`63f1c98`)

vjt noticed visitors don't rejoin channels post-restart. Users do.
Root cause: `Visitors.list_autojoin_channels/1` read from a
`visitor_channels` table that had **never been written to** since
creation — the schema comment admitted `"writes will land when the
visitor-rejoin-on-restart cluster lands a producer"`, and that
cluster never landed. Also `Session.Server.persist_last_joined/4`
had a silent `defp persist_last_joined({:visitor, _}, _, _, _),
do: :ok` short-circuit swallowing every visitor snapshot.

Fix: schema-mirror with users. Migration `20260527123810` adds
`visitors.last_joined_channels` (JSON array, same shape as
`network_credentials.last_joined_channels`) and DROPs the unused
`visitor_channels` table. `Visitors.SessionPlan` carries the same
`last_joined_persister` closure pattern as Networks; the visitor
no-op in `persist_last_joined/4` is gone. Test fixture
`visitor_channel_fixture/2` rewritten to update the new column —
same signature, zero test-callsite churn. Deploy: cold (Bootstrap
reads the new column at boot).

### AdminEventsTest CI flake killed at the source (`3d196c7`)

CI run 26511978748 failed 10× `Grappa.AdminEventsTest`. Setup-time
drain (`wait_for_empty_session_registry!`) saw `Registry.select`
return leftover entries, called `Session.stop_session/2` for each,
but `stop_session` raced the DynamicSupervisor's restart of a
`:transient` worker whose Client just crashed on `:tcp_closed` —
`whereis → nil` between the dying pid and the new one, the drain
loop never converged.

Root cause traced to `auth_fixtures.ex`'s
`start_session_for/2` + `start_visitor_session_for/2` — they
spawned `Session.Server` workers and returned the pid with NO
teardown registered. End-of-test IRCServer death triggered
respawn-loops that outlived the test pid and poisoned the
SessionRegistry for the next singleton-lane test.

Two-pronged fix at the source:
1. `register_session_cleanup(pid)` helper —
   `ExUnit.Callbacks.on_exit` callback that calls
   `DynamicSupervisor.terminate_child(Grappa.SessionSupervisor,
   pid)`. Atomic, sync, idempotent on dead pids.
2. `AdminEventsTest` setup-time drain replaced with the canonical
   `Grappa.AdmissionStateHelpers.reset_session_supervisor/0`
   (walks `which_children/1` + `terminate_child/2`, no race).
   ~110 lines of inferior re-implementation deleted.

Stress-tested locally with `--repeat-until-failure 20` + full
`mix coveralls.json` (CI-identical path) — clean.

### Production zombie respawn fix (`4b16330`)

`bin/grappa list-sessions` showed a visitor pid alive without a
DB row. Trace: anon visitor created with nick `vjt`, the same as
a logged-in user already on the azzurra IRC server. Upstream 433
nickname-in-use → `Session.Server` crash → transient restart →
433 again → loop. vjt fired three admin `DELETE
/admin/sessions/visitor:<uuid>:1` and one `POST .../disconnect`;
each killed the live pid but the supervisor had already
scheduled the next restart from a previous crash, so a fresh pid
spawned with cached `init_opts` seconds later. Backoff hit 25
minutes (failure_count=9) but the slot was poisoned forever —
no path to clean shutdown short of a full app restart.

Same mechanism as the test-fixture fix above, now solved
architecturally. `Session.Server.init/1` consults an optional
`subject_row_present?` closure at the top of init; if it returns
`false`, init returns `:ignore` — DynamicSupervisor drops the
child permanently because `:transient` only restarts on abnormal
exit. The closure is supplied by both
`Networks.SessionPlan.build_plan/4` (calls
`Credentials.get_credential_by_ids/2`) and
`Visitors.SessionPlan.build_plan/3` (calls `Visitors.get/1`).
Boundary-clean: opaque function-refs, same pattern as
`credential_failer` + `last_joined_persister`.

Plumbing extended through the spawn chain:
- `SpawnOrchestrator.spawn/4` adds `{:ok, :ignored}` outcome
  distinct from `:already_started`.
- `Bootstrap.Result` adds `subject_row_gone` counter +
  `classify_outcome/3` arm. Logger metadata allowlist extended.
- `NetworksController` maps `:ignored` to `{:error, :not_found}`
  (likely cause: racing unbind during PATCH /connect).
- `Visitors.Login` maps to `:upstream_unreachable`.

Four new tests pin the contract (plan-closure tests +
SpawnOrchestrator stub + Bootstrap classify_outcome).

### Side incident: VAPID keys recovered

vjt reported push notifications failing. Logs: FCM 403 + Apple
Web Push 400 on every endpoint. Root cause: during bastille
migration `mix grappa.gen_vapid` generated NEW VAPID keypair
(`BKSBT...`) while existing `push_subscriptions` in the DB had
been firmed by browsers against the Docker prod keypair
(`BFslT...`). Push services reject deliveries whose VAPID JWT is
signed by a different key than subscription-creation key.

Fix: replaced `VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` in
`/usr/local/etc/grappa/grappa.env` with the Docker values, clean
restart (`pkill -9 beam.smp + epmd`). 4 existing subscriptions
recovered (modulo the 1 already created against the new key,
which required a browser re-subscribe).

### Memories saved

- `feedback_session_fixture_on_exit_cleanup.md` — test-side
  fixture teardown contract.
- `feedback_subject_row_gone_init_gate.md` — production-side
  init/1 gate counterpart, linked.
- `feedback_vapid_keys_are_state.md` — VAPID keypair is state
  (like Cloak key), migrate alongside DB, never regenerate
  across substrates.

### Commits + stats

```
4b16330 fix(session): kill zombie respawn loop when operator deletes subject row
3d196c7 fix(test): kill the AdminEventsTest flake at its source
63f1c98 fix(visitors): rejoin channels on bouncer restart
```

Tests: 2516 → 2520 (+4 new). Dialyzer + credo + format clean.
CI run 26513650345 ✓ (after `3d196c7`). Cold-deploy to bastille
m42 ✓.

### Next session

Open production observation: visitor with nick collision against
a logged-in user is still creatable. The init-gate fix recovers
cleanly NOW (operator deletes the row, respawn loop stops on
first cycle), but the upstream UX is "user creates anon visitor,
gets a session that fails forever, operator must manually delete
it." A bind-time pre-check (reject visitor login if nick is
already taken by a connected user on the same network) would
prevent the class entirely. Smaller bucket; future-session
candidate.

Otherwise: pick from ★ ROADMAP per `docs/todo.md`.

## S3 — 2026-05-27 — zombie kazamobile → respawn-rereads-DB + admin last_seen_at + bastille hot-deploy

Triggered by an operator observation: visitor `kazamobile` on
Azzurra was "disconnected" in cic, but live BEAM still had a
Session.Server for the visitor row. Investigation surfaced a
class bug, fed three landed fixes + one infra capability.

### Diagnosis: zombie respawn with stale child-spec opts

Live state via `:sys.get_state` on the Session.Server pid showed
`nick: "kazam02"` (the original boot-time nick), but the visitor
DB row said `kazamobile` (the user `/NICK`-renamed 22 minutes
after spawn, persisted). Log triage caught the trigger:

- 18:27:18 — visitor spawn, nick=`kazam02`
- 18:28:16 — `/NICK kazamobile` echoed, row rotated
- 18:37:21 — manual JOINs (#sniffo, #sbiffo), `last_joined_channels` rotated
- 18:49:27 — upstream `:ssl_closed` → Session.Server crash
- 18:49:31 — `:transient` restart re-registered upstream as
  `kazam02` with empty autojoin → DB and live state divergent

Root cause already documented in `Session.Server` moduledoc since
Phase 1 as a known trade-off: `DynamicSupervisor.start_child/2`
caches the original child spec, `:transient` restart replays the
SAME `init_opts` — credential / visitor row rotations in the DB
do NOT propagate.

### Fix: generalize `subject_row_present?` → `refresh_plan`

The prior closure shape `(-> boolean())` already had a slot in
init for the operator-delete fail-fast case. Same closure, strictly
more informative shape: `(-> {:ok, plan} | {:error, :not_found})`.

`Session.Server.init/1` calls it on EVERY init (boot AND
`:transient` restart). On `{:ok, fresh}`, `Map.merge(opts,
fresh)` — fresh wins on shared keys (`:nick`,
`:autojoin_channels`, `:password`, `:host`, `:port`, `:tls`);
opts-only keys (`:network_id`, `:notify_pid`, test fixtures)
survive. On `{:error, :not_found}`, `:ignore` (subsumed
operator-delete fail-fast → DynamicSupervisor drops the child
permanently).

Both `Networks.SessionPlan` and `Visitors.SessionPlan` inject the
closure: re-fetch row by id, re-invoke `resolve/1`, fold
`:network_unconfigured`/`:no_server` errors into `:not_found` for
uniform `:ignore` semantics.

Deploy fixed both kazamobile-shape (immediate, cold restart
re-spawned via Bootstrap with fresh plan) and every future
`:ssl_closed` mid-session.

### Feature: `last_seen_at` on `/admin/sessions`

Direct fallout of the kazamobile investigation. The diagnosis
required remsh + `:sys.get_state` to confirm "user gone, bouncer
still connected". Surface `accounts_sessions.last_seen_at` as a
top-level field on each `/admin/sessions[]` row so the same
diagnosis is a glance at the admin table.

- `Accounts.max_last_seen_by_subject_ids/2` — batched GROUP-BY-and-MAX, keyed by `:user` or `:visitor`
- `LiveIntrospection.AdminWire.session_to_admin_json/3` — third arg, ISO8601 via `DateTime.to_iso8601/1`
- `SessionsController.index/2` — two batched calls parallel to the labels lookup
- Cic: `AdminSession.last_seen_at`, `renderLastSeen/1` relative-time helper
- Dropped `channels` column — `LiveBadge` already shows joined count

MAX across multi-device cookies → single per-subject timestamp,
duplicated across `(subject, network_id)` rows because cookies
are not network-aware. `null` is the U-0 honesty signal for
"bouncer-spawned-with-no-browser-login".

### Infra: bastille jail hot deploy

`infra/freebsd/deploy.sh` was "always cold" since cp50's bastille
bring-up. Rewrote to use the same preflight-driven hot/cold split
as `scripts/deploy.sh`, sharing the `Grappa.Deploy.Preflight`
classifier (extended with three jail triggers: `rc.d/grappa`,
`deploy.sh`, `nginx.conf`; operator-on-demand `jail_*.sh` and
template-only `grappa.env.example` stay HOT).

Two coupled fixes made the path actually work:

1. **Phoenix.CodeReloader replaced with `:code` primitives.**
   Live-verified on the jail: `Phoenix.CodeReloader.reload(Endpoint)`
   returned `:ok` but `Grappa.Accounts.max_last_seen_by_subject_ids/2`
   (a newly-shipped function) was `UndefinedFunctionError`. The
   reloader requires Mix (absent in `mix release` artifacts) AND
   silently no-ops in `MIX_ENV=prod` even on Docker (the 2026-05-16
   M-4 incident class, `feedback_hot_deploy_silent_noop_prod`).
   `:code.modified_modules/0` + `:code.load_file/1` are
   release-friendly OTP primitives — no Mix, no gate, no no-op.
   `POST /admin/reload` now returns JSON
   `%{"reloaded" => [...], "failed" => [...]}` for positive
   evidence.

2. **`mix release --overwrite` is mandatory in the HOT path.**
   The daemon's `code:get_path/0` points at
   `_build/prod/rel/grappa/lib/grappa-X.Y/ebin/` — the parallel
   `_build/prod/lib/grappa/ebin/` (where `mix compile` writes) is
   NOT on the daemon's code path. Verified: pre-release-rebuild
   `:code.modified_modules() == []`, post-rebuild → the three
   expected modules from the cold-pending commit `aaa7eed`.

### Bastille jail tweak: `lo0 + 127.0.0.1`

`POST /admin/reload` is loopback-gated. Bastille thin jails ship
with `lo0` UP but no IP assigned — `remote_ip` saw `10.66.6.7`
(the jail IP, not `127.0.0.1`) and 403'd. Fix via
`bastille network -a grappa add lo0 127.0.0.1` (auto-restarts
jail, persists via `jail.conf` edit). Memory saved
(`project_bastille_jail_lo0_127`) for jail recreation.

### Smoke test — hot deploy end-to-end

`mix.exs` `@version` bump 0.3.0 → 0.3.1 → 0.3.2 (two iterations
because the first one needed a deploy.sh bugfix landed mid-test:
`git rev-parse` was running as root in a grappa-owned tree →
"dubious ownership" rejection. Fixed by routing the SHA reads
through `run_as_grappa`).

Final hot deploy result:

```
[deploy] mix release --overwrite ✓
[deploy] POST http://127.0.0.1:4000/admin/reload
[deploy] reload response: {"failed":[],"reloaded":[]}
[deploy] ✓ hot deploy complete (sessions preserved, daemon pid unchanged) after 0 retries
```

Live sessions before & after the hot deploy: same pids
(`<0.2336.0>` user vjt, four visitor pids). `Grappa.Version.current/0`
post-deploy returns `"0.3.2"` (live-read from `mix.exs`,
demonstrating why the version-reader bypasses
`Application.spec/2`'s post-load `.app` staleness).

### Memories written

- `project_bastille_jail_lo0_127.md` — `lo0` alias reapply
  procedure on jail recreate

### Commits + stats

```
6ed668c chore: bump version 0.3.1 → 0.3.2 (hot-deploy smoke retry)
2573eff fix(deploy): read git SHA as the grappa user, not root
6e11939 chore: bump version 0.3.0 → 0.3.1 (hot-deploy smoke test)
9301507 feat(deploy): hot-deploy on the bastille jail + replace dev-only CodeReloader
aaa7eed feat(admin): surface last_seen_at per session row + drop redundant channels column
34670cd fix(session): respawn re-reads DB instead of replaying frozen child-spec opts
```

Tests: 2520 → 2532 (+12 new across respawn refresh_plan + last_seen_at
wire + preflight jail rules + admin reload JSON shape). Dialyzer
+ credo + format + sobelow + doctor + audit clean. Cic 1648 tests
green, biome + tsc clean. Production: cold deploy of cp51-S2 carryover,
then hot deploy of `--force-hot` to 0.3.2 — sessions preserved
throughout the latter.

### Next session

Hot deploy validated on a `--force-hot` mix.exs bump. Real test
is auto-detect on a pure `lib/*.ex` change — should HOT
automatically without `--force-*`. First eligible bucket from
★ ROADMAP that lands a `lib/*.ex` edit will exercise this path.

Otherwise: pick from ★ ROADMAP per `docs/todo.md`. Pending visitor
nick-collision pre-check from S2 still open.
