---
status: active
---

# CP52

Previous: cp51 (bastille SHIPPED + log routing + visitor rejoin +
zombie respawn fix + admin last_seen_at + bastille hot deploy).
Closed at HEAD `6ed668c` with 2532 tests, prod live on
0.3.2 (hot-deployed).

## State at checkpoint creation

- origin/main: `6ed668c` (chore: bump version 0.3.1 → 0.3.2)
- Working tree clean
- m42 bastille jail prod: live, healthcheck green, sessions
  preserved across the cp51-S3 hot deploy
- 2532 tests / 8 doctests / 34 properties / 0 failures
- Dialyzer + credo + format + sobelow + doctor + audit clean
- Cic: 1648 vitest tests green, biome + tsc clean

## Next session

Pick from `docs/todo.md` ★ ROADMAP. Two specific carry-overs from
cp51:

- **Visitor nick-collision pre-check** (cp51 S2 carry-over):
  bind-time reject of a visitor login when the nick is already
  taken by a connected user on the same network. Avoids the
  "create-then-zombie" UX where the operator has to manually
  delete the failing visitor row. Smaller bucket.
- **Auto-detect hot deploy smoke** (cp51 S3 follow-up): hot path
  was validated via `--force-hot` on a mix.exs bump (preflight
  forces COLD on mix.exs). First eligible bucket landing a pure
  `lib/*.ex` edit will exercise the auto-detect path naturally
  — no separate work needed, just observation on the next deploy.
