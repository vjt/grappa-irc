---
status: closed
---

# CP23 — code-reload dev workflow + /names UX silent bugs

Opened 2026-05-11 after CP22 rotation (CP22 closed at 627 lines / 5
sessions; ship-state of CP22 S5 — EXTRA_CHECK_ORIGINS + clean QUIT +
user-topic dedup — all green at origin/main `9d4988d`, prod healthy
at voygrappa.bad.ass + 192.168.53.12).

## Carry-forward from CP22

- **/names UX silent bugs** (memory `project_names_ux_silent_bugs`,
  3 OPEN):
  - Joined-target `/names` silent (no MembersPane refresh signal)
  - Non-joined `/names` output lands in `$server` with no notice marker
  - Cold-load no-selection → empty members aside

- **Phase 5 hardening backlog** (todo.md High tier; one item closed
  in CP22 S5 — terminate/2 clean QUIT shipped, todo entry stale,
  remove on next sweep): TLS verify_none → CA chain, post-`+r` umode
  check on connect, NickServ NOTICE parsing (success/failure +
  GHOST/RECOVER), NickServ REGISTER proxy.

## Bucket plan (vjt 2026-05-11)

vjt's stated next ask: **hot code reload for grappa dev workflow**.
Currently every `.ex` edit requires `scripts/deploy.sh` (~20-30s
release rebuild + lost ETS state + lost session connections). Want
Phoenix's standard dev workflow: edit → save → seconds later live in
BEAM, sessions preserved.

Plan pinned at `docs/plans/2026-05-11-code-reload.md`:
- One env: `mix phx.server` everywhere, drop `mix release`.
- `Phoenix.CodeReloader.reload!/1` via `POST /admin/reload`.
- `scripts/hot-deploy.sh` (sessions live) + `scripts/cold-deploy.sh`
  (compose recreate).
- Cic decoupled: independent bundle hash + `bundle_changed`
  broadcast.
- Image label `grappa.hot_deployable` for ghcr-pull users.

11 buckets B1-B11 + Z documented in the plan.

## Operational notes

- Codebase review gate tripped on session count (~17 sessions since
  2026-05-08 codebase review across CP19+CP20+CP21+CP22) but only
  3 calendar days. vjt accepted "rotate first then code-reload",
  deferring the review as ceremony-over-substance for the moment.
  Re-evaluate at end of code-reload cluster.
- Working tree clean, branch `main` up to date with `origin/main`.

## Open — code-reload implementation

Plan agreed. 11 buckets B1-B11 + Z documented. Next session opens
`cluster/code-reload` worktree and starts B1 (Dockerfile rewrite +
verify locally).

## S1 — B1 LANDED + DEPLOYED + must-do sweep

Single-stage image substrate shipped + deployed live.

**Commits on main (B1 main + 3 deploy hotfixes):**
- `39f90bf` — single-stage `elixir:1.19-otp-28-alpine` Dockerfile
  (`hexpm/elixir` doesn't publish alpine for 1.19/28.5; switched to
  upstream `library/elixir`), `LABEL grappa.hot_deployable=true`,
  `bin/start.sh` (renamed from `rel/env.sh.eex`) execs `mix phx.server`,
  unified `compose.yaml` with `--profile prod` for nginx + cicchetto-build,
  `127.0.0.1:4000` loopback default + `GRAPPA_PUBLISH`/`NGINX_PUBLISH`
  overrides, dropped `compose.prod.yaml` + `compose.prod.override.yaml.example`
  + `lib/grappa/release.ex` + `mix.exs` `releases:` block + `iex.sh --remsh`
  branch + `_lib.sh` `GRAPPA_PROD` switch + mix-probe defensive branch.
  `scripts/deploy.sh` migrated to `mix ecto.migrate` oneshot + `--profile prod`.
  Verified: `scripts/check.sh` exit 0 (1447 tests / 0 failures, dialyzer 0,
  docs built), worktree image boot + healthz + iex attach OK.
- `214a8a7` — hotfix 1: `XDG_CACHE_HOME=/app/.cache` (elixir_make
  couldn't write to root-owned `/.cache` when oneshot dropped to host
  UID via `user:`).
- `1dcb941` — hotfix 2: dropped named volumes (`grappa_deps`,
  `grappa_build`, `grappa_mix`, `grappa_hex`) — fresh named volumes
  are root:root, oneshot writes hit perm-denied. Bind-mount alone
  suffices since the host never runs mix. Bumped healthcheck.interval
  30s → 5s for fast healthy flip.
- `63f887b` — hotfix 3: `deploy.sh` runs `mix deps.get` before
  migrate. Bind-mount `./:/app` shadows image-baked deps with stale
  host content — explicit sync is the idempotent fix.

**Deploy evidence:**
- grappa container: `Up (healthy)` on `192.168.53.12:4000`, image
  `grappa:latest`, command `bin/start.sh`.
- nginx container: `Up (healthy)` on `192.168.53.12:80`.
- `scripts/healthcheck.sh` → `ok`.
- Bootstrap log: `credentials=1 spawned=1 skipped=0 failed=0`.
- vjt@azzurra session up.
- HTTP smoke: `GET /` → 200 (1289 bytes / sub-5ms), `/healthz` → 200,
  `/me` → 401 (correct, requires bearer), bundle `index-Ck6YHVo3.js`.

**Three lessons that earned new memory entries (not in plan):**
1. `feedback_named_volume_uid_trap` — Docker named volume + compose
   `user:` drop = root-owned-init permission trap.
2. `feedback_bind_mount_shadows_image` — bind-mount over image-baked
   `_build`/`deps`; deploy MUST `mix deps.get` + accept first-boot
   recompile + XDG_CACHE_HOME inside `/app`.
3. `feedback_compile_env_validation` rewritten — drift class only
   exists when compile + runtime split into separate processes;
   `mix phx.server` model collapses them, no build-args needed.

**Must-do sweep (this session, post-deploy):**
- Stale `bin/grappa rpc` / `compose.prod.yaml` refs in `lib/`,
  `config/`, `CLAUDE.md`, `.claude/skills/review/SKILL.md` — all
  swept to the unified-compose / `scripts/iex.sh` shape.
- README.md operator-onboarding section rewritten: `mix.sh` is the
  uniform path now (no release shell, no `bin/grappa rpc`,
  no `RELEASE_COOKIE`).
- `.env` cleaned: dropped `RELEASE_COOKIE` (dead env post-release-drop).
- MEMORY.md index updated: 2 new entries above + `unified compose`
  marked LANDED.

**Status:** code-reload SUBSTRATE live. Hot-reload itself NOT yet
active — `code_reloader: true` only in `config/dev.exs`, not in
`runtime.exs`/prod. B2+B3 wire that next this session: `code_reloader`
flag in runtime.exs + `POST /admin/reload` endpoint.

**Code review deferred** to dedicated next session per vjt direction.

## Open — B2/B3

- B2: enable `Phoenix.CodeReloader` in `config/runtime.exs` for prod,
  audit endpoint pipeline (the reloader plug is dev-default).
- B3: `GrappaWeb.AdminController.reload/2` POST `/admin/reload`,
  auth gate decision (Phase 5 admin auth not yet shipped — for now
  localhost-only or basic-auth env? open question from plan).
- Verification end-to-end: edit a controller, POST reload, assert
  new response without container restart, confirm session pid unchanged.

## S2 — B2 + B3 LANDED + HOT RELOAD VERIFIED LIVE

Hot-reload activated end-to-end. Live prod container hot-swaps modules
without restart, sessions preserved.

**Commits on main:**
- `b1a1376` — B2 (`code_reloader: true` + `reloadable_apps: [:grappa]`
  in `config/runtime.exs`'s prod branch + `listeners: [Phoenix.CodeReloader]`
  in `mix.exs` per Elixir 1.18+ pattern) + B3 (`POST /admin/reload`
  via `GrappaWeb.AdminController` calling `Phoenix.CodeReloader.reload/1`,
  gated by `GrappaWeb.Plugs.LoopbackOnly` — 127.0.0.1 / ::1 only,
  403 `loopback_only` otherwise; `:admin` pipeline in router;
  4-test suite covers allow + deny on both IPv4 and IPv6).

  Side fixes the cluster surfaced:
  - Dockerfile: `HOME=/app` + `XDG_DATA_HOME=/app/.local/share` (in
    addition to existing `XDG_CACHE_HOME=/app/.cache`) so dev tools
    (sobelow's elixir-security-advisories-mirego fetcher, mix_audit,
    etc.) don't trip on root-owned `/.sobelow` and `/.local/share/...`
    when the dropped UID has no HOME.
  - `scripts/_lib.sh in_container_or_oneshot`: forces `MIX_ENV=dev`
    + `HOME=/app` + XDG vars on every exec into the live container.
    Live container runs `MIX_ENV=prod` (deploy.sh sets it), so dev-only
    deps (credo, dialyxir, sobelow, etc., behind `only: [:dev, :test]`
    in mix.exs) wouldn't be loaded otherwise. Forcing dev keeps
    `scripts/credo.sh`, `scripts/check.sh`, etc. working against a
    prod-mode container.

**`scripts/check.sh` evidence:**
```
8 doctests, 28 properties, 1451 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done (passed successfully)
```
(1451 = 1447 baseline + 4 new admin loopback gate tests.)

**Hot-reload end-to-end verification:**

Edited `lib/grappa_web/controllers/health_controller.ex` to return
`"ok-RELOADED"` instead of `"ok"`. POSTed `/admin/reload` from inside
the container:

```
$ docker exec grappa curl -fsS -X POST http://localhost:4000/admin/reload
ok

$ curl -fsS http://192.168.53.12:4000/healthz
ok-RELOADED
```

Container ID + StartedAt unchanged across the reload (`9e5bd13e3443...`,
`2026-05-11T21:40:03.816077549Z` both before AND after). Session
`pid=<0.745.0>` from the original `bootstrap done` log entry still
alive — no second `bootstrap done` in the container logs, so the
supervision tree wasn't re-spawned. Phoenix.CodeReloader hot-swapped
the controller's beam in place; the BEAM stayed up the whole time.

Reverted the test edit + reloaded again, /healthz returned `"ok"`
again with same container ID. Two reload cycles, no restart,
sessions preserved.

**This is the cluster's actual goal: edit code → POST reload →
new code live in the running BEAM, sessions keep going.**

**Worktree cleanup:** `cluster/code-reload` branch + worktree
removed (work merged to main, no further use).

**Pushed to `origin/main` `b1a1376`** per push-autonomy memory
(ship gates green, no need to wait).

## Status — code-reload cluster

**Substrate (B1) + hot-reload (B2 + B3) LIVE. Cluster's core goal achieved.**

Remaining buckets are productization for the hot-deploy story:
- B4-B5: cic bundle hash sync (server pushes bundle_hash, cic
  shows refresh banner on mismatch — independent of grappa
  hot-reload).
- B6: `scripts/hot-deploy.sh` (git-pull + admin reload, the
  operator-facing wrapper).
- B7: `scripts/cold-deploy.sh` (rename current `scripts/deploy.sh`).
- B8: `scripts/deploy-cic.sh` (cic bundle deploy independent path).
- B9: CI image-label computation (`grappa.hot_deployable=true|false`
  based on mix.lock / supervision-tree diff).
- B10: e2e — edit controller → hot-deploy → assert via Playwright,
  WS reconnect not required.
- B11: docs (CLAUDE.md "How to run scripts" section update).
- Z: full check.sh + meta-deploy-via-hot-deploy.sh + README sweep.

**Code review** for everything shipped this session deferred to a
new dedicated session (next), per vjt direction.

## S3 — CTCP VERSION feat + 0.1.0 + bug bash (IN-FLIGHT)

Shipped + reloaded:
- `b6ba4b5` feat(ctcp): respond to VERSION query, bump to 0.1.0 (PUSHED)
- `a106ff6` fix(ctcp): CRLF guard in send_line, persist VERSION query
  for cic visibility (PUSHED)

Both reloaded into the live BEAM via `POST /admin/reload`. Container
ID + StartedAt unchanged across all reloads, single session pid
`<0.745.0>` from 21:40:05 still alive. Real-IRC verification: peer
`/ctcp grappa version` from `vjt` nick on azzurra → reply seen
correctly in IRC client AS `grappa 0.0.1` (NOT 0.1.0 — see bug
below).

### Discovered bugs (2 fixed live, 3 outstanding)

**FIXED via reload:**
1. CRLF footgun — `send_line/2` callers had to remember `\r\n`; my
   CTCP reply forgot it; second outbound frame concatenated +
   garbled the wire. Fixed at the transport boundary in
   `Grappa.IRC.Client.handle_call({:send, line}, ...)` via
   `ensure_crlf/1`. All callers now safe-by-default.
2. CTCP query invisible in cic — server now emits a `:notice`
   persist effect with body "CTCP VERSION query → grappa <vsn>"
   so cic can show inbound CTCP traffic (subject to Outstanding
   Bug #2 below).

**OUTSTANDING (uncommitted in tree, see `git status`):**

1. **`Grappa.version()` returns 0.0.1 not 0.1.0.** `mix.exs`
   `@version "0.1.0"` only takes effect on full `mix compile`
   (regenerates `.app` resource file). `Phoenix.CodeReloader.reload/1`
   recompiles `lib/*.ex` but NOT mix.exs → `Application.spec(:grappa,
   :vsn)` reads stale 0.0.1 from the loaded `.app`. **Next
   `scripts/deploy.sh` (cold deploy) will fix this.** No code
   change needed; cluster's hot-reload story has a documented
   limitation (struct-shape changes, mix.exs changes, supervision-
   tree changes need cold deploy — already in plan B9 image-label
   rules).

2. **Inbound CTCP NOTICE didn't auto-open cic DM window.**
   Investigation found:
   - The dm-listener arm in `cicchetto/src/lib/subscribe.ts` only
     auto-opens for `kind: "privmsg" | "action"` — NOTICE was
     dropped (legacy "Bug A fix" assumed all notices on own-nick
     topic were service notices like NickServ).
   - vjt's correct guidance: "do for notice the same fucking thing
     we do for privmsg" — peer NOTICEs on own-nick topic ARE
     legitimate DMs and should auto-open.

   **In-flight uncommitted edits (4 files, NOT TESTED YET):**
   - `lib/grappa/scrollback/message.ex` — added `:notice` to
     `@dm_with_eligible_kinds` (was `[:privmsg, :action]`). Allows
     notice rows to carry `dm_with` per the schema changeset.
   - `lib/grappa/scrollback.ex` — extended `dm_peer/4` guard to
     include `:notice`. Same DM-detection rule applied to peer
     notices targeting our nick.
   - `cicchetto/src/lib/subscribe.ts` — added a `kind === "notice"
     && sender !== ownNick` arm in `installDmListenerHandler`. Peer
     notices auto-open sender's window same as PRIVMSG; own
     outbound NOTICE echoes drop silently.
   - `cicchetto/src/__tests__/subscribe.test.ts` — replaced the
     old "NOTICE dropped" test with two new tests: (a) inbound
     peer NOTICE auto-opens, (b) own outbound NOTICE drops.

3. **Manual cic refresh required to see persisted notice rows.**
   Existed before Outstanding #2 fix; may or may not be
   independently fixed once #2 lands. Symptom: vjt sent CTCP
   VERSION queries; rows DID land in DB; cic only showed them
   after page refresh. Likely the same root cause as #2 (no
   auto-open path → no subscription → broadcast missed).

### Handoff state

- Tree: `git status` shows 4 modified files (above), uncommitted
  + UNTESTED. Need: `scripts/check.sh` + cic vitest (the test
  file edits especially), then commit + cold-deploy (the version
  bump needs full recompile, can't ride a hot reload).
- Live container: still healthy on `grappa:latest` from earlier,
  reports version 0.0.1, has the CTCP VERSION arm + visibility
  notice from previous reloads.
- Pushed to `origin/main`: through `cc49a92` (S2 entry) +
  `b6ba4b5` + `a106ff6` (the two committed CTCP work pieces).
- Next session must: (a) run check.sh, (b) cic vitest, (c) fix
  any breakage (the new tests use APIs I haven't verified —
  mock shapes may need tweaks), (d) commit, (e) cold deploy
  via `scripts/deploy.sh` to land 0.1.0 + the 4-file fix
  package, (f) verify peer CTCP query end-to-end (cic shows
  notice in auto-opened backgrounded window with unread badge,
  IRC reply shows `grappa 0.1.0`).

## S3 — CTCP NOTICE auto-open + version live-read LANDED HOT

S3 closed live + verified end-to-end with vjt's real IRC client.
Single session pid `<0.745.0>` from 21:40:05Z survived the entire
arc — three hot-reloads + cic bundle rebuild + browser hard-refresh.
NO cold deploy used.

**Three commits pushed to origin/main:**
- `5bf1002` feat(ctcp/dm): peer NOTICE auto-opens cic query window
  (4 files: `Scrollback.dm_peer/4` extended with `:notice`,
  `Scrollback.Message.@dm_with_eligible_kinds` includes `:notice`,
  cic dm-listener arm `kind === "notice" && sender !== ownNick`,
  vitest: peer NOTICE auto-opens + own outbound drops).
- `a1aea66` fix(version): `Grappa.Version.current/0` reads `mix.exs`
  live (zero-dep boundary, both `Grappa.version/0` + EventRouter
  CTCP-reply composer call it). Defeats the
  `Phoenix.CodeReloader`-doesn't-rerun-mix.exs staleness class.
  `@version` bumps now flow through next call without recompile.
- `a1567f8` fix(ctcp): visibility row persists at `channel = own_nick`
  (was `channel = sender`). Critical: was emitting broadcast on the
  peer-window topic, dm-listener never observed it, auto-open arm
  was unreachable. Now matches inbound-PRIVMSG routing shape.

**Verification timeline:**
- After commits 1+2 pushed + hot-reloaded: reply 0.1.0 ✓ but window
  did NOT auto-open. Direct DB query (`SELECT ... WHERE kind='notice'
  AND body LIKE '%CTCP%'`) showed row at `channel=vjt` (peer nick),
  confirmed dm-listener bypass.
- Commit 3 fixed routing → row now lands at `channel=grappa` (own
  nick) with `dm_with=vjt`. Hot-reloaded, but cic still didn't open.
- ROOT CAUSE: stale cic bundle. `runtime/cicchetto-dist/assets/index-Ck6YHVo3.js`
  was from `May 11 23:39` — pre-fix. Server changes hot-reloaded
  fine; cic bundle is a SEPARATE deploy path (this is exactly what
  B4-B5 + B8 of the cluster plan exist to automate).
- Fix: `docker compose --profile prod run --rm cicchetto-build`
  produced `index-RvD22cM9.js` containing the new arm. Browser
  hard-refresh → end-to-end PASS:
  * Reply IRC: `grappa 0.1.0` ✓
  * cic auto-opens `vjt` window in background with unread + line
    "CTCP VERSION query → grappa 0.1.0" ✓

**Three lessons earned:**
1. **Hot-reload doesn't touch cic bundle.** Server hot-reload AND cic
   bundle rebuild are independent paths. Cluster plan B4-B5 (bundle
   hash sync + refresh banner) and B8 (`scripts/deploy-cic.sh`) are
   directly motivated by this stair-step we walked manually tonight.
2. **`Grappa.Version.current/0` pattern.** Anything stale-on-hot-reload
   should read live from disk, not from `Application.spec/2` or
   compile-time consts, while we're in `mix phx.server` mode.
3. **Server-emitted DM-shaped visibility rows MUST persist at
   `channel = own_nick`**, never `channel = peer`. The dm-listener
   arm in cic only re-keys onto sender-bucket when the broadcast
   arrives on the own-nick topic. Persisting elsewhere bypasses it
   silently — the window only opens if the peer's window already
   exists.

**Outstanding bugs CLOSED by S3:**
- ~~#1 `Grappa.version()` returns 0.0.1~~ → fixed via live-read.
- ~~#2 Inbound CTCP NOTICE didn't auto-open cic DM window~~ → fixed
  via dm-listener arm + visibility-row routing.
- ~~#3 Manual cic refresh required~~ → was actually a stale-bundle
  symptom of #2; closed by the same fix + bundle rebuild.

## Open — code-reload remaining buckets (S4 plan)

Cluster's CORE GOAL (B1+B2+B3 hot-reload of running sessions) shipped
in S1+S2. CTCP cluster (S3) closed. Remaining buckets are
productization for the hot-deploy story:

**S4 priority cut (vjt 2026-05-12):**

HIGH (pain felt in S3, motivated by tonight's manual workaround):
- **B4** `Grappa.Cic.Bundle` module (renamed from plan's
  `Grappa.Version` — that namespace is now taken by the app-version
  live-reader). Boot-time read of `priv/static/index.html` script tag
  hash. User-topic join push `{kind: "bundle_hash", hash}`.
- **B5** `GrappaWeb.AdminController.cic_bundle_changed/2` →
  broadcast on `grappa:system` (or fan out via existing
  user-topic broadcaster). Cic mirror: store-level
  `bundleHashFromServer` + mismatch detector + refresh banner.

MEDIUM (ergonomics — wrappers around the manual sequence we ran):
- **B6** `scripts/hot-deploy.sh` (git pull + admin reload, ~5 lines).
- **B7** `scripts/cold-deploy.sh` = rename of current
  `scripts/deploy.sh` (preserve exact behavior).
- **B8** `scripts/deploy-cic.sh` (compose run cicchetto-build
  + POST cic-bundle-changed, ~8 lines).
- **B11** docs CLAUDE.md "How to run scripts" section update +
  hot vs cold safe-change matrix.

DEFER (substantive work, separate cluster):
- **B9** CI image label `grappa.hot_deployable` heuristic. Needs
  CI workflow edits + struct-change detection. Defer.
- **B10** Playwright e2e (edit controller → hot-deploy → assert
  WS-stable + new response). Defer.

**Z** close: full `scripts/check.sh` exit-0 + literal tail per memory
`feedback_landed_claim_evidence`, README sweep, hot-deploy proof
against itself (meta-deploy), checkpoint/MEMORY.md flush.

**Live container state at S3 close:**
- Image `grappa:latest`, healthy.
- Session pid `<0.745.0>` still alive from 21:40:05Z (now ~3h uptime
  across 3+ hot-reloads).
- Cic bundle: `index-RvD22cM9.js` (new, contains NOTICE auto-open arm).
- Pushed to `origin/main` through `a1567f8`.
- Working tree: clean except this checkpoint update.

## S4 — productization buckets B4-B8+B11+e2e infra fix LANDED

S4 closes the cluster: B4 (`Grappa.Cic.Bundle`) + B5 (admin endpoint
+ cic refresh banner + Playwright e2e) + B6+B7 (unified `deploy.sh`
with hot-vs-cold auto-detect) + B8 (`deploy-cic.sh`) + B11
(CLAUDE.md docs). Also unblocked CI (silently failing since S2 b1a1376
on the e2e infra `target=build` Dockerfile reference). 8 commits.

**Commits on `cluster/code-reload-s4` → ff-merged main:**
- `b8c0db7` feat(cic-bundle): live-read deployed bundle hash + push
  on user-topic join (B4)
- `0852a7d` feat(admin): POST /admin/cic-bundle-changed broadcasts new
  bundle hash (B5 server)
- `53a7b5a` fix(e2e): unblock integration suite after CP23 S1
  single-stage Dockerfile (3 separate fallouts collapsed)
- `c8633bc` feat(cic): bundle refresh banner on hash mismatch (B5 cic
  + Playwright e2e)
- `54d1b4c` feat(deploy): unified deploy.sh with auto-detect +
  deploy-cic.sh (B6+B7+B8)
- `1d7ebd5` docs(CLAUDE.md): hot-vs-cold deploy + deploy-cic.sh in
  scripts table (B11)
- `18bee50` fix(cic-bundle/ws_presence): green check.sh — credo +
  dialyzer
- `399e6f7` docs(README) + version: 0.2.0 — hot-deploy + cic bundle
  refresh banner

**`scripts/check.sh` evidence (literal tail):**
```
8 doctests, 28 properties, 1468 tests, 0 failures
Total errors: 0, Skipped: 0, Unnecessary Skips: 0
done (passed successfully)
```

**Full Playwright e2e (after `scripts/integration.sh` fix):**
- 50 specs total (chromium + webkit-iphone-15)
- 48 passed first attempt; 2 retry-passed (cp15-b6-kicked +
  m9-cicchetto-part-x-click) — flake from `database is locked` Exqlite
  contention during seeder→grappa concurrent boot. Pre-existing,
  surfaces only on CI = retry-tolerant. Tracked for follow-up.
- BundleRefreshBanner spec: 2/2 pass — mismatch → banner → click reloads;
  same hash → banner stays hidden.

**Three S4-cluster lessons earned:**
1. **Phoenix.CodeReloader doesn't refuse unsafe diffs.** Reload returns
   ok on mix.lock change, struct change, supervision tree change —
   crash arrives N hours later. Preflight in `deploy.sh` is the only
   line of defense. Conservative bias built into the auto-detect.
2. **`docker compose up --wait` exits non-zero on oneshot normal exit.**
   `--wait` treats container completion as a healthcheck failure
   (since the container is no longer running to be probed). Use
   `compose run --rm` for sync oneshots.
3. **CP23 S1 single-stage Dockerfile drop silently broke CI.** Three
   independent fallouts (target=build ref, named-volume root-trap,
   `--wait` exit semantics) all surfaced simultaneously when the e2e
   suite hadn't been re-run since the substrate cutover. Memory
   `feedback_landed_claim_evidence` covers the broader pattern; this
   is a concrete instance.

**Live container state at S4 close:**
- Image `grappa:latest`, healthy (still S3-deployed — meta-deploy via
  the new `scripts/deploy.sh` deferred to next session for vjt's own
  hands-on validation of the auto-detect against its first real
  cross-bucket diff).
- Session pid `<0.745.0>` still alive (uptime ~3h+S4 work duration).
- Working tree: clean.
- `cluster/code-reload-s4` worktree at `/tmp/grappa-cp23-s4` still
  exists; remove with `git worktree remove /tmp/grappa-cp23-s4` after
  push verification.

**Cluster `code-reload` CLOSED.** All 11 buckets shipped except B9
(CI image-label heuristic) + B10 (Playwright e2e for hot-deploy
correctness) — both deferred per S4 plan as substantive separate
work.
