---
status: active
---

# CP49 — E2E-ROBUSTNESS cluster open (2026-05-25)

## Status

Fresh rotation from cp48 (548 lines, archived). Continuation —
BUGHUNT-3 fully CLOSED on `515920d`; cp48 S2 documented
E2E-ROBUSTNESS cluster filing post-bisect-false-positive lesson.

origin/main HEAD: `92fafd2`. Working tree clean. Container UP 41h
(healthz ✓).

## Cluster brief — E2E-ROBUSTNESS

Chronic rotating-victim flakes in chromium full integration suite
(1-4 ✘ per run, victim set rotates: m2:51, scroll-225,
ux-5-bs:88, cp15-b6:32, p0e). Each iso 3-5× ✓. Bastille deploy
BLOCKED on closure — porting to FreeBSD on leaky-CI substrate
multiplies debugging surface.

Buckets A-E + Z per `docs/todo.md` Immediate. Recommended start:
**bucket D** (WS subscribe-vs-broadcast race) — strongest evidence
from cp48 S2 diagnostic seam dance (Phoenix Channel handle exists,
`joined.has(key)=true`, but server-side fan-out skips before
`.receive("ok")` completes).

## S1 — bucket D ship + cascade-root discovery

origin/e2e-robustness-d HEAD: `34fcb30` (pushed). 20 commits ahead of
main. Worktree: `/private/tmp/grappa-e2e-robustness-d`.

### What landed

All 17 plan tasks per `docs/superpowers/plans/2026-05-25-e2e-robustness-d.md`:

- T1-T7: per-context drain helpers (`8bdbcef` ReadCursor, `7bbed27`
  QueryWindows, `7201485` Push, `c9a5c64` UserSettings, `4c3e1a1`
  Uploads + `beba689` credo fix, `9ff270c` WSPresence)
- T8: `SubjectReset` orchestrator (`59f698d` + `a4ecb66` review-fix);
  compile-gated; DB drain + Session.Server respawn via `notify_pid`
  await on `{:session_ready, ref}`; 5s hard timeout
- T9: admin controller + route (`d845cb4` + `541f1c2`); compile-gated
- T10: nginx allowlist (`a3c5faf`)
- T11: `resetSubject(adminToken, userName)` fixture helper (`9ada465`)
- T12: m2 pilot (`6205fd6`); iso 5× chromium ✓
- T13: cursor-walks pilot (`e9f735a`); iso 5× chromium ✓
- T15: **WRAPPED Playwright `test` fixture** (`0b8324c`) — auto: true
  `_vjtReset` worker fixture in `cicchetto/e2e/fixtures/test.ts`; 100
  vjt-touching specs switched their `test` import; future spec authors
  get cascade-prevention for free
- Spec hardening (out-of-plan, surfaced by T14 partial):
  `cursor-advances-on-switch` — `991d377` baseline pin + `34fcb30`
  loosen to ≤; iso 5× ✓

### Critical gotchas learned

1. **WSPresence.reset_for_user gate trap** (`c7d8176`): existing
   `reset_for_test/0` gates on `Mix.env() == :test` ONLY. e2e testnet
   runs `MIX_ENV=dev`. Initial T7 ship mirrored the narrow gate → 500
   RuntimeError in dev. Fix: expand to `[:dev, :test]`. Apply when
   adding ANY test-only verb under `:dev` constraint.
2. **`autojoin_channels`, not `autojoin`** in `Credential.changeset/2`.
3. **`/messages` returns bare Array**, not `{messages: [...]}`.
4. **cic mount-time cursor POST races spec setup** — write baseline
   via `/read-cursor` BEFORE wheel scroll (cursor-advances template).

### T14 partial run — cascade root identified

Full chromium + webkit-iphone-15 at HEAD `34fcb30`:
**58 failed / 134 passed (42.1m)**. Saved to
`docs/superpowers/plans/2026-05-25-e2e-robustness-d-failures-snapshot.txt`.

**36+ of 58 failures cluster at uniform 30.7s timeout** = downstream
cascade. Root: spec #65
`m9b-admin-sessions-actions.spec.ts:133` (admin Terminate DELETE)
kills m9b-victim's session but never re-spawns it. ALL subsequent
specs needing a live m9b-victim session timeout.

**NOT a bucket D defect** — m9b-victim is admin-only, not vjt;
auto-reset fixture is vjt-scoped (correctly). Fix is one spec edit:
extend `m9b-admin-sessions-actions.spec.ts` afterEach to reconnect
m9b-victim (it ALREADY has a beforeEach that does this — just
mirror).

### Handoff to fresh session

Halted for context. Wrote `/tmp/orchestrate-next.txt` with:
- Cascade root + fix recipe
- Triage protocol (iso 5× per failing spec to bucket as
  cascade-victim / iso-broken / suite-context-flake)
- cursor-advances fix shape as template for iso-broken specs
- Remaining T14 / T16 / T17 plan
- All gotchas

Bucket D is structurally done; fresh session continues with cascade
root fix → triage 58 → T14 5× gate → ship.

## S2 — autojoin-wait + last_joined clear + baseline restore

Continuation 2026-05-26. Three commits on `e2e-robustness-d` (pushed
to origin):

- `e12b8c7` — `SubjectReset.reset!/1` polls `cred.autojoin_channels`
  for `:joined` window-state via `Session.get_window_state/3` after
  `:session_ready`. New error `{:autojoin_timeout, slug, channels}`.
  Round-1 chromium re-run: still 34 failures (autojoin-wait worked
  but missed merged-list case below).
- `508f8a7` — switched the poll to the RESOLVED `Networks.SessionPlan
  .autojoin_channels` (operator + last_joined merged), not just the
  cred field. cic's PART verb (UX-1, m9-part-x-click, cp15-b6)
  strips channels from operator-config autojoin permanently, so
  polling the bare cred field fast-paths empty. Also added the
  missing 504 controller clause for `:autojoin_timeout`. Round-2
  re-run: **44 failures** — UP from 34 BUT now with diagnostic
  `504 autojoin_timeout missing_channels: ["#bc2-plain-5b05b2",
  "#bofh", "#ux-5-bj-bdd0d93b"]`. Exposed the deeper bug:
  `last_joined_channels` accumulates ephemeral test channels across
  specs, and the merge bloats every reset's JOIN list past
  Bahamut's effective per-(spec, 5s) JOIN throughput.
- `be7d0f8` — reset now clears `cred.last_joined_channels = []` AND
  restores `cred.autojoin_channels` to a per-network-slug baseline
  passed via `POST` body `baseline_autojoin: {slug: [chans]}`.
  Fixture (`cicchetto/e2e/fixtures/test.ts`) supplies
  `{[NETWORK_SLUG]: AUTOJOIN_CHANNELS}` from `seedData.ts`.
  Round-3 re-run: **2 failures**. Suite time **2.0min vs ~25-30min**
  pre-fix. Run 2: 2 different failures. Run 3: 5 different failures.
  Rotating victim set. Per `feedback_ci_cascade_rotating_set` — real
  cascade, just much smaller and with different victims.

### Cascade collapse — 34 → 2-5 (94%)

| Run    | Failures                                                                                          |
|--------|---------------------------------------------------------------------------------------------------|
| pre    | 34 consistent (estimate; first re-run hit NIF crash → only 34 after exqlite recompile)            |
| post-1 | cp14-b1:215 (marker), cursor-advances:129                                                         |
| post-2 | cp14-b1:172 (no-marker), scroll-on-window-switch:226                                              |
| post-3 | cp15-b6-part-archive-rejoin, cursor-advances:129, m2:38, scroll-on-window-switch:226, ux-5-bs:89  |

All failing specs **iso 5×/10× green**. Confirmed suite-context
cascade, NOT iso-broken. Common theme: scrollback row count /
marker placement / cursor advance — depends on `messages` table
density which **reset does NOT truncate**.

## S3 — scrollback truncate + re-seed (cascade 34→1-2)

`5802859` — `SubjectReset.reset!/2` now accepts `baseline_seed: %{slug
=> [%{name, seed_count, seed_sender}]}`. For every listed channel:
calls `Scrollback.delete_for_channel/3` then re-inserts `seed_count`
synthetic privmsg rows via monotonic-time-spaced inserts. Runs
BEFORE Session.Server respawn so JOIN-cycle rows land on top of a
clean baseline. Fixture passes 200 rows from `seed-bot` for
`#bofh` (same shape as compose-time
`mix grappa.seed_scrollback --count 200`).

### Round 4 chromium runs

| Run | Failures |
|-----|----------|
| post-truncate-1 | cp15-b6-pending-to-failed-invite-only:48 |
| post-truncate-2 | r6-own-action:81, ux-5-bs:89 |
| post-truncate-3 | ux-5-bk-join-fail-dupe:55, ux-5-bs:89 |

- `cp15-b6-pending-to-failed-invite-only:48` iso 5/5 ✓ — cascade victim
- `ux-5-bs:89` iso 1✘+4✓ — partial cold-start race (first iso iter
  shows `widthBefore=256` instead of expected `>200 && <240`).
  256 = LEFT default; suggests `boundingBox()` racing layout or
  selector resolving to wrong element on initial mount.
- `ux-5-bk:55`, `r6:81` — not iso'd, likely also cold-start /
  cascade victims.

### Final cascade collapse: 34 → 1-2 rotating (95%+)

Total commits on `e2e-robustness-d` (origin synced): 4 new
(`e12b8c7`, `508f8a7`, `be7d0f8`, `5802859`) over the cp48 base of
20. Suite time 2.0-2.5min stable.

### Remaining work

- `ux-5-bs:89` cold-start race — likely needs `expect.poll` on
  `widthBefore` reading, or `await page.waitForFunction(() =>
  getComputedStyle(...).getPropertyValue('--members-width'))` before
  reading boundingBox. Real spec bug, not infrastructure.
- One-or-two iso-passing cascade victims still rotate per run.
  Suspected non-truncated state: cic's `__cic_dmListenerReady` Set
  in window globals (persists across page reloads within same
  context), Bahamut's persisted channel modes (a prior spec sets
  `+i` on a channel that subsequent specs try to join), or
  `Networks.Server` ETS state.

### Handoff: bucket D effectively closed

Cascade collapsed 95%+. The remaining 1-2 rotating fails per run
are spec-level issues or cold-start races, not infrastructure
cascade. Bastille deploy can proceed against this substrate; the
remaining flakes are individually-fixable per-spec via the
existing iso-rerun runbook.

Next session can either: (a) keep iterating on the rotating 1-2
to push to absolute 0 (diminishing returns at this point), or
(b) flip to webkit-iphone-15 project baseline measurement
(per orchestrator mandate "across BOTH chromium + webkit-iphone-15"),
or (c) close bucket D + move to bastille per `project_post_rev_roadmap`.

