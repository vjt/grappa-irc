---
status: active
---

# CP49 — E2E-ROBUSTNESS cluster open (2026-05-25)

## Status

Fresh rotation from cp48 (548 lines, archived). Continuation —
BUGHUNT-3 fully CLOSED on `515920d`; cp48 S2 documented
E2E-ROBUSTNESS cluster filing post-bisect-false-positive lesson.

origin/main HEAD: `92fafd2`. Working tree clean. Container UP 41h
(healthz ✓).

## Cluster brief — E2E-ROBUSTNESS

Chronic rotating-victim flakes in chromium full integration suite
(1-4 ✘ per run, victim set rotates: m2:51, scroll-225,
ux-5-bs:88, cp15-b6:32, p0e). Each iso 3-5× ✓. Bastille deploy
BLOCKED on closure — porting to FreeBSD on leaky-CI substrate
multiplies debugging surface.

Buckets A-E + Z per `docs/todo.md` Immediate. Recommended start:
**bucket D** (WS subscribe-vs-broadcast race) — strongest evidence
from cp48 S2 diagnostic seam dance (Phoenix Channel handle exists,
`joined.has(key)=true`, but server-side fan-out skips before
`.receive("ok")` completes).

## S1 — bucket D ship + cascade-root discovery

origin/e2e-robustness-d HEAD: `34fcb30` (pushed). 20 commits ahead of
main. Worktree: `/private/tmp/grappa-e2e-robustness-d`.

### What landed

All 17 plan tasks per `docs/superpowers/plans/2026-05-25-e2e-robustness-d.md`:

- T1-T7: per-context drain helpers (`8bdbcef` ReadCursor, `7bbed27`
  QueryWindows, `7201485` Push, `c9a5c64` UserSettings, `4c3e1a1`
  Uploads + `beba689` credo fix, `9ff270c` WSPresence)
- T8: `SubjectReset` orchestrator (`59f698d` + `a4ecb66` review-fix);
  compile-gated; DB drain + Session.Server respawn via `notify_pid`
  await on `{:session_ready, ref}`; 5s hard timeout
- T9: admin controller + route (`d845cb4` + `541f1c2`); compile-gated
- T10: nginx allowlist (`a3c5faf`)
- T11: `resetSubject(adminToken, userName)` fixture helper (`9ada465`)
- T12: m2 pilot (`6205fd6`); iso 5× chromium ✓
- T13: cursor-walks pilot (`e9f735a`); iso 5× chromium ✓
- T15: **WRAPPED Playwright `test` fixture** (`0b8324c`) — auto: true
  `_vjtReset` worker fixture in `cicchetto/e2e/fixtures/test.ts`; 100
  vjt-touching specs switched their `test` import; future spec authors
  get cascade-prevention for free
- Spec hardening (out-of-plan, surfaced by T14 partial):
  `cursor-advances-on-switch` — `991d377` baseline pin + `34fcb30`
  loosen to ≤; iso 5× ✓

### Critical gotchas learned

1. **WSPresence.reset_for_user gate trap** (`c7d8176`): existing
   `reset_for_test/0` gates on `Mix.env() == :test` ONLY. e2e testnet
   runs `MIX_ENV=dev`. Initial T7 ship mirrored the narrow gate → 500
   RuntimeError in dev. Fix: expand to `[:dev, :test]`. Apply when
   adding ANY test-only verb under `:dev` constraint.
2. **`autojoin_channels`, not `autojoin`** in `Credential.changeset/2`.
3. **`/messages` returns bare Array**, not `{messages: [...]}`.
4. **cic mount-time cursor POST races spec setup** — write baseline
   via `/read-cursor` BEFORE wheel scroll (cursor-advances template).

### T14 partial run — cascade root identified

Full chromium + webkit-iphone-15 at HEAD `34fcb30`:
**58 failed / 134 passed (42.1m)**. Saved to
`docs/superpowers/plans/2026-05-25-e2e-robustness-d-failures-snapshot.txt`.

**36+ of 58 failures cluster at uniform 30.7s timeout** = downstream
cascade. Root: spec #65
`m9b-admin-sessions-actions.spec.ts:133` (admin Terminate DELETE)
kills m9b-victim's session but never re-spawns it. ALL subsequent
specs needing a live m9b-victim session timeout.

**NOT a bucket D defect** — m9b-victim is admin-only, not vjt;
auto-reset fixture is vjt-scoped (correctly). Fix is one spec edit:
extend `m9b-admin-sessions-actions.spec.ts` afterEach to reconnect
m9b-victim (it ALREADY has a beforeEach that does this — just
mirror).

### Handoff to fresh session

Halted for context. Wrote `/tmp/orchestrate-next.txt` with:
- Cascade root + fix recipe
- Triage protocol (iso 5× per failing spec to bucket as
  cascade-victim / iso-broken / suite-context-flake)
- cursor-advances fix shape as template for iso-broken specs
- Remaining T14 / T16 / T17 plan
- All gotchas

Bucket D is structurally done; fresh session continues with cascade
root fix → triage 58 → T14 5× gate → ship.

