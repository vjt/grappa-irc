---
status: active
---

# CP01

The active checkpoint. Append a `## Sn` section per session. Rotate (mark
this `status: done`, create `cp02`) when this file hits ~200 lines or
8 sessions.

---

## State at checkpoint creation

- Repository moved from `/home/vjt/code/IRC/grappa-irc` to `/srv/grappa`
  (parallel to `/srv/gastone`, `/srv/eve`, etc.). Symlink at the old
  path for cwd continuity during the move session.
- Phase 0 (spec) complete: README + DESIGN_NOTES + Phase 1
  walking-skeleton plan.
- Server stack decision: Elixir/OTP 28 + Phoenix 1.8 + Ecto 3 +
  ecto_sqlite3 + own IRC client. Locked 2026-04-25 in DESIGN_NOTES
  with rationale.
- Docker scaffold landed: Dockerfile (multi-stage build → runtime),
  compose.yaml (dev, vlan53 192.168.53.11), compose.prod.yaml (prod
  release), scripts/ (mix, iex, test, credo, dialyzer, format, check,
  db, healthcheck, monitor, observer, deploy, shell).
- Mix project skeleton landed: mix.exs (deps + tooling + dialyzer +
  sobelow + boundary + ci.check alias), .formatter.exs (line 120),
  .credo.exs (strict + Specs), .gitignore (Elixir + sqlite WAL),
  .tool-versions, lib/grappa.ex + application.ex (skeleton), config/*
  (config + dev + test + runtime + prod), test/test_helper.exs,
  priv/repo/migrations/.gitkeep.
- CI workflow + Dependabot: full gate pipeline, weekly hex/actions/docker
  bumps grouped by concern.
- CLAUDE.md (430 lines): engineering principles ported from gastone +
  OTP playbook + Phoenix/Ecto patterns + charset rule.
- docs/ memory system scaffolded: todo, project-story (S1 episode written),
  project-evolution (Phase 0 marked complete).

## S1 — 2026-04-25 — Phase 0 complete: language pivot + scaffold

What landed:
- Two distinct git commits frame the language pivot: commit b7b0837
  preserves the Rust walking-skeleton plan as historical artifact;
  commit 493aa3e flips README + DESIGN_NOTES + plan to Elixir cleanly.
- Repo relocated to `/srv/grappa`. Old path `/home/vjt/code/IRC/grappa-irc`
  is a symlink for cwd-continuity (cleanable later).
- Docker baseline: multi-stage Dockerfile, dev compose with vlan53
  attachment at 192.168.53.11, prod compose targeting mix release,
  scripts/ wrappers patterned on gastone.
- Mix project scaffold matches Phase 1 Task 0 exactly (compiling-but-empty
  Elixir/Phoenix app ready for incremental TDD).
- CLAUDE.md is the rules-of-engagement contract for future sessions.

Key technical decisions documented in DESIGN_NOTES (2026-04-25 entries):
- Elixir/OTP+Phoenix over Rust+axum+sqlx+irc-crate. Reasons: BEAM
  architectural fit (~600-800 LOC saved vs hand-rolled supervision),
  Phoenix Channels has no Rust library equivalent (~1500 LOC saved),
  Phase 6 IRCv3 listener is materially easier in binary pattern matching,
  35-year BEAM backwards-compat track record outweighs Rust async
  ecosystem churn.
- Reconnect-on-deploy accepted; hot-reload explicitly NOT the reason for
  picking Elixir.
- Phoenix Channels (multiplexed WS) replaces SSE as the streaming facade.
- Rigid CI tooling baseline (Dialyzer, Credo strict, Sobelow, mix_audit,
  doctor, Boundary, all gates mandatory) compensates for Claude's
  ~15-20% first-pass code-quality gap in Elixir vs Rust.

Stats:
- 6 commits on main (initial spec + voice I/O + Rust draft + Elixir pivot
  + Docker scaffold + Mix scaffold + CI + CLAUDE.md... varies, run
  `git rev-list --count main` for current).
- 0 lines of Elixir compiled (Task 0 done; Tasks 1-10 pending).
- 0 tests (TDD-enforced once Task 1 starts).

Pending for next session:
- Phase 1 Task 1 — `Grappa.Config` TOML loader (start with failing
  tests, then implement).
- First `scripts/check.sh` smoke run on the Pi to confirm the docker
  compose pipeline actually compiles + runs `mix deps.get` cleanly.
- Generate `SECRET_KEY_BASE` and `RELEASE_COOKIE` for prod env, stash in
  `.env` (which is gitignored).

## S2 — 2026-04-25 — Phase 1 Task 1 + scaffold hardening

What landed (5 commits on main):

- **0942e96** fix: pin existing hexpm/elixir + debian image tags. The
  scaffold from S1 referenced unpublished tags (`OTP_VERSION=28.0`,
  `bookworm-20251023-slim`); both 404'd on first build. Bumped to
  `OTP_VERSION=28.5` (matches `.tool-versions`) and
  `bookworm-20260421-slim`. Bootstrapped `mix.lock` (64 deps, 20 KB)
  with a one-shot run, gitignored the leaked `.hex/` `.mix/` dirs.
- **37e0e20** refactor: extracted `in_container_or_oneshot()` helper
  into `scripts/_lib.sh`; migrated `mix.sh test.sh credo.sh
  dialyzer.sh format.sh check.sh`. Stateless tools now work without
  pre-booting `phx.server`. Live-state tools (iex, observer, shell,
  db, monitor, healthcheck) keep the strict in_container guard.
- **bb93cb4** ci: stabilized `ci.check` on the actual Phase 1 codebase.
  Five tooling drift fixes: dropped `ecto.create+migrate` from `test`
  alias (Repo doesn't exist till Task 2); dropped `--cover` from
  ci.check (excoveralls is `:test`-only and can't load in dev where
  the alias runs); moved Sobelow knobs into `.sobelow-conf` (which is
  what `mix sobelow --config` actually reads, not mix.exs `:sobelow`)
  and ignored `Config.HTTPS` with a Phase-5 marker; added LICENSE to
  ExDoc `extras` so README's MIT link resolves; fixed
  `application.ex` `_type/_args` → bare `_` per project Credo
  convention.
- **b9620e8** config: TOML loader with server + users + networks
  shape. Phase 1 Task 1. Three plan deviations folded in:
  (1) Dialyzer caught `File.read/1` returns `{:error, atom}` not
  `%File.Error{}`, and `Toml.decode/1` returns 2-tuple
  `{:invalid_toml, msg}` not 3-tuple — plan was wrong on both.
  (2) The "rejects missing required fields" test fed `[[users]]`
  with no body, which `toml` decodes as `users: []` and never reaches
  `build_user/1`; replaced with `[[users]]\nnickname="x"` to actually
  trigger the missing-name path, plus added a test for the empty-users
  case (also a malformed Phase-1 config).
  (3) `reduce_while` + pipe-to-case rewritten as a 3-clause recursive
  `traverse/2,3` — tail-recursive, no `{:ok, acc}` wrapping, reusable.
  CLAUDE.md gained a new code-shape rule: prefer recursive pattern
  match over `reduce_while` for collect-or-bail traversal.
- **(no commit)** Generated `.env` with fresh `SECRET_KEY_BASE` (64-char
  base64 from `phx.gen.secret`) and `RELEASE_COOKIE` (64-hex from
  `openssl rand -hex 32`). Gitignored. `.env.example` already in repo
  from S1.

Stats:
- Sessions: 2
- Commits on main: 11 (was 6 at S1 close; +5 from S2)
- LOC of Elixir: ~150 (Config + Application + Grappa, was 0)
- Tests: 4 (was 0)
- All `ci.check` gates green: format, credo --strict, mix_audit,
  hex.audit, sobelow, doctor (100% spec cov), test
  --warnings-as-errors, dialyzer (0 errors), ExDoc.

Pending for next session:
- Phase 1 Task 2 — Ecto Repo + sqlite migration for
  networks/channels/messages, `DataCase` test helper, sandbox in
  test_helper.exs, uncomment Repo in `application.ex`. The `test`
  alias's ecto bits will return alongside the actual Repo module.
- Update `docs/plans/2026-04-25-walking-skeleton.md` Task 1 section
  to reflect the three deviations (so the next person reading the
  plan doesn't re-introduce the dialyzer-killed patterns).

## S3 — 2026-04-25 — Phase 1 Task 2 + worktree-aware scaffold

What landed (8 commits on main):

- **024cf71** docs: lock single-sqlite + backfill Task 1 deviations
  + Task 2 enum fix. Pre-code design pressure-test: per-user `.db`
  vs single sqlite, MySQL/Postgres ruled out. Settled on coherence
  > theoretical isolation (CLAUDE.md: "the codebase IS the instruction
  set"). DESIGN_NOTES gained the sub-decision with full rationale +
  flip-condition (multi-tenant adversarial isolation). Plan Task 1
  backfilled to match shipped reality (S2 deviations); plan Task 2
  caught a spec violation before code — `field :kind, :string +
  validate_inclusion` rewritten as `Ecto.Enum, values: [:privmsg,
  :notice, :action]` per CLAUDE.md "atoms or @type literal — never
  untyped strings for closed sets."
- **126d935** scripts: worktree-aware compose project sharing.
  Created worktree at `/home/vjt/code/IRC/grappa-task2`; docker
  compose's project-name-from-CWD created a parallel `grappa-task2`
  project = new image build + new volumes + dialyzer PLT rebuild
  (~5 min on the Pi). Killed it. Ported gastone's `_lib.sh` pattern:
  SRC_ROOT (worktree, via PWD `lib/` + `.git` file marker) +
  REPO_ROOT (main, via `git rev-parse --git-common-dir`). `cd
  REPO_ROOT` so docker compose project = main = same image + named
  volumes (deps, _build, hex, mix, PLT). WORKTREE_VOLUMES array
  bind-mounts editable surfaces from SRC_ROOT on top. Verified:
  fresh worktree ci.check ran in 14s (was 5+ min). `in_container_*`
  refuses live exec from worktree (live container has main's
  source, not worktree's).
- **86a123d** docs: CLAUDE.md — note worktree-aware scripts behavior.
  Documented the new mechanism so next-Claude doesn't think it needs
  to cd to /srv/grappa from a worktree.
- **310f74f** docs: silence ExDoc warning on
  `Ecto.Repo.put_dynamic_repo/1` reference. Backtick `Module.fun/arity`
  triggers ExDoc resolution; rephrased.
- **a0b5188** docs: drop CHECK constraint from Task 2 plan — sqlite
  doesn't support it. Plan had `create constraint(:messages,
  :kind_must_be_known, check: ...)` from my earlier edit. Sqlite
  refused: `SQLite3 does not support ALTER TABLE ADD CONSTRAINT.`
  CHECK must be inline in CREATE TABLE; Ecto's migration DSL doesn't
  expose that for the sqlite adapter. Plan now matches shipped:
  Ecto.Enum at the cast layer is sufficient (CLAUDE.md already
  forbids raw SQL DDL bypasses, so the belt-and-suspenders argument
  is weaker than I claimed pre-implementation).
- **de5e0d0** db: Ecto Repo + sqlite migration for
  networks/channels/messages. The actual Task 2. networks (string PK,
  user_name, host/port/tls/nick), channels (composite PK
  (network_id, name) + FK), messages (auto PK, indexed on
  (network_id, channel, server_time) for the per-channel paginated
  DESC scan Task 3 + Phase 6 CHATHISTORY both need). server_time
  is integer epoch ms. updated_at: false on messages (immutable
  scrollback rows). Ecto.Enum on `:kind`. Grappa.DataCase per-test
  sandbox with `start_owner!`. test_helper sandbox :manual. mix.exs:
  test_load_filters added (Elixir 1.19 idiom), ecto/test aliases
  reintroduced, ci.check test step shellouts via `cmd env
  MIX_ENV=test mix test ...` because Mix aliases inherit the parent
  process env (otherwise ci.check in :dev runs Repo with the dev
  pool, not Sandbox). 10/10 tests green in 3.5s.
- **685a020** db: address Task 2 code review findings. Code review
  caught: a test name lying about its assertion ("rejects a string
  kind" body asserted Ecto.Enum casts strings — renamed to match
  actual contract); Phase 6 schema gap undocumented (added moduledoc
  paragraph noting `msgid` IRCv3 column intentionally deferred);
  missing FK from `messages.network_id` to `networks.id` read as
  oversight (annotated as INTENTIONAL — scrollback is operator-
  archival, not lifecycle-bound to networks); redundant alias in
  smoke test (DataCase already aliases Repo); Repo moduledoc sparse
  ("single shared Repo, resist put_dynamic_repo"); belt-and-
  suspenders test_load_filters + test_ignore_filters (kept the
  load filter, dropped the ignore).

Stats:
- Sessions: 3
- Commits on main: 19 (was 11 at S2 close; +8 from S3)
- LOC of Elixir: ~280 (was ~150; +Repo, Message schema, DataCase,
  migration)
- Tests: 10 (was 4; +Message.changeset 5 cases, Repo round-trip 1)
- All `ci.check` gates green in 14s on the Pi (PLT cache shared
  via worktree-aware scripts).
- Schema applied + verified via host sqlite3: networks + channels +
  messages tables, FK on channels, index on
  (network_id, channel, server_time), schema_migrations 20260425000000.

## S4 — 2026-04-25 — Phase 1 Task 3, Scrollback context

The first session that walked through the worktree → TDD → review →
merge cycle end-to-end without a single tooling detour. Tells you the
scaffolding from S2-S3 is paying off.

Created worktree `phase1-task3-scrollback` from main HEAD `6c12a57`,
wrote 8 failing tests for `Grappa.Scrollback.insert/1` + `fetch/4`
first, implemented the context, ran `scripts/check.sh` from the
worktree — 13s, all gates green on the first attempt after a single
formatter nit (blank line between two-clause `defp maybe_before/2`)
and one Credo nit (`_first` → `_` to match the codebase's anonymous-
unused convention). PLT cache hot via gastone-pattern volume sharing,
exactly as designed.

Code review (superpowers:code-reviewer agent) caught two should-fix
items: (1) `defp sample(i, overrides \\ %{})` violated CLAUDE.md
"no default arguments via `\\`" — split into `sample/1` + `sample/2`;
(2) `@spec ... pos_integer()` mismatched the runtime clamp accepting
`limit ≤ 0` — tightened the runtime to `when is_integer(limit) and
limit > 0` (let-it-crash per CLAUDE.md OTP rules) and the spec stayed
honest. Plus three nice-to-haves: cursor is `server_time`-only and
loses ms-tie rows across page boundaries (documented in moduledoc as
Phase 6 work); the clamp test inserted only 6 rows so couldn't prove
the cap actually fires (rewrote to insert `max_page_size + 5`); added
`max_page_size/0` introspection helper for upstream pagers.

Then Dialyzer caught `max_page_size/0` returning literal `500` while
the spec said `pos_integer()` — `:underspecs` flag from mix.exs flagged
the spec as wider than reality. CLAUDE.md "Dialyzer warnings are
design signals" — fixed by tightening the spec to `unquote(@max_limit)`
so it tracks the constant. The kind of round-trip you want from the
strict tooling: review caught the helper was useful, dialyzer caught
the helper's spec was sloppy, both fixed in the same commit.

User flagged a separate CI bug mid-session: `mix docs` step in
`.github/workflows/ci.yml:81` runs in `MIX_ENV=test` (job-level env
line 23) but `ex_doc` is `only: [:dev]` in mix.exs — the step would
fail on first push. Discovered via reading, not via watching CI burn.
Fix queued as a separate commit; CI hasn't fired yet because all S4
work is still local.

Single commit `cd829b4` for Task 3 itself: 88 LOC of context module +
108 LOC of tests, 19 total tests green, 13s gate. Fast-forward merged
to main, pushed.

**Law (sub-1):** the value of `:underspecs` is forcing helpers to be
honest about what they return. A `pos_integer()`-returning constant
function is a lie — either narrow the spec or compute the value.

**Law (sub-2):** "let it crash" applies to spec contracts too. When
the @spec says `pos_integer()`, runtime must enforce — guard clauses
(`when is_integer(x) and x > 0`) over silent `max(1, x)` clamps.
Silent clamping turns caller bugs into "works but not how you think,"
which is the worst kind of bug.

Stats:
- Sessions: 4
- Commits on main: 20 (+1 from S4: cd829b4)
- LOC of Elixir: ~480 (+196 — Scrollback context + tests)
- Tests: 19 (+9 — 8 scrollback tests + 1 FunctionClauseError test)
- ci.check: 13s on Pi (worktree, PLT hot)

Pending for next session:
- CI fix: `mix docs` step in test job. Move to dialyzer job (already
  MIX_ENV=dev) or add `:test` to ex_doc's `only:` in mix.exs.
- Phase 1 Task 4 — Phoenix Endpoint + `/healthz`. First HTTP surface;
  retires the "deploy step is N/A" caveat in todo.
- Worktree cleanup: `git worktree remove /home/vjt/code/IRC/grappa-task3`
  and `git branch -D phase1-task3-scrollback` once S4 fully closed.
