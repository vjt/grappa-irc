# UX-4 cluster — bug-hunting round (post-iPhone dogfooding)

**Status**: DRAFT 2026-05-18 — vjt-blessed bucket order, awaiting bucket-A start.

**Prereq**: UX-3 must be CLOSED first (docs sweep — DESIGN_NOTES + S53 +
README + memory + MEMORY.md). UX-3 shipped 9 ux-3-* commits + UNDEC + Z/
Z2/Z-arch/Z3-R4 — fully landed live but the cluster-close docs sweep was
deferred mid-stream by the new bug cascade. **First action of UX-4 is to
flush that sweep.**

## Source of truth

Twenty UX/server bugs vjt reported 2026-05-18 after iPhone dogfooding the
iOS + UX clusters. Per per-bucket cadence: 15 production buckets + Z
close. Each bucket = atomic concern, reviewer-loop'd where non-trivial,
deployed + browser-smoked at close.

Bucket order chosen so foundation work (case-fix migration, home-window
arch) ships first; atomic UX polish lands incrementally; the big
settings overhaul + upload-TTL DB pref ship last; Z journey at the end.

## Foundation choices (vjt-blessed)

1. **Channel case-insensitivity**: lowercase-on-write going forward **+
   backfill migration** of existing rows. Total-consistency rule
   (CLAUDE.md). Cold deploy.
2. **Home window**: first-class window kind. Visitor=help (no input).
   Registered=networks list + connect affordance. State lives in cic
   `windowStateByChannel` (or a new home-typed slot) AND surfaces via a
   typed server event when registered selects a network to connect.
3. **Cluster ID**: UX-4 (fresh). UX-3 closed cleanly before A starts.

## Buckets

### Pre-A: UX-3 docs sweep close

- `docs/DESIGN_NOTES.md` — new section "2026-05-18 — UX-3 reopened +
  chrome-gesture saga" covering 6-revert keyboard saga + UNDEC R1+R2+R3
  + Quart-DEC + TER-DEC + BIS-DEC + Z + Z2 + Z-arch + Z3-R4.
- `docs/project-story.md` S53 episode.
- `README.md` — UX bullet under closed-clusters-recent.
- Memory `project_ux_cluster_closed.md` — extend with the second round.
- `MEMORY.md` — index tweak.

Lessons to capture:
- Real-iPhone smoke NON-NEGOTIABLE; webkit Playwright can't simulate iOS
  keyboard OR scroll-chain/chrome gesture
- CSS has no `:has-overflow` selector — JS DOM-measurement canonical for
  iOS chrome-gesture rejection
- Scrollback read/write asymmetry (`list_archive` COALESCE vs
  `delete_for_dm` strict match) — fixed in Z, generalize to "every
  read+write pair MUST share the same key predicate"
- `pointerdown` ≠ `mousedown` on iOS: pointerdown blocks scroll, mousedown
  is focus-only
- "Setting selectedChannel" ≠ "live subscribing" — archive open needed
  explicit `openQueryWindowState` (Z-arch)
- Silent-swallow CLAUDE.md UD10 violation flagged in ArchiveModal —
  carry to UX-4 as discovered debt.

NO new commits in this bucket — pure docs. Single commit `docs(ux-3-Z-flush)`.

### Bucket A — Channel case-insensitivity + backfill migration

**Scope**: Server-side normalization. All channel names lowercased on
read AND write. Migration backfills existing `messages`, `query_windows`,
`read_cursors`, `last_joined_channels`, `archive` tables. Parser
lowercases at the IRC.Parser boundary.

**Files (proposed)**:
- `lib/grappa/irc/parser.ex` — lowercase channel param on JOIN/PART/
  PRIVMSG/MODE/NOTICE/TOPIC/KICK/INVITE wire-decode
- `lib/grappa/scrollback.ex` — lowercase channel on all queries
  (`fetch/*`, `list_archive`, `delete_for_dm`, etc.)
- `lib/grappa/query_windows.ex`, `lib/grappa/read_cursor.ex`,
  `lib/grappa/session/server.ex` (window_states key normalization),
  `lib/grappa/networks/credential.ex` (autojoin + last_joined)
- `lib/grappa_web/channels/grappa_channel.ex` — topic naming normalization
- `priv/repo/migrations/<ts>_backfill_lowercase_channels.exs` — UPDATE
  every channel column in every table, idempotent
- Tests: unit + property test (random-case round-trip), parser case
- E2E: parity matrix — JOIN #Chan vs join #chan vs JOIN #CHAN routes to
  same window

**Deploy**: COLD (migration + Bootstrap reads channel keys at boot).

**Reviewer**: yes — migration + boundary work.

### Bucket B — Home window first-class

**Scope**: New window kind `:home`. Per-identity (visitor vs registered).

Visitor: pure cic help-text pane. No input. Lists what IRC is + Grappa
overview + how to use cic + connect/disconnect commands (TBD whether
input gated to `/connect` `/disconnect` or just disabled outright per
vjt note). **Single source of truth: cic** (no server roundtrip).

Registered: first-class server-backed window. Lists configured networks
+ connection state (read from `Networks.list_credentials_for_user/1` +
live-state via `Session.whereis/2`). Click a row = `/connect <network>`
(re-uses T32 unpark verb). Future: minimal help below.

**Files**:
- `cicchetto/src/lib/homeWindow.ts` — new window kind; sidebar slot
  ABOVE first network
- `cicchetto/src/components/HomePane.tsx` (visitor) / `HomePaneRegistered.tsx`
- Sidebar projection: home is always pinned first, before any network
- Server: extend `subject_envelope` (login response) with
  `home_data: %{networks: [...]}` for registered (read on cold-load +
  on `:network_changed` typed event); visitor gets `null`
- Tests: cic unit (HomePane render per identity)
- E2E: visitor lands on home + sees help + no input; registered lands
  on home + sees networks list + click-connects

**Deploy**: HOT (cic-only on visitor side; server adds login-envelope
field which is additive).

**Reviewer**: yes — new window kind + envelope shape.

### Bucket C — Collapse network+server window, emoji prefix

**Scope**: The network-named window (currently shows server numerics) IS
the server window. Display name = `:server_icon: <network_slug>` (single
emoji `⚙️` or per-network configurable). Removes the duplicate row in
sidebar where "Azzurra" and the Azzurra server window both appeared.

**Files**:
- `cicchetto/src/components/Sidebar.tsx` — collapse logic + emoji prefix
- Possibly `lib/grappa/networks/network.ex` — optional `display_emoji`
  column (TBD, may be cic-only fallback)
- Tests: cic unit
- E2E: visible after JOIN-self that no duplicate row exists

**Deploy**: HOT cic if cic-only; HOT or COLD if server column added.

**Reviewer**: optional — cic-only is mechanical.

### Bucket D — Server window closeable = /disconnect

**Scope**: Closing the server window calls T32 `/disconnect` verb. All
channels + queries on that network close. Visitor: equivalent to `/quit`
(falls back to login flow). Registered: returns to home window.

**Files**:
- `cicchetto/src/components/Sidebar.tsx` or wherever close-× lives — wire
  server-window close to `/disconnect`
- `lib/grappa/session/server.ex` — verify `/disconnect` cascade closes
  all per-network windows (likely already does via T32)
- Tests: cic unit (close dispatches the right verb)
- E2E: registered close server window → home shown; visitor close server
  window → login pane

**Deploy**: HOT.

**Reviewer**: yes — verb routing.

### Bucket E — Close-chan → MRU next; home as last resort

**Scope**: When current window closes, focus shifts to most-recently-
viewed other window (channel or query). If none, server window. If
that's also closed (post-D), home. Order: MRU window > server > home.

**Files**:
- `cicchetto/src/lib/windowState.ts` — track MRU; close-handler picks next
- Replace existing "select chan to see scrollback" empty pane
- Tests: cic unit (MRU eviction order, fallback chain)
- E2E: close 3 windows in order, verify focus chain

**Deploy**: HOT cic.

**Reviewer**: yes — focus state machine.

### Bucket F — `/join #chan key` (+k channel support)

**Scope**: Parse second positional arg of `/join` as channel key. Pass
through to IRC wire `JOIN #chan key`. Handle `475 ERR_BADCHANNELKEY`
numeric → cic surfaces error in the network's server window + closes
the optimistic pending window.

**Files**:
- `cicchetto/src/lib/compose.ts` — `/join` parse with optional key
- `lib/grappa/session/server.ex` — pass key through to JOIN wire frame
- `lib/grappa/irc/numerics.ex` — handle 475 → typed event
- Tests: parser + session unit + cic compose unit
- E2E: registered `/join #priv-chan secretkey` → JOIN-self event; bad-key
  case → 475 surfaces

**Deploy**: HOT.

**Reviewer**: yes — wire-format change + numeric handling.

### Bucket G — *serv routing: keep in server window, never auto-open

**Scope**: All `*serv` (NickServ, ChanServ, OperServ, MemoServ, BotServ,
HostServ, HelpServ — case-insensitive `(.+)serv` regex) PRIVMSG/NOTICE
arrivals route to network's server window. NO auto-open query window.
NO PRIVMSG-to-*serv 500. Sending TO *serv: cic compose detects
`/msg <Xserv> ...`, routes wire frame, response lands in server window.
If user manually `/query nickserv` opens a window, *serv messages STAY
in server window (don't honor open query).

**Files**:
- `lib/grappa/session/server.ex` — sender classification (regex match)
  + persistence-route override (channel = server-window-marker)
- `lib/grappa_web/controllers/messages_controller.ex` — fix 500 on
  PRIVMSG to *serv (current behaviour spurious-opens window)
- `cicchetto/src/lib/subscribe.ts` — dispatch *serv events to server
  window even if matching query window exists
- Tests: server-side classification, controller endpoint
- E2E: `/msg nickserv identify ...` → server window shows response;
  no query window opened

**Deploy**: HOT.

**Reviewer**: yes — routing + 500 root cause.

### Bucket H — PART-fail still closes window

**Scope**: When PART fails (e.g. +n channel message rejection scenario or
404 ERR_CANNOTSENDTOCHAN), still close cic window. The user is NOT on
the channel — closing is semantic-correct. Fix: cic dispatches close on
PART-attempt regardless of server response; server best-effort sends PART
but doesn't gate window close.

**Files**:
- `cicchetto/src/lib/windowState.ts` — close locally on user-initiated
  close, don't wait for PART-ok
- `lib/grappa/session/server.ex` — clean up window_states even if PART
  numeric returned error
- Tests: cic unit (close fires immediately) + server unit (state cleanup)
- E2E: send to +n chan unjoined → window opens → close → no orphan

**Deploy**: HOT.

**Reviewer**: optional — small fix.

### Bucket I — Suppress umodes-ghost-window on connect

**Scope**: On connect, ircd sends `004 RPL_MYINFO` listing supported
umodes — currently cic parses as a NOTICE with nick = mode-letters and
opens a ghost query window. Suppress: numeric 004 / 005 / etc routes to
server window only, never opens a query window.

**Files**:
- `lib/grappa/irc/numerics.ex` or session message router — explicit
  allowlist of numerics that auto-route to server window
- `cicchetto/src/lib/subscribe.ts` — defensive: ignore typed events with
  source = numeric-bundle when no window exists
- Tests: server unit (numeric → server-window route, no open_query event)
- E2E: connect → no ghost window with umode-letter nick

**Deploy**: HOT.

**Reviewer**: yes — numeric routing.

### Bucket J — Member re-sort on MODE +o/+v etc.

**Scope**: On user mode change (channel MODE +o nick / +v nick / -o / -v
/ +h / -h), re-sort members list in MembersPane: ops > halfops > voice >
regulars > then alpha within group. Currently no re-sort on MODE arrival.

**Files**:
- `cicchetto/src/components/MembersPane.tsx` — derive sort from prefix
  on every render (already might) AND re-render on MODE event
- `cicchetto/src/lib/windowState.ts` — MODE handler updates member-prefix
  state
- Tests: cic unit (sort order after MODE)
- E2E: op self via /mode +o → self moves to top

**Deploy**: HOT cic.

**Reviewer**: optional — local cic logic.

### Bucket K — Scroll-on-activate canonical (kill all hacks)

**Scope**: On window activation (focus change OR PWA visibility-change
back to visible), ALWAYS scroll to unread marker OR to bottom if no
marker. Remove all ad-hoc scrollTop preserves + restore hacks. Single
source of truth in ScrollbackPane mount/effect.

**Activation triggers** (both converge on same scroll routine):
1. `selectedChannel` change (focus switch between windows)
2. `document.visibilitychange` → visible (PWA backgrounded + re-opened)

**Files**:
- `cicchetto/src/components/ScrollbackPane.tsx` — single activation
  effect; on `selectedChannel` change → scroll to marker or bottom;
  + `document.visibilitychange` listener on `visible` → same routine
- Audit + remove: any window-state scrollTop persistence, any per-window
  scroll restore
- Tests: cic unit (activation triggers right scroll for both paths)
- E2E: scroll up in window-A, switch to window-B (which has no scroll
  history), switch back to window-A → window-A scrolled to bottom (not
  preserved); future: marker case; PWA visibility hide → show → current
  window scrolled to bottom-or-marker

**Deploy**: HOT cic.

**Reviewer**: yes — touches scroll state which has been bug-prone.

### Bucket L — Settings overhaul (chrome/UX)

**Scope**: All cic-only settings pane fixes bundled.
- Close button (top-right × OR bottom large btn — vjt prefers either, use
  both: top-right × for desktop parity, bottom "Done" for mobile thumb
  reach)
- Internal-scroll: pane's content area scrolls, NOT viewport. Add
  `overflow-y: auto` to pane content + cap pane height to viewport.
- xxl width: cap pane max-width or font-size scope so xxl text doesn't
  blow horizontal layout
- Active-devices: replace full UA string with device-class icon +
  parsed name (`Chrome on macOS`, `Safari on iOS`). Parse on server or
  cic? Cic-side parser (ua-parser-js or simple regex) — KISS.
- Hamburger + cog button size parity: cog grows to hamburger size
- Archive button: move from bottom bar to top-right (next to cog or
  settings group); audit BottomBar archive chip placement
- **Settings always reachable, including from server window**: the
  Settings entry-point (cog button) must be visible from every window
  including the server `:server` window kind. Audit current
  conditional rendering — if the cog hides when the selected window
  is server/admin/home, that's the bug. Per cluster-wide rule below.

**Files**:
- `cicchetto/src/components/SettingsPane.tsx` (or wherever)
- `cicchetto/src/components/BottomBar.tsx` — drop archive chip, repoint
  archive to top-right new component
- `cicchetto/src/lib/userAgent.ts` — new tiny module
- Tests: cic unit (all)
- E2E: open settings → scroll internal → xxl text → close × → reopen

**Deploy**: HOT cic.

**Reviewer**: yes — settings touches multiple concerns; ensure no UA
parser regex regression.

### Bucket M — Upload-TTL → settings + DB pref

**Scope**: Move the upload-duration select OUT of compose box (beside
camera button) INTO settings pane. Persist as user preference in DB.
New column on `user_settings` table.

**Files**:
- `priv/repo/migrations/<ts>_add_upload_ttl_user_setting.exs` — add
  `upload_ttl_seconds :integer, default: 3600` (or whatever current
  default is)
- `lib/grappa/user_settings.ex` — schema + getter/setter
- `cicchetto/src/components/ComposeBox.tsx` — drop the select
- `cicchetto/src/components/SettingsPane.tsx` — add the select; reads +
  writes via REST
- `lib/grappa_web/controllers/user_settings_controller.ex` (or wherever)
  — PATCH endpoint
- Tests: server-side (migration + endpoint), cic unit (settings field
  reads + writes)
- E2E: change in settings → reload → still set → upload uses new TTL

**Deploy**: COLD (migration).

**Reviewer**: yes — migration + new endpoint.

### Bucket N — Admin window in sidebar (after Home)

**Scope**: Promote the Admin console from a Settings-drawer modal to a
first-class sidebar window. Slot ordering in sidebar:

```
home → admin (NEW, admin users only) → networks/$server windows → channels → queries → archive
```

Visibility gate: only renders for `is_admin: true` subjects (same
predicate that gates the current Settings-drawer "Admin" entry +
`AdminPane` mount). Non-admin users see the slot collapsed (no row).

Click selects the admin window; pane content is the existing
`AdminPane.tsx` (Visitors / Sessions / Networks / Events tabs) — no
content rebuild, just remount it under the standard window-pane chrome
instead of a drawer overlay. Settings-drawer "Admin" entry stays as a
secondary entry point (KISS — two doors, one room).

**Files**:
- `cicchetto/src/lib/adminWindow.ts` — new window kind `:admin`, slot
  position constant `ADMIN_SIDEBAR_ORDER` placed right after `:home`
- `cicchetto/src/Sidebar.tsx` — render admin row conditional on
  `is_admin`, between home row and first network row; reuse the
  collapsed `sidebar-network-header` shape from bucket C (single ⚙️
  emoji repurposed or a different glyph — recommend 🛠 to disambiguate)
- `cicchetto/src/Shell.tsx` — route `selectedWindow.kind === :admin` to
  `<AdminPane>` (already mounted via drawer; just an additional mount
  point on the window-pane side)
- `cicchetto/src/lib/windowState.ts` — admit `:admin` as a valid
  window kind alongside `:home` / `:channel` / `:query` / `:server` /
  `:archive`
- Tests: Sidebar.test.tsx (admin row visibility per identity), Shell
  test (admin window selection → AdminPane mount)
- E2E: admin login → see Admin slot in sidebar → click → AdminPane
  visible in main pane (mirrors bucket B home-cold-load shape)

**Deploy**: HOT (cic-only; server already exposes `is_admin` on
subject envelope via M-cluster work).

**Reviewer**: yes — new window kind + visibility gating + dual-mount.

### Bucket Z — Cluster journey e2e + docs

**Scope**: Single `@webkit iPhone 15` Playwright spec replaying all 14
buckets back-to-back per parity matrix (registered driven; visitor +
nickserv documented skips). Plus all docs sweep:
- `docs/DESIGN_NOTES.md` — UX-4 section
- `docs/project-story.md` — S54 episode
- `README.md` — UX-4 bullet
- Memory: NEW `project_ux_4_closed.md` + MEMORY.md index entry
- Update UX-3 memory to point to UX-4 as successor

## Per-bucket cadence (CARRY from UX-3)

- `scripts/check.sh` — server gate (must pass before commit)
- `scripts/bun.sh run check` + `scripts/bun.sh run test` — cic gate
- Baseline 1248 vitest tests — DO NOT regress
- Reviewer-loop for non-trivial buckets (Plan agent for design, code_search
  for exploration, code-review:loop for cross-surface migrations)
- Commit + push + deploy:
  - `scripts/deploy.sh` (HOT default; `--force-cold` for migrations/
    structs/supervision/cap-tree changes)
  - `scripts/deploy-cic.sh` for cic-only bundle broadcast
- Browser smoke via vjt iPhone (refresh banner ships hash)

## Cluster-wide rules CARRY

- One-feature-one-code-path
- KISS
- Italian profanity per global pref
- meta_codesearch:code_search agent over Explore
- Caveman mode active
- Push autonomy granted (skip "push?" turn once gates green)
- Live-state verbs: `bin/grappa` if needed
- **Settings entry-point always reachable** from every window kind
  (home, admin, server, channel, query, archive) — bucket L enforces
  this; any future window kind inherits it

## HALT criteria

- 25% ctx → flush + clear-cycle
- vjt deviation request
- Big design Q
- Reviewer BLOCK/CRIT finding
- vjt says "duct-tape" or similar → STOP, revert, find root cause

## Next workstream (post-UX-4)

Per `project_post_tmu_full_review_scheduled`: full codebase review.
Bastille deploy issue #8 per `project_bastille_deploy_workstream`.
